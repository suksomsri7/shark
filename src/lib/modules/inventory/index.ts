// inventory/index.ts — facade เดียวที่โมดูลอื่นได้รับอนุญาตให้ import (fitness F2 · ใบ CRM v2 C0.3 ส่วน D)
//
// 🔴 **re-export ล้วน ไม่มีตรรกะ ไม่มี DB**: ห้ามเปิดทางเข้าฐานข้อมูลของตัวเองที่นี่ (ไม่ import
//    ตัวไคลเอนต์ของเคอร์เนล · ไม่สร้าง PrismaClient ใหม่)
//    (ข้อสอบ C0.3-SD.7) — ตรรกะทั้งหมดยังอยู่ที่ `service.ts` ที่เดียวเหมือนเดิม
// 🔴 ไฟล์ของโมดูลอื่นที่ import `inventory/service` ตรงอยู่แล้ว (pos · shop · clinic · booking · account)
//    ยังใช้ได้เหมือนเดิม — ไฟล์นี้ **เพิ่มทางเข้าใหม่** ไม่ได้ปิดทางเก่า
//
// 🔴 ข้อควรรู้ของผู้เรียกฝั่ง CRM: `InvItem` **ไม่มีคอลัมน์ VAT** — ราคาขายคือ `priceSatang`
//    ส่วนอัตราภาษีมาจากสินค้า/การตั้งค่าของโมดูลบัญชี ⇒ อย่าเดา VAT จากแคตตาล็อก

export type { Ctx as InventoryCtx } from "./service";
export {
  /** สินค้า (PRODUCT) ที่ยังไม่เลิกขาย */
  listItems,
  /** บริการ (SERVICE) ที่ยังไม่เลิกขาย — ต้นฉบับเดียวของทั้งระบบ */
  listServices,
  /** สินค้า/บริการรายตัว — ไม่พบ/เลิกขาย/ข้ามร้าน-ข้ามระบบ → null */
  getItem,
  /** ช่องค้นหา: ชื่อ · SKU · บาร์โค้ด (ของเลิกขายไม่โผล่ · เพดาน 50 แถว) */
  searchItems,
} from "./service";
