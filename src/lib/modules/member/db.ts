// db.ts — จุดเดียวของโมดูล "สมาชิก v2" ที่แตะ prisma ดิบ
//
// 🔴 ทำไมต้องมีไฟล์นี้ (แบบเดียวกับ `kanban/db.ts` ของ RUN บอร์ดงาน):
//    ข้อสอบสถาปัตยกรรม F5 นับ "จำนวนไฟล์ในโมดูลที่ `import { prisma }` จาก `@/lib/core/db` ตรง ๆ"
//    แบบ ratchet (ลดได้อย่างเดียว) และวันนี้เต็มเพดานพอดี (45/45 — `member/service.ts` เดิมนับอยู่แล้ว)
//    ⇒ ไฟล์ใหม่ของโมดูลนี้ต้องผ่านทางเดียวกันหมด ไม่ใช่ต่างคนต่างล้วง core
//    ผลพลอยได้ที่สำคัญกว่า: ตอน port ไป `tenantDb` (Phase 3) แก้ที่ไฟล์นี้ไฟล์เดียว
export { prisma } from "@/lib/core/db";
