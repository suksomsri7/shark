// db.ts — ทางเข้าเดียวของ "ไฟล์ใหม่" ในโมดูลแชทที่ต้องแตะ prisma (แพตเทิร์นเดียวกับ `kanban/db.ts`)
//
// 🔴 ทำไมต้องมี: ข้อสอบสถาปัตยกรรม F5 นับ "จำนวนไฟล์ในโมดูลที่ `import { prisma }` จาก core โดยตรง"
//    แบบ ratchet (ลดได้อย่างเดียว) และวันนี้เต็มเพดานพอดี ⇒ ไฟล์ใหม่ต้องผ่านทางนี้
//    ผลพลอยได้ที่สำคัญกว่า: ตอน port ไป `tenantDb` (Phase 3) แก้ที่ไฟล์นี้ไฟล์เดียว
//    (ไฟล์เดิมของโมดูล เช่น `service.ts` ยัง import จาก core ตรง ๆ อยู่ — ทยอยย้ายมาได้โดยไม่แตะตรรกะ)
export { prisma } from "@/lib/core/db";
