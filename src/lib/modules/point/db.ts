// db.ts — จุดเดียวของโมดูล "แต้ม" ที่แตะ prisma ดิบ
//
// 🔴 ทำไมต้องมีไฟล์นี้ (แบบเดียวกับ `member/db.ts` · `kanban/db.ts`):
//    ข้อสอบสถาปัตยกรรม F5.1 นับ "จำนวนไฟล์ในโมดูลที่ `import { prisma }` จาก `@/lib/core/db` ตรง ๆ"
//    แบบ ratchet (ลดได้อย่างเดียว) และวันนี้เต็มเพดานพอดี (45/45 — `point/service.ts` เดิมนับอยู่แล้ว)
//    ⇒ M2.1 เพิ่ม `rules.ts` / `lots.ts` / `internal.ts` เข้ามา ทุกตัวต้องผ่านทางนี้ทางเดียว
//    ผลพลอยได้ที่สำคัญกว่า: ตอน port ไป `tenantDb` (Phase 3) แก้ที่ไฟล์นี้ไฟล์เดียว
export { prisma } from "@/lib/core/db";
