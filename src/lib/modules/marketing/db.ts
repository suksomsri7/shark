// db.ts — ทางเข้าเดียวของ "ไฟล์ใหม่" ในโมดูลการตลาดที่ต้องแตะ prisma
// (แพตเทิร์นเดียวกับ `chat/db.ts` · `kanban/db.ts`)
//
// 🔴 ทำไมต้องมี: ข้อสอบสถาปัตยกรรม F5 นับ "จำนวนไฟล์ในโมดูลที่ `import { prisma }` จาก core ตรง ๆ"
//    แบบ ratchet (ลดได้อย่างเดียว) และวันนี้เต็มเพดานพอดี (45/45) ⇒ ไฟล์ใหม่ต้องผ่านทางนี้
//    ผลพลอยได้ที่สำคัญกว่า: ตอน port ไป `tenantDb` ทั้งโมดูล (Phase 3) แก้ที่ไฟล์นี้ไฟล์เดียว
//
// 🔴 ผู้เรียกต้องใส่ `tenantId` (+ `systemId` เมื่อเป็นตารางแกน system) ใน where **ทุกคำสั่ง**
//    — ตัวนี้ไม่มี guard ให้เหมือน `tenantDb`
export { prisma, tenantDb } from "@/lib/core/db";
