// system/index.ts — facade เดียวที่โมดูลอื่นได้รับอนุญาตให้ import (fitness F2 · พิมพ์เขียว §5.11)
//
// 🔴 ทำไมเพิ่งมีไฟล์นี้ที่ M2.7: ผู้เรียกเดิม (booking/pos/restaurant/school/… รวมถึง `member/subscription.ts`)
//    import `system/service` ตรง ๆ ตามหนี้เดิมที่ fitness ตรึงไว้ · `member/wallet.ts` เป็นผู้เรียกรายใหม่
//    ⇒ เริ่มกติกาใหม่ที่นี่ (เหมือน `point/index.ts` ที่ M1.4 · `coupon/index.ts` ที่ M2.7)
//    หนี้ที่เหลือ: ย้ายผู้เรียกเดิมมาที่ไฟล์นี้ทีละโมดูลตอนแตะไฟล์นั้นอยู่แล้ว
// 🔴 ไฟล์นี้ห่อบาง ๆ ล้วน: ห้ามมีตรรกะธุรกิจ (ตรรกะอยู่ที่ `service.ts`)

export type { UnitSystems } from "./service";

export {
  /** ระบบชนิดหนึ่งที่ผูกกับสาขานี้ (null = สาขานี้ยังไม่ได้เปิดใช้ระบบชนิดนั้น) */
  systemForUnit,
  /** สาขาทั้งหมดที่ระบบหนึ่งผูกอยู่ — ใช้ resolve ข้ามระบบผ่านสาขาร่วม */
  unitsForSystem,
  /** ระบบทุกชนิดที่ผูกกับสาขานี้ (อ่านครั้งเดียวแทนการยิงทีละชนิด) */
  getUnitSystems,
  /** ระบบทั้งหมดของร้าน (กรองตามชนิดได้) — ใช้ตอนระบบนั้นไม่ได้ผูกสาขาไว้ */
  listSystems,
} from "./service";
