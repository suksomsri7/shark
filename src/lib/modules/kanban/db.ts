// db.ts — จุดเดียวของโมดูล "บอร์ดงาน" ที่แตะ prisma ดิบ
//
// 🔴 ทำไมต้องมีไฟล์นี้: ข้อสอบสถาปัตยกรรม F5 นับ "จำนวนไฟล์ในโมดูลที่ `import { prisma }` จาก core
//    โดยตรง" แบบ ratchet (ลดได้อย่างเดียว) วันนี้เต็มเพดานพอดี (45/45) ⇒ ไฟล์ใหม่ของโมดูลนี้
//    ต้องผ่านทางเดียวกันหมด ไม่ใช่ต่างคนต่างล้วง core
//    ผลพลอยได้ที่สำคัญกว่า: ตอน port ไป `tenantDb` (Phase 3) แก้ที่ไฟล์นี้ไฟล์เดียว
//    (service.ts เดิมยัง import จาก core ตรง ๆ อยู่ — ทยอยย้ายมาที่นี่ได้ทีหลังโดยไม่ต้องแตะตรรกะ)
export { prisma } from "@/lib/core/db";
