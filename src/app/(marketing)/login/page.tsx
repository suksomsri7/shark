import { AuthForm } from "@/components/auth-form";

export default function LoginPage() {
  return (
    <main className="flex flex-1 items-center justify-center px-6 py-24">
      <AuthForm heading="เข้าสู่ระบบ" />
    </main>
  );
}
