# แผน — กล่องแชท V2 "ให้เหมือนแอปแชทจริง" (ชุด WO-CV)

> เขียน 1 ก.ย. 2026 · ผู้วางแผน+QC: Fable · ผู้ลงมือ: Opus 5 (sub agent)
> 🔴 อ่าน `ledger/PLAN-CHAT-WHATSAPP.md` ก่อน — ไฟล์นี้ต่อยอดจากรอบนั้น **ไม่ทับ**
> 🔴 **แบบร่างคือข้อสอบ**: `docs/design/chat-v2/mockup.html` + `ref-mobile.png` + `ref-desktop.png`
> สถานะสดอยู่ท้ายไฟล์ (§9)

---

## §0 มติเจ้าของ 1 ก.ย. 2026 (ห้ามรื้อ)

| # | คำตัดสิน | คำพูดเจ้าของ |
|---|---|---|
| V1 | **ทำให้ครบทุกอย่าง ไม่เอาของครึ่ง ๆ** | "ทำไมเราไม่พัฒนาให้มีของครบละครับ" |
| V2 | **ไอคอนเป็นเส้นบาง minimal clean ทั้งชุด — ห้ามมี emoji** | "Icon ต้องปรับใหม่เป็น minimal clean" |
| V3 | **หน้าตาต้องเหมือนแบบร่าง ห้ามผิด · แต่ปรับให้ดีขึ้นได้** | "ต้องเหมือนในรูปห้ามผิด แต่สามารถปรับปรุงให้ดีขึ้นได้" |
| V4 | **realtime ใช้บริการสำเร็จรูป** (Pusher/Ably) ไม่เขียน SSE เอง ไม่ลง VPS | "ใช้บริการ realtime สำเร็จแบบที่คุณแนะนำ" |

🔴 **V3 แปลว่า "แบบร่างเป็นสัญญา ไม่ใช่แรงบันดาลใจ"** — สายไหนอยากเปลี่ยนจากแบบร่าง
ต้อง **รายงานเหตุผลก่อน** ไม่ใช่เปลี่ยนเงียบ ๆ · ของที่ปรับได้เองคือรายละเอียดที่แบบร่างไม่ได้พูดถึง
(เช่น สถานะ hover/disabled/จอเล็กมาก) ไม่ใช่โครงหรือองค์ประกอบที่วาดไว้แล้ว

---

## §1 สภาพจริง (สำรวจจากโค้ด 1 ก.ย. 2026)

### 1.1 ของที่ "มีข้อมูลครบแล้ว แต่ไม่มีหน้าจอ" — ได้เกือบฟรีในรอบนี้
| ของ | หลักฐาน |
|---|---|
| **คำตอบสำเร็จรูป** | ตาราง `ChatQuickReply` มีครบ (shortcut/title/body/channelTypes/usageCount) แต่ **grep ทั้ง `src/` เจอแค่บรรทัดลงทะเบียนใน `scope.ts` เท่านั้น — ไม่มีโค้ดไหนอ่านหรือเขียนเลย** |
| **ป้ายกำกับห้องแชท** | `ChatConversation.tags Json @default("[]")` — ไม่มีใครใช้ในโมดูลแชทเลย |
| **"กำลังดูหน้า…"** | `ChatConversation.meta.pageUrl` ถูกเขียนไว้แล้ว (`service.ts:948`) แต่ไม่เคยถูกแสดง |
| **SLA "ตอบครั้งแรกใน X นาที"** | `firstCustomerMessageAt` + `firstResponseAt` มีครบ |
| **สถานะส่ง 🕐/✓/✓✓/✗** | `deliveryStatus` + `markCustomerRead` มีครบ (วาดแล้วบางส่วนใน `bubble.tsx`) |

### 1.2 ของที่ต้องเพิ่มจริง
ปักหมุด · ปิดเสียงรายห้อง · ข้อความเสียง · กำลังพิมพ์ · ค้นหาในห้อง · ประวัติการจอง

### 1.3 หนี้จากรอบก่อนที่ยังค้าง (ปิดในรอบนี้)
- **H4**: ลิสต์ช่องทางพิมพ์มือซ้ำ 3 ที่ (`CHANNEL_LABEL` ใน ui.tsx · `CHAT_CHANNEL_TH` ใน ai/tools.ts · `CHANNEL_LABEL_TH` ใน chat/service.ts) → ยุบเหลือทะเบียนเดียว
- **G11**: `AppNotification` เป็น tenant-wide ไม่มีช่องผู้รับ ⇒ ตอนนี้ตัด preview ออกชั่วคราว → ปิดให้จบ

---

## §2 หน้าตา — อ่านจากแบบร่างเท่านั้น

**ไฟล์อ้างอิง (สายงานต้องเปิดดูก่อนเขียนโค้ด):**
- `docs/design/chat-v2/mockup.html` — ต้นฉบับ เปิดในเบราว์เซอร์ได้ ดู CSS จริงได้
- `docs/design/chat-v2/ref-mobile.png` — 4 จอมือถือ: รายการ · ห้องแชท · เมนู ⋮ + โหมดโน้ต · แผ่น ＋
- `docs/design/chat-v2/ref-desktop.png` — เดสก์ท็อป 3 คอลัมน์

**โทเคนที่ล็อกไว้ (คัดจาก mockup.html — ห้ามคิดใหม่):**
```
--ink #0f172a · --muted #71767f · --line #e8e9ed · --surface2 #f4f5f7
--accent #1d4ed8 · --out #e9eefc (ฟองขาออก) · --wall #f3f4f7 (พื้นหลังห้อง)
--note #fffaed / --note-line #ecd9a4 / --note-ink #8a6414 (โน้ตภายใน)
ไอคอน: viewBox 24 · stroke 1.7 (เล็ก 1.9) · round cap/join · currentColor · **ห้าม fill**
มุม: avatar 14px · ฟอง 14px (มุมติดหาง 4px) · ปุ่ม/ชิป 8–11px
```

🔴 **ห้ามใช้ emoji เป็นไอคอนอีกเด็ดขาด** (มติ V2) — ของเดิมใช้ 📎 📷 ✨ 🌐 🔒 🔔 ทั่วทั้งโมดูล

---

## §3 Work Order

### WO-CV1 — ระบบไอคอน (ฐานของทุกสาย ต้องเสร็จก่อน)
**ไฟล์:** `src/lib/modules/chat/icons.tsx` (ใหม่) · แก้ `channel-icon.tsx`
- SVG sprite ตาม §2 · export `<Icon name="…"/>` ชุดเดียว ~28 ตัว (ชื่อครบใน mockup.html `<symbol id="i-*">`)
- ช่องทาง: **ใช้โลโก้แบรนด์จริง** ไม่ใช่เส้นที่วาดเอง — แบดจ์ 19px ในแบบร่างอ่านไม่ออกว่าแบรนด์ไหน
  ⇒ **ปรับให้ดีขึ้นตามสิทธิ์ในมติ V3**: แบดจ์ 22px + โลโก้ทางการ (LINE/WhatsApp/Messenger/IG/TikTok)
  · `WEBCHAT`=โลก · `APP`=มือถือ · ทั้งหมดยังเป็น glyph สีขาวบนวงสีแบรนด์เหมือนแบบร่าง
- 🔴 **ปิดหนี้ H4**: ทะเบียนช่องทาง (ไอคอน+ป้ายไทย+สี+`isSupported`) อยู่ที่ไฟล์นี้ที่เดียว

### WO-CV2 — Schema รอบ 4 (additive)
| # | เปลี่ยน | เพื่อ |
|---|---|---|
| P1 | `ChatConversation` + `pinnedAt DateTime?` + `pinnedByUserId String?` | ปักหมุด (ระดับร้าน ไม่ใช่รายคน — ทีมเห็นตรงกัน) |
| P2 | ตารางใหม่ `ChatConversationPref` (`conversationId`+`userId` unique · `mutedUntil`) | ปิดเสียง **รายคน** — คนละเรื่องกับปักหมุด |
| P3 | `ChatMessageType` + `AUDIO` | ข้อความเสียง |
| P4 | `ChatAttachment` + `durationMs Int?` | ความยาวคลิปเสียง (แสดง 0:12 โดยไม่ต้องโหลดไฟล์) |
| P5 | `ChatQuickReply` — ไม่ต้องแก้ ใช้ของเดิมทั้งดุ้น | |
🔴 `ALTER TYPE ADD VALUE` แยก migration จากตาราง (บทเรียน 31 ส.ค.) · ตารางใหม่ต้องลงทะเบียน `core/scope.ts`

### WO-CV3 — รายการแชท
ตาม `ref-mobile.png` จอ 1 + `ref-desktop.png` คอลัมน์ซ้าย
หัวข้อ **ปักหมุด / วันนี้ / เมื่อวาน** · avatar 46px มุม 14px + แบดจ์ช่องทาง · ชิปกรอง **เพิ่ม "ยังไม่มีคนรับ"**
· แถวที่ทีมตอบล่าสุดขึ้น **"คุณ:"** + ติ๊ก · ห้องที่มีคนรับขึ้นชื่อคนรับ · ไอคอนสรุปชนิดข้อความ (รูป/เสียง)
· ห้องปิดเสียงขึ้นไอคอนกระดิ่งขีด · ห้องปิดแล้วขึ้นวงกลมติ๊ก

### WO-CV4 — ห้องแชท
หัวเหลือ **‹ · avatar · ชื่อ · บรรทัดบริบท · ⌕ · ⋮** เท่านั้น (ปุ่ม 5 ตัวเดิมย้ายเข้าเมนู ⋮ ครบ)
· เมนู ⋮ 8 รายการตามแบบร่าง · **จัดกลุ่มข้อความ** (คนเดียวกันภายใน 3 นาที = ก้อนเดียว ชื่อขึ้นครั้งเดียว)
· ฟองขาเข้าขาว/ขาออก `--out` มีหาง · ฟองรูป · **ฟองเสียง** (ปุ่มเล่น + คลื่น + เวลา) · ฟองโน้ตสีเหลืองมีป้ายกุญแจ
· ตัวคั่นวันที่แบบเม็ดยา · **กำลังพิมพ์** 3 จุด · ค้นหาในห้อง

### WO-CV5 — กล่องพิมพ์
**＋ · ช่องพิมพ์ · ไมค์ · ➤** · แผ่น ＋ 8 เครื่องมือตามแบบร่าง (รูป · ถ่ายรูป · ไฟล์ · แผนที่ร้าน · AI ช่วยร่าง · แปลก่อนส่ง · คำตอบสำเร็จรูป · โน้ตภายใน)
· **โหมดโน้ตภายใน = กล่องเปลี่ยนสีทั้งกล่อง + แถบเตือน** (ของเดิมเป็นช่องติ๊กเล็ก ๆ = พลาดง่าย)
· ปุ่มส่งเทาเมื่อว่าง ติดสีเมื่อพร้อม · พิมพ์ `/` เรียกคำตอบสำเร็จรูป · Enter ส่ง / Shift+Enter ขึ้นบรรทัด

### WO-CV6 — คำตอบสำเร็จรูป + ป้ายกำกับ (ปลุกตารางที่ตายอยู่)
CRUD คำตอบสำเร็จรูปในหน้า "เชื่อมช่องทาง" · เรียกใช้ด้วย `/` ในกล่องพิมพ์ · นับ `usageCount`
· แทนที่ตัวแปร `{{contact.name}}` `{{staff.name}}` ตามที่สคีมาเขียนไว้ · ป้ายกำกับติด/ถอดได้จากเมนู ⋮ และคอลัมน์ขวา

### WO-CV7 — คอลัมน์บริบทลูกค้า (เดสก์ท็อป)
ตาม `ref-desktop.png` คอลัมน์ขวา: โปรไฟล์ + ผูกสมาชิก · **บริบทตอนนี้** (กำลังดูหน้า/เข้ามาจาก/ทักครั้งแรก/ตอบครั้งแรกใน)
· ป้ายกำกับ · **คำตอบที่ทีมใช้บ่อยกับคำถามนี้** (ดึงจากคลัง `ChatAnswerExample` ของ WO-CW3) · ประวัติการจอง

### WO-CV8 — ข้อความเสียง
อัดด้วย `MediaRecorder` (กดค้างที่ไมค์) → อัปผ่านเส้นทางไฟล์แนบเดิม → ฟองเสียงเล่นกลับได้
🔴 **ต้องทำฝั่งลูกค้าบนเว็บ SiamDive ด้วย** ไม่งั้นทีมส่งเสียงไปแล้วลูกค้าฟังไม่ได้ = ของที่ส่งแล้วหาย
🔴 ช่องทางที่ไม่รองรับเสียงต้องบอกก่อนกด ไม่ใช่ปล่อยให้ส่งแล้ว FAILED

### WO-CV9 — Realtime (มติ V4)
- ชั้นกลาง `src/lib/realtime/` — interface เดียว + adapter ของผู้ให้บริการ + **ตกกลับไป polling ถ้าไม่มีกุญแจ**
- ใช้กับ: ข้อความใหม่เด้งทันที · **กำลังพิมพ์** · ตัวนับยังไม่อ่าน · สถานะ ✓✓
- 🔴 **ห้ามให้ realtime เป็นเงื่อนไขของความถูกต้อง** — ต่อไม่ติด/โควตาหมด ต้องยังใช้งานได้ด้วย polling เดิม
- 🔴 กุญแจเป็นของเจ้าของ (§8) · จนกว่าจะได้ ให้ทำงานในโหมด polling และข้อสอบต้องเขียวทั้ง 2 โหมด
- ⚠️ ห้ามส่งเนื้อความลูกค้าผ่านช่องทางของผู้ให้บริการภายนอกโดยไม่จำเป็น — ส่งแค่ "มีของใหม่ ไป fetch"
  (PDPA + ลดความเสี่ยงข้อมูลรั่ว · เนื้อความยังดึงจากเซิร์ฟเวอร์เราเองเสมอ)

### WO-CV10 — ปักหมุด · ปิดเสียง · ค้นหาในห้อง · ประวัติการจอง

### WO-CV11 — ข้อสอบ (เขียนก่อนโค้ด)
| ไฟล์ | คุม |
|---|---|
| `qc-chat-v2-icons.mts` | ไม่มี emoji เหลือในโมดูลแชท · ทุกไอคอนมาจากทะเบียนเดียว · stroke/viewBox ตรงโทเคน |
| `qc-chat-v2-list.mts` | หัวข้อ/ชิป/แบดจ์/ติ๊ก/ไอคอนสรุปชนิด ครบตามแบบร่าง |
| `qc-chat-v2-room.mts` | หัวเหลือ 6 ชิ้น · เมนู ⋮ ครบ 8 · จัดกลุ่ม · ฟอง 5 ชนิด · กำลังพิมพ์ |
| `qc-chat-v2-composer.mts` | แผ่น ＋ 8 ตัว · โหมดโน้ตเปลี่ยนสีทั้งกล่อง · `/` เรียกคำตอบสำเร็จรูป |
| `qc-chat-v2-quickreply.mts` | ปลุก `ChatQuickReply` · แทนตัวแปร · นับ usage · สิทธิ์ |
| `qc-chat-v2-voice.mts` | อัด/อัป/เล่น · ช่องทางไม่รองรับต้องกันก่อนส่ง · ลูกค้าฟังได้ |
| `qc-chat-v2-realtime.mts` | ไม่มีกุญแจ = ยังใช้งานได้ · ไม่ส่งเนื้อความออกนอก · กำลังพิมพ์หมดอายุเอง |
| `visual-qc-chat-v2.mts` (รันมือ) | **เทียบจอจริงกับ `ref-*.png`** — ดู §6 |

---

## §6 QC หน้าตา — "เหมือนแบบร่าง" วัดยังไงไม่ให้เป็นความรู้สึก

3 ชั้น:
1. **โครงสร้าง (อัตโนมัติ)** — ข้อสอบ grep/parse ว่ามีองค์ประกอบครบและเรียงถูก
2. **โทเคน (อัตโนมัติ)** — อ่านค่าสีจริงที่เบราว์เซอร์คำนวณ (`getComputedStyle`) แล้วเทียบกับตารางใน §2
   🔴 อ่านจากจอจริง ไม่ใช่ grep ไฟล์ CSS — คลาสที่เขียนไว้แต่ถูกทับ จะจับไม่ได้ถ้า grep
3. **สายตา (Fable ทำเอง)** — เรนเดอร์ prod แล้ววางเทียบ `ref-*.png` ทีละจอ
🔴 **ห้ามเทียบพิกเซลตรง ๆ** — ข้อมูลจริงยาวไม่เท่าข้อมูลตัวอย่าง ระยะจะไม่มีวันตรง 100%
   สิ่งที่ล็อกได้คือ **องค์ประกอบ · ลำดับ · สี · ขนาด · ระยะห่างมาตรฐาน**

---

## §7 การแบ่งงาน sub agent (เครื่อง 2 คอร์ · ขนานได้ครั้งละ 2)

| รอบ | สาย | WO | ไฟล์ที่ถือ |
|---|---|---|---|
| 1 | **A** | CV1 ไอคอน + CV2 schema | `chat/icons.tsx` · `channel-icon.tsx` · `prisma/schema/chat.prisma` · `core/scope.ts` · migrations |
| 1 | **B** | CV11 ข้อสอบ | `scripts/qc-chat-v2-*.mts` · `scripts/visual-qc-chat-v2.mts` |
| 2 | **C** | CV3 รายการ + CV10 ปักหมุด/ปิดเสียง | `inbox-client.tsx` (ส่วนรายการ) · `inbox-actions.ts` |
| 2 | **D** | CV6 คำตอบสำเร็จรูป + ป้ายกำกับ | `chat/quick-reply.ts` (ใหม่) · `chat/actions.ts` · หน้า channels |
| 3 | **E** | CV4 ห้องแชท + CV5 กล่องพิมพ์ | `inbox-client.tsx` (ส่วนห้อง) · `bubble.tsx` |
| 3 | **F** | CV7 คอลัมน์บริบท | `chat/context-panel.tsx` (ใหม่) · `inbox-actions.ts` |
| 4 | **G** | CV9 realtime | `src/lib/realtime/**` |
| 4 | **H** | CV8 ข้อความเสียง | `chat/voice.tsx` · storage · siamdive2 |

🔴 `inbox-client.tsx` ถูกแตะโดยสาย C และ E ⇒ **ห้ามพร้อมกัน** · C อยู่รอบ 2 · E อยู่รอบ 3
**หน้าที่ Fable ทุกรอบ**: ประกอบ → `typecheck` + `fitness` + `qc:all` + **`pnpm build`** → เดินจอจริง → เทียบแบบร่าง → เขียนสถานะ

---

## §8 สิ่งที่ต้องให้เจ้าของทำ (ไม่มีทางทำแทนได้)

1. **สมัคร Pusher หรือ Ably** (แพ็กฟรีพอ) แล้วส่งกุญแจมา 4 ค่า:
   `APP_ID` · `KEY` (ใช้ในเบราว์เซอร์ได้) · `SECRET` (ห้ามหลุด) · `CLUSTER/REGION`
   → ผมตั้งบน Vercel ให้เอง · **ระหว่างรอ ระบบทำงานในโหมด polling ได้ปกติ ไม่บล็อกงาน**
2. ยืนยันว่ายอมให้ผู้ให้บริการภายนอกรู้ว่า "ร้านไหนมีข้อความใหม่เมื่อไหร่" (ไม่ส่งเนื้อความ — ดู CV9)

---

## §9 สถานะสด

| WO | สถานะ |
|---|---|
| **CV1 ไอคอน · CV2 schema** | ✅ **เสร็จ 1 ก.ย.** — `icons.tsx` 33 ตัว · แบดจ์ 22px โลโก้แบรนด์ทึบ · schema P1–P4 · migration 2 ตัว · ปิดหนี้ H4 · `build` EXIT=0 |
| **CV11 ข้อสอบ** | ✅ **เสร็จ 1 ก.ย.** — 8 ชุด **234 ข้อ** (7 ชุดพักที่ `scripts/pending/` ตามมติ D5 · `visual-qc-chat-v2` 27 ข้อรันมือ) |
| **CV3 รายการ · CV10 ปักหมุด/ปิดเสียง** | ✅ **เสร็จ 1 ก.ย. (สาย C)** — `qc-chat-v2-list` **41/41** · ย้ายเข้า `scripts/` เป็นด่าน CI ถาวรแล้ว |
| **CV6 คำตอบสำเร็จรูป + ป้ายกำกับ** | ✅ **เสร็จ 1 ก.ย. (สาย D)** — `qc-chat-v2-quickreply` **28/28** · ย้ายเข้า `scripts/` แล้ว · ปลุก `ChatQuickReply` ที่เป็นตารางตายสำเร็จ |
| CV4 · CV5 | 📋 **รอบ 3 — ปล่อยได้ทันทีที่ migration ลง prod** (สาย E) |
| CV7 | 📋 **รอบ 3** (สาย F) — ต้องสร้าง `qc-chat-v2-context.mts` ตามมติ D6 |
| CV9 realtime | 📋 รอบ 4 |
| CV8 เสียง | 📋 รอบ 4 |

**บันทึกความคืบหน้า**
- 1 ก.ย. 2026 (บ่าย) — **รอบ 2 ปิด (C+D) + เจอแชทบน prod ดับเงียบ** · 3 เรื่องที่ต้องจดไว้:
  1. 🚨 **migration รอบ 1 ไม่เคยถูก apply ลง prod** ทั้งที่โค้ดถูก deploy ไปแล้ว ⇒ แชทดับตั้งแต่ 10:50 น.
     🔴 สิ่งที่ทำให้เสียหายกว้างกว่าที่ควร: `findFirst` ที่ **ไม่ระบุ `select`** ทำให้ Prisma ขอทุกคอลัมน์
     ⇒ คอลัมน์ใหม่ที่ DB ยังไม่มี ทำให้ **การอ่านตารางนั้นพังทั้งตาราง** ไม่ใช่แค่ฟีเจอร์ใหม่
     ⇒ บทเรียนเชิงออกแบบ: **schema เพิ่มคอลัมน์ = deploy โค้ดกับ apply DB ต้องเป็นขั้นเดียวกัน**
     ห้ามแยกจังหวะ · รายละเอียดครบใน `ledger/RESUME.md` บล็อกบนสุด
  2. 🔴 **ด่าน F6 (fitness) วัดผิดทางมาตลอด** — grep หาสตริง `assertCan` ในไฟล์ action ตรง ๆ
     พอยุบ `assertChatCan` ที่ซ้ำ 2 ชุดไปไว้ที่ `guard.ts` ด่านแดงทันที
     = **ด่านที่ลงโทษการยุบโค้ดซ้ำ และให้รางวัลกับการ copy-paste ตรรกะความปลอดภัย**
     แก้ให้ตามรอย import ลึก 1 ชั้นแล้วอ่านตัวฟังก์ชันปลายทาง (ไม่ใช่เติมชื่อลง allowlist)
     · พิสูจน์ fail-closed ด้วย positive control 2 ตัว · **บทเรียนเดิมรอบที่ 5** (วัดพฤติกรรม ไม่ใช่ล็อกชื่อ)
  3. ✅ ทั้งสาย C และ D **รายงานของที่ไม่ใช่ไฟล์ตัวเองแทนที่จะแก้เอง** ตามมติ V3/D7 —
     สาย C เจอ `unitAccessWhere` ถูกเขียนซ้ำ · สาย D เจอธง `planned` ค้างใน `core/permissions.ts`
     ⇒ Fable เป็นคนปิดให้ทั้งคู่ · กติกานี้ทำงานได้จริง ไม่ต้องแก้

- 1 ก.ย. 2026 — เจ้าของเคาะแบบร่าง (V1–V4) · ย้ายแบบร่างเข้ารีโปเป็นข้อสอบด้านหน้าตา
  🔴 พบว่า **`ChatQuickReply` เป็นตารางตาย** — มีสคีมาครบตั้งแต่ต้น แต่ไม่มีโค้ดไหนแตะเลยสักบรรทัด
  (เจอด้วย grep ทั้ง `src/` เหลือแค่บรรทัดลงทะเบียนใน `scope.ts`) · `tags` และ `meta.pageUrl` ก็เช่นกัน
  ⇒ 3 ฟีเจอร์ในแบบร่างได้ชั้นข้อมูลมาฟรี
- 1 ก.ย. 2026 — **CV1+CV2 เสร็จ** · 🔴 3 เรื่องที่ต้องจดไว้:
  1. **ต้นเหตุที่แบดจ์ช่องทางอ่านไม่ออกไม่ใช่ขนาด แต่คือ "เส้น stroke 2.1px ที่ 11px"**
     สาย A เรนเดอร์เทียบจริง 3 ชั้น (22px · ขยาย 4 เท่า · เทียบกับ 19px เดิม) แล้วสรุปว่า
     ตัวแปรที่ได้ผลคือเปลี่ยนเป็น **glyph ทึบ (fill)** ส่วน 22px เป็นแค่ระยะเผื่อ
     ⇒ Fable ตรวจภาพเอง ยืนยันว่าแยกออกทุกแบรนด์แล้ว
  2. 🔴 **ข้อสอบ 2 อันสั่งขัดกันเอง**: `IU-4.7` บังคับให้มี object ชื่อ `CHANNEL_LABEL_TH` ใน `service.ts`
     ซึ่ง **คือความซ้ำที่หนี้ H4 สั่งให้ยุบ** · สาย A ทำตาม H4 แล้วรายงานแทนที่จะดัดโค้ดให้เข้าข้อสอบ (ถูกต้อง)
     → Fable ย้าย IU-4.7 ไปวัดที่ทะเบียนจริง + ตรวจว่า service เรียกใช้ ไม่พิมพ์ซ้ำ
     **นี่คือรอบที่ 4 ของบทเรียนเดียวกัน: ข้อสอบต้องวัดพฤติกรรม ไม่ใช่ล็อกชื่อตัวแปร/ไฟล์**
     (ก่อนหน้านี้: ล็อก `drainAll()` · ล็อกว่า action ต้องอยู่ `actions.ts` · ล็อกว่าความสดมาจาก `<AutoRefresh>`)
  3. 🔴 **ไอคอน `history` ในแบบร่างของ Fable เองวาดผิด** — เรนเดอร์ออกมาเป็น "วงกลมมีเส้นทแยงพาด"
     = อ่านเป็น "ห้าม/ยกเลิก" ตรงข้ามกับความหมาย · สาย A รายงานแต่ไม่แก้เอง (ถูกตามมติ V3)
     → Fable แก้ทั้ง `icons.tsx` **และ `mockup.html`** แล้วเรนเดอร์ `ref-*.png` ใหม่ให้ตรงกัน
     ⇒ **แบบร่างที่เป็นข้อสอบ ต้องแก้ที่ต้นฉบับเสมอ ไม่ใช่แก้แต่โค้ด**
  ⚠️ ค้าง: emoji ใน `bubble.tsx` + `inbox-client.tsx` ยังอยู่ 12 จุด (ไฟล์ของสายรอบ 3) · ไอคอนพร้อมครบแล้ว

---

## §10 มติของ Fable หลังรอบ 1 (สายรอบ 2–4 ต้องทำตาม)

| # | เรื่อง | มติ |
|---|---|---|
| **D1** | **`meta.pageUrl` มีจริงไหม** — สาย B ท้วงว่าไม่มีบรรทัดไหนในรีโปเอ่ยคำนี้ (จริง · SHARK merge context ทั้งก้อน) | ✅ **มีจริง — พิสูจน์จากข้อมูล prod: 10 จาก 13 ห้องมี `pageUrl`** แต่เป็น **path (`"/new"`) ไม่ใช่ชื่อหน้า** · บางห้อง `null` · บางค่าเป็นขยะ (`"qc"`)<br>⇒ CV7 ต้อง **แปลง path เป็นชื่อที่อ่านออก** (ทะเบียนเส้นทางที่รู้จัก) · แปลงไม่ได้ก็แสดง path · **ไม่มีค่า = ซ่อนบรรทัดทิ้ง ห้ามโชว์ป้ายเปล่า**<br>❌ ห้ามพึ่ง `pageTitle` เพราะฝั่งลูกค้าไม่ได้ส่งมา (ไม่สร้างงานข้ามรีโปโดยไม่จำเป็น) |
| **D2** | แบบร่างมีเมนู **"แปลอัตโนมัติในห้องนี้"** แต่มติ W3 (31 ส.ค.) เคาะว่า "กดแปลเมื่อต้องการ" เพื่อคุมค่า AI | ✅ **เก็บเมนูไว้** — W3 พูดถึง **ค่าตั้งต้นของทั้งระบบ** ส่วนนี้คือ **ทีมเลือกเปิดเองเป็นรายห้อง**<br>🔴 เงื่อนไข: ปิดไว้เป็นค่าเริ่มต้น · ต้องมีข้อความบอกว่ามีค่าใช้จ่ายต่อข้อความ · ผูกสิทธิ์ `chat.translate.use` |
| **D3** | แบบร่างมีชิปกรอง 4 ตัว **หายไป "ปิดแล้ว" ที่ของเดิมมี** ⇒ ทำตามแบบร่างเป๊ะ = ทำของเดิมหาย | ✅ **"ปิดแล้ว" ย้ายไปอยู่หลังไอคอนกรวย (filter) บนหัว** — ซึ่งในแบบร่างวาดไว้แต่ยังไม่มีหน้าที่<br>กรวย = ตัวกรองเพิ่มเติม: **ปิดแล้ว · ตามช่องทาง · ตามผู้รับผิดชอบ** · ชิป 4 ตัวคือของที่ใช้บ่อยที่สุดเท่านั้น |
| **D4** | WO-CV5 บอกแถบล่างมีไมค์ แต่**เดสก์ท็อปในแบบร่างไม่มีไมค์** | ✅ **ทำตามแบบร่าง**: มือถือมีไมค์ในแถบ · เดสก์ท็อปไม่มี · **แต่เดสก์ท็อปต้องอัดเสียงได้ผ่านแผ่น ＋** (ของครบตามมติ V1 โดยหน้าตาไม่ผิดแบบร่าง) |
| **D5** | ข้อสอบ contract-first 7 ชุด จะทำ **CI แดงค้างหลายวัน** ระหว่างรอบ 2–4 | ✅ **พักไว้ที่ `scripts/pending/`** — `qc:all` ค้นเฉพาะ `scripts/` ⇒ CI ยังเป็นสัญญาณที่เชื่อได้<br>🔴 **รอบไหนเสร็จและชุดเขียวครบ → `git mv` เข้า `scripts/` ทันที** (Fable เป็นคนย้าย ไม่ใช่สายงาน)<br>⛔ ห้ามทำ "ยังไม่มีโค้ด = ข้ามแล้วเขียว" เด็ดขาด · รันมือ: `pnpm qc:v2` |
| **D6** | ไม่มีชุดไหนคุม **ป้ายกำกับ (tags) · คอลัมน์บริบท CV7 · หนี้ G11** | ✅ ป้ายกำกับ → ฝากใน `qc-chat-v2-quickreply` (สาย D รอบ 2) · CV7 + G11 → ชุดใหม่ `qc-chat-v2-context.mts` (สาย F รอบ 3) |
| **D7** | แบบร่างถูกแก้ระหว่างรอบ 1 (path ของ `i-history`) โดยไม่มีขั้นตอนรองรับ | ✅ **แก้แบบร่างได้เฉพาะ Fable** และต้องรัน `node docs/design/chat-v2/render.mjs` ให้ภาพตรงกันทุกครั้ง<br>สายงานที่เห็นว่าแบบร่างผิด → **รายงาน ห้ามแก้เอง** (สาย A ทำถูกแล้ว) |

🔴 **แผน §1.1 แก้แล้ว**: หนี้ H4 เดิมเขียนว่าลิสต์ซ้ำ 3 ที่ — ของจริงเหลือ **2 ที่** (`ui.tsx` ถูกถอดไปตั้งแต่รอบก่อน)
