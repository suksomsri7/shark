# แผน — กล่องแชท V2 "ให้เหมือนแอปแชทจริง" (ชุด WO-CV)

> เขียน 1 ก.ย. 2026 · ผู้วางแผน+QC: Fable · ผู้ลงมือ: Opus 5 (sub agent)
> 🔴 อ่าน `ledger/PLAN-CHAT-WHATSAPP.md` ก่อน — ไฟล์นี้ต่อยอดจากรอบนั้น **ไม่ทับ**
> 🔴 **แบบร่างคือข้อสอบ**: `docs/design/chat-v2/mockup.html` + `ref-mobile.png` + `ref-desktop.png`
> สถานะสดอยู่ท้ายไฟล์ (§9)

---

## §0 มติเจ้าของ 1 ก.ย. 2026 (ห้ามรื้อ)

| # | คำตัดสิน | คำพูดเจ้าของ |
|---|---|---|
| V1 | **ทำให้ครบทุกอย่าง ไม่เอาของครึ่ง ๆ** | "ทำไมเราไม่พัฒนาให้มีของครบละครับ" |
| V2 | **ไอคอนเป็นเส้นบาง minimal clean ทั้งชุด — ห้ามมี emoji** | "Icon ต้องปรับใหม่เป็น minimal clean" |
| V3 | **หน้าตาต้องเหมือนแบบร่าง ห้ามผิด · แต่ปรับให้ดีขึ้นได้** | "ต้องเหมือนในรูปห้ามผิด แต่สามารถปรับปรุงให้ดีขึ้นได้" |
| V4 | **realtime ใช้บริการสำเร็จรูป** (Pusher/Ably) ไม่เขียน SSE เอง ไม่ลง VPS | "ใช้บริการ realtime สำเร็จแบบที่คุณแนะนำ" |

🔴 **V3 แปลว่า "แบบร่างเป็นสัญญา ไม่ใช่แรงบันดาลใจ"** — สายไหนอยากเปลี่ยนจากแบบร่าง
ต้อง **รายงานเหตุผลก่อน** ไม่ใช่เปลี่ยนเงียบ ๆ · ของที่ปรับได้เองคือรายละเอียดที่แบบร่างไม่ได้พูดถึง
(เช่น สถานะ hover/disabled/จอเล็กมาก) ไม่ใช่โครงหรือองค์ประกอบที่วาดไว้แล้ว

---

## §1 สภาพจริง (สำรวจจากโค้ด 1 ก.ย. 2026)

### 1.1 ของที่ "มีข้อมูลครบแล้ว แต่ไม่มีหน้าจอ" — ได้เกือบฟรีในรอบนี้
| ของ | หลักฐาน |
|---|---|
| **คำตอบสำเร็จรูป** | ตาราง `ChatQuickReply` มีครบ (shortcut/title/body/channelTypes/usageCount) แต่ **grep ทั้ง `src/` เจอแค่บรรทัดลงทะเบียนใน `scope.ts` เท่านั้น — ไม่มีโค้ดไหนอ่านหรือเขียนเลย** |
| **ป้ายกำกับห้องแชท** | `ChatConversation.tags Json @default("[]")` — ไม่มีใครใช้ในโมดูลแชทเลย |
| **"กำลังดูหน้า…"** | `ChatConversation.meta.pageUrl` ถูกเขียนไว้แล้ว (`service.ts:948`) แต่ไม่เคยถูกแสดง |
| **SLA "ตอบครั้งแรกใน X นาที"** | `firstCustomerMessageAt` + `firstResponseAt` มีครบ |
| **สถานะส่ง 🕐/✓/✓✓/✗** | `deliveryStatus` + `markCustomerRead` มีครบ (วาดแล้วบางส่วนใน `bubble.tsx`) |

### 1.2 ของที่ต้องเพิ่มจริง
ปักหมุด · ปิดเสียงรายห้อง · ข้อความเสียง · กำลังพิมพ์ · ค้นหาในห้อง · ประวัติการจอง

### 1.3 หนี้จากรอบก่อนที่ยังค้าง (ปิดในรอบนี้)
- **H4**: ลิสต์ช่องทางพิมพ์มือซ้ำ 3 ที่ (`CHANNEL_LABEL` ใน ui.tsx · `CHAT_CHANNEL_TH` ใน ai/tools.ts · `CHANNEL_LABEL_TH` ใน chat/service.ts) → ยุบเหลือทะเบียนเดียว
- **G11**: `AppNotification` เป็น tenant-wide ไม่มีช่องผู้รับ ⇒ ตอนนี้ตัด preview ออกชั่วคราว → ปิดให้จบ

---

## §2 หน้าตา — อ่านจากแบบร่างเท่านั้น

**ไฟล์อ้างอิง (สายงานต้องเปิดดูก่อนเขียนโค้ด):**
- `docs/design/chat-v2/mockup.html` — ต้นฉบับ เปิดในเบราว์เซอร์ได้ ดู CSS จริงได้
- `docs/design/chat-v2/ref-mobile.png` — 4 จอมือถือ: รายการ · ห้องแชท · เมนู ⋮ + โหมดโน้ต · แผ่น ＋
- `docs/design/chat-v2/ref-desktop.png` — เดสก์ท็อป 3 คอลัมน์

**โทเคนที่ล็อกไว้ (คัดจาก mockup.html — ห้ามคิดใหม่):**
```
--ink #0f172a · --muted #71767f · --line #e8e9ed · --surface2 #f4f5f7
--accent #1d4ed8 · --out #e9eefc (ฟองขาออก) · --wall #f3f4f7 (พื้นหลังห้อง)
--note #fffaed / --note-line #ecd9a4 / --note-ink #8a6414 (โน้ตภายใน)
ไอคอน: viewBox 24 · stroke 1.7 (เล็ก 1.9) · round cap/join · currentColor · **ห้าม fill**
มุม: avatar 14px · ฟอง 14px (มุมติดหาง 4px) · ปุ่ม/ชิป 8–11px
```

🔴 **ห้ามใช้ emoji เป็นไอคอนอีกเด็ดขาด** (มติ V2) — ของเดิมใช้ 📎 📷 ✨ 🌐 🔒 🔔 ทั่วทั้งโมดูล

---

## §3 Work Order

### WO-CV1 — ระบบไอคอน (ฐานของทุกสาย ต้องเสร็จก่อน)
**ไฟล์:** `src/lib/modules/chat/icons.tsx` (ใหม่) · แก้ `channel-icon.tsx`
- SVG sprite ตาม §2 · export `<Icon name="…"/>` ชุดเดียว ~28 ตัว (ชื่อครบใน mockup.html `<symbol id="i-*">`)
- ช่องทาง: **ใช้โลโก้แบรนด์จริง** ไม่ใช่เส้นที่วาดเอง — แบดจ์ 19px ในแบบร่างอ่านไม่ออกว่าแบรนด์ไหน
  ⇒ **ปรับให้ดีขึ้นตามสิทธิ์ในมติ V3**: แบดจ์ 22px + โลโก้ทางการ (LINE/WhatsApp/Messenger/IG/TikTok)
  · `WEBCHAT`=โลก · `APP`=มือถือ · ทั้งหมดยังเป็น glyph สีขาวบนวงสีแบรนด์เหมือนแบบร่าง
- 🔴 **ปิดหนี้ H4**: ทะเบียนช่องทาง (ไอคอน+ป้ายไทย+สี+`isSupported`) อยู่ที่ไฟล์นี้ที่เดียว

### WO-CV2 — Schema รอบ 4 (additive)
| # | เปลี่ยน | เพื่อ |
|---|---|---|
| P1 | `ChatConversation` + `pinnedAt DateTime?` + `pinnedByUserId String?` | ปักหมุด (ระดับร้าน ไม่ใช่รายคน — ทีมเห็นตรงกัน) |
| P2 | ตารางใหม่ `ChatConversationPref` (`conversationId`+`userId` unique · `mutedUntil`) | ปิดเสียง **รายคน** — คนละเรื่องกับปักหมุด |
| P3 | `ChatMessageType` + `AUDIO` | ข้อความเสียง |
| P4 | `ChatAttachment` + `durationMs Int?` | ความยาวคลิปเสียง (แสดง 0:12 โดยไม่ต้องโหลดไฟล์) |
| P5 | `ChatQuickReply` — ไม่ต้องแก้ ใช้ของเดิมทั้งดุ้น | |
🔴 `ALTER TYPE ADD VALUE` แยก migration จากตาราง (บทเรียน 31 ส.ค.) · ตารางใหม่ต้องลงทะเบียน `core/scope.ts`

### WO-CV3 — รายการแชท
ตาม `ref-mobile.png` จอ 1 + `ref-desktop.png` คอลัมน์ซ้าย
หัวข้อ **ปักหมุด / วันนี้ / เมื่อวาน** · avatar 46px มุม 14px + แบดจ์ช่องทาง · ชิปกรอง **เพิ่ม "ยังไม่มีคนรับ"**
· แถวที่ทีมตอบล่าสุดขึ้น **"คุณ:"** + ติ๊ก · ห้องที่มีคนรับขึ้นชื่อคนรับ · ไอคอนสรุปชนิดข้อความ (รูป/เสียง)
· ห้องปิดเสียงขึ้นไอคอนกระดิ่งขีด · ห้องปิดแล้วขึ้นวงกลมติ๊ก

### WO-CV4 — ห้องแชท
หัวเหลือ **‹ · avatar · ชื่อ · บรรทัดบริบท · ⌕ · ⋮** เท่านั้น (ปุ่ม 5 ตัวเดิมย้ายเข้าเมนู ⋮ ครบ)
· เมนู ⋮ 8 รายการตามแบบร่าง · **จัดกลุ่มข้อความ** (คนเดียวกันภายใน 3 นาที = ก้อนเดียว ชื่อขึ้นครั้งเดียว)
· ฟองขาเข้าขาว/ขาออก `--out` มีหาง · ฟองรูป · **ฟองเสียง** (ปุ่มเล่น + คลื่น + เวลา) · ฟองโน้ตสีเหลืองมีป้ายกุญแจ
· ตัวคั่นวันที่แบบเม็ดยา · **กำลังพิมพ์** 3 จุด · ค้นหาในห้อง

### WO-CV5 — กล่องพิมพ์
**＋ · ช่องพิมพ์ · ไมค์ · ➤** · แผ่น ＋ 8 เครื่องมือตามแบบร่าง (รูป · ถ่ายรูป · ไฟล์ · แผนที่ร้าน · AI ช่วยร่าง · แปลก่อนส่ง · คำตอบสำเร็จรูป · โน้ตภายใน)
· **โหมดโน้ตภายใน = กล่องเปลี่ยนสีทั้งกล่อง + แถบเตือน** (ของเดิมเป็นช่องติ๊กเล็ก ๆ = พลาดง่าย)
· ปุ่มส่งเทาเมื่อว่าง ติดสีเมื่อพร้อม · พิมพ์ `/` เรียกคำตอบสำเร็จรูป · Enter ส่ง / Shift+Enter ขึ้นบรรทัด

### WO-CV6 — คำตอบสำเร็จรูป + ป้ายกำกับ (ปลุกตารางที่ตายอยู่)
CRUD คำตอบสำเร็จรูปในหน้า "เชื่อมช่องทาง" · เรียกใช้ด้วย `/` ในกล่องพิมพ์ · นับ `usageCount`
· แทนที่ตัวแปร `{{contact.name}}` `{{staff.name}}` ตามที่สคีมาเขียนไว้ · ป้ายกำกับติด/ถอดได้จากเมนู ⋮ และคอลัมน์ขวา

### WO-CV7 — คอลัมน์บริบทลูกค้า (เดสก์ท็อป)
ตาม `ref-desktop.png` คอลัมน์ขวา: โปรไฟล์ + ผูกสมาชิก · **บริบทตอนนี้** (กำลังดูหน้า/เข้ามาจาก/ทักครั้งแรก/ตอบครั้งแรกใน)
· ป้ายกำกับ · **คำตอบที่ทีมใช้บ่อยกับคำถามนี้** (ดึงจากคลัง `ChatAnswerExample` ของ WO-CW3) · ประวัติการจอง

### WO-CV8 — ข้อความเสียง
อัดด้วย `MediaRecorder` (กดค้างที่ไมค์) → อัปผ่านเส้นทางไฟล์แนบเดิม → ฟองเสียงเล่นกลับได้
🔴 **ต้องทำฝั่งลูกค้าบนเว็บ SiamDive ด้วย** ไม่งั้นทีมส่งเสียงไปแล้วลูกค้าฟังไม่ได้ = ของที่ส่งแล้วหาย
🔴 ช่องทางที่ไม่รองรับเสียงต้องบอกก่อนกด ไม่ใช่ปล่อยให้ส่งแล้ว FAILED

### WO-CV9 — Realtime (มติ V4)
- ชั้นกลาง `src/lib/realtime/` — interface เดียว + adapter ของผู้ให้บริการ + **ตกกลับไป polling ถ้าไม่มีกุญแจ**
- ใช้กับ: ข้อความใหม่เด้งทันที · **กำลังพิมพ์** · ตัวนับยังไม่อ่าน · สถานะ ✓✓
- 🔴 **ห้ามให้ realtime เป็นเงื่อนไขของความถูกต้อง** — ต่อไม่ติด/โควตาหมด ต้องยังใช้งานได้ด้วย polling เดิม
- 🔴 กุญแจเป็นของเจ้าของ (§8) · จนกว่าจะได้ ให้ทำงานในโหมด polling และข้อสอบต้องเขียวทั้ง 2 โหมด
- ⚠️ ห้ามส่งเนื้อความลูกค้าผ่านช่องทางของผู้ให้บริการภายนอกโดยไม่จำเป็น — ส่งแค่ "มีของใหม่ ไป fetch"
  (PDPA + ลดความเสี่ยงข้อมูลรั่ว · เนื้อความยังดึงจากเซิร์ฟเวอร์เราเองเสมอ)

### WO-CV10 — ปักหมุด · ปิดเสียง · ค้นหาในห้อง · ประวัติการจอง

### WO-CV12 — หน้าแชท "แบบตัด" (เจ้าของเคาะ 1 ก.ย. ค่ำ · ภาพ `proposal-mobile-header.png` + `proposal-desktop-header.png`)
"ส่วนที่ผมวงกลมและ SIAM DIVE CENTER เอาออก และย้ายแถบบาร์ แชทลูกค้า และเครื่องมือด้านขวาขึ้นไปแทน" + เดสก์ท็อป "แบบตัด"
- **มือถือ**: ไม่มีหัวแอป/ชื่อหน้า/แท็บ · แถบ `☰ · แชทลูกค้า · กรวย · ⋮` เป็นหัวจอ (= จอ 1 แบบร่าง) · ☰ เปิด drawer ของแอปผ่าน CustomEvent `app:drawer-open`
- **เดสก์ท็อป**: คงแถบบน + sidebar · ตัดชื่อหน้า/คำอธิบาย/แท็บ · กล่องแชทชิดใต้แถบบน (+17% พื้นที่)
- "เชื่อมช่องทาง" ที่เคยเป็นแท็บ → เมนู ⋮ ของรายการ (+ คำตอบสำเร็จรูป · ห้องที่ปิดแล้ว · จัดการการเชื่อมระบบ เมื่อหลายสาขา) · `chatTabs()` คงไว้ให้เมนูซ้าย
- กลไก: AppShell ตัดสิน `chatFullscreen` จาก `chatSystemIds` + pathname → Topbar `hidden lg:block` · AppMain ลด padding-top · โมดูลแชทห้าม import app-shell
- ข้อสอบ: `qc-chat-v2-shell.mts` (26 ข้อ · อยู่ใน `scripts/` เลย ไม่พัก pending) + `visual-qc-chat-v2` VR-6.1–6.5 (กด ☰ จริงบนมือถือ)

### WO-CV11 — ข้อสอบ (เขียนก่อนโค้ด)
| ไฟล์ | คุม |
|---|---|
| `qc-chat-v2-icons.mts` | ไม่มี emoji เหลือในโมดูลแชท · ทุกไอคอนมาจากทะเบียนเดียว · stroke/viewBox ตรงโทเคน |
| `qc-chat-v2-list.mts` | หัวข้อ/ชิป/แบดจ์/ติ๊ก/ไอคอนสรุปชนิด ครบตามแบบร่าง |
| `qc-chat-v2-room.mts` | หัวเหลือ 6 ชิ้น · เมนู ⋮ ครบ 8 · จัดกลุ่ม · ฟอง 5 ชนิด · กำลังพิมพ์ |
| `qc-chat-v2-composer.mts` | แผ่น ＋ 8 ตัว · โหมดโน้ตเปลี่ยนสีทั้งกล่อง · `/` เรียกคำตอบสำเร็จรูป |
| `qc-chat-v2-quickreply.mts` | ปลุก `ChatQuickReply` · แทนตัวแปร · นับ usage · สิทธิ์ |
| `qc-chat-v2-voice.mts` | อัด/อัป/เล่น · ช่องทางไม่รองรับต้องกันก่อนส่ง · ลูกค้าฟังได้ |
| `qc-chat-v2-realtime.mts` | ไม่มีกุญแจ = ยังใช้งานได้ · ไม่ส่งเนื้อความออกนอก · กำลังพิมพ์หมดอายุเอง |
| `visual-qc-chat-v2.mts` (รันมือ) | **เทียบจอจริงกับ `ref-*.png`** — ดู §6 |

---

## §6 QC หน้าตา — "เหมือนแบบร่าง" วัดยังไงไม่ให้เป็นความรู้สึก

3 ชั้น:
1. **โครงสร้าง (อัตโนมัติ)** — ข้อสอบ grep/parse ว่ามีองค์ประกอบครบและเรียงถูก
2. **โทเคน (อัตโนมัติ)** — อ่านค่าสีจริงที่เบราว์เซอร์คำนวณ (`getComputedStyle`) แล้วเทียบกับตารางใน §2
   🔴 อ่านจากจอจริง ไม่ใช่ grep ไฟล์ CSS — คลาสที่เขียนไว้แต่ถูกทับ จะจับไม่ได้ถ้า grep
3. **สายตา (Fable ทำเอง)** — เรนเดอร์ prod แล้ววางเทียบ `ref-*.png` ทีละจอ
🔴 **ห้ามเทียบพิกเซลตรง ๆ** — ข้อมูลจริงยาวไม่เท่าข้อมูลตัวอย่าง ระยะจะไม่มีวันตรง 100%
   สิ่งที่ล็อกได้คือ **องค์ประกอบ · ลำดับ · สี · ขนาด · ระยะห่างมาตรฐาน**

---

## §7 การแบ่งงาน sub agent (เครื่อง 2 คอร์ · ขนานได้ครั้งละ 2)

| รอบ | สาย | WO | ไฟล์ที่ถือ |
|---|---|---|---|
| 1 | **A** | CV1 ไอคอน + CV2 schema | `chat/icons.tsx` · `channel-icon.tsx` · `prisma/schema/chat.prisma` · `core/scope.ts` · migrations |
| 1 | **B** | CV11 ข้อสอบ | `scripts/qc-chat-v2-*.mts` · `scripts/visual-qc-chat-v2.mts` |
| 2 | **C** | CV3 รายการ + CV10 ปักหมุด/ปิดเสียง | `inbox-client.tsx` (ส่วนรายการ) · `inbox-actions.ts` |
| 2 | **D** | CV6 คำตอบสำเร็จรูป + ป้ายกำกับ | `chat/quick-reply.ts` (ใหม่) · `chat/actions.ts` · หน้า channels |
| 3 | **E** | CV4 ห้องแชท + CV5 กล่องพิมพ์ | `inbox-client.tsx` (ส่วนห้อง) · `bubble.tsx` |
| 3 | **F** | CV7 คอลัมน์บริบท | `chat/context-panel.tsx` (ใหม่) · `inbox-actions.ts` |
| 4 | **G** | CV9 realtime | `src/lib/realtime/**` |
| 4 | **H** | CV8 ข้อความเสียง | `chat/voice.tsx` · storage · siamdive2 |

🔴 `inbox-client.tsx` ถูกแตะโดยสาย C และ E ⇒ **ห้ามพร้อมกัน** · C อยู่รอบ 2 · E อยู่รอบ 3
**หน้าที่ Fable ทุกรอบ**: ประกอบ → `typecheck` + `fitness` + `qc:all` + **`pnpm build`** → เดินจอจริง → เทียบแบบร่าง → เขียนสถานะ

---

## §8 สิ่งที่ต้องให้เจ้าของทำ (ไม่มีทางทำแทนได้)

1. **สมัคร Pusher หรือ Ably** (แพ็กฟรีพอ) แล้วส่งกุญแจมา 4 ค่า:
   `APP_ID` · `KEY` (ใช้ในเบราว์เซอร์ได้) · `SECRET` (ห้ามหลุด) · `CLUSTER/REGION`
   → ผมตั้งบน Vercel ให้เอง · **ระหว่างรอ ระบบทำงานในโหมด polling ได้ปกติ ไม่บล็อกงาน**
2. ยืนยันว่ายอมให้ผู้ให้บริการภายนอกรู้ว่า "ร้านไหนมีข้อความใหม่เมื่อไหร่" (ไม่ส่งเนื้อความ — ดู CV9)

---

## §9 สถานะสด

| WO | สถานะ |
|---|---|
| **CV1 ไอคอน · CV2 schema** | ✅ **เสร็จ 1 ก.ย.** — `icons.tsx` 33 ตัว · แบดจ์ 22px โลโก้แบรนด์ทึบ · schema P1–P4 · migration 2 ตัว · ปิดหนี้ H4 · `build` EXIT=0 |
| **CV11 ข้อสอบ** | ✅ **เสร็จ 1 ก.ย.** — 8 ชุด **234 ข้อ** (7 ชุดพักที่ `scripts/pending/` ตามมติ D5 · `visual-qc-chat-v2` 27 ข้อรันมือ) |
| **CV3 รายการ · CV10 ปักหมุด/ปิดเสียง** | ✅ **เสร็จ 1 ก.ย. (สาย C)** — `qc-chat-v2-list` **41/41** · ย้ายเข้า `scripts/` เป็นด่าน CI ถาวรแล้ว |
| **CV6 คำตอบสำเร็จรูป + ป้ายกำกับ** | ✅ **เสร็จ 1 ก.ย. (สาย D)** — `qc-chat-v2-quickreply` **28/28** · ย้ายเข้า `scripts/` แล้ว · ปลุก `ChatQuickReply` ที่เป็นตารางตายสำเร็จ |
| **CV4 ห้องแชท · CV5 กล่องพิมพ์** | ✅ **เสร็จ 1 ก.ย. (สาย E)** — room **42/42** · composer **37/37** · icons **35/35** (emoji หมดทั้งโมดูล) · ไฟล์ใหม่ `composer.tsx` `room-actions.ts` `page-label.ts` |
| **CV7 คอลัมน์บริบท + G11** | ✅ **เสร็จ 1 ก.ย. (สาย F)** — context **53/53** · `AppNotification.recipientUserId` (migration additive) · คืน preview ข้อความให้ศูนย์แจ้งเตือนแล้วเพราะกรองผู้รับได้ |
| **CV9 realtime** | ✅ **เสร็จ 1 ก.ย. (สาย G)** — `qc-chat-v2-realtime` **26/26 ทั้ง 2 โหมด** · Ably REST ผ่าน fetch · token route ผ่าน `requireChatRead` · ตัดสินโหมดตอนรัน · payload ตัดเหลือตัวชี้ (PDPA) · D15 autoTranslate ขาเข้าต่อแล้ว · **รอเจ้าของตั้ง `ABLY_API_KEY`** (`docs/REALTIME-SETUP.md`) — ระหว่างนี้ polling |
| **CV12 หน้าแชทแบบตัด** | ✅ **เสร็จ 1 ก.ย. ค่ำ (สาย I)** — `qc-chat-v2-shell` **26/26** · มือถือ: หัวแอป/ชื่อหน้า/แท็บหาย · ☰ ในแถบแชทเปิด drawer ผ่าน `app:drawer-open` · เดสก์ท็อป: ตัดชื่อหน้า/แท็บ คงแถบบน+sidebar · orb ซ่อนบนมือถือ (D26) · รอ QC สายตา VR-6 บน prod |
| **CV8 ข้อความเสียง** | ✅ **เสร็จ 1 ก.ย. (สาย H)** — `qc-chat-v2-voice` **25/25** · อัด (webm/opus → mp4 Safari · เพดาน 120 วิ) → อัปเส้นทางเดิม (mime+นามสกุลถูก) → `sendReply` กัน capability **ก่อน** create → API v1 + webhook พา `durationMs` → siamdive2 `BookingChat` เล่นได้ทั้งโหมด shark/dual · **LINE = `audio:false` ตามจริง** (รับแค่ m4a ยังไม่มี transcoder) · ⚠️ ยังไม่พิสูจน์การอัดจริงในเบราว์เซอร์ |

**บันทึกความคืบหน้า**
- 1 ก.ย. 2026 (ดึก) — **รอบ 4 ปิด (G+H)** · ข้อสอบ V2 ครบ **8 ชุดเป็นด่าน CI ถาวรแล้ว** (list 41 · quickreply 28 · room 42 · composer 37 · icons 35 · context 53 · realtime 26 · voice 25 = **287 ข้อ**) · `scripts/pending/` ว่าง
  · ข้อสอบเก่าที่ต้องขยายเพราะสัญญาโตขึ้น (additive): core-v2 XC-3.2 · api-v1 CA-4.3 · Fable ทำ D20 (ฟองกำลังพิมพ์บอกว่าใคร) + D19 เอง
  · **ค้างรอเจ้าของ**: ตั้ง `ABLY_API_KEY` บน Vercel (`docs/REALTIME-SETUP.md`) — ไม่ตั้งก็ใช้ได้แบบ polling
  · **ค้างงานถัดไป** (เข้าคิว ไม่ใช่ของรอบนี้): ลูกค้า subscribe realtime + typing ฝั่งลูกค้า (ข้ามรีโป) · transcoder webm→m4a ให้ LINE ส่งเสียงได้ · ลบไฟล์จริงบน Bunny ตอน retention (หนี้ WO-C12) · ลูกค้าอัดเสียงขาเข้า · ปิด D16 ครึ่งหลัง (ถอด `loadRoomContextAction` + ส่ง `channel` เข้า composer) · ไอคอน `pause`/`stop` · หน้าตั้งค่าที่อยู่สาขา (D14) · แก้ `strip()` ที่ราก (D24)
- 1 ก.ย. 2026 (ค่ำ) — **QC สายตาบน prod รอบแรก (visual-qc-chat-v2 กับ https://shark.in.th)**: 24/35
  โครงใหญ่ตรงแบบร่างหมด (หัวห้อง 6 · เมนู ⋮ 8 เรียงถูก · ชิป 4 · คอลัมน์ 3 = 280px · ฟองขาว · ไม่ล้น 390px · กดจริงได้)
  🔴 ที่แดง 11 ข้อ **มี 8 ข้อเป็นไม้บรรทัดผิดเอง** ไม่ใช่โค้ด:
  · เลือก "ห้องล่าสุด" ซึ่งเป็นห้องทดสอบที่มีแค่ "u" ขาเข้า ⇒ ฟองขาออก/โน้ตไม่มีให้วัด (6 ข้อ) → เลือกห้องที่มีขาออก+โน้ตจริง
  · วัดปุ่มส่งตอนช่องว่าง ⇒ เทาตามสเปค → พิมพ์ก่อนวัด · วัด `svg` ตัวแรกของหน้า = sidebar ของแอป (w/h 0) → วัดของแชท
  · กด ＋ ตอนเมนู ⋮ ยังเปิด ⇒ คลิกโดนฉากหลัง ปิดเมนูแทน → กด Escape ก่อน
  · `__name is not defined` — tsx ห่อฟังก์ชันใน `page.evaluate` ด้วย helper ที่เบราว์เซอร์ไม่มี → ฉีด shim
  ✅ ที่เป็นโค้ดจริง (แก้แล้ว): hint "Enter = ส่ง" โผล่บนมือถือทั้งที่แบบร่างไม่มี · ชิป AI/แปล บนเดสก์ท็อป**หายไป**เมื่อยังไม่เปิดใช้
  (แบบร่างมี 4 เสมอ → ปิดปุ่ม+บอกที่เปิด เหมือนแผ่น ＋) · บรรทัดสรุปคอลัมน์บริบทโชว์ "th" แทน "ไทย" (ใช้ `langLabelTh` ของตัวแปล)
  · ที่ตั้งใจให้ต่างจากแบบร่าง (มีเหตุผล): ชิป "ผูกกับสมาชิก" ซ่อนเมื่อร้าน**ยังไม่เชื่อมระบบสมาชิก** (กดแล้วไม่มีอะไรให้ทำ — F ให้เหตุผลไว้ในโค้ด)
  🔴 บทเรียน: **ไม้บรรทัดสายตาต้องเลือก "ฉากที่มีของครบ" เอง** — ห้องจริงบน prod ส่วนใหญ่ไม่มีโน้ต/เสียง ⇒ วัดกับห้องสุ่ม = แดงหลอก
- 1 ก.ย. 2026 (เย็น) — **รอบ 3 ปิด (E+F)** · typecheck 0 · fitness 17/17 · ข้อสอบ V2 ทั้ง 6 ชุดเขียว (room 42 · composer 37 · icons 35 · context 53 · list 41 · quickreply 28)
  🔴 ข้อสอบเก่าที่ต้องแก้เพราะขัดมติใหม่ 2 จุด (แก้ที่ข้อสอบ ไม่ใช่โค้ด): `qc-chat-inbox-ui` IU-5.x ล็อกลิสต์ไฟล์ (ไม่รู้จัก composer.tsx) · IU-7.1 บังคับตัวอักษร ✓✗ ที่มติ V2 ห้าม
  · สายงานทั้ง 2 รายงานความไม่ตรง 15 จุดรวมกัน โดยไม่ดัดโค้ดเข้าข้อสอบ — กติกา "รายงาน ห้ามแก้เอง" ทำงานได้จริง (มติ D8–D16)
  · ค้างจากรอบนี้ที่ไปทำรอบ 4: ไมค์เดสก์ท็อป (D13) · autoTranslate ขาเข้า (D15) · ยุบ loadRoomContextAction (D16) · ไอคอน `pause` ยังไม่มีในทะเบียน
- 1 ก.ย. 2026 (บ่าย) — **รอบ 2 ปิด (C+D) + เจอแชทบน prod ดับเงียบ** · 3 เรื่องที่ต้องจดไว้:
  1. 🚨 **migration รอบ 1 ไม่เคยถูก apply ลง prod** ทั้งที่โค้ดถูก deploy ไปแล้ว ⇒ แชทดับตั้งแต่ 10:50 น.
     🔴 สิ่งที่ทำให้เสียหายกว้างกว่าที่ควร: `findFirst` ที่ **ไม่ระบุ `select`** ทำให้ Prisma ขอทุกคอลัมน์
     ⇒ คอลัมน์ใหม่ที่ DB ยังไม่มี ทำให้ **การอ่านตารางนั้นพังทั้งตาราง** ไม่ใช่แค่ฟีเจอร์ใหม่
     ⇒ บทเรียนเชิงออกแบบ: **schema เพิ่มคอลัมน์ = deploy โค้ดกับ apply DB ต้องเป็นขั้นเดียวกัน**
     ห้ามแยกจังหวะ · รายละเอียดครบใน `ledger/RESUME.md` บล็อกบนสุด
  2. 🔴 **ด่าน F6 (fitness) วัดผิดทางมาตลอด** — grep หาสตริง `assertCan` ในไฟล์ action ตรง ๆ
     พอยุบ `assertChatCan` ที่ซ้ำ 2 ชุดไปไว้ที่ `guard.ts` ด่านแดงทันที
     = **ด่านที่ลงโทษการยุบโค้ดซ้ำ และให้รางวัลกับการ copy-paste ตรรกะความปลอดภัย**
     แก้ให้ตามรอย import ลึก 1 ชั้นแล้วอ่านตัวฟังก์ชันปลายทาง (ไม่ใช่เติมชื่อลง allowlist)
     · พิสูจน์ fail-closed ด้วย positive control 2 ตัว · **บทเรียนเดิมรอบที่ 5** (วัดพฤติกรรม ไม่ใช่ล็อกชื่อ)
  3. ✅ ทั้งสาย C และ D **รายงานของที่ไม่ใช่ไฟล์ตัวเองแทนที่จะแก้เอง** ตามมติ V3/D7 —
     สาย C เจอ `unitAccessWhere` ถูกเขียนซ้ำ · สาย D เจอธง `planned` ค้างใน `core/permissions.ts`
     ⇒ Fable เป็นคนปิดให้ทั้งคู่ · กติกานี้ทำงานได้จริง ไม่ต้องแก้

- 1 ก.ย. 2026 — เจ้าของเคาะแบบร่าง (V1–V4) · ย้ายแบบร่างเข้ารีโปเป็นข้อสอบด้านหน้าตา
  🔴 พบว่า **`ChatQuickReply` เป็นตารางตาย** — มีสคีมาครบตั้งแต่ต้น แต่ไม่มีโค้ดไหนแตะเลยสักบรรทัด
  (เจอด้วย grep ทั้ง `src/` เหลือแค่บรรทัดลงทะเบียนใน `scope.ts`) · `tags` และ `meta.pageUrl` ก็เช่นกัน
  ⇒ 3 ฟีเจอร์ในแบบร่างได้ชั้นข้อมูลมาฟรี
- 1 ก.ย. 2026 — **CV1+CV2 เสร็จ** · 🔴 3 เรื่องที่ต้องจดไว้:
  1. **ต้นเหตุที่แบดจ์ช่องทางอ่านไม่ออกไม่ใช่ขนาด แต่คือ "เส้น stroke 2.1px ที่ 11px"**
     สาย A เรนเดอร์เทียบจริง 3 ชั้น (22px · ขยาย 4 เท่า · เทียบกับ 19px เดิม) แล้วสรุปว่า
     ตัวแปรที่ได้ผลคือเปลี่ยนเป็น **glyph ทึบ (fill)** ส่วน 22px เป็นแค่ระยะเผื่อ
     ⇒ Fable ตรวจภาพเอง ยืนยันว่าแยกออกทุกแบรนด์แล้ว
  2. 🔴 **ข้อสอบ 2 อันสั่งขัดกันเอง**: `IU-4.7` บังคับให้มี object ชื่อ `CHANNEL_LABEL_TH` ใน `service.ts`
     ซึ่ง **คือความซ้ำที่หนี้ H4 สั่งให้ยุบ** · สาย A ทำตาม H4 แล้วรายงานแทนที่จะดัดโค้ดให้เข้าข้อสอบ (ถูกต้อง)
     → Fable ย้าย IU-4.7 ไปวัดที่ทะเบียนจริง + ตรวจว่า service เรียกใช้ ไม่พิมพ์ซ้ำ
     **นี่คือรอบที่ 4 ของบทเรียนเดียวกัน: ข้อสอบต้องวัดพฤติกรรม ไม่ใช่ล็อกชื่อตัวแปร/ไฟล์**
     (ก่อนหน้านี้: ล็อก `drainAll()` · ล็อกว่า action ต้องอยู่ `actions.ts` · ล็อกว่าความสดมาจาก `<AutoRefresh>`)
  3. 🔴 **ไอคอน `history` ในแบบร่างของ Fable เองวาดผิด** — เรนเดอร์ออกมาเป็น "วงกลมมีเส้นทแยงพาด"
     = อ่านเป็น "ห้าม/ยกเลิก" ตรงข้ามกับความหมาย · สาย A รายงานแต่ไม่แก้เอง (ถูกตามมติ V3)
     → Fable แก้ทั้ง `icons.tsx` **และ `mockup.html`** แล้วเรนเดอร์ `ref-*.png` ใหม่ให้ตรงกัน
     ⇒ **แบบร่างที่เป็นข้อสอบ ต้องแก้ที่ต้นฉบับเสมอ ไม่ใช่แก้แต่โค้ด**
  ⚠️ ค้าง: emoji ใน `bubble.tsx` + `inbox-client.tsx` ยังอยู่ 12 จุด (ไฟล์ของสายรอบ 3) · ไอคอนพร้อมครบแล้ว

---

## §10 มติของ Fable หลังรอบ 1 (สายรอบ 2–4 ต้องทำตาม)

| # | เรื่อง | มติ |
|---|---|---|
| **D1** | **`meta.pageUrl` มีจริงไหม** — สาย B ท้วงว่าไม่มีบรรทัดไหนในรีโปเอ่ยคำนี้ (จริง · SHARK merge context ทั้งก้อน) | ✅ **มีจริง — พิสูจน์จากข้อมูล prod: 10 จาก 13 ห้องมี `pageUrl`** แต่เป็น **path (`"/new"`) ไม่ใช่ชื่อหน้า** · บางห้อง `null` · บางค่าเป็นขยะ (`"qc"`)<br>⇒ CV7 ต้อง **แปลง path เป็นชื่อที่อ่านออก** (ทะเบียนเส้นทางที่รู้จัก) · แปลงไม่ได้ก็แสดง path · **ไม่มีค่า = ซ่อนบรรทัดทิ้ง ห้ามโชว์ป้ายเปล่า**<br>❌ ห้ามพึ่ง `pageTitle` เพราะฝั่งลูกค้าไม่ได้ส่งมา (ไม่สร้างงานข้ามรีโปโดยไม่จำเป็น) |
| **D2** | แบบร่างมีเมนู **"แปลอัตโนมัติในห้องนี้"** แต่มติ W3 (31 ส.ค.) เคาะว่า "กดแปลเมื่อต้องการ" เพื่อคุมค่า AI | ✅ **เก็บเมนูไว้** — W3 พูดถึง **ค่าตั้งต้นของทั้งระบบ** ส่วนนี้คือ **ทีมเลือกเปิดเองเป็นรายห้อง**<br>🔴 เงื่อนไข: ปิดไว้เป็นค่าเริ่มต้น · ต้องมีข้อความบอกว่ามีค่าใช้จ่ายต่อข้อความ · ผูกสิทธิ์ `chat.translate.use` |
| **D3** | แบบร่างมีชิปกรอง 4 ตัว **หายไป "ปิดแล้ว" ที่ของเดิมมี** ⇒ ทำตามแบบร่างเป๊ะ = ทำของเดิมหาย | ✅ **"ปิดแล้ว" ย้ายไปอยู่หลังไอคอนกรวย (filter) บนหัว** — ซึ่งในแบบร่างวาดไว้แต่ยังไม่มีหน้าที่<br>กรวย = ตัวกรองเพิ่มเติม: **ปิดแล้ว · ตามช่องทาง · ตามผู้รับผิดชอบ** · ชิป 4 ตัวคือของที่ใช้บ่อยที่สุดเท่านั้น |
| **D4** | WO-CV5 บอกแถบล่างมีไมค์ แต่**เดสก์ท็อปในแบบร่างไม่มีไมค์** | ✅ **ทำตามแบบร่าง**: มือถือมีไมค์ในแถบ · เดสก์ท็อปไม่มี · **แต่เดสก์ท็อปต้องอัดเสียงได้ผ่านแผ่น ＋** (ของครบตามมติ V1 โดยหน้าตาไม่ผิดแบบร่าง) |
| **D5** | ข้อสอบ contract-first 7 ชุด จะทำ **CI แดงค้างหลายวัน** ระหว่างรอบ 2–4 | ✅ **พักไว้ที่ `scripts/pending/`** — `qc:all` ค้นเฉพาะ `scripts/` ⇒ CI ยังเป็นสัญญาณที่เชื่อได้<br>🔴 **รอบไหนเสร็จและชุดเขียวครบ → `git mv` เข้า `scripts/` ทันที** (Fable เป็นคนย้าย ไม่ใช่สายงาน)<br>⛔ ห้ามทำ "ยังไม่มีโค้ด = ข้ามแล้วเขียว" เด็ดขาด · รันมือ: `pnpm qc:v2` |
| **D6** | ไม่มีชุดไหนคุม **ป้ายกำกับ (tags) · คอลัมน์บริบท CV7 · หนี้ G11** | ✅ ป้ายกำกับ → ฝากใน `qc-chat-v2-quickreply` (สาย D รอบ 2) · CV7 + G11 → ชุดใหม่ `qc-chat-v2-context.mts` (สาย F รอบ 3) |
| **D7** | แบบร่างถูกแก้ระหว่างรอบ 1 (path ของ `i-history`) โดยไม่มีขั้นตอนรองรับ | ✅ **แก้แบบร่างได้เฉพาะ Fable** และต้องรัน `node docs/design/chat-v2/render.mjs` ให้ภาพตรงกันทุกครั้ง<br>สายงานที่เห็นว่าแบบร่างผิด → **รายงาน ห้ามแก้เอง** (สาย A ทำถูกแล้ว) |

| **D8** | โทเคนสี §2 (จากแบบร่าง `#71767f/#e8e9ed`) ≠ ตัวแปรใน `globals.css` (`--color-muted #737373` / `#e5e5e5`) — สาย F ถามว่ายึดอะไร | ✅ **โมดูลแชท V2 ยึด §2 (แบบร่างคือสัญญา — มติ V3)** · ค่าต่างกันระดับที่ตามองไม่ออก จึงไม่ไปแตะตัวแปรกลางของทั้งแอป · `visual-qc-chat-v2` วัดค่าจาก §2 อยู่แล้ว · ไฟล์ในโมดูลที่ยังใช้ `var(--color-muted)` (tags-ui 5 จุด · inbox-client 2 จุด) ให้ทยอยเปลี่ยนเป็นค่า §2 ตอนสายที่ถือไฟล์นั้นแตะครั้งถัดไป — ไม่ตั้งเป็นงานแยก |
| **D9** | G11: `AppNotification` เขียนรายผู้รับ ทำให้ `qc-chat-notify-v2` NV-6.2 (`=== 1 แถว`) แดงทั้งที่ถูก · และ fake prisma ของ `qc-chat-push-badge` ไม่รู้จัก `createMany` | ✅ Fable แก้ NV-6.2 เป็น "≥1 · จ่าหน้าทุกแถว · ไม่ซ้ำคน" · ผู้สร้างใช้ `create` วนทีละคน (ผู้รับหลักหน่วย · อยู่ในทรานแซกชันเดียวกัน) แทน `createMany` — **fake ที่ไม่รู้จักคำสั่ง = ไม้บรรทัดที่บังคับสไตล์โค้ดโดยไม่ตั้งใจ** จดไว้ ถ้าวันหน้าต้องใช้ createMany ให้สอน fake ก่อน |
| **D10** | คอลัมน์บริบท "แสดง" คำตอบที่ทีมใช้บ่อยทุกครั้งที่เปิดห้อง แต่ `searchAnswerExamples` นับ `useCount` ทุกครั้งที่ถูกเรียก ⇒ ตัวเลข "ทีมยอมรับ" ถูกปั๊มด้วยการแค่มอง | ✅ เพิ่ม `countUse?: boolean` (ค่าเริ่มต้น true = เดิม) · คอลัมน์บริบทเรียกแบบ `false` · การนับที่มีความหมาย = ตอนทีมกดใช้จริง |
| **D11** | `bodyOf()` ในข้อสอบนับปีกกาจาก `{` ตัวแรกหลังชื่อฟังก์ชัน ⇒ `function f(args: {…}) {` ได้แค่ type ของพารามิเตอร์ → G11.4–6 แดงทั้งที่โค้ดถูก (สาย F เจอ) | ✅ แก้ทั้ง `qc-chat-v2-context` และ `fitness.mts` ให้ข้ามวงเล็บพารามิเตอร์ก่อน · 🔴 บทเรียนตอนแก้: `re.sub` ของ Python ตีความ `\\` ในสตริงแทนที่ → regex ในไฟล์กลายเป็น `\s` เดี่ยว → ข้อสอบ 5 ข้อแดงเงียบ ๆ ด้วยเหตุผลผิด · **แก้ข้อสอบแล้วต้องรันซ้ำทันทีทุกครั้ง** |
| **D12** | `setConversationTagAction` (สาย D) เรียก `revalidatePath` ทั้งหน้า ⇒ ติดป้ายจากคอลัมน์ขวาอาจกวาดร่างที่ทีมกำลังพิมพ์ทิ้ง (ข้อห้ามหัว `inbox-actions.ts`) | ✅ ตัด revalidate ออก — action คืน `tags` ล่าสุดให้จออัปเดตเองอยู่แล้ว · ฟอร์ม add/remove ที่ redirect ยัง revalidate ได้ |

| **D13** | มติ D4 (เดสก์ท็อปอัดเสียงผ่านแผ่น ＋) ขัดกับแบบร่าง+ข้อสอบที่ล็อกแผ่น ＋ = 8 ช่องพอดี (สาย E รายงาน) | ✅ **เดสก์ท็อปมีไมค์ในแถบเหมือนมือถือ** (คอมโพเนนต์เดียว · ไม่มีช่องที่ 9) — ✅ **ทำแล้ว 1 ก.ย. ค่ำ**: แก้ `mockup.html` เดสก์ท็อป + เรนเดอร์ `ref-desktop.png` ใหม่ + ถอด `lg:hidden` ออกจากปุ่มไมค์ใน `composer.tsx` (ปุ่มยัง disabled จนสาย H ต่อการอัดจริง) · D4 ถูกแทนด้วยข้อนี้ |
| **D14** | แบบร่างมีปุ่ม "แผนที่ร้าน" แต่ทั้งสคีมาไม่มีที่อยู่/พิกัดร้าน (สาย E เจอ) | ✅ อ่านจาก `BusinessUnit.settings` (`address/mapUrl/lat/lng`) · ยังไม่ได้ตั้ง = ปุ่มบอกตรง ๆ ว่ายังไม่ได้ตั้ง ไม่เดา · **งานค้าง**: หน้าตั้งค่าสาขาให้กรอก 4 ช่องนี้ (นอกขอบเขตแชท V2 — เข้าคิวหลังรอบ 4) |
| **D15** | "แปลอัตโนมัติในห้องนี้" เก็บเจตนาไว้ที่ `meta.autoTranslate` แล้ว แต่เส้นทางรับข้อความยังไม่อ่านค่านี้ (สาย E รายงาน) | ✅ **ต่อในรอบ 4 (สาย G ถือ service.ts ขาเข้า)** — ปุ่มบอกค่าใช้จ่ายและยังกดแปลรายข้อความได้ จึงไม่โฆษณาเกินจริง |
| **D31** | LINE ส่งเสียง: ไฟล์เป็น m4a แล้ว (worker แปลงทุก 5 นาที) แต่ตอนกดส่งยังเป็น wav — เลือกทาง (ก) sendReply รอ transcode หรือ (ข) ส่งช้า async (วิเคราะห์เต็มในรายงานสาย K) | ✅ **เลือก (ข)** — ไม่เปิดพื้นผิว HTTP ใหม่บน VPS · เงื่อนไขบังคับก่อนเปิด `audio:true` ให้ LINE: (1) cron ถี่ขึ้นเป็นทุก 1 นาที (2) ฟองต้องมีสถานะ "กำลังส่งเข้า LINE" จริง ห้ามโชว์ ✓ ก่อนส่งถึง (3) ลงทะเบียน consumer ใน outbox (บทเรียนคิวตันเงียบ) — **ยังไม่ทำในรอบนี้** เข้าคิวถัดไป · cron ตัวแปลงติดตั้งแล้ว 2 ก.ย. (`*/5` · flock · log `/var/log/shark-voice-transcode.log`) · 🔑 รอเจ้าของ: `BUNNY_ACCOUNT_KEY` เพื่อ purge edge cache (ตอนนี้ลบที่ต้นทางแล้วแต่ edge ถือต่อได้ถึง 30 วัน — ห้ามเขียน /privacy ว่าลบทันที) |
| **D16 ✅ ปิดครบ 2 ก.ย.** | — | `ThreadSnapshot` พา `pageUrl/tags/autoTranslate` มาก้อนเดียวกับข้อความ · ถอด `loadRoomContextAction` ทิ้งทั้งก้อน (ตัด 1 คำขอ/การเปิดห้อง · บรรทัดบริบทเลิกกระพริบ) · `audioMs` ไม่ต้องใช้แล้ว (อ่านจาก `attachment.durationMs`) |
| **D24 ✅ ปิดที่ราก 2 ก.ย.** | — | `strip()` ของข้อสอบ 15 ชุดถูกอัปเป็นตัวกัน `image/*`/`audio/*` ในสตริง (จองด้วย \u0000 ก่อนตัดคอมเมนต์) · spot-check 5 ชุดเขียว |
| **D16 (เดิม)** | `ThreadSnapshot`/`ThreadAttachment` ไม่มี `meta.pageUrl`/`durationMs` ⇒ สาย E ต้องยิง `loadRoomContextAction` แยก | ✅ **รอบ 4 สาย H** ใส่ `durationMs` ลง `ThreadAttachment` (ต้องใช้กับฟองเสียงอยู่แล้ว) + `pageUrl` ลง `ThreadSnapshot` แล้วถอด `loadRoomContextAction` ทิ้ง |

| **D17** | QC สายตารอบ 3 (30/31): ขอบฟองโน้ตได้ `#e5e5e5` ทั้งที่ utility `border-[color:var(--color-note-line)]` ถูก generate | ✅ ต้นเหตุคือ `globals.css` มีกฎ `* { border-color }` **นอก @layer** — ใน Tailwind v4 กฎไม่มี layer ชนะ utility ทุกตัว ⇒ สีขอบที่ประกาศเองทั้งแอปถูกทับเงียบ ๆ มาตลอด · ย้ายเข้า `@layer base` (แก้ที่รากครั้งเดียว) |
| **D18** | เปิดห้องแล้วจอไม่เลื่อนไปข้อความล่าสุด (เช็ค nearBottom ตอน scrollTop=0) · ลิงก์ "บันทึกเป็นตัวอย่างคำตอบ" โผล่ใต้ฟองขาออกทุกฟอง ทำให้รกและแบบร่างไม่มี | ✅ สั่งสาย G (inbox-client): เลื่อนล่างสุดแบบบังคับเมื่อเปลี่ยนห้อง · สั่งสาย H (bubble): เดสก์ท็อปโชว์เมื่อ hover/focus · มือถือย่อเป็นไอคอน+ป้ายสั้น — **ไม่ตัดฟีเจอร์** |

| **D19** | `setRoomAutoTranslateAction` เปิดได้แม้ร้านยังไม่เปิดสวิตช์แปลระดับร้าน ⇒ เมนูรายห้องกดแล้วไม่มีอะไรเกิดขึ้นเงียบ ๆ (สาย G รายงาน) | ✅ เปิด (on) ต้องผ่าน `guardTranslateEnabled` ตัวเดียวกับตัวแปล → ไม่ผ่านคืน reason ให้จอบอก "เปิดได้ที่ เชื่อมช่องทาง → การแปลและ AI" · ปิดทำได้เสมอ |
| **D20** | สัญญาณ "กำลังพิมพ์" วันนี้มาจาก**ทีมงาน**เท่านั้น (ลูกค้าต้องทำข้ามรีโป) แต่ `TypingBubble` วาดชิดซ้าย + aria "ลูกค้ากำลังพิมพ์" (สาย G รายงาน) | ✅ ฟองต้องบอกว่า**ใคร**พิมพ์: ทีมงาน → ชิดขวา + "{ชื่อ} กำลังพิมพ์" · ลูกค้า (เมื่อมี) → ชิดซ้ายแบบเดิม · ทำใน `bubble.tsx` หลังสาย H ส่งงาน (Fable ทำเอง) · ฝั่งลูกค้า subscribe/typing = งานข้ามรีโป เข้าคิวถัดไป |
| **D21** | `qc-chat-core-v2` XC-3.2 ล็อก payload webhook `chat.message.sent` ไว้ 7 ฟิลด์เป๊ะ · สาย H เพิ่ม `type` + `attachments` (จำเป็นให้ลูกค้าฟังเสียง) | ✅ เพิ่มฟิลด์ = additive ไม่ทำผู้รับ webhook เดิมพัง ⇒ ข้อสอบวัด "ต้องมี 7 ฟิลด์เดิมครบ + ห้ามหาย" แทน "เท่ากับ 7 เป๊ะ" (Fable แก้ตอนประกอบ) |

| **D22** | capability เสียงไม่ได้อยู่ใน `CHANNEL_META` ตามที่สั่ง — หัวไฟล์ `channel-icon.tsx` ห้ามไว้เอง ("ความจริงอยู่ที่ registry ของ adapter") และข้อสอบ VO-3.1 อ่านจาก `getAdapter().capabilities` (สาย H รายงาน) | ✅ ถูกแล้ว — ทะเบียนความสามารถ = adapter (`capabilities.audio` + ทางเข้าเดียว `canSendAudio()`) · `CHANNEL_META` = หน้าตา/ป้าย เท่านั้น · คำสั่งของ Fable ผิดเอง |
| **D23** | แบบร่างเขียน "กดค้างที่ไมค์" · สาย H ทำเป็นแตะเริ่ม/แตะหยุด | ✅ รับ — กดค้างแล้วเมาส์หลุดนอกปุ่ม = อัดค้างโดยไม่รู้ · คีย์บอร์ดกดค้างไม่ได้ · เป็น "ปรับให้ดีขึ้น" ตามสิทธิ์ V3 ที่รายงานก่อน |
| **D24** | 🔴 `strip()` ในข้อสอบทุกชุดตีความ `audio/*` (ในคอมเมนต์หรือสตริง) เป็น `/*` เปิดบล็อกคอมเมนต์ → **กินโค้ดหาย 12,145 ตัวอักษร** → ข้อแดงหลอก (สาย E เจอกับ `image/*` · สาย H เจอซ้ำ) | ✅ จดเป็นกับดักถาวร · ทางแก้ที่รากคือให้ `strip()` ข้าม `/*` ที่อยู่หลัง `/` ของ mime (`[a-z]+\/\*`) — **งานค้างของ Fable** (แตะข้อสอบ ~10 ชุด ทำเป็น helper กลางครั้งเดียว) |
| **D25** | `scripts/qc-storage.mts` ไม่ทับ `DATABASE_URL` → สาย H เผลอรันแล้วสร้างแถว `FileAsset` บน prod (สาย H รายงานเอง) | ✅ เพิ่มลงรายการ "ชุดที่แตะ prod" ใน memory · ชุดนี้ควรถูกทำให้ทับ URL เหมือนชุด chat — งานค้าง |

| **D26** | WO-CV12: orb ผู้ช่วย AI (`fixed bottom-4 right-4`) ทับปุ่มส่งบนมือถือถ้าให้กล่องแชทเต็มขอบล่างตามแบบร่าง (สาย I รายงาน) | ✅ **ซ่อน orb บนหน้าแชท <lg** (`AiDock hideOnMobile`) · กล่องแชทเต็มขอบ `pb-2` · ผู้ใช้ยังเรียก AI จากปุ่ม "AI ช่วยร่าง" ในกล่องพิมพ์ · เดสก์ท็อปยังมี orb |
| **D27** | ภาพข้อเสนอมี "ตั้งค่าแชท" ในเมนู ⋮ แต่มันชี้หน้าเดียวกับ "เชื่อมช่องทาง" (`/chat/channels`) | ✅ **ไม่ใส่** — ทางเข้าซ้ำที่ไปที่เดิม ขัด §6.1 (เคยตัดแท็บ "สนทนา" ด้วยเหตุผลเดียวกัน) · เมนู ⋮ = เชื่อมช่องทาง · คำตอบสำเร็จรูป (`#quick-replies`) · ห้องที่ปิดแล้ว (state เดียวกับกรวย) · จัดการการเชื่อมระบบ (หลายสาขา) |
| **D28** | VR-6.2 ใช้ `offsetParent !== null` วัดว่า Topbar ซ่อน — element `position:fixed` มี offsetParent = null เสมอ ⇒ เขียวฟรี (สาย I เจอ) | ✅ แก้เป็น `display !== "none" && rect.height > 0` · **บทเรียน: วัด "มองเห็น" ต้องใช้ขนาดจริง ไม่ใช่ offsetParent** |

| **D30** | 🔴 **แก้ D29 รอบแรกยังไม่พอ (เจ้าของเทสซ้ำ 2 ก.ย. — เงียบทุกจอ)**: m4a จาก MediaRecorder ของ Chrome เป็น **fragmented MP4** (`ftyp iso5/hlsf/cmfc`) — iOS เปิดเป็นไฟล์ตรง ๆ ไม่ได้ และ `canPlayType("audio/mp4")` ตอบว่าเล่นได้ = จับไม่ได้ · แถมคลิปเทสอัดมาแทบเงียบ (max −39.7dB — เพิ่ม `ctx.resume()` กัน Safari suspended) | ✅ **WAV ผ่าน Web Audio เป็นเส้นทางเดียว** (`CANDIDATE_TYPES = []` · MediaRecorder ไม่ถูกใช้ผลิตไฟล์อีก — deterministic ทุกเบราว์เซอร์) · ฟองดัก `onError`/`play().catch` → สลับเป็นลิงก์ (กันไฟล์ "ชนิดถูกโครงผิด" ทุกชนิดในอนาคต) · แอปเล่น**ในแอป**ผ่าน WebView (เจ้าของสั่ง "ไม่ควรเปิด browser") · ไฟล์เสีย 2 ไฟล์บน prod ถูก transcode เป็น m4a แท้ (ffmpeg บน VPS) + ชี้ DB ใหม่ · VO-10.1/10.5 อัปเดตคุมถาวร · 🔴 บทเรียนซ้อน: **"ชนิดไฟล์ถูก" ≠ "โครงไฟล์ถูก" — ตรวจ ftyp/โครงจริง และ fallback ต้องดักตอนเล่นจริง ไม่ใช่แค่ canPlayType** |
| **D29** | 🔴 **เจ้าของเทสเสียงจริง 2 ก.ย. แล้วพัง 3 จอ (iPhone ทั้งหมด)** — ไฟล์อยู่บน CDN ครบ (200/CORS ok) แต่เป็น `webm/opus` ซึ่ง **iOS เล่นไม่ได้โดยสิ้นเชิง** · รากคือตัวอัดเลือก webm ก่อน (ตัดสินจาก "เครื่องนี้อัดได้" ไม่ใช่ "ทุกเครื่องเล่นได้") | ✅ **ชนิดไฟล์ต้องเล่นได้ทุกเครื่อง**: อัด `audio/mp4` (m4a) เป็นหลัก (iOS+Android+ทุกเบราว์เซอร์+LINE) · เบราว์เซอร์ที่อัด m4a ไม่ได้ (Firefox) ตกไป **WAV ผ่าน Web Audio** (16kHz โมโน · 120 วิ ≈ 3.8MB) · **เลิกผลิต webm/ogg เด็ดขาด** · ฟองทุกฝั่ง (SHARK + เว็บลูกค้า) ตรวจ `canPlayType` — ไฟล์เก่าที่เครื่องเล่นไม่ได้ = ลิงก์เปิด/ดาวน์โหลด ไม่ใช่ปุ่มเงียบ · แอป: เรนเดอร์ไฟล์แนบ + เปิดฟังผ่าน `Linking` (สาย J · เล่นในแอปเองต้องรอเพิ่ม native module ตอน build หน้า) · ข้อสอบ VO-10.1–10.4 คุมถาวร · 🔴 **บทเรียน: format ของไฟล์ที่ผู้ใช้แลกกัน ต้องเลือกจากฝั่ง "ผู้เล่น" ที่แย่ที่สุด ไม่ใช่ฝั่ง "ผู้อัด" ที่สะดวกที่สุด — และของแบบนี้ข้อสอบ static จับไม่ได้ ต้องเทสด้วยเครื่องจริงต่างค่ายเสมอ** |

🔴 **แผน §1.1 แก้แล้ว**: หนี้ H4 เดิมเขียนว่าลิสต์ซ้ำ 3 ที่ — ของจริงเหลือ **2 ที่** (`ui.tsx` ถูกถอดไปตั้งแต่รอบก่อน)

---

## §11 WO-CV9 — transcoder + ลบไฟล์จริง (สาย K · 2 ก.ย. 2026)

| # | เรื่อง | สภาพ |
|---|---|---|
| **CV9-1** | `scripts/voice-transcode-worker.mts` — WAV บน CDN → M4A (AAC 64k · faststart) ด้วย ffmpeg บน VPS | ✅ **รันจริงบน prod แล้ว 2/2 ชิ้น** · 163KB→39KB (5,208ms) · 88KB→20KB (3,754ms) · `storageKey`/`FileAsset` ชี้ไฟล์ใหม่ · wav เดิมถูกลบจาก storage zone (ตรวจแล้ว 404) · รันซ้ำ = "ไม่มีงาน" exit 0 |
| **CV9-2** | `deleteStoredFile()` ใน `storage/service.ts` + retention เรียกใช้ | ✅ `qc-chat-retention` **50/50** (เดิม 37) · RT-5 พิสูจน์ fail-before แล้ว (ถอดการเรียกออก → RT-5.1/5.4 แดง) |
| **CV9-3** | LINE `audio:true` | ⛔ **ยังไม่เปิด** — ไฟล์ ณ วินาทีที่กดส่งยังเป็น wav (worker เป็น cron) · ทางเลือก (ก) sendReply รอ transcode / (ข) ส่งเข้า LINE แบบ async — **รอเจ้าของ/Fable เคาะ** |

🔴 **ข้อจำกัดที่ยังปิดไม่ได้ — CDN edge cache (ต้องรู้ก่อนประกาศว่า "ลบแล้ว")**
Bunny ลบวัตถุที่ **storage zone** ทันที แต่ **edge ที่เคยแคชไว้ยังเสิร์ฟ url เดิมต่อ** จนหมดอายุ
(พิสูจน์แล้วจากงานรอบนี้: wav 2 ไฟล์หายจาก storage เป็น 404 แต่ url เดิมบน `b-cdn.net` ยังคืน 200
เพราะ `cache-control: public, max-age=2592000` = **30 วัน**)
purge ราย URL ต้องยิง `https://api.bunny.net/purge` ด้วย **account key ระดับบัญชี** — `SHARK_BUNNY_KEY`
คือรหัสของ storage zone ใช้ purge ไม่ได้ ⇒ ต้องตั้ง env ใหม่ **`BUNNY_ACCOUNT_KEY`** (ยังไม่มีในระบบ)
โค้ดเผื่อไว้แล้ว: มี env ก็ยิง purge ให้อัตโนมัติ ไม่มีก็ข้ามเงียบ ๆ (ไม่ถือว่าลบล้มเหลว)
⇒ **จนกว่าจะตั้ง env นี้ "ลบตามอายุเก็บ" = ลบที่ต้นทาง ไม่ใช่หายจาก edge ทันที** — ห้ามเขียนหน้า /privacy
ว่าลบทันที และห้ามใช้ผลลัพธ์ "url เก่ายังเปิดได้" มาสรุปว่าโค้ดลบไม่ทำงาน
