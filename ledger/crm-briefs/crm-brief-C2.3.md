# C2.3 — Assignment rules
Read `crm-brief-COMMON.md` first. Contract: CRM-RUN §2 "C2.3". Spec: blueprint §5.7, §11.5, mockup 07 (right).

## Deliverables
`assignment.ts`: ordered rules; conditions on sourceKind/sourceChannel/province (Party address)/interested product (field)/company size/language/custom fields; modes FIXED, ROUND_ROBIN (cursor advanced with ONE statement `UPDATE … SET "rrCursor" = ("rrCursor"+1) % n … RETURNING`), TEAM_LEAD, LEAST_OPEN; skip users with `TeamMember.acceptingLeads=false`, users on approved leave (`hr.isOnLeave`), suspended memberships, users at `maxOpenPerUser`; fallback user from settings; nobody → unassigned + notify MANAGER; `simulate(rows)`; replace the C1.4 stub `assignment.pick`. UI `/settings/assignment`.

## Acceptance (oracle `qc-crm-c2.3`)
CRM-RUN S1–S6 (22).
X3 20 parallel `pick` calls over 4 users → 5 each (exact); LEAST_OPEN under 10 parallel creations never exceeds `maxOpenPerUser` · X1 rule cannot reference users/teams of another tenant; assignee must be able to see the record afterwards · X9 rule changes audited.
Regressions: C1.4, C1.7, `qc-hr-leave-booking`.

## Controller addendum (oracle author · 19 Sep 2569) — rulings R1–R12 CONFIRMED by the controller 19 Sep (binding)
Oracle: `scripts/qc-crm-c2.3.mts` (**74 checks after ORACLE-EDIT 24 Sep** · S0 5 · S1–S6 22 · S7 7 · S8 7 · S9 3 · X1 6 · X3 8 · X4 3 · X8 2 · X9 3 · U 7 · CLEAN — was 60). Every item below is **CONFIRMED (controller 19 Sep)** — it is what the oracle currently asserts; the controller confirms or changes it (then ORACLE-EDIT the listed checks).
- **R1 CONFIRMED (controller 19 Sep) — when rules run.** `pick` stays one function: explicit owner ⇒ FIXED; a human creator and no `auto` ⇒ CREATOR (C1.4 behaviour unchanged, `qc-crm-c1.4` stays green); rules run only on the automatic path = no human creator (form/chat bridges, API without a user) or `createContact({ ownerUserId: "auto" })` (blueprint §5.3 `ownerUserId?|auto`). Mockup "ใช้ทุกทางเข้า lead ใหม่" is read as "every automatic entry". Checks: S7.1 S7.7.
- **R2 CONFIRMED (controller 19 Sep) — pick is async and runs inside the caller's transaction** (`pick(ctx, input, db?)`, contacts.ts awaits it with `tx`). Needed so LEAST_OPEN/maxOpenPerUser can serialise per rule and see the load the same tx writes. The C1.4 comment "signature unchanged" is superseded (async). Checks: S0.2 X3.3 X3.4.
- **R3 CONFIRMED (controller 19 Sep) — first MATCHING rule decides** (no fall-through to later rules); its candidates exhausted ⇒ settings fallback ⇒ NOBODY (blueprint §11.5 wording). Alternative (fall through to the next matching rule) = change S7.5 only.
- **R4 CONFIRMED (controller 19 Sep) — eligibility** = accepted Membership (`acceptedAt` not null — there is NO suspended flag on Membership; "suspended" = removed or not accepted) + `crmCan(member, "crm.contact.read")` (X1: assignee can see) + not `acceptingLeads=false` (rule's team row; rule without team ⇒ any team row of the tenant) + not `hr.isOnLeave(…, now)` + load < maxOpenPerUser. Fallback user only needs membership + read key (last resort; leave/cap ignored). Checks: S4.* S5.1 X1.5.
- **R5 CONFIRMED (controller 19 Sep) — open load** (LEAST_OPEN + maxOpenPerUser) = contacts owned with lifecycleStage LEAD, leadStatus ≠ UNQUALIFIED, not archived/merged + deals owned with kind OPEN, not archived — **per CRM system**. Counting deals only (blueprint "นับดีล OPEN") would make the cap meaningless for lead assignment (a new lead never changes it) and X3 "LEAST_OPEN never over-assigns" untestable. Exposed as `openLoadOf` for the UI "คิว" column. Checks: S3.1 S4.1 X3.3 X3.4.
- **R6 CONFIRMED (controller 19 Sep) — conditions**: six kinds = sourceKind · sourceChannel · province · companySize · language · `f.<key>` custom field ("interested product" is a contact custom field — no separate kind). Province = CONTAINS over free-text `Party.address` (contact Party, else company Party) — there is no province column. `f.<key>` is key-format validated only (fields may be added later). Ops eq/neq/in/contains; one level AND/OR. Checks: S1.* S7.3 S7.6.
- **R7 CONFIRMED (controller 19 Sep) — RR candidate order**: `userIds` as listed when non-empty (teamId then only labels `contact.teamId`), else team members by (joinedAt, userId). n = eligible count at pick time; one `UPDATE … SET "rrCursor" = ("rrCursor"+1) % n … RETURNING`. Checks: S2.* X3.1 X3.2.
- **R8 CONFIRMED (controller 19 Sep) — NOBODY notification**: one `AppNotification` per accepted OWNER/MANAGER membership (recipientUserId set, Thai, body = link with contact id, no PII), at most once per contact, written by the contact-creation path (automatic path, v2 only). No new outbox event in C2.3 (`crm.contact.assigned` unchanged; may add ruleId/teamId ids). Checks: S5.2 X4.3 X8.2.
- **R9 CONFIRMED (controller 19 Sep) — multi-HR (CRM-RUN §4 note from C0.3):** C2.3 calls `hr.isOnLeave` through the facade as is (first HR system of `employeeOfUser`). Extending it to every HR system = an HR-module change outside default ownership → backlog (hand-over), not tested here.
- **R10 CONFIRMED (controller 19 Sep) — settings**: `settings.crm.assignment.fallbackUserId` written with one jsonb_set via the settings service (C1.5 `setCrmSettingsKey` needs an `assignment` key — the builder extends settings.ts inside a `// CRM C2.3 ▸ … ◂` block). `notifyChannel` not used until C2.10.
- **R11 CONFIRMED (controller 19 Sep) — out of C2.3**: wiring `FormDef.assignRuleId` into the form bridge (C2.6 owns `crm-bridges/forms.ts`; until then v2 form leads use the ordered rules) · `importContacts` `assignRuleId` option (C1.4 import path — not required by CRM-RUN S1–S6; builder may pass `ruleId` if trivial, untested) · REST ops / AI tools (C2.11) · rule `stats` Json (UI may show counts; not asserted).
- **R12 CONFIRMED (controller 19 Sep) — file ownership** (R-D default): `src/lib/modules/crm/assignment.ts` · `assignment-shared.ts` · `assignment-actions.ts` ("use server") · `src/app/app/sys/[id]/crm/settings/assignment/**` · `src/components/crm/assignment/**` · blocks `// CRM C2.3 ▸ … ◂` in `contacts.ts` (insertContactInTx + createContact "auto"), `settings.ts`, `index.ts`, `nav.ts`, `scripts/crm-ui-inventory.json`.
- Regressions to run with the oracle: qc-crm-c1.4 · c1.7 · c1.8 · c1.11 · qc-crm-v1 · qc-hr-leave-booking · qc-form · qc-forms-notify · qc-member-m1.9.

## Controller ruling (24 ก.ย. 2569 · Opus 5 · binding — หลังผู้ตรวจอิสระ · **builder ยังต้องแก้ ยังไม่รับงาน**)
ผู้ตรวจ (opus · อ่านทุกไฟล์ + ข้อสอบ 1112 บรรทัด): **ไม่มี BLOCKER ฝั่งร้าน v1** (ไล่ทั้ง 3 เส้นทีละบรรทัด: เส้นระบุเจ้าของเอง/เส้นผู้สร้าง/เส้น v1 = พฤติกรรมเหมือนฐานทุกไบต์ ต่างแค่อ่าน DB เพิ่ม 2 ครั้งต่อลีด v1 ที่ไม่มีเจ้าของ) · **ไม่มี BLOCKER ฝั่งการยิงพร้อมกัน** (advisory lock ต่อระบบครอบ read-decide-write ถึง commit · คืนล็อกเองตอน ROLLBACK · cursor เป็นคำสั่งเดียว · ไม่มี deadlock) · `Party.address` ไม่รั่วออกไปไหน (ตรวจครบ: ไม่คืน ไม่ log ไม่ใส่ payload/แจ้งเตือน)

### ต้องแก้ก่อนรับงาน
1. 🔴 **B1 · `assignment.ts:777` (`pageData`) เปิดเผยสถานะ "ลาอยู่วันนี้" ของพนักงานทุกคนให้คนที่ไม่มีคีย์ `hr.leave.read`** — หน้านี้กันด้วย `crm.assignment.manage` ซึ่ง MANAGER ได้โดยปริยาย · repo ถือว่านี่เป็นคีย์แยก (`permissions.ts:497`) **คำตัดสิน: แก้** — ถ้าผู้ดูไม่มี `hr.leave.read` ให้ **ตัดฟิลด์ `onLeave` ออกจาก payload ทั้งก้อน** (ไม่ใช่ซ่อนบนจอ) · เครื่องยนต์ยังใช้ข้อมูลลาภายในได้ตามเดิม ปัญหาคือการเปิดเผย
2. 🔴 **S2 · `contacts.ts:668` ไม่ใส่ `locale` ลง draft ⇒ เงื่อนไข `language` ตายสนิทบนเส้นทางจริง** (อ่านได้เป็น `"th"` เสมอ · ไม่มีโค้ดไหนใน `modules/crm/` เขียน `CrmContact.locale` เลย) = **1 ใน 6 ชนิดเงื่อนไขที่สัญญาบังคับ ใช้งานไม่ได้จริง** · ร้านดำน้ำที่ตั้งกฎ "ลูกค้าภาษาอังกฤษ → เซลส์ที่พูดอังกฤษ" จะไม่เข้าเลย **แก้ให้เส้นทางจริงส่ง locale (และเขียนคอลัมน์)** + ข้อสอบต้องพิสูจน์ผ่านเส้นทางสร้างจริง ไม่ใช่เรียก `pick()` ตรง ๆ
3. **S1 · `pageData → contactFields` เสนอฟิลด์ระบบ 14 ตัวให้ตั้งเงื่อนไข `f.<key>` ซึ่ง 11 ตัวไม่มีทางเข้า** (`splitFields` ดึงไปเป็นคอลัมน์ หรือ throw เพราะอยู่ใน `GOVERNED_CRM_SYSTEM_KEYS`) · เจ้าของร้านเลือก "เบอร์โทร (ฟิลด์กำหนดเอง)" หรือ "ที่มา" ที่วางอยู่ข้างตัวจริงชื่อเกือบเหมือนกัน ⇒ **ทุกลีดจากเว็บฟอร์มตกไปกฎสำรอง/NOBODY และเด้งแจ้งเตือนผู้จัดการทุกใบ** · แก้เป็น `!isSystem && !sensitive` ตามที่โมดูลนี้ทำอยู่แล้ว 2 แห่ง (`contacts.ts:2385` · `companies.ts:1778`) — และ label ของฟิลด์ `sensitive` ต้องไม่หลุดออกไปฝั่ง client
4. **S3 · ปุ่ม "ทดลอง" ใช้เงื่อนไขได้แค่ 3 จาก 6 ชนิด** (`actions.ts:124` ทิ้ง `partyId`/`companyId`/`fields`) ⇒ ร้านที่กฎแรกเป็น "จังหวัด = ภูเก็ต" กดทดลองแล้วได้คำตอบ **ตรงข้ามกับของจริง** และไม่มีทางรู้ · แก้ให้ส่งต่อสิ่งที่จอกรอกได้ และเขียนบนจอตรง ๆ ว่าอะไรที่การทดลองยังไม่ครอบ
5. **S4 · ยืมคอนเนกชันตัวที่สองเข้าไปใน tx ของผู้เรียก** (`onLeave` ตกไป `hrFacade().isOnLeave` + `logOps` บน client กลาง) ซึ่งผิดกฎข้อ 4 ที่เขียนไว้บนหัวไฟล์เอง · ทริกเกอร์จริง: `leaveSnapshot` กลืน error ทุกชนิดแล้วคืน `null` · ผู้สมัคร >300 คน · กฎ/สมาชิกทีมเพิ่มระหว่างทาง · snapshot เก่าเกิน 10 นาที ⇒ ฟอร์มยิงเป็นชุด = pool หมด (tx ข้างนอกถือคอนเนกชันรอ query ข้างในคิวอยู่)
6. **S5 · `openLoadOf` อยู่บน facade โดยไม่มี actor/คีย์/ประตู/ตัวกรองการมองเห็น** · ยังไม่มีผู้เรียกใน `src/` **คำตัดสิน: ถอดออกจาก `crm/index.ts` (ยัง export จาก `assignment.ts` ให้ข้อสอบเรียกได้)** จนกว่า C2.11/C3.2 จะต้องใช้จริง แล้วค่อยใส่ actor + คีย์ตอนนั้น — วิธีนี้ไม่ต้องแก้ CONTRACT BLOCK
7. **S6 · `loadsOf` นับผู้ติดต่อ/ดีลโดยไม่ผ่าน `visibleWhere`** ⇒ ร้านที่ตั้งนโยบาย OWN ให้ MANAGER ยังเห็นตัวเลขคิวของคนที่ตัวเองเปิดเรคคอร์ดไม่ได้ · ใส่ตัวกรองบนเส้น `pageData`
8. **S7 · `data-testid` 3 ตัวในแถวเงื่อนไขไม่มีเลขกำกับ** (`crm-assign-cond-field/op/value`) ⇒ มี 2 เงื่อนไข = id ซ้ำใน DOM ⇒ เทสต์ UI แตะแถวที่สองไม่ได้เลย · ใส่ `-${idx}` ให้ตรงกับปุ่มลบที่ทำถูกอยู่แล้ว + แก้ทะเบียนให้เป็นแบบ pattern
9. **S8 · `cleanRule` ไม่บังคับว่า `userIds` ต้องอยู่ในทีม `teamId` แต่เอา `rule.teamId` ไปปั๊มบนผู้ติดต่อ** (`contacts.ts:698`) ⇒ กฎ `{teamId: T, userIds: [u9]}` ที่ u9 ไม่ได้อยู่ทีม T ทำให้ **สมาชิกทีม T ทุกคนอ่านลีดของคนนอกทีมได้** ใต้นโยบาย TEAM ขณะที่ทีมของ u9 เองอ่านไม่ได้ · แก้: ปั๊ม `teamId` เฉพาะเมื่อผู้รับงานเป็นสมาชิกทีมนั้นจริง
10. **S10 · `page.tsx:82` พิมพ์ `simMax: 20` ซ้ำเป็นเลขลอย** ขณะที่ขอบจริงคือ `ASSIGN_SIMULATE_MAX_ROWS = 200` และหัวไฟล์เขียนเองว่าไม่พิมพ์ลิมิตซ้ำ · ดึงจาก `assignment-shared.ts`
11. **NOTE 1 · `pageData:777` ใช้ `m.user?.email` เป็นชื่อสำรอง** ⇒ อีเมลพนักงานหลุดไปอยู่ใน payload ฝั่ง client · ใช้ชื่อเท่านั้น

### ผู้คุมงานต้องทำเอง (ORACLE-EDIT — ยังไม่ทำ)
- 🔴 **`pageData` ไม่อยู่ใน CONTRACT BLOCK เลย ⇒ ไม่มีข้อสอบข้อไหนแตะ** และ **B1 · S1 · S6 อยู่ในพื้นที่ที่ไม่มีข้อสอบทั้งหมด** · `C2.3-S0.1` ตรวจ export 11 ตัวที่ต้องมี แต่ "ปล่อยของแถมได้ไม่จำกัด" **คำตัดสิน: เพิ่มบรรทัดสัญญา + ข้อสอบสำหรับ `pageData`** (ประตูสิทธิ์ · คืนนิยามฟิลด์อะไรได้ · ห้ามเปิดเผยข้อเท็จจริงเรื่องการลา/การมองเห็น)
- **`C2.3-U.2` มีช่องโหว่**: ข้อสอบคุม `createContactFromLegacy` แค่บนร้าน uiVersion 1 · บนร้าน **v2** ฟังก์ชันเดียวกัน (เครื่องมือ AI `crm_create_lead` + ฟอร์มสาธารณะ v1) เดินกฎมอบหมายและยิง `notifyUnassigned` ได้ = **พฤติกรรมเปลี่ยนจริงสำหรับร้านนำร่อง โดยไม่มีข้อสอบข้อไหนคุม** → เพิ่มข้อ
- **`C2.3-X3.3/X3.4` พิสูจน์เพดานเฉพาะใต้ LEAST_OPEN** · **ROUND_ROBIN + `maxOpenPerUser` ไม่มีใครทดสอบ** ซึ่งเป็นคู่ที่ `n = eligible.length` หดลงเมื่อคนชนเพดาน ขณะที่ `rrCursor` เก็บเป็น modulo ของ n ที่หดอยู่ ⇒ ความหมายของ cursor เปลี่ยนระหว่างการหยิบ → เพิ่มข้อ
- **`C2.3-S6.4` (ตรวจ testid กำพร้า) ใช้ `includes` เฉย ๆ** ⇒ id ที่ render ซ้ำ N ครั้งผ่านเหมือน render ครั้งเดียว (ต้นเหตุที่ S7 หลุด) → ตรวจจำนวนด้วย
- **`C2.3-S6.5` (client ห้าม import ถึง prisma) ไม่ตามขอบที่ใช้จริง** — regex จับแค่ `@/lib/core/db` · `@prisma/client` · `@/lib/modules/crm/*` แต่ client import ผ่าน `.../assignment/actions` (ปลอดภัยจริงเพราะ Next แทนด้วย client reference แต่ข้อสอบไม่ได้พิสูจน์อะไรเลย)

### รับไว้ / ไม่ต้องแก้
- **S9 · advisory lock เป็นต่อระบบ ไม่ใช่ต่อกฎ** (สัญญาขอต่อกฎ) — **ของที่ทำมาแข็งกว่าและปลอดภัยกว่า รับไว้** · หนี้เรื่องปริมาณ (ฟอร์มยิงพร้อมกัน ~100 ใบบนระบบเดียวจะเริ่มเจอ P2028 เพราะช่วงวิกฤตกิน `personParty` + insert + ฟิลด์ + outbox 2 ตัว ใต้ `timeout: 30_000`) → **เฟส C5**
- **ที่ตั้ง `actions.ts` ในโฟลเดอร์ route (ไม่ใช่ `crm/assignment-actions.ts` ตาม R12) — รับ** ผู้ตรวจยืนยันกลไกเองแล้ว: `src/components/**` ไม่ใช่ self-dir ของ F2.3 ⇒ ทางของ R12 จะทำด่านแดง MAJOR · และมีแบบอย่างอยู่แล้ว 2 แห่ง (`VisibilitySettings` · `CrmAutomationBuilder`) · ของที่ส่งไปฝั่ง client เป็นทะเบียนข้อความไทย/ขอบ validation ไม่มีคีย์สิทธิ์
- **`setCrmAssignmentKey` เป็นตัวเขียนตัวที่สองใน `settings.ts` (ไม่ขยาย `setCrmSettingsKey` ตาม R10) — รับ** เพราะขยาย type เดิมจะเปลี่ยนรูปที่ข้อสอบเก่าเทียบทั้งก้อน · เป็น `UPDATE` คำสั่งเดียว scope ครบ ไม่มี read-modify-write
- **NOTE 3/4** (snapshot ที่คิดแล้วทิ้งเมื่อเป็นลูกค้าเดิมใน `leadFromBridge:2242` · แจ้งเตือน NOBODY ส่งถึง OWNER/MANAGER ที่ไม่มีสิทธิ์ CRM = ลิงก์ตาย ไม่มี PII) → หนี้เล็ก บันทึกไว้ ไม่บล็อก

### 🔴 กติกากระบวนการที่เพิ่งได้ (ผู้ตรวจจับได้)
**brief ในกลุ่ม worktree ไม่ตรงกับทรีหลัก** — สำเนา `crm-brief-C2.3.md` ใน `shark-crm-c23` **ไม่มี Controller addendum (R1–R12)** เลย ⇒ ถ้า builder อ่านแต่สำเนาในกลุ่มตัวเอง มันทำงานโดยไม่เห็นคำตัดสินผูกพัน · **ก่อน spawn builder ทุกครั้ง: ก๊อป `ledger/crm-briefs/` จากทรีหลักเข้า worktree นั้น** (หรือสั่งให้ agent อ่าน path ของทรีหลักเสมอ)

## Controller ruling round 3 (24 ก.ย. 2569 · Fable 5.1 · binding — หลังผู้ตรวจของผู้คุมงานยืนยัน 11 ข้อแก้จริง)
- **B1-bis (BLOCKER)** สถานะลายังรั่วผ่าน `nextUserId` ของทุกกฎ (คำนวณด้วย env ที่รู้ว่าใครลา) และปุ่มทดลอง ⇒ ผู้ดูที่ไม่มี `hr.leave.read` เห็น "คิวถัดไป: B" = รู้ว่า A ลา · แก้: ผู้ดูที่ไม่มีคีย์ → คำนวณด้วย env ที่ไม่มีข้อมูลลา (หรือตัด `nextUserId`) · simulate เช่นกัน · ข้อสอบ S8.4 เดิม grep แค่สตริง `"onLeave"` จึงไม่จับ → ORACLE-EDIT S10.1
- **B2 (BLOCKER)** `BridgeLeadInput.locale` ไม่มีผู้เรียกเลย (forms.ts · chat.ts · crm-panel-actions.ts) ⇒ เงื่อนไข `language` ยังตายบนเส้นทางที่กฎทำงานจริง (R1) — คำตัดสิน S2 รอบก่อนยังไม่บรรลุ · แก้: บล็อก `// CRM C2.3 ▸` ในสะพาน chat/forms ส่ง locale จากแหล่งที่มีจริง (ห้องแชท/ภาษาบัญชี LINE · FormDef/FormSubmission/คำตอบ locale) · ถ้าฟอร์มไม่มีแหล่งจริง = หนี้ C2.6 (ต้องรายงานว่าอ่านโมเดลแล้ว)
- **B3** `f.<key>` ไม่ถึงลีดจากสะพาน (`insertContactInTx` จาก `leadFromBridge` ไม่ส่ง `custom`) → **ไม่บล็อก C2.3**: การแมปคำตอบฟอร์ม→ฟิลด์เป็นของ C2.6 · C2.3 แค่เปิดช่อง `fields` ใน `BridgeLeadInput` ให้ไหลเข้า custom
- **S1** บันทึกกฎ `f.<ฟิลด์ลับ>` ได้และทำงานจริง ⇒ MANAGER ที่ไม่มีสิทธิ์อ่านฟิลด์ลับ อ่านค่าได้จากว่าลีดตกมาหาใคร · แก้: `cleanRule` ตรวจกับ `customFieldLayout` ปฏิเสธ sensitive/isSystem → ORACLE-EDIT S10.2
- **S2** ผู้รับที่อยู่นอกทีมของกฎ ปิดรับ lead ในทีมตัวเองแล้วยังได้ lead (query `closed` กรองด้วย `rule.teamId`) · แก้: มี `userIds` = ไม่กรองทีม → S10.3
- **S3** ร้าน v2 ที่ยังไม่ตั้งกฎเลย: ทุกลีด = NOBODY = แจ้งเตือน OWNER/MANAGER ทุกคน (แคมเปญ 100 ใบ × 3 คน = 300 แจ้งเตือน) · แก้: ไม่มีกฎที่เปิดอยู่เลย = ไม่แจ้ง · มีกฎแต่ไม่แมตช์/ไม่มีคน = แจ้งตามเดิม → S10.4
- **S4** `setFallbackUser` ตรวจแค่ membership ไม่ตรวจ `crm.contact.read` ⇒ ผู้รับสำรองตาย ทุกลีดตก NOBODY · แก้: ใช้ `readableMembers` → S10.5
- **S5** ทุก mutation ของกฎเขียน audit นอก tx · แก้: `$transaction` + ส่ง tx ให้ `writeAudit`
- N1 simulate 200 แถว = ~1,000+ query → memoise ต่อกฎ (ทำถ้าถูก) · N2 `leaveSnapshot` ≤75 round-trip/ลีด (300 คน) → หนี้ C5 · N3 คอมเมนต์เก่า :514 · `expect.target` ของ `crm-assign-cond-add` · `cleanLocale` เก็บค่า normalised + รับ BCP-47 → แก้
- รับไว้/ไม่แก้: RR cursor หารศูนย์ไม่ได้ (return ก่อนเมื่อ eligible ว่าง) · LEAST_OPEN tie-break กำหนดได้ · เพดานใช้กับ FIXED/TEAM_LEAD ด้วย · ลำดับล็อก advisory→row ไม่มี cycle · v1 ไม่แตะ · มือถือ 390 ผ่าน · testid 40 = แถวทะเบียน 40
- 🔴 กระบวนการ: `regressions.log` ของ builder ไม่ครบ (c1.4/c1.7/c1.11 ไม่มี · c1.8/forms-notify ไม่มี JSON_SUMMARY) — คำว่า "regression เขียว" ของ agent ไม่ใช่หลักฐาน · unit `crm-c22c23-verify` ของผู้คุมงานครอบทั้งหมด
