# C2.4 — Activity capture: call logging + AI, chat → activity, calendar merge
Read `crm-brief-COMMON.md` first. Contract: CRM-RUN §2 "C2.4". Spec: blueprint §5.5, mockup 08 (left/right), 13, decision C5.

## Facts
There is NO speech-to-text anywhere in the codebase (only an ffmpeg transcode worker on the VPS). Vision exists (`src/lib/ai/service.ts` takes `imageUrls`). AI credit: `src/lib/ai/credit.ts` (`canSpend`, `chargeUsageSafe`). Chat events: `chat.message.received` payload `{conversationId, channel}`; `chat.conversation.status` payload `{conversationId, status, externalUserId}`.

## Deliverables
- Call-log modal (click-to-call `tel:` opens it; outcome, duration, direction, note, next task, recording upload ≤ 25 MB through the PRIVATE file path C0.4).
- `CrmTranscriber` adapter interface + `settings.crm.ai.callTranscribe`; default provider = none → button shows a calm "ยังไม่ได้เชื่อมบริการถอดเสียง" state (owner decision Q2 pending). With a fake provider in QC: job → AI summary + next step → **proposal** that fills `transcript/aiSummary/aiNextStep` (never auto-writes); charges AI credit; refuses when `canSpend` is false.
- Business-card scan (vision) → proposal `crm_create_lead`.
- `chat.conversation.status` RESOLVED → ONE CHAT activity per conversation (dedupe `sourceRef=conversationId`), optional AI summary; `chat.message.received` stops sequences waiting for a reply and bumps `lastActivityAt` (no activity per message).
- Calendar merges read-only appointments of the same Party from booking/clinic/school through small read facades `appointmentsByParty` (add them to those modules' facades; read only).
- `CrmCallProvider` interface + webhook shape (stub).

## Acceptance (oracle `qc-crm-c2.4`)
CRM-RUN S1–S7 (24).
X4 RESOLVED event twice/parallel → one activity · X8 transcript/summary never appear in outbox payloads, OpsEvent or logs; AI prompt contains no phone/e-mail · X10 recording DTO has only an expiring link; other viewer/tenant cannot fetch · X6 upload mime allowlist (audio/*), size cap · X1 calendar shows only appointments of parties the actor can see.
Regressions: `qc-chat-core-v2`, `qc-chat-v2-context`, `qc-ai-vision`, `qc-ai-credit`, `qc-ai-proposals`, C1.6.

## Controller addendum — CONFIRMED by the C2.4 oracle author (19 Sep 2569 · awaiting controller confirmation)
Oracle: `scripts/qc-crm-c2.4.mts` (78 checks · CONTRACT BLOCK A–G at the top of the file is the builder's API). Every item below is **CONFIRMED** — the oracle already encodes it; the controller confirms or edits (ORACLE-EDIT) before spawning the builder.

1. **CONFIRMED — no migration in C2.4.** Everything fits existing columns (`CrmActivity.recordingFileId/transcript/aiSummary/aiNextStep/remindAt/sourceRef`, `AiProposal`, `AiCreditTxn`, `AppNotification`, `FileAsset`). Chat dedupe has no unique index ⇒ flag-first with `pg_advisory_xact_lock(conversationId)` + existence by `(systemId, source CHAT, sourceRef)`.
2. **CONFIRMED — AI credit source = `MEMBER_ASSIST`** ("ค่า AI ของงานลูกค้า") because adding `CRM_ASSIST` to `AiCreditSource` needs a migration; `note` carries ids only and is the dedupe/test key: `crm.call.transcribe#<activityId>` · `crm.chat.summary#<conversationId>` · `crm.card.scan…`. A dedicated enum value can be added in C3.0 (`crm_v2_c`) if the owner wants a separate line on the bill.
3. **CONFIRMED — "job" = synchronous call in the server action**, guarded by an advisory lock per activity (10 parallel clicks ⇒ one proposal, one charge, one transcriber call). No queue/table in C2.4 (R-B: no STT provider; the default transcriber is `null` ⇒ calm `NOT_CONFIGURED` state "ยังไม่ได้เชื่อมบริการถอดเสียง").
4. **CONFIRMED — the call-AI proposal is an `AiProposal`** kind `crm.activity.ai_fill`, conversationId sentinel `crm:activity:<activityId>`, payload `{activityId, systemId, transcript, aiSummary, aiNextStep}`; accepted/rejected through CRM-owned `acceptCallAiProposal` / `rejectCallAiProposal` (atomic PENDING→EXECUTED claim, edits win) — NOT through `ai/proposals.executeProposal` (its `crm.*` kinds map to REST ops and it has no activity-level visibility check).
5. **CONFIRMED — refusal order and codes**: v2 gate → visibility (404) → key `crm.activity.create` (403) → CALL with recording (`VALIDATION`) → `settings.crm.ai.callTranscribe` (`AI_DISABLED`) → transcriber (`NOT_CONFIGURED`) → `canSpend` (`NO_CREDIT`). Every refusal calls nothing and charges nothing.
6. **CONFIRMED — business-card scan** lives in `calls.ts` (`scanBusinessCard`) with key `crm.contact.create`; image ≤ 5 MB, jpeg/png/webp/heic (no SVG); sent to the vision model ONLY as a `data:` URL; image not stored; proposal kind stays `crm_create_lead` (brief) but payload carries `systemId`, and acceptance goes through CRM-owned `acceptLeadProposal` into THAT system (the legacy dispatch writes to the tenant's first CRM system with the v1 service — unchanged, still used by the AI chat).
7. **CONFIRMED — chat target system** = the FIRST open CRM system (crmGates order) that holds a contact of the conversation's Party ⇒ exactly one CHAT activity per conversation tenant-wide. Chat AI summary behind a new key `settings.crm.ai.chatSummary` (default **false**), written straight into `aiSummary` of the system-authored CHAT activity (no proposal — no human in the loop), only when the activity is created, only with credit; failure never blocks the activity.
8. **CONFIRMED — calendar merge** extends `activities.calendar` with `appointments[]` (additive; C1.6 shape kept). Sources: BOOKING = `Appointment` (not CANCELLED) · CLINIC = `ClinicVisit` (ClinicAppointment has no partyId) with NO symptom/diagnosis/fee · SCHOOL = `SchoolEnrollment` ENROLLED|PAID at `class.startDate`. Scope = Parties of contacts the actor can see ∩ actor unit scope (health rows gated by unit — CRM-RUN §4 C1.1 note). Booking/clinic/school have no `index.ts` today ⇒ C2.4 creates the three facades (read-only) + edges `crm→booking` `crm→clinic` `crm→school` in ALLOWED_EDGES.
9. **CONFIRMED — reminders** (`remind` cron of blueprint §5.5, CRM-RUN S5): `crm/reminders.ts` + minute job `crm.activity.remind` every 5 min; in-app `AppNotification` to the owner (+ push) once per `(activityId, remindAt)`; recommended mechanism = outbox flag key `crm.activity.reminder#<id>#<remindAtMs>` + consumer (then the event also needs its label — one registry, ids-only payload, per COMMON). A throwing push must not lose or double the reminder.
10. **CONFIRMED — `CrmCallProvider` is interface + pure validator/mapper only; NO HTTP webhook route in C2.4** (a public route would pull in X7 rate limit/HMAC work with no provider to test against). The mapper never copies `from/to` or `recordingUrl` into title/body.
11. **CONFIRMED — recordings**: 25 MB cap (above the 5 MB storage default — oracle proves a 6 MB file passes), audio allowlist = audio subset of storage `ALLOWED_UPLOAD_TYPES`, one recording per CALL (second ⇒ CONFLICT), `removeRecording` is a danger op (confirm + reason ≥ 5), purge `purgeRecordings(now)` with `settings.crm.retention.recordingDays` default **730** (same 2-year default as e-mail bodies, blueprint §C6) — registered as a daily job by C2.10 (R-A).
12. **CONFIRMED — activity DTOs** gain `hasRecording: boolean` only; the link comes from `getRecording` (visibility checked BEFORE `privateFileUrl`, per the `/api/files/[id]` header rule).
13. **CONFIRMED — R-A "book via the booking system"**: `bookingLinkFor` → `/app/u/<booking unit slug>/booking?partyId=…` (no PII in the URL) or `null`; the booking page reading `partyId` is NOT required in C2.4.

Ambiguities resolved by the oracle (flag if you disagree): S5 "remind cron" read as blueprint §5.5 `remind(ctx)` · S7 "ภาพ (6)" checked statically (labels/testids/responsive/read-only chips); the PARITY verdict stays the controller's D7 · X2 covered at service level with API-key-shaped actors (no new REST op/AI tool in C2.4; `crm_transcribe_call` not built, R-E.4) · the "reopen → resolve" cycle of a chat room still yields ONE activity.


### Controller rulings on the proposals (19 Sep · binding)
- R1–R13 CONFIRMED **except R2**.
- **R2 REPLACED**: CRM AI is charged under a new credit source **`CRM_ASSIST`** (added by C2.0 in migration `20261101000001_crm_v2_b_ai_source`, with Thai labels + an owner on/off switch). `note` still carries ids only (`crm.call.transcribe#<activityId>`) as the dedupe/test key. The oracle's credit checks must look for `source: "CRM_ASSIST"` — ORACLE-EDIT by the controller when C2.0 lands (the author wrote MEMBER_ASSIST under the old assumption).
- S1.2/U.4 file-location assumption (CrmClickToCall under the contact/deal page trees) stands; the builder places the button there.

## Controller ruling — หลัง builder (24 ก.ย. 2569 · Fable 5.1 · binding)
builder จบ 78/78 (baseline 9/78) · ถอยหลัง 12 ชุดพิมพ์ผลจริง · probe before/after 6 ตัว (`shark-crm-c20/.qc-shots/c24/`) · ยืนยันการตัดสินใจ 11 ข้อ: ปุ่มโทร C1.4 (`contact-call-link`) ถูกแทนด้วย `crm-call-tel` ที่เปิดโมดัลด้วย (แถวทะเบียนแทนที่) · client components ไม่แตะโมดูล CRM (F2.3) ผ่าน `_actions/calls.ts` "use server" · แปลงเวลา/ระยะเวลาเฉพาะฝั่ง server · `appointmentsByParty` อยู่ใน `service.ts` ของแต่ละโมดูล + `index.ts` re-export อ่านอย่างเดียว (ratchet f5RawPrisma) · `crm/index.ts` export `reminders` · `crm.activity.reminder` ใน `webhooks/labels.ts` (ธง dedupe ของ cron ไม่ใช่ trigger ธุรกิจ) · push ยิงครั้งเดียวไม่ retry (ธงปลูกแล้ว = แจ้งเตือนในแอปมีอยู่แน่) · ถอดเสียงไม่ถือ tx ข้าม STT/AI (claim row แทน) · `acceptLeadProposal force:true` + `sourceDetail.via="card-scan"` · docs regen · expected.json ของ QC2 ห้ามเข้า commit
→ ผู้ตรวจอ่านอย่างเดียวก่อนรวม (สั่ง 08:35 UTC)
