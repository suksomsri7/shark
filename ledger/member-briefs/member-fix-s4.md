# S4 — PDPA, reports, referral fraud, webhooks, misc
Read `member-fix-COMMON.md` first, then the matching rows in `ledger/AUDIT-2026-09-16-MEMBER.md`.

## Files this batch OWNS
`src/lib/modules/member/privacy.ts` · `reports.ts` · `reports-actions.ts` · `referrals.ts` · `join.ts` · `reviews.ts` · `push-devices.ts` · `src/lib/webhooks/service.ts` · `src/lib/modules/member/api/ops/webhooks.ts`
Read-only: `customer-session.ts` (owned by S1 — you may IMPORT `revokeAllCustomerSessions`), `src/lib/core/csv.ts`, `src/lib/core/rate-limit-db.ts`.

## Findings → acceptance criteria
- **M5 + M6 (erase half)** `eraseMember`, in the same transaction: blank `MemberReview.body/replyBody`, clear `photoFileIds` and delete those FileAssets (follow how files are deleted elsewhere); `Referral.refereeContact = null` for referrals where this member is referee (and scrub name where they are referrer-side contact only if it is theirs); blank `MemberNotification.subject/body` (keep row + status for audit); revoke every `CustomerSession`; delete `CustomerOtp` rows of this customer/target; delete the avatar FileAsset. Report PLAUSIBLE leftovers you did not touch (AutomationRun.payload, AiMessage) with a recommendation.
- **L8** `logAccess` also records API-key access with no `userId` (store key id / "apikey:<id>" in the actor column that exists; no schema change — if impossible without schema, DEFERRED + reason).
- **M2** report CSV cells go through the shared `csvCell`/`neutralizeFormula` from `src/lib/core/csv.ts` (cells starting with `= + - @ \t \r` are neutralised); BOM + Thai stay intact.
- **M13** reports: no unbounded `findMany` — aggregate in DB (`groupBy`/`count`/`aggregate`) or hard caps with a visible "แสดงบางส่วน" flag; actors with a non-empty `unitAccess` see only members whose scope matches the list rule in `access.ts` (same rule as the member list). OWNER numbers unchanged (existing oracle `qc-member-m3.8.mts` must stay green).
- **M14** referral anti-fraud: fingerprint + hashed IP are checked across the WHOLE tenant (any referrer) within the programme window, not only the same referrer; `monthlyCap` null is treated as a safe default (e.g. 30) server-side. Client-supplied deviceId is still accepted but no longer the only signal (add server-derived ip hash passed from `join.ts`).
- **M1** outgoing webhooks (shared service — keep kanban/account behaviour compatible): block private/loopback/link-local/metadata hosts at create AND at delivery time (resolve DNS, reject 10/8, 172.16/12, 192.168/16, 127/8, 169.254/16, ::1, fc00::/7, `localhost`); allow them only when `APP_ENV !== "production"` AND env `WEBHOOK_ALLOW_PRIVATE=1` (QC oracles use 127.0.0.1 receivers — check existing webhook oracles and keep them green). Add header `X-Shark-Timestamp` and a second signature header `X-Shark-Signature-V2 = HMAC(secret, timestamp + "." + body)` (keep the old header for compatibility). Retry: exponential backoff per delivery (skip rows whose next attempt time has not come — derive from `attempts` + `updatedAt`, no schema change).
- **L2** review tokens expire (30 days from `requestSentAt`; expired ⇒ Thai "ลิงก์หมดอายุ"). **L3** the "done" branch of `reviewLiffView` verifies the token hash too. **L6** push device re-bind: a token already bound to ANOTHER customer is re-bound only when the same `installationId` is presented (else create is refused / old binding kept) — if no installationId column exists, DEFERRED + reason.
