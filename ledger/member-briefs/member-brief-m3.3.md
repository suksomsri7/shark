คุณคือ builder ของ WO **M3.3** ระบบสมาชิก v2 (SHARK) — journey อัตโนมัติ: AutomationRule scope MEMBER_JOURNEY · trigger 24 event สมาชิก + cron (member.birthday.upcoming{daysBefore} · member.inactive{days} · member.tier.review_due) · condition = SegmentDefinition (engine M3.1) · action 10 (WAIT_THEN → AutomationRun WAITING scheduledAt + cron รายชั่วโมง) · re-entry · quota MEMBER · loop guard · holdout (hash คงที่) · dry-run · stats ขั้น×คน + holdout เทียบ · 6 journey สำเร็จรูป · migration `member_v2_g3` · หน้า /member/journeys (+/new +/[journeyId]) ภาพ 07 บน · 22

อ่านก่อนเริ่ม (ตามลำดับ):
1. `ledger/member-briefs/member-builder-common.md` — กติกาตายตัว + วิธีส่งมอบ (**'use client' ห้าม import โมดูลที่ลากถึง prisma** → `*-shared.ts` + server action · use server ห้าม export type · parity ของ Fable)
2. `ledger/MEMBER-RUN.md` §0.1 · §2 M3.3 · §4 (มติล่าสุด)
3. **ข้อสอบ `scripts/qc-member-m3.3.mts` = สัญญาฉบับเต็ม** (หัวไฟล์ = schema/API/deps/ไฟล์/testid/ป้าย) — อ่านทุกข้อ · SKIP guard บอกชื่อ migration/ไฟล์ · ข้อสอบฉีด `deps: { line, email, sms, push, kanban }` ⇒ ตัวส่งจริงต้องเป็นค่าปริยายที่แทนที่ได้
4. `ledger/wo-notes/member-M3.2.md` (campaigns v2: ตัวส่ง 4 ช่องทาง + consent ณ เวลาส่ง + hashPct holdout — **ใช้ตัวส่งเดิมซ้ำ ห้ามเขียนใหม่** · `core/sms.ts` · `chat/index.ts pushToContact` · `MemberPushDevice`) · M3.1 (segments facade `evaluateSegment` — ประเมินลูกค้ารายคน) · M2.5 (voucher issue origin JOURNEY/AUTOMATION idempotent) · M2.2 (points give) · `automation/*` เดิม (AutomationRule/Run v1 + AutomationBuilder component — ขยาย additive · reuse builder ด้วย scope prop ถ้าทำได้)
5. ภาพ `ledger/design-member/07-promotion-journey.png` (บน: builder ประโยค + ตาราง Journey ที่เปิดใช้อยู่) และ `22-*.png` (หน้ารายละเอียด journey) — UI ต้องตรงภาพ · ไอคอนผ่าน `MemberIcon` · กริดยุบบนมือถือ

ขอบเขตไฟล์: `prisma/schema/automation*.prisma`/`member.prisma` (additive) + `prisma/migrations/20261027000000_member_v2_g3/` · `src/lib/modules/member/{journeys.ts, journey-presets.ts, journeys-actions.ts, journeys-shared.ts}` · หน้า `src/app/app/sys/[id]/member/journeys/{page,new/page,[journeyId]/page}.tsx` · `src/components/member/Journey*.tsx` · Edit เฉพาะจุด: `member/index.ts` · `member/nav.ts` (journeys → ready) · `member/limits.ts` (journeys 50 · automationRunsPerMonth 5000) · `outbox-consumers.ts` (withAutomation → runForEvent best-effort) · `automation/labels.ts` · `webhooks/labels.ts` · `platform/cron.ts` (รายชั่วโมง journeyWaits · รายวัน emitJourneyCronEvents) · `core/scope.ts`

regressions ต้องผ่านเท่าเดิม: `qc-member-m3.2` 27/27 · `m3.1` 20/20 · `m2.5` 26/26 · `m2.2` · `qc-automation*` เดิมถ้าใช้ QC env (เปิดหัวไฟล์ดูก่อน — ถ้า loadEnvFile(".env") ห้ามรัน) · fitness 2 โหมด · **ก่อนส่งมอบ grep ไฟล์ 'use client' ของคุณว่าไม่ import โมดูล prisma**

ส่งมอบตาม common.md (wo-notes `ledger/wo-notes/member-M3.3.md` · ตอบสั้น: ไฟล์ · ผลข้อสอบ x/y · ข้อค้าง+เหตุผล) · ห้าม build/commit/push · ห้ามรัน suite ใดที่ใช้ `.env` (prod) — M3.2 เคยละเมิด (qc-webhook.mts) ถูกบันทึกไว้แล้ว
