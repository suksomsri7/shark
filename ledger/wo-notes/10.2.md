# WO 10.2 — เอกสาร/handover

เริ่ม: 2026-09-05 (agent เอกสาร; ทำงานคู่กับอีก agent ที่แก้ UI WO 10.1 ใน src/** — ห้ามแตะ src/**,scripts/** ยกเว้นลบ dev-components ถ้าเข้าเงื่อนไข)

## ขอบเขต
- A. docs/modules/12-account.md — อัปเดตให้ตรง V2
- B. docs/UI_STANDARD.md — เพิ่มกติกา UI V2 accounting
- C. docs/sds/account-v2.md (หรืออัปเดตของเดิม)
- D. ledger/HANDOVER-2026-09-05-ACCOUNT-V2.md
- E. cleanup dev-components (ถ้าปลอดภัย) + list TODO(WO x.y) markers
- F. ต่อท้าย ACCOUNT-V2-RUN.md ส่วน "ของที่ต้องส่งต่อ" (ไม่แตะ current-WO block)

## Log

- อ่านครบ: ACCOUNT-V2-RUN.md ทั้งไฟล์ (ตาราง WO 0.1–9.4 + บันทึกเหตุการณ์ + "ของที่ต้องส่งต่อ"), docs/modules/12-account.md ทั้งไฟล์ (1164 บรรทัด — สเปคเดิม P1–P4 ก่อน V2), docs/UI_STANDARD.md ทั้งไฟล์, docs/design/account-v2/SITEMAP.md (แม็ป 9 หมวด → f2–f10), BLUEPRINT-ACCOUNT-V2.md §0–§2, nav.ts (9 กลุ่ม: home/revenue/expense/contacts/products/finance/accounting/documents/settings), permissions.ts (36 คีย์ account.*), prisma/schema/account.prisma + account_gl.prisma (model list เต็ม), party.prisma (Party), inventory.prisma (InvItem), รายชื่อ migration 20260903–20260916 (24 ตัว), รายชื่อ qc-acc-v2-*.mts (46 suites)
- grep dev-components: อ้างอิงใน guard.ts (คาดหวัง) + qc-acc-v2-components.mts + **scripts/visual-acc-v2.mts:687 (key "gallery" — ฮาร์ดเนส visual ที่อีก agent เป็นเจ้าของ)** → ตามกติกา WO: **ห้ามลบ** dev-components เพราะ visual harness อ้างอิงอยู่ → บันทึกไว้ให้ Fable ตัดสินใจแทน (ดู §E ท้ายไฟล์นี้)
- grep TODO(WO: เจอ 2 จุด — src/proxy.ts:18 (TODO(WO-0025 ต่อ) — ไม่เกี่ยวกับ account v2, เป็นงาน storefront เก่า) และ src/lib/modules/account/contacts-list.ts:553 (TODO(WO 3.4): แทนที่ด้วยแผงเลื่อน 360° — WO 3.4 ทำ DONE ไปแล้วตาม RUN.md แต่คอมเมนต์ยังไม่ถูกลบ — ค้างค้นให้ Fable/agent coding ตัดสิน)
- เริ่มเขียน A (12-account.md) ต่อจากนี้ — แผน: ไม่ rewrite ทั้งไฟล์ ใช้วิธีแทรก/แก้ block ต่อหัวข้อที่ WO สั่ง (§1,§3,§4,§6,§8,§9,§11) ให้สะท้อน V2 จริง โดยคงโครงเดิมและ badge เฟส P1-P4 (ของเดิมยังถูกต้องเชิงสถาปัตยกรรม เปลี่ยนแค่ "หน้าตา/เมนู/ของที่เพิ่ม")

## สรุปเสร็จ (5 ก.ย. 2026)

**A. `docs/modules/12-account.md`** (1164→1270 บรรทัด) — เพิ่ม: หมายเหตุ V2 หัวไฟล์ · §1.3 มาร์ก 🕓 ยังไม่ทำ 6 รายการ (e-Tax/bank feed/FIFO/e-WHT/DBD/Smart Insight) พร้อมเหตุผล · §3.9 ใหม่ สรุปของที่ V2 ส่งจริงต่อ 9 หมวดเมนู (อ้าง WO) · §4.15 ใหม่ ตาราง 19 ตาราง/คอลัมน์ใหม่ผูกกับ migration จริง (grep `CREATE TABLE` จากทุกไฟล์ 20260902160000–20260916000000) · §6.1 ใหม่ แม็ป f1–f14/g1–g20 → หมวด/route/WO (จาก SITEMAP.md + DESIGN-SPEC-V2.md) · §8.4 ใหม่ Party/InvItem/POS-lines/chat-inbox/outbox event/Beam ตัวจริง · §9 เพิ่มหมายเหตุ 36 คีย์จริง (ของเดิมมี ~15) · §11 เพิ่มข้อ 18-24 (ล็อกก่อนวันที่/ปิดงวด/เลขรัน/idempotency/rate limit/undo/as-of) อ้าง chokepoint จริงในโค้ด (`gl.ts: commitEntry`, `assertNotLockedGl`, `service.ts` FOR UPDATE)

**B. `docs/UI_STANDARD.md`** (324→361 บรรทัด) — เพิ่ม `--color-accent-soft` เข้า allowlist token (มีจริงใน globals.css) · §2.11 ใหม่ DocTable/RowActions/PortalMenu (page size 8 ตาม f3, bulk bar tint, trigger มีป้าย) · §2.12 ใหม่ HelpTip/DateInput/UndoToast/AccountIcon/โหมดง่าย-นักบัญชี (ตรวจจากโค้ดจริงทุกตัว: HelpTip.tsx, DateInput.tsx, UndoToast.tsx, mode.ts) · §6 ใหม่ กติกามือถือบัญชี V2 (อ้าง DESIGN-SPEC-V2 §13 + เฟรม f11-f14/g17-g20) · Checklist (เดิม §5 → เลื่อนเป็น §7) เพิ่ม 3 ข้อสำหรับบัญชี V2

**C. SDS** — พบไฟล์เดิมอยู่แล้ว `docs/sds/modules/account.md` (54→104 บรรทัด) → อัปเดตแทนสร้างใหม่ตามกติกา WO (มีไฟล์อยู่แล้ว = อัปเดต ไม่สร้าง `account-v2.md` ใหม่) — เพิ่ม: module/file map 70+ ไฟล์จัดกลุ่มตามหน้าที่ (grep `ls *.ts` จริง) · GL chokepoints (ยืนยัน `commitEntry` เป็นชื่อจริงใน gl.ts บรรทัด 219, `assertNotLockedGl`, row-lock ใน service.ts บรรทัด ~2344-2404) · QC harness (acc-v2-env.mts/seed-acc-v2-qc.mts/expected generators/qc:all/visual-acc-v2.mts/prod gate) · cron 2 บรรทัดจริงจาก `crontab -l` · env vars ยืนยันจาก grep `process.env.*` จริงในซอร์ส (BEAM_*, DBD_*, RESEND_API_KEY, SHARK_BUNNY_*, APP_URL) · deployment (Vercel build รัน migrate)

**D. `ledger/HANDOVER-2026-09-05-ACCOUNT-V2.md`** (ใหม่ 114 บรรทัด) — ภาษาไทยล้วน ไม่มี jargon: ใช้งานได้บน prod ต่อ 9 หมวด+route, ต้องตั้งค่าเอง 6 ข้อ (Beam/DBD/Resend/Bunny/crontab/ลบ tenant ทดสอบ), ของค้างจัดกลุ่ม 8 หมวด (ดึงจาก RUN.md ทุกแถว WO dedupe แล้ว) แยก 🕓 ประกาศล่วงหน้ากับของที่เลื่อนจริง, หลักฐาน QC (227/227·46 suites·security 20/20·perf 29 หน้า·visual 34 เฟรม), วิธีกลับมาทำต่อ 6 ขั้น · **หมายเหตุสำคัญที่เขียนไว้ในไฟล์**: ระบุชัดว่า WO 10.1/10.3 ยังไม่จบตอนเขียน (main = a4611ba หลัง 9.4, session/accounting นำหน้า 5 commit) — บอกผู้อ่านให้เชื่อ RUN.md มากกว่าถ้าไม่ตรง

**E. Cleanup** — `dev-components` **ไม่ลบ** (grep ยืนยัน `scripts/visual-acc-v2.mts:687` อ้างอิงเป็น key "gallery" ในฮาร์เนสภาพที่อีก agent เป็นเจ้าของ — เข้าเงื่อนไข "ห้ามลบ" ตาม WO ตรง ๆ) · จดไว้ให้ Fable ใน RUN.md ส่วน "ของที่ต้องส่งต่อ" แล้ว · TODO(WO x.y) markers พบ 2 จุด: `src/proxy.ts:18` (ไม่เกี่ยว account — เป็นเลขงาน WO-0025 ของ storefront เก่า) และ `src/lib/modules/account/contacts-list.ts:553` (ค้างจริง — WO 3.4 ปิดไปแล้วแต่คอมเมนต์ยังอยู่ จดไว้ใน RUN.md ให้คนไปดูว่าโค้ดเก่ายังใช้อยู่จริงไหม) — ไม่ได้แก้/ลบทั้งคู่ ตามขอบเขต (docs/ledger เท่านั้น)

**F. `ledger/ACCOUNT-V2-RUN.md`** — ไม่แตะ "WO ปัจจุบัน" block ตามกติกา · เพิ่ม 3 บรรทัดใหม่ท้าย "ของที่ต้องส่งต่อ": RESEND_API_KEY+crontab email-reports (8.2), SHARK_BUNNY_* (7.1/7.2 — ตั้งบน prod แล้วหรือยังไม่ยืนยัน), TODO stale ใน contacts-list.ts:553

**ที่ตรวจสอบไม่ได้จาก source ที่มี (บอกตรง ๆ แทนเดา)**:
- ไม่ได้ยืนยันว่า `SHARK_BUNNY_*` ที่ WO 7.1/7.2 ใช้ เป็นชุดเดียวกับที่ memory reference บอกว่า "_bunny_shark ขาด pull zone" หรือคนละชุด — เขียนไว้เป็นคำถามเปิดใน RUN.md ไม่ได้ฟันธง
- ไม่ได้ยืนยันด้วยตาว่า main (`a4611ba`) deploy ขึ้น prod จริงบน Vercel แล้วหรือยัง (ไม่ได้รัน build/curl ตามขอบเขต WO ที่ห้ามรัน build/qc) — เขียนกำกับไว้ชัดในทั้ง 12-account.md หัวไฟล์และ HANDOVER ว่ารอ WO 10.3 ยืนยัน
- ไม่ได้เปิดอ่าน wo-notes/*.md ทั้ง 46 ไฟล์แบบเต็ม (ใช้สรุปในคอลัมน์ RUN.md ซึ่งละเอียดพอแทน) — ถ้ามีรายละเอียดย่อยที่ตกหล่นจาก wo-notes ที่ไม่ได้ขึ้น RUN.md อาจไม่ถูกจับ
