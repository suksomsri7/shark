# WO 6.1 — ผังบัญชี V2 (recovery log)

> อัปเดตทุกขั้น · ถ้า agent ตาย ให้ตัวใหม่อ่านไฟล์นี้แล้วทำต่อจาก "ขั้นที่ถึง"

## ขั้นที่ถึง
- [x] 0. อ่าน RUN.md + BLUEPRINT §1–2 + แถว 6.1 + SPEC §11.1/§14
- [x] 1. ดูภาพ f8 / f8-menu / f13 → เขียน checklist parity
- [x] 2. สำรวจโค้ดเดิม ✅ (coa.ts · accounts/page.tsx เดิม · ledger/page.tsx `?account=<id>` · finance-ui/FinancePanel เป็น pattern · import-shared/import-actions/ImportWizard · seed §9 เขียนเฉลย · visual PAGES/ASSERT · qc-all auto-discovery `qc-*.mts`) (coa.ts · accounts/page.tsx · ledger/page.tsx · reports.ts · schema)
- [x] 3. migration additive ✅ `20260908000000_account_v2_coa_v2` (ADD COLUMN nullable 6 ตัวบน AccountLedger: description/defaultWhtRateBp/defaultWhtType/vatTreatment/level/sortOrder) · deploy บน QC (ep-plain-art) ✅ · generate ✅ · drift = No difference ✅ · **ยังไม่แตะ prod**
- [x] 4. B ตรรกะ ✅ — `coa-v2.ts` (บริสุทธิ์ ไม่ import prisma: ชื่อหมวด 2/3 หลัก · levelOf/prefixOf · codeRangeOf ·
      buildChartTree · subGroupOptions · flattenChart · archiveBlockReason · validateLedgerInput · ป้าย VAT/WHT)
      + ส่วนที่แตะ DB ต่อท้าย `coa.ts` (chartTree 2 query · ledgerDetail · createLedgerV2 · updateLedgerV2 ·
      setLedgerActive · usedLedgerCodes · createLedgerFromImport)
      🔴 เหตุผลที่ไม่เอา prisma ไว้ใน coa-v2.ts: fitness F5.1 นับไฟล์ในโมดูลที่ import prisma ตรง (baseline 45 ·
      ratchet ลงได้อย่างเดียว) — ไฟล์ใหม่ = 46 → แดงทันที (วัดจริงด้วย grep ก่อนเขียน)
- [x] 5. C UI ✅ — `ChartPanel.tsx` (client: หัวหน้า+ปุ่ม 4 · ค้นหากรองสด · ต้นไม้ 3 ระดับ ปุ่ม −/+ ·
      ย่อ/ขยายทั้งหมด · มือถือสลับต้นไม้/รายละเอียด) · `coa-ui.tsx` (server: แผงขวา 2 การ์ดตาม f8) ·
      `LedgerActiveToggle.tsx` · `LedgerModal.tsx` · `accounts/page.tsx` เขียนใหม่ · `accounts/print` ·
      `accounts/mapping` (ย้ายบล็อกผูกบัญชีอัตโนมัติ) · `accounts/actions.ts` · `import/chart-of-accounts` ·
      guard 4 route ใหม่ · AccountIcon +minus/plus/printer · typecheck 0 ✅
- [x] 6. D seed/เฉลย/fixture ✅ — seed บล็อก 8.9 (+6301 ค่าโฆษณาออนไลน์ WHT 2%/ภาษีซื้อขอคืนได้ · +4031 รายได้
      ค่าเช่าอุปกรณ์ · +6302 ปิดใช้งาน) **ไม่มี movement ⇒ ตัวเลขเงินเดิมไม่ขยับ** · เฉลยคีย์ `coa` (SQL ดิบ:
      activeAccounts 52 · byType · byGroup2 · samples 1000-01/1100/4000 balance+month · custom ids) ·
      fixture `scripts/fixtures/acc-v2/coa-import.csv` (10 บัญชี · ซ้ำ 4910 · นอกช่วง 6410) ·
      seed-check +หมวด O (8 ข้อ) → **139/139**
- [x] 7. qc-acc-v2-coa.mts ✅ **92/92** (T1 ต้นไม้ · T2 ค้นหา · T3 ยอด=SQL+งบทดลอง · T4 เคลื่อนไหว 5 แถว · T5 ป้าย/ฟิลด์ · T6 ตรรกะบริสุทธิ์ · T7 สร้าง · T8 ปิดไม่ได้ 4 กรณี · T9 ปิด+กู้คืน · T10 นำเข้า CSV 8/0 · T11 ปักหมุด · T12 guard · T13 แยกร้าน)
- [ ] 8. visual 6.1
- [ ] 9. gates

## บันทึก
- (เริ่ม) HEAD = ddb51f7 · working tree สะอาด

## 1. Parity checklist จาก f8-chart-of-accounts.png (1440) — เขียนก่อนเขียนโค้ด
หัวหน้า
- [ ] breadcrumb "บัญชี › ผังบัญชี"
- [ ] H1 "ผังบัญชี" + ตัวนับสีจาง "N บัญชี" ต่อท้ายบรรทัดเดียวกัน
- [ ] ปุ่มขวาเรียง ซ้าย→ขวา: "นำเข้าผังบัญชี" (ไอคอน import) · "พิมพ์" (ไอคอนเครื่องพิมพ์) · "+ เพิ่มบัญชีเงิน" · "+ เพิ่มบัญชี" (ปุ่มดำ)
การ์ดซ้าย (มีกรอบ มุมมน)
- [ ] หัวการ์ด "รายการบัญชี" (หนา) + ลิงก์ฟ้าขวา "ย่อ/ขยายทั้งหมด"
- [ ] ช่องค้นหา ไอคอนแว่น placeholder "ค้นหาชื่อหรือเลขที่บัญชี"
- [ ] tree: ปุ่มกล่องสี่เหลี่ยม −/+ หน้าโหนดกลุ่ม · ระดับ 1/2 แสดง "ชื่อ (รหัส)" · ระดับ 3 แสดงชื่อ · ใบ = "รหัส · ชื่อ"
- [ ] แถวที่เลือก = พื้นเทาอ่อน มุมมน ตัวหนา
- [ ] เยื้องเพิ่มทีละระดับ
การ์ดขวาบน
- [ ] บรรทัดหัว: "รหัส · ชื่อ" ตัวหนา + ลิงก์ฟ้า "ดูบัญชีแยกประเภท ›" · ขวาสุด "เปิดใช้งาน" + สวิตช์
- [ ] แถวป้าย: "บัญชีระบบ" (ป้ายขอบเข้ม) · "แก้ชื่อได้ ลบไม่ได้" · "ผูกกับบัญชีเงิน" (ป้ายเทา)
- [ ] ตารางฟิลด์ 2 คอลัมน์ มีเส้นคั่นบาง: ซ้าย=ผังบัญชีหลัก/ผังบัญชีรอง/ผังบัญชีย่อย/ประเภทบัญชี · ขวา=ผูกกับบัญชีเงิน(ลิงก์ฟ้า)/อัตราหัก ณ ที่จ่ายเริ่มต้น/ประเภทภาษี/คำอธิบาย
- [ ] ท้ายการ์ด: "ยอดคงเหลือ ณ <วันที่ไทย>" + ตัวเลขใหญ่ · "เคลื่อนไหวเดือนนี้" + ตัวเลข · ปุ่มขอบ "แก้ไขบัญชี" (ไอคอนดินสอ) ชิดขวา
การ์ดขวาล่าง
- [ ] หัว "เคลื่อนไหวล่าสุด" + ลิงก์ฟ้า "ดูทั้งหมด ›"
- [ ] ตาราง 6 คอลัมน์: วันที่ · ใบสำคัญ (ลิงก์ฟ้า JV) · รายการ · เดบิต · เครดิต · คงเหลือ (3 ตัวท้ายชิดขวา) · 5 แถว · ช่องว่าง = "—"
f8-menu = เมนู "บัญชี" บนแถบ nav กางอยู่ (ของ WO 0.4 อยู่แล้ว) → ถ่ายซ้ำเป็นสถานะที่ 2
มือถือ 390 (แบบ f13): tree เป็นลิสต์ accordion เต็มความกว้าง · แตะบัญชี = แผ่นเต็มจอรายละเอียด

## 8–9. ภาพ + ด่าน (ทำแล้ว)
- [x] 8. `visual-acc-v2.mts 6.1 --assert` → **ผ่านทั้งหมด** · 8 ภาพใน `.qc-shots/acc-v2/6.1/`:
      chart-of-accounts-desktop · chart-of-accounts-menu-desktop · chart-add-modal-desktop ·
      chart-import-preview-desktop · chart-print-desktop · chart-mapping-desktop ·
      chart-tree-mobile-mobile · chart-detail-mobile-mobile
- [x] 9. ด่าน (ทีละอย่าง · export env จาก .env.qc · ปิดเซิร์ฟเวอร์ก่อนรัน tsc/ชุดทดสอบ)

| ด่าน | ผล |
|---|---|
| `with-gate-lock.sh pnpm typecheck` | 0 error (ต้องตั้ง `NODE_OPTIONS=--max-old-space-size=3584` ไม่งั้น tsc OOM) |
| `pnpm exec tsx scripts/fitness.mts` | 17/17 |
| qc-acc-v2-coa | **92/92** |
| qc-acc-v2-components | 78/78 |
| qc-acc-v2-seed-check | **139/139** (เดิม 129 + หมวด O 10 ข้อ) |
| qc-acc-v2-guard | 162/0 |
| qc-acc-v2-schema | 61/0 |
| qc-acc-v2-nav | 323/0 |
| qc-acc-v2-list | 159 |
| qc-acc-v2-import | 114/0 |
| qc-acc-v2-dashboard | 174/0 |
| qc-acc-v2-home | 87/0 |
| qc-acc-v2-finance | 59/0 |
| qc-acc-v2-cheap-routes | 105/0 |
| `pnpm run drift` | No difference detected |
| `pnpm qc:account` (CPA) | 107/107 |
| `visual-acc-v2.mts 6.1 --assert` | ผ่านทั้งหมด |

## ต่างจาก f8 ตรงไหน + เพราะอะไร
1. **ตัวเลขจำนวนบัญชีชิดขวาของทุกโหนดกลุ่ม** — f8 ไม่มี แต่ SPEC §11.1 บังคับ "จำนวนต่อหมวด" ⇒ ใส่เป็นตัวเลขจาง
   (วงเล็บหลังชื่อกลุ่มใน f8 = **รหัส** ไม่ใช่จำนวน — ทำตาม f8 แล้ว)
2. **ป้ายบนแผงขวาของบัญชีในภาพ** เหลือ "ผูกกับบัญชีเงิน" ใบเดียว (f8 มี 3 ป้าย) — ข้อมูลจริงบังคับ:
   บัญชีที่ผูกช่องทางเงินของเรา (1000-01) ไม่ใช่ `isSystem` ⇒ ไม่มีป้าย "บัญชีระบบ/แก้ชื่อได้ ลบไม่ได้"
   (เปิดบัญชีระบบเช่น 1150 จะเห็นครบ 2 ป้ายนั้นแทน)
3. **ความยาวรหัส** — f8 เป็นผัง PEAK 6 หลัก (111101) ผังจริงของ SHARK เป็น 4 หลัก + บัญชีลูกช่องทางเงิน
   (1000-01) ⇒ ระดับกลุ่มคิดจากคำนำหน้า 2/3 หลัก โครง 4 ชั้นเท่ากับ f8 แต่ตัวเลขสั้นกว่า
4. **หมวดระดับ 1 ผูกกับ "ประเภทบัญชี" ไม่ใช่ตัวเลขหลักแรก** — ได้ 6 หมวดตรง f8 พอดี · ผลข้างเคียง: 9999
   (ประเภทสินทรัพย์) ไปอยู่ใต้ "สินทรัพย์ (1)" ในหมวดรอง "บัญชีพัก (99)"
5. **ปุ่ม "+ เพิ่มบัญชีเงิน"** — ใช้ป้ายตาม f8 (ใบสั่งงานเขียน "+ เพิ่มช่องทางเงิน") · ปลายทาง = modal 5.1
6. **แถวฟิลด์ "ใช้ลงบัญชีอัตโนมัติ"** เพิ่มจาก f8 เฉพาะบัญชีที่มี AccountMapping ชี้อยู่ — เพราะบล็อก
   "การผูกบัญชีอัตโนมัติ" ของหน้าเดิมถูกย้ายไป `/accounts/mapping` (f8 ไม่มีบล็อกนั้น) จึงต้องมีทางเข้า
7. **มือถือ**: ปุ่มรอง 3 ตัวยุบเป็นเมนู "⋯" + เคลื่อนไหวล่าสุดเป็นลิสต์ย่อ (ตาม f13/f14 ไม่ใช่ตารางเลื่อนข้าง)
8. **เมนู "บัญชี" (f8-menu)**: แก้ให้ตรงเฟรม — "บัญชีรายวัน"→"สมุดรายวัน" · "งบฐานะการเงิน"→"งบแสดงฐานะการเงิน" ·
   "สินทรัพย์"→"ทะเบียนสินทรัพย์" · "อายุหนี้"→"อายุหนี้ (ลูกหนี้-เจ้าหนี้)" · เรียงใหม่ ปิดงวด→ทะเบียนสินทรัพย์→อายุหนี้
   ยังต่าง 2 จุด: (ก) "DBD e-Filing" ไม่เติมคำว่า "ยื่นงบ" เพราะ qc-acc-v2-nav ตรวจว่าป้าย 🕓 ต้องตรง SPEC §2
   (ข) f8 มีลูกศร › (flyout) ที่ "บัญชีแยกประเภท"/"ทะเบียนสินทรัพย์" — ยังไม่มี flyout (ของ shell WO 0.4)
9. **breadcrumb** เดิมขึ้น "บัญชี › บัญชี › ผังบัญชี" (หมวดชื่อซ้ำกับราก) — แก้ใน AccountBreadcrumb ให้เหลือ
   "บัญชี › ผังบัญชี" ตาม f8

## ค้าง / ส่งต่อ
- `level`/`sortOrder` ยังเป็นคอลัมน์เปล่า (บัญชีที่สร้างใหม่ตั้ง level=4) — ใช้ตอนมีผังหลายระดับจริงในอนาคต
- flyout ของเมนู "บัญชีแยกประเภท"/"ทะเบียนสินทรัพย์" ตาม f8-menu (งานของ shell)
- `/accounts/mapping` ยังไม่มีทางเข้าจากเมนู (เข้าจากแผงขวาเท่านั้น) — ควรผูกเข้า §8.2 นโยบายบัญชี (WO 8.2)
- ป้ายเมนู DBD ("ยื่นงบ DBD e-Filing" ตาม f8) ต้องแก้ SPEC §2 ก่อนถึงจะเปลี่ยนได้
- 🐞 พบระหว่างทาง (ไม่ได้แก้ในนี้): `account/ledger/page.tsx` (บัญชีแยกประเภท) กรอง `entry.status = POSTED`
  ⇒ รายการที่ถูกกลับรายการจะเหลือแต่ขากลับ ยอดในหน้านั้นเพี้ยนได้ (reports.ts/finance.ts/หน้านี้รวมทุกสถานะ)
  → ส่งต่อ WO 6.2 (สมุดรายวัน/แยกประเภท V2)
- 🐞 ด่าน `expect` ของ visual อ่าน innerText แค่ 20,000 ตัวแรก — หน้ายาว ๆ ตรวจข้อความท้ายหน้าไม่เจอ (จดไว้ 9.3)

## รอบ 2 (REVIEW ของ Fable) — แก้ 3 เรื่อง
### R1 ยอด "ณ วันที่" จริง ๆ (ไม่ใช่ผลรวมทั้งสมุด)
ปัญหา: ป้ายเขียน "ยอดคงเหลือ ณ <วันนี้>" แต่ตัวเลขรวมทุกบรรทัดรวมถึงรายการที่ลงวันที่ล่วงหน้า
(ชุด QC มีข้อมูลถึง 30 ก.ย. แต่วันนี้ 4 ก.ย.) — **1000-01: ทั้งสมุด 12,488,000 · ณ 4 ก.ย. 11,488,000 สตางค์**
แก้:
- `coa-v2.ts` +`asOfCutoff()` (ตัดที่เที่ยงคืนของ **วันถัดไป** เวลาไทย — รายการทั้งวันนี้ต้องนับครบ)
  +`bkkMonthStart()` +`bkkDayKey()` · `LedgerDetail` +ฟิลด์ `asOf` (UI ใช้ค่านี้ติดป้าย ไม่ใช่ new Date() ของตัวเอง)
- `coa.ts`: `chartTree(ctx,{asOf})` · `ledgerDetail(ctx,id,{asOf})` — ยอด/เคลื่อนไหวเดือนนี้/5 แถวล่าสุด ตัดที่ cutoff
  ⚠️ ตัวนับ "ถูกใช้แล้ว" ของกติกาปิดใช้งาน **ยังนับทุกบรรทัด** (รายการล่วงหน้าก็ถือว่าใช้แล้ว)
- `finance.ts`: `financeBalances(tenantId, systemId, asOf = now)` + `financeMonthChanges` ปิดท้ายช่วงที่สิ้นวันนี้
  ⇒ 5.1 (หน้าช่องทางการเงิน) · 5.2 (ภาพรวม/สำรองจ่าย ผ่าน cashPosition+pettyCashList) · 2.2 (หน้าหลัก) ใช้กติกาเดียวกันหมด
- `dashboard.ts` ส่ง `now` เข้า financeBalances ทั้ง 2 จุด · `finance-overview.pettyCashList(ctx, names, {asOf})`
- เฉลย/ข้อสอบ **ตรึง asOf = QC.today (30 ก.ย. 2026)** ทุกที่: seed (`QC_ASOF`, SQL ของคีย์ `coa` ใส่ cutoff ใน ON) ·
  seed-check · qc-acc-v2-coa (`ASOF`) · qc-acc-v2-finance (`ASOF`/`MONTH_FROM`/`MONTH_TO` แทน now() ของ DB) ·
  finance-overview (pettyCashList asOf) — dashboard/home ส่ง now อยู่แล้วจึงนิ่งเอง ⇒ **ตัวเลขเฉลยไม่เปลี่ยนสักตัว**
  (ยืนยัน: ไม่มี JV หลัง 30 ก.ย. ในชุด QC ⇒ ยอด ณ QC.today = ยอดทั้งสมุดพอดี)
- ภาพ: ตัดการ pin "ยอดเงิน" ออกจาก ASSERT_MAP (6.1 · 5.1 · 5.2 · 2.2 kpi-cash) เพราะหน้าจอคิด ณ วันที่ถ่ายภาพจริง
  เหลือ pin ที่ไม่ขึ้นกับวันที่ (จำนวนบัญชี/ไทล์ปฏิทิน) · ความถูกต้องของตัวเลขพิสูจน์ในข้อสอบที่ตรึง asOf ได้
- ข้อสอบใหม่ **T14** (8 ข้อ): ยอดก่อน/วันเดียวกัน/หลังรายการ · เคลื่อนไหวเดือนนี้ · ป้าย asOf · ต้นไม้ ·
  financeBalances(5.1) = ledgerDetail(6.1) ณ วันเดียวกัน

### R2 🐞 หน้าบัญชีแยกประเภทกรอง status=POSTED (ปลายทางลิงก์ "ดูบัญชีแยกประเภท")
กลับรายการ = ใบเดิมเป็น REVERSED (บรรทัดยังอยู่) + ใบตรงข้าม POSTED ⇒ กรอง POSTED เหลือแต่ขากลับ ยอดเพี้ยน
แก้: ย้าย query ไป `coa.ledgerRunning()` (รวมทุกสถานะเหมือน reports.ts) · หน้าเรียกใช้ตัวเดียวกับข้อสอบ ·
ติดชิป "กลับรายการแล้ว" บนแถวของใบที่ถูกกลับ · เปลี่ยนวันที่ในหน้านี้ให้ใช้ `formatDateTh` (ค.ศ.) แทน พ.ศ. เดิม
ข้อสอบใหม่ **T15** (5 ข้อ): ยอดยกไปของหน้า = ยอดในแผงขวา · หลังกลับรายการ = 0 (เดิมได้ −25,000) · เห็น 2 ขา ·
ชิป 1 ใบ · Dr/Cr ในงวดเท่ากัน

### R3 ด่านภาพอ่าน innerText แค่ 20,000 ตัว
`visual-acc-v2.mts` ขยายเป็น 200,000 + คอมเมนต์อธิบาย ⇒ ภาพ `chart-mapping` กลับมาตรวจคีย์ท้ายหน้าได้
(`VAT_INPUT`/`SUSPENSE`) · เพิ่มภาพ `ledger-page` (หน้าแยกประเภทหลังแก้บั๊ก)

### ด่านรอบ 2 (รันใหม่หลังแก้ · reseed + เฉลย 3 ตัวใหม่)
typecheck 0 · fitness 17/17 · **qc-acc-v2-coa 105/105** (เดิม 92 +T14/T15) · seed-check 139/139 ·
finance 59/0 · finance-overview 45/0 · dashboard 174/0 · home 87/0 · guard 162 · list 159 · import 114 ·
cheap-routes 105 · components 78/78 · nav 323 · schema 61 · drift "No difference detected" · CPA 107/107 ·
visual 6.1 --assert ผ่านทั้งหมด (9 ภาพ)
