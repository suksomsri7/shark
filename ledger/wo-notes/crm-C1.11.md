# WO C1.11 — มือถือ 390 · แผง CRM ในห้องแชท · เทมเพลตธุรกิจ 16 · นำเข้า/ซ้ำ/รวม · สวิตช์ v1↔v2 (ปิดเฟส C1)

> RUN "CRM v2" · 19 ก.ย. 2569 · ผู้คุมงาน Opus 5 · ข้อสอบ `qc-crm-c1.11` 66 ข้อ (รันด้วย `CRM_V2_SWITCH=all`) + `qc-crm-v1` 17 ข้อ · probe-c111-review 20/20 · builder บน QC2 (worktree `shark-crm-c111`)
> ORACLE-EDIT: **C1.11-X9.3** (หน้ารวมส่งสถานะติ๊กยืนยันจริง ไม่ใช่ `confirm: true` ตายตัว · SF-5) · **C1.10-S11.8** (หน้า `/crm/settings` เป็นหน้าสวิตช์ด้วย จึงแยกสาขา uiVersion เองแทน requireCrmV2Page) · probe ประตู V2_ONLY +3 หน้า (นำเข้า · ซ้ำผู้ติดต่อ · ซ้ำบริษัท)

## 1. สิ่งที่ส่งมอบ
- หน้า CRM ทุกหน้าที่ 390 px (กระดาน = แท็บขั้นปัด · ตาราง → การ์ด)
- แผง CRM ในห้องแชท (ร้าน v2 เท่านั้น · ตัดสินที่ server — ร้าน v1 ไม่ mount ไม่ยิง request) · เปิดดีล/บันทึกกิจกรรม/สร้าง lead (audit เป็นคนกด)
- เทมเพลตธุรกิจ 16 แบบ + ตัวเลือกบนหน้าแรก (เลือกแล้วกดยืนยัน · "ไม่ใช้เทมเพลต") · วัตถุในเทมเพลตต้องมีคีย์ `crm.object.manage`
- นำเข้า (จับคู่คอลัมน์ · ซ้ำ = เติมเฉพาะช่องว่างใน tx เดียว · ห้ามแมปฟิลด์ระบบผ่าน `f.`) · หน้าซ้ำ + รวม (เลือกทีละฟิลด์ · เห็นค่าทั้งสองฝั่ง)
- **สวิตช์ v1↔v2**: ร้านจริงไม่เห็น (env `CRM_V2_SWITCH_TENANTS`/`CRM_V2_SWITCH` ไม่ตั้ง) · 2→1 ทำได้เสมอ (ไม่ติดค้าง v2)
- แก้ของ C1.5 ที่ขึ้น prod แล้ว: ข้อความแจ้ง (`?notice=`) ของการสร้าง/ย้ายดีลบนหน้า v1 ไม่แสดง → แสดงแล้ว (markup เดิมเมื่อไม่มี param)

## 2. ผู้ตรวจ
ไม่มี BLOCKER · SHOULD-FIX 7 (ออกใบเสนอราคา v1 เปลี่ยนพฤติกรรม · แผงแชทยิงทุกห้องทุกร้าน · เปิดดีลไม่เติมผู้ติดต่อ · นำเข้าทับฟิลด์ระบบผ่าน REST · รวมบริษัทล้างค่า · lead จากแชท audit เป็น SYSTEM · เลือกเทมเพลตแตะเดียว) + NOTE 9 → แก้ครบ (probe 20/20)

## 3. ด่าน
| # | ผล | หลักฐาน |
|---|---|---|
| D2 | ✅ | 66/66 + v1 17/17 บน QC1 seed ใหม่ (`c111-close.log`) |
| D3 | ✅ | probe-c111-review 20/20 · X3/X6/X9 ในข้อสอบ |
| D4 | ✅ | **qc:all ปิดเฟส C1 = 305/345** (`c1-qcall.log`) → 40 ชุดรันเดี่ยวหลัง reseed + เซิร์ฟเวอร์ (`c1-reds.log`, `c1-reds2.log`) → จำแนก `ledger/crm-c1-close-reds.md`: ที่เหลือแดงมีแต่ข้อที่อ่านภาพ `.qc-shots/member|kanban` (ถูกลบตอนเคลียร์ดิสก์) + K2.3-S3.2 (dnd-kit ของ M1.3 · มีมาก่อน CRM) · m3.1/m3.10 นับสมาชิกเกิน 1 เฉพาะตอนรันต่อจากชุดอื่น = order-dependent (เดี่ยวหลัง reseed เขียว `m31-alone.log`) · ORACLE-EDIT: acc-v2-security S8 · chat-v2-voice VO-11.x (ผลของ C0.4) · pages RG-1 · ลบร้านทดสอบค้าง QCSEC92 · qc-all สร้างเฉลยบัญชีครบ |
| D5/D6 | ✅ | typecheck · fitness 32/32 ×2 · build |
| D7 | ✅ | `.qc-shots/crm/1.11/` owner 390 ทุกหน้า + หน้าแรก/นำเข้า/ซ้ำ/สวิตช์ · thana: นำเข้า/ซ้ำ = 404 ตามแบบ (ไม่มีคีย์) · ประตู v1 14/14 แบบไม่มี env |

## 4. PARITY ภาพ 13
(ก) ดีลของฉัน → กระดานมือถือแท็บขั้น + การ์ด ✅ · ปุ่ม "โทร" บนการ์ด + (ข) โมดัลบันทึกสาย + (ค) สแกนนามบัตร = C2.4
