# WO 9.2 — audit ความปลอดภัยโมดูลบัญชี V2 (รายงานฉบับเต็ม)

> ผู้ทำ: Opus (executor) · Fable นำ/ตรวจ · 4 ก.ย. 2026 · branch `session/accounting` · ฐาน `8834c47` (9.1)
> **ยังไม่ commit** ตามกติกา run · ไฟล์ที่แก้ทั้งหมดอยู่ใน `git status`

## วิธีรันซ้ำ (ทุกคำสั่งต้อง export env ของ QC ก่อน)
```bash
export DATABASE_URL=$(grep -m1 '^DATABASE_URL=' .env.qc | cut -d= -f2-)
export DIRECT_URL=$(grep -m1 '^DIRECT_URL=' .env.qc | cut -d= -f2-)
export APP_ENV=development
# host ต้องมี ep-plain-art · ห้าม ep-royal-night
pnpm exec tsx scripts/qc-acc-v2-security.mts     # 298 ข้อ
```

---

## ตาราง 20 ข้อ (หลักฐาน · ผล · การแก้)

### A. AuthZ / tenancy

| # | หัวข้อ | หลักฐาน | ผล | การแก้ |
|---|---|---|---|---|
| **1** | ทุกหน้า/ทุก action มีด่านสิทธิ์ | `guard.ts:74-190` ทะเบียน `ACCOUNT_PAGE_PERMISSIONS` ครอบ **100 ไฟล์ route ครบทุกไฟล์** · `qc-acc-v2-guard` G1 (ขยายแล้ว) · `qc-acc-v2-security` S1 สแกน **169 `*Action`** + **inline server action ในตัว page.tsx** | **PASS** (แก้ข้อสอบให้เข้มขึ้น) | เดิม G1 เช็กแค่ "ไฟล์มีสตริงชื่อ action" → คอมเมนต์ก็ผ่าน · เพิ่ม: ต้อง **เรียกฟังก์ชันด่านจริง** (`requireAccountPage`/`loadAccountSystem`/`assertAccountCan`) หลังตัดคอมเมนต์ทิ้ง + ต้องไม่มี route ที่ยังไม่ลงทะเบียน · S1 เพิ่มการสแกน inline action ในหน้า (ของเดิมไม่มีใครตรวจ) |
| | ข้อยกเว้นที่บันทึกไว้ | `finance/actions.ts:152 archiveFinanceFormAction` → `archiveFinanceActionDirect` · `payment-actions.ts:202 refundDepositFormAction` → `refundDepositAction` | ยอมรับได้ | 2 ตัวนี้เป็น wrapper บาง ๆ ที่ delegate ต่อ · ข้อสอบยืนยันซ้ำว่ายัง **ไม่แตะ prisma เอง** (ถ้าวันหนึ่งแตะ = แดงทันที) |
| **2** | IDOR (ทุก action ที่รับ id ต้อง scope `tenantId`+`systemId`) | `qc-acc-v2-security` S2 — ยิง **17 ฟังก์ชัน × 2 สโคปผู้บุกรุก** (ร้าน B คนละ tenant · ระบบ A2 tenant เดียวกันคนละระบบ) + ยืนยัน 10 ค่าใน DB ว่าไม่ถูกเขียน + positive control | **FAIL → แก้แล้ว** | 🐞 `product.ts updateProduct`/`updateUnit` ใช้ `updateMany` (scope ถูก ข้อมูลไม่รั่ว) แต่ **คืน `ok:true` ทั้งที่เขียน 0 แถว** → หน้าจอขึ้น "บันทึกแล้ว" ทั้งที่ไม่ได้บันทึก และข้อสอบ IDOR แยกไม่ออกว่าถูกกันจริง ⇒ เช็ก `res.count === 0` แล้วคืน "ไม่พบสินค้า/บริการนี้" (`product.ts:568,196`) |
| **3** | บทบาท STAFF ไม่มีสิทธิ์ → 403 | S3 — 10 action (รับเงิน · ยกเลิก · ยกเลิกชำระ · อนุมัติ · ปิดงวด · ตั้งค่า · ผังบัญชี · ปรับสมุดรายวัน · นำเข้า · รวมผู้ติดต่อ) + positive control OWNER + "สิทธิ์แคบไม่ลามไปกว้าง" + API key/สิทธิ์ผู้ใช้ต้องมี `settings.manage` | **PASS** | — |
| **4** | ลิงก์สาธารณะ `/pay/<token>` · `/r/<token>` | S4 (30 ข้อ) — เอนโทรปี token · หมดอายุ · ปิดสวิตช์ · ไม่รั่ว PII · token มั่วตอบเหมือนกัน · rate limit | **FAIL ×4 → แก้แล้ว** | 🔴 **(ก) ข้อใหญ่สุดของ WO นี้:** `service.ts issuePublicTaxInvoice` **ไม่ตรวจ** `publicView.enabled` / `expiryDays` / `taxRequest.enabled` เลย — ด่านพวกนี้อยู่แค่ตอนเรนเดอร์หน้า (`getPublicTaxContext`) ⇒ เจ้าของ **ปิดลิงก์สาธารณะไปแล้ว/ลิงก์หมดอายุแล้ว** ใครถือ token เก่าก็ยังยิง server action ตรงเข้ามาสร้างคำขอใบกำกับภาษี (DRAFT) ในร้านได้เงียบ ๆ → ใส่ด่านชุดเดียวกันที่ service (`service.ts:3196-3206`)<br>🔴 **(ข)** ฟังก์ชันเดียวกันคืน `e.message` ดิบของ Prisma ออกหน้าสาธารณะ (ชื่อตาราง/คอลัมน์รั่ว) → คืนข้อความไทยกลาง + log ฝั่งเรา<br>🔴 **(ค)** ไม่มี rate limit บน token lookup ทั้ง 2 เส้น → เพิ่ม 60/นาที/IP (หน้า) + 10/นาที/IP (ส่งฟอร์ม)<br>(ง) `?err=` ที่สะท้อนกลับหน้าใบเสร็จ ตัดความยาวที่ 200 ตัว (React escape อยู่แล้ว — กันใช้หน้าร้านเป็นสื่อหลอกลวง) |
| **5** | webhook Beam | S5 (14 ข้อ) — HMAC บน raw body · body ถูกแก้ 1 ตัวอักษรลายเซ็นเดิมใช้ไม่ได้ · ยิงซ้ำ chargeId เดิม → `duplicated:true` payment ใบเดียว paidTotal ไม่เบิ้ล · JSON เพี้ยน = 200 | **PASS ยกเว้น log → แก้แล้ว** | ⚠️ `webhook/route.ts:24` เดิม `logOps(..., detail: raw.slice(0,200))` ตอนลายเซ็น**ไม่ผ่าน** = ทิ้ง body ของคนนอกที่ยังไม่ยืนยันตัวตนลง ops log → เปลี่ยนเป็นเก็บ `ขนาด + sha256 16 ตัว + มี/ไม่มีลายเซ็น` |

### B. Input / data

| # | หัวข้อ | หลักฐาน | ผล | การแก้ |
|---|---|---|---|---|
| **6** | เงินเป็นสตางค์ integer ทุกทาง | S6 (16 ข้อ) — `"1,234.56"`→123456 · `"1e3"`→100000 · `"(250.00)"`→−25000 · `"฿ 1234"` · ยอดทศนิยมที่ยิงเข้า `recordPayment` ถูกเก็บเป็น integer จริงใน DB · ทุกแถวเอกสาร/ทุกบรรทัด GL ในระบบทดสอบเป็น integer | **PASS** | ไม่มี `parseFloat` บนทางเงินที่ไม่ปัดเป็น integer (4 จุดที่มี ล้วนตามด้วย `Math.round(×100)`) · `editor-actions.ts:51 int()` กัน NaN/Infinity ครบ |
| **7** | CSV injection | S7 (20 ข้อ) — ตั้งชื่อคู่ค้าเป็น `=HYPERLINK(...)` แล้วส่งออกจริงทั้ง ภ.ง.ด. · เครดิตภาษีถูกหัก · ภ.พ.30 · เทมเพลตนำเข้า | **FAIL ×4 → แก้แล้ว** | 🔴 มีตัวหนี CSV เขียนมือ **3 ชุดที่ไม่กันสูตรเลย**: `wht.ts:498` (ภ.ง.ด.3/53 + เครดิต) · `reports.ts:625 pp30Cell` (ภ.พ.30) · `ReportToolbar.tsx:18 toCsv` (**ปุ่ม Excel ของทุกหน้ารายงาน**) ⇒ สร้าง `csvCell()`/`csvRow()` กลางใน `src/lib/core/csv.ts` (บริสุทธิ์ ใช้ได้ทั้ง server/client) แล้วเปลี่ยนทั้ง 3 จุด + `import-shared` delegate มาที่เดียวกัน · ยกเว้น "ตัวเลขล้วนรวมยอดติดลบ" ไม่เติม `'` (ไม่งั้นคอลัมน์เงินกลายเป็นข้อความ SUM ไม่ได้)<br>🔴 `tax/export/route.ts` เอา `?period=`/`?year=` **ดิบ ๆ** ไปต่อเป็นชื่อไฟล์ใน header `Content-Disposition` → กรองเหลือ `[0-9-]` |
| **8** | อัปโหลดไฟล์ | S8 (24 ข้อ) — `.html` เปลี่ยนชื่อเป็น `.jpg` · SVG แจ้งเป็น png · 21MB · `../../x.pdf` · ชื่อไทย/อีโมจิ | **FAIL → แก้แล้ว** | 🔴 ด่านเดิมเชื่อ `File.type` ที่เบราว์เซอร์แจ้งอย่างเดียว ⇒ ไฟล์อะไรก็เข้าคลังเอกสารได้ถ้าประกาศ mime ถูก → เพิ่ม `sniffAttachmentMime()` + `validateAttachmentBytes()` อ่าน **magic bytes จริง** (PDF/JPEG/PNG/WEBP/HEIC-HEIF) และ **เก็บด้วยชนิดที่ sniff ได้** ไม่ใช่ที่ browser แจ้ง (`attachment-shared.ts` · `documents/actions.ts:79`)<br>+ `sanitizeAttachmentFileName()` ตัด `/ \` + อักขระควบคุม (กัน header injection ตอนดาวน์โหลด) เก็บยูนิโค้ดไทยไว้<br>✅ ที่ผ่านอยู่แล้ว: **key ที่เก็บจริงไม่ใช่ของผู้ใช้** (`t/<tenantId>/<kind>/<uuid>.<ext>` · ext จาก allowlist ไม่มี fallback) · ไม่มี svg/html/xml ใน allowlist ⇒ ไม่มีทางเรนเดอร์ active content |
| **9** | คำค้น / SQL | S9 (16 ข้อ) — คำค้น 10,000 ตัว · `' OR 1=1 --` · `%` ข้ามร้าน · ดึงสตริง SQL ของทุก raw call มาตรวจทีละตัว | **FAIL (ขอบเขต q) → แก้แล้ว** | ✅ `$queryRawUnsafe` มี 2 จุด (`doc-numbering.ts:233,365`) — **ผูกค่าด้วย `$1…$7` ทั้งคู่ ไม่มี template interpolation** · ORDER BY มาจาก `DOC_SORT_ORDER` (Record คงที่) + หน้ารายการกรอง `sort` จาก URL ก่อนส่งเข้า service<br>🔴 `q` **ไม่มีเพดานความยาว** ทุกจุด (ส่งตรงเข้า `contains` ILIKE หลายคอลัมน์) → สร้าง `search-input.ts clampSearch()` (200 ตัว) แล้วต่อที่ **9 จุดใน 6 ไฟล์** (service ×4 · product ×3 · contacts-list · expense · attachment · journal-v2)<br>ℹ️ วัดแล้ว: Prisma `contains` **ไม่ escape `%`/`_`** — ค้น `%` = ตรงทุกแถว · **ไม่ใช่ช่องโหว่** เพราะ where ผูก tenant+system เสมอ (ข้อสอบยืนยันว่า `%` จากร้านอื่น = 0 แถว) |
| **10** | XSS | S10 (5 ข้อ) | **PASS** | `dangerouslySetInnerHTML` ในขอบเขตบัญชี+หน้าสาธารณะ **มีจุดเดียว** = `AccountIcon.tsx:99` ซึ่งอ่านจากตาราง `ICONS` คงที่ในไฟล์ (`ICONS[name] ?? FALLBACK`) ไม่ใช่ค่าจากผู้ใช้ · หน้าพิมพ์ไม่ใช้เลย · ชื่อที่มีแท็ก HTML ถูกเก็บ/อ่านกลับเป็นข้อความดิบ |
| **11** | rate limit | S11 (13 ข้อ) — ยิงเท่าเพดานผ่าน · เกิน 1 ครั้งถูกปฏิเสธ · ข้อความไทย + `retryAfterSec` · ถังแยกตาม scope | **FAIL (ไม่มีเลยทั้งโมดูล) → แก้แล้ว** | 🔴 **โมดูลบัญชีทั้งโมดูลไม่มีเพดานอัตราสักจุดเดียว** ทั้งที่มีผิวสาธารณะ 2 เส้นที่ไม่ต้องล็อกอิน → สร้าง `src/lib/modules/account/rate-limit.ts` (บน `rate-limit-db` = Postgres ทนหลาย instance · fail-open) แล้วต่อ 6 จุด:<br>· เปิดหน้า `/pay` + `/r` 60/นาที/IP · ส่งฟอร์มขอใบกำกับ 10/นาที/IP<br>· สร้างลิงก์ชำระเงิน (5.5) 60/ชม./ระบบ · นำเข้า CSV (1.8) 20/ชม./ระบบ<br>· ส่งอีเมลรายงาน (8.2) 20/วัน/ระบบ · **อ่านบิลด้วย AI (7.2) 200/วัน/ร้าน** (นับ**หลัง** credit gate `canSpend` — cache/ปิด AI ไม่กินโควตา) |

### C. Concurrency / integrity

| # | หัวข้อ | หลักฐาน | ผล | การแก้ |
|---|---|---|---|---|
| **12** | แข่งกันรับชำระ | S12 (8 ข้อ) — `Promise.all` ยิงเต็มยอด 2 ครั้ง · ครึ่งยอด 3 ครั้ง · `idempotencyKey` เดียวกัน 2 ครั้ง | **FAIL (รุนแรง) → แก้แล้ว** | 🔴🔴 **`recordPayment` ไม่ล็อกแถว** — Postgres อยู่ READ COMMITTED ⇒ 2 คำขอที่มาพร้อมกันอ่าน `paidTotal` ค่าเดียวกัน แล้ว **ผ่านด่าน "ยอดชำระเกินยอดคงเหลือ" ทั้งคู่** → ได้ payment 2 ใบเต็มยอด + JV 2 ชุด (ลูกค้าจ่ายรอบเดียวแต่ระบบบันทึกรับ 2 เท่า)<br>วัดจริงด้วย `Promise.all` แล้วยืนยันว่าเกิดขึ้นได้ ⇒ เพิ่ม `lockDocumentRow()` = `SELECT … FOR UPDATE` ผูก `id+tenantId+systemId` ที่หัว transaction (`service.ts:2338`) · หลังแก้: สำเร็จ 1/2 · ครึ่งยอด 3 ครั้งผ่าน 2 ตกไป 1 · Σdr=Σcr |
| **13** | แข่งกันออกเลขที่ + P2002 index อื่น | S13 (16 ข้อ) — ออกเอกสาร 5 ใบพร้อมกัน · สร้างผู้ติดต่อ/สินค้าอย่างละ 5 รายพร้อมกัน · SKU ซ้ำ | **FAIL (หนี้ที่ยกยอดมา) → แก้แล้ว** | ✅ เลขที่เอกสาร: `reserveSeq` เป็น `INSERT … ON CONFLICT DO UPDATE RETURNING` คำสั่งเดียว → 5 ใบพร้อมกันได้เลขไม่ซ้ำ ตัวนับเดินครบ<br>🔴 **หนี้ "retry เลขที่ยังวน 6 รอบเมื่อชน index อื่น" — reproduce ได้แล้ว และแรงกว่าที่จด**: ตัวตรวจเดิมเอา `err.message` ทั้งก้อนมา `.test(/code/)` แต่ **Prisma 7 แนบซอร์สโค้ดรอบบรรทัดที่ล้มมาในข้อความด้วย** ซึ่งบรรทัดนั้นคือ `create({ data: { ...base, code } })` — **มีคำว่า `code` อยู่ในโค้ดเอง** ⇒ การชน **SKU ซ้ำ** ถูกอ่านว่า "เลขที่ซ้ำ" ทุกครั้ง → วน 6 รอบ → ตกมาที่ create ก้อนสุดท้ายซึ่งชน SKU อีก → **โยน `PrismaClientKnownRequestError` ดิบออกจากฟังก์ชันที่ประกาศว่าคืน `{ok:false,reason}`** (ผู้ใช้พิมพ์ SKU ซ้ำ = เจอ 500 ภาษาอังกฤษ)<br>⇒ สร้าง `unique-conflict.ts` ที่ดึง **ชื่อฟิลด์/ชื่อ constraint เป็นชิ้น ๆ** (4 ทาง: driver `constraint.fields`/`.name` · `meta.target` · วงเล็บหลัง `failed on the fields:` · `violates unique constraint "…"`) แล้วเทียบชื่อฟิลด์เป๊ะ — `sku`/`barcode`/`branchCode` จึงไม่มีทางเข้าใจผิดเป็น `code` อีก · ใช้ร่วมทั้ง product และ contact · + ครอบ try/catch ที่ create ก้อนสุดท้ายไม่ให้ error ดิบหลุด |
| **14** | กดออก/อนุมัติ/ยกเลิกรัว = idempotent | S14 (10 ข้อ) — `Promise.all` ทุกปุ่ม | **FAIL ×3 → แก้แล้ว** | 🔴 `issueDocument` เช็ก DRAFT ในtx แต่ไม่ล็อกแถว → กดรัวได้ 2 ใบ **กินเลขที่เอกสาร 2 เลข** (เลขหาย 1 ตัวโดยไม่มีใบ) → `lockDocumentRow`<br>🔴 `voidPayment`/`voidDocument` เช็กสถานะแบบ read-then-write → `lockDocumentRow` (หลังแก้: ยกเลิกพร้อมกัน 2 ครั้งสำเร็จ 1 · `paidTotal` กลับเป็น 0 ไม่ลบซ้ำ · reversal ชุดเดียว)<br>🔴 `expense.ts approvePurchaseOrder`/`rejectPurchaseOrder` **ไม่มี transaction เลย** → เปลี่ยนเป็น `updateMany` ที่มีเงื่อนไข `status: "AWAITING_APPROVAL"` ในคำสั่งเดียว (ผู้ชนะคนเดียวเสมอ · audit/webhook ไม่ยิงซ้ำ) |
| **15** | ล็อกงวด + `lockBeforeDate` ที่คอขวด GL | S15 (11 ข้อ) — โครงสร้าง + **ทดสอบจริง 3 เส้นทาง** | **PASS** | `gl.ts:239-241 commitEntry` เรียก `assertPeriodOpen` + `assertNotLockedGl` และ **ตัวโพสต์บัญชีทุกตัวลงท้ายที่นี่** (ข้อสอบไล่ทุก `post*` ว่าไม่มีตัวไหนเลี่ยง) · ทดสอบงวดปิดจริง 3 ทาง: **เอกสารปกติ · POS `postExternalSale` · JV กระทบยอด `postManualJV`** ปฏิเสธครบ + positive control เปิดงวดกลับแล้วทำได้ · `lockBeforeDate` ตั้งแบบ**สัมพัทธ์** (พรุ่งนี้) ไม่ผูกวันที่ตายตัว |
| **16** | audit trail | S16 (18 ข้อ) — โครงสร้าง 11 action + รันจริง + ไม่รั่วข้ามร้าน | **PASS** | writeAudit ครบทุกรายการที่ระบุ · ⚠️ ปิด/เปิดงวดอยู่ที่ **inline server action ใน `periods/page.tsx:61,90`** ไม่ใช่ `period-close.ts` (ข้อสอบชี้ที่ถูกแล้ว) · `listAuditLogs` ผูก `tenantId` ทุกครั้ง — ร้าน B อ่านประวัติเอกสารร้าน A ได้ 0 แถว · `writeAudit` ครอบ try/catch (audit ล้มไม่ทำงานหลักพัง) |

### D. Hygiene

| # | หัวข้อ | หลักฐาน | ผล | การแก้ |
|---|---|---|---|---|
| **17** | สุขอนามัยโค้ด/ความลับ | S17 (11 ข้อ) | **PASS + 1 หนี้ที่รู้ตัว** | ✅ `grep -rnE "color-(fg\|bg)"` = **0** · `any` ในโมดูลบัญชี = **0** (คงไว้ตามที่ Fable วัด) · ไม่มี `console.log/info/debug` · `console.warn/error` 8 จุดไม่มีข้อมูลลูกค้า · ไม่มีคีย์/ความลับในรีโป<br>⚠️ **หนี้ (ไม่ใช่ขอบเขต 9.2)**: สี hex ดิบ 23 บรรทัดใน 6 ไฟล์ = `DashChart` · `DashStackChart` · `DashDonut` · `FinanceOverviewPanel` (SVG กราฟ/มิเตอร์) + `ReconcilePanel` · `ImportWizard` (สีสถานะ 2 จุด) — ต้องออก **token สีกราฟ** ก่อนถึงแก้ได้ (งานดีไซน์) · ข้อสอบตั้งเป็น **allowlist กันลาม**: ไฟล์ใหม่มีสีดิบ = แดงทันที |
| **18** | ข้อสอบรุ่นเก่าเคารพ QC env + กัน prod | พิสูจน์ 3 การทดลอง (ดูด้านล่าง) | **FAIL → แก้แล้ว** | 🔴 หนี้ที่ยกมาจาก 3.2 (เคยพา `qc-account-cpa` ไปสร้าง tenant บน prod จริง 3 ก.ย.) ⇒ สร้าง `scripts/qc-env-guard.mts`: เคารพ `QC_ENV_FILE` · env ที่ export มาก่อนชนะไฟล์ (**คืนทั้ง `DATABASE_URL` และ `DIRECT_URL`** — คืนแค่ตัวเดียวจะได้สภาพ "อ่าน QC แต่ migrate ไป prod" ซึ่งแย่กว่าเดิม) · host prod = **ตาย exit 1** เว้นแต่ `ALLOW_PROD_QC=1`<br>ต่อเข้า **7 ชุด**: cpa · deep · doc-settings · deletion · gatea · qc7 · p2p3 · และเพิ่มด่านเดียวกันที่ **`qc-all.mts`** |
| **19** | `AccountDocumentPayment.entryId` เป็น null เสมอ (หนี้ 5.5) | S18 (5 ข้อ) | **FAIL → แก้แล้ว** | ยืนยันแล้วว่า `gl.postPayment` **ไม่เคยเขียน `entryId` กลับ** เลย (ผู้อ่านทุกคนต้องไล่หา JV เอง — `payment-request.ts:612` ถึงกับเขียนหมายเหตุว่า "อย่าใช้ entryId") ⇒ เขียนกลับหลัง `commitEntry` (`gl.ts:762`) · additive ล้วน ไม่มีใครอ่านค่าเดิมอยู่ ⇒ พฤติกรรมเดิมไม่กระทบ · ข้อสอบยืนยัน entryId ชี้ JV ของ payment ใบนั้นจริง (refType/refId/systemId ตรง) |
| **20** | `pnpm audit --prod` | รันจริง | **NOTE (ไม่แก้ในนี้)** | **32 ช่องโหว่ — high 17 · moderate 14 · low 1 · ทั้งหมดเป็น dependency ทางอ้อมใต้ `next` (16) กับ `@prisma/client` (16) · ไม่มีตัวไหนเป็น dependency ตรงของเรา**<br>🔴 ตัวที่ควรบอกเจ้าของ: **`next` 16.2.10 → 16.2.11 (bump patch ตัวเดียว) ปิด 4 high + 5 moderate** รวม *Middleware/Proxy bypass ใน App Router* · *DoS ผ่าน Server Actions* · *SSRF ใน Server Actions* · *เปิดเผย endpoint ของ Server Function โดยไม่ต้องยืนยันตัวตน* — โมดูลบัญชีทั้งโมดูลสร้างบน Server Actions ⇒ เกี่ยวตรง<br>ที่เหลือ (mysql2 · fast-uri · hono · postcss · nanoid · sharp · deepmerge-ts) อยู่ใต้ `prisma`/`next` แก้เองไม่ได้ต้องรอ upstream |

---

## ไฟล์ที่แก้

**ใหม่ (6)**
- `scripts/qc-acc-v2-security.mts` — ข้อสอบ 9.2 · **298 ข้อ** (เกณฑ์ ≥ 40) · สร้าง 2 ร้านทิ้งแล้วลบเองทั้งคู่ · ลงทะเบียนอัตโนมัติกับ `qc-all` (ชื่อไฟล์ `qc-*.mts`)
- `scripts/qc-env-guard.mts` — ด่าน env ของข้อสอบรุ่นเก่า + กัน prod
- `src/lib/modules/account/rate-limit.ts` — เพดานอัตราของโมดูลบัญชี (6 ชนิด · ข้อความไทย)
- `src/lib/modules/account/search-input.ts` — `clampSearch()` (บริสุทธิ์ ไม่มีวงจร import)
- `src/lib/modules/account/unique-conflict.ts` — อ่าน P2002 ว่าชนที่ index ไหน (แม่นยำ)
- `ledger/wo-notes/9.2.md` — ไฟล์นี้

**แก้ (24)** — `service.ts` · `gl.ts` · `expense.ts` · `product.ts` · `attachment.ts` · `attachment-shared.ts` · `doc-settings.ts` · `payment-request.ts` · `inbox-ai.ts` · `import-actions.ts` · `import-shared.ts` · `contacts-list.ts` · `journal-v2.ts` · `reports.ts` · `wht.ts` · `src/lib/core/csv.ts` · `pay/[token]/page.tsx` · `r/[token]/page.tsx` · `r/[token]/actions.ts` · `payment/beam/webhook/route.ts` · `documents/actions.ts` · `reports/ReportToolbar.tsx` · `tax/export/route.ts` · `qc-acc-v2-guard.mts` · `qc-all.mts` + 7 ชุด `qc-account-*.mts`

**ไม่มี migration** — ทุกอย่างเป็นการแก้โค้ดล้วน (`entryId` ใช้คอลัมน์ที่มีอยู่แล้วใน schema)

---

## ด่านที่รันจริง (บรรทัดจริง · env QC `ep-plain-art` · เซิร์ฟเวอร์หยุดแล้ว · ทีละ 1 อย่าง)

```
typecheck (with-gate-lock)          0 errors
fitness (มี .env)                    ผ่าน 17/17 · CRITICAL 0 MAJOR 0 MINOR 0
fitness (env -i ไม่มี env เลย)        ผ่าน 17/17 · CRITICAL 0 MAJOR 0 MINOR 0
drift (prisma migrate diff)         No difference detected
pnpm audit --prod                   32 (high 17 · moderate 14 · low 1) — ทางอ้อมทั้งหมด
```

**ชุด acc-v2 ทั้ง 42 ชุด เขียวหมด:**
```
security 298 · guard 173 · seed-check 279 · schema 61 · nav 326 · editor 200 · dashboard 174
recurring 163 · groups 174 · payments 161 · permissions 160 · list 159 · policy 150 · inbox 128
period-assets 121 · doc-settings 116 · import 114 · reconcile 109 · coa 105 · cheap-routes 105
products 100 · adjust 96 · contact-modal 96 · journal 94 · invitem 88 · home 87 · pos-lines 87
detail 85 · promptpay 84 · overview 73 · gl-inclvat 71 · wht-cheque 69 · attachments 66
finance 59 · contact-profile 59 · reports-drill 57 · contact-merge 56 · contacts 49
finance-overview 45 · party 36 · components 78/78 · mobile SKIPPED (ต้องมีเซิร์ฟเวอร์ build จริง)
```

**ชุด account รุ่นเก่า (หลังต่อด่าน env) เขียวหมด:**
```
qc-account-cpa 107/107 · deep 10/10 · doc-settings 23/23 · deletion 18/18
qc-account-gatea ✅ · qc-account-qc7 ✅ · qc-account-p2p3 ✅
ทุกชุดพิมพ์ [env] … DB ep-plain-art-… (ไม่ใช่ prod)
```

**พิสูจน์ข้อ 18 (3 การทดลอง):**
```
(ก) QC_ENV_FILE=.env.qc            → [env] ไฟล์ .env.qc · DB ep-plain-art-…        ✅
(ข) env -i + export เฉพาะตัวแปร QC  → [env] ไฟล์ /nonexistent.env · DB ep-plain-art-… ✅
                                      (ไม่มี .env ให้โหลด · ใช้ค่าที่ export มาล้วน)
(ค) negative control: ยัด host prod → 🔴 หยุด! … ep-royal-night… · exit=1            ✅
    qc-all.mts ด้วย host prod       → 🔴 หยุด! qc:all … · exit=1                     ✅
```

---

## เรื่องที่ต้องรู้ / ยังค้าง

1. 🔴 **แจ้งเจ้าของทันที**: `next` 16.2.10 มี 4 high + 5 moderate ที่ปิดได้ด้วยการ bump เป็น **16.2.11** (patch) — รวม *Middleware/Proxy bypass*, *SSRF/DoS ผ่าน Server Actions*, *เปิดเผย endpoint ของ Server Function* ซึ่งเกี่ยวตรงกับโมดูลบัญชีที่สร้างบน Server Actions ทั้งหมด · **ไม่ได้ทำใน 9.2** เพราะต้อง reinstall + rebuild + รันด่านใหม่ทั้งชุด และ WO นี้ห้าม commit — ควรออกเป็น WO ย่อยของตัวเอง
2. ⚠️ ระหว่างรัน `qc-acc-v2-contact-merge` รอบแรก ตัว re-seed ท้ายสคริปต์ล้มชั่วคราว (seed รายงาน "บิลขายหน้าร้าน: ได้ 0 ต้องการ 2") → `acc-v2-expected.json` ค้างของเก่า → `qc-acc-v2-journal` แดงตาม (0/152) · **ไม่ใช่ผลจากโค้ดที่แก้**: รัน `seed-acc-v2-qc.mts` ใหม่ผ่านฉลุย แล้วทั้ง 2 ชุดเขียว (journal 94 · contact-merge 56) · ⚠️ แต่แปลว่า **contact-merge มีโอกาสทิ้งเฉลยค้างไว้ให้ชุดอื่นแดงตาม** ถ้า re-seed ล้ม — ควรทำให้มันดังกว่านี้ (WO ถัดไป)
3. ℹ️ `scripts/acc-v2-expected.json` + `fixtures/acc-v2/kbank-*.expected.json` ขึ้น modified ใน git — เป็นผลปกติของการ seed ใหม่ ไม่ใช่การแก้มือ
4. ℹ️ ไม่ได้ทำ: **Content-Disposition ตอนดาวน์โหลดไฟล์แนบ** — ไฟล์เสิร์ฟจาก Bunny CDN ตรง ไม่ผ่าน route ของแอป จึงตั้ง header เองไม่ได้ · ความเสี่ยงถูกกันด้วย allowlist แทน (ไม่มี svg/html/xml ⇒ ไม่มี active content ให้เรนเดอร์) — ถ้าจะทำต้องเพิ่ม proxy route (งานคนละก้อน)
5. ℹ️ ข้อสอบชุดนี้ **ไม่ผูกกับ "วันที่ N ของเดือน"** เลย — ทุกวันที่คำนวณสัมพัทธ์จาก `now` (งวดปัจจุบัน · `lockBeforeDate` = พรุ่งนี้ · อายุลิงก์เลื่อน `issueDate` ย้อนหลัง)
