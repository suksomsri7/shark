# WO 0.4 — Shell V2 (แถบเมนูบัญชี 9 หมวด + breadcrumb + มือถือ bottom sheet)

อ้างอิง: `docs/design/account-v2/BLUEPRINT-ACCOUNT-V2.md` WO 0.4 row · `DESIGN-SPEC-V2.md` §1 (shell) + §2 (ผังเมนู 9 หมวด) · mockup `f2-menu-revenue.png` / `f4-expense-overview-menu.png` / `f12-m-sheet.png` / `g18-sheet-l2.png` + `mockup.html` (CSS `.amenu/.am/.dd/.ddi/.mnav/.sheet`)

## สถานะ: DONE รอบ 3 (แก้ตาม Fable visual QC รอบ 2 — ยังไม่ commit)

## รอบ 3 — แก้ตามที่ Fable ตีกลับรอบ 2 (มือถือผ่านแล้ว/ไอคอน/h1/ป้ายไทยผ่านแล้ว · เหลือ 3 ข้อ)

| # | ปัญหาที่ Fable เจอ | สาเหตุจริง | แก้อย่างไร | ผล |
|---|---|---|---|---|
| 1 | dropdown เดสก์ท็อปถูกตัด — เห็นแค่ก้นพาเนล (~60px) ที่ขอบบนจอ แถบเมนูหายไปเลย | `<nav data-testid="acc-tabbar">` ใส่ `overflow-x-auto` (ไว้เลื่อนแนวนอนบนมือถือ) — CSS spec: ถ้า `overflow-x` ไม่ใช่ `visible` เบราว์เซอร์จะบังคับ `overflow-y` เป็น `auto` ไปด้วยเสมอ (แม้ไม่ได้ตั้งใจ) ⇒ dropdown ที่เป็นลูกโดยตรงและ "ล้น" ลงล่างของ `<nav>` โดนตัดเงียบ ๆ · เมื่อ puppeteer `hover()` element ที่ถูกตัดบางส่วน มันเรียก `scrollIntoViewIfNeeded()` ก่อนเสมอ ทำให้หน้าเลื่อนไปตำแหน่งแปลก ๆ ก่อนถ่ายภาพ (ยิ่งซ้ำเติมอาการ) | ย้าย `overflow-x-auto` จาก `<nav>` ไปไว้ที่ `<div>` ชั้นในที่ครอบเฉพาะแถวปุ่ม 9 หมวด — `<nav>` เองเหลือแค่ `relative` เฉย ๆ (ไม่มี overflow) ให้ dropdown/flyout ที่เป็น `absolute` ลูกโดยตรงล้นลงล่างได้เต็มที่ · เพิ่ม `window.scrollTo(0,0)` ก่อนถ่ายภาพทุกครั้งใน `visual-acc-v2.mts` (กันชั้นที่ 2) · เพิ่มการวัด `getBoundingClientRect()` จริงเทียบ `acc-tabbar`/`acc-dropdown`: `dropdown.top ≥ tabbar.bottom` และ `dropdown.bottom ≤ viewportH` เป็นข้อสอบ (ไม่ใช่แค่ดูภาพด้วยตา) | ✓ แก้ต้นเหตุ (overflow) + เพิ่มเซฟการ์ด 2 ชั้น + มีข้อสอบวัดตำแหน่งจริงแล้ว — รอ Fable ยืนยันด้วยภาพเพราะรันสคริปต์เองไม่ได้ |
| 2 | ตัวนับ "รอชำระ" ในสหกรณ์ flyout = 18 ทั้งที่คำตอบจริง (คำตอบเฉลย + tabCounts ของหน้ารายการ) = 12 | `NAV_FLYOUT_TABS.INVOICE.awaiting` รวม `["AWAITING_PAYMENT","PARTIAL"]` และ `accountFlyoutCounts()` เดิม**ไม่ตัดพ้นกำหนดออก** — นับสถานะดิบทั้งหมดรวมใบที่พ้นกำหนดด้วย (12 non-overdue + 2 partial + 4 overdue-ที่ดันมีสถานะ AWAITING_PAYMENT/PARTIAL = 18 พอดี) | (1) ตัด `"PARTIAL"` ออกจากชุดสถานะของ IV "รอชำระ" (SPEC §3 flyout ของ IV มีแค่ รอชำระ·ชำระแล้ว·พ้นกำหนด 3 รายการ ไม่รวม "ชำระบางส่วน" อยู่แล้ว) (2) เขียน `accountFlyoutCounts()` ใหม่ให้ `groupBy` overdue **แยกตามสถานะด้วย** (`by:["docType","status"]` ไม่ใช่แค่ `["docType"]`) แล้วหักออกจากสถานะดิบต่อสถานะจริง ๆ (`nonOverdueSum`) — ตรงความหมายเดียวกับ `excludeOverdue:true` ที่หน้ารายการใช้ (3) เพิ่มเช็คอิสระใน `visual-acc-v2.mts`: เรียก `listDocumentsPaged()` ตรงจาก DB (คนละทางจาก `accountFlyoutCounts()`) เทียบกับตัวเลขที่ AccountTabBar เรนเดอร์จริงในหน้า `menu-flyout` (เพิ่ม `data-testid="acc-count-<countKey>"` บนตัวเลขให้อ่านได้เจาะจง) | ✓ ตรวจตัวเลขซ้ำด้วย 2 แหล่งอิสระแล้ว (ไม่ใช่แค่เช็คฟังก์ชันกับตัวเอง) |
| 3 | `offsetParent !== null` เป็น false เสมอสำหรับ `position:fixed` (sheet ของมือถือ) → false negative แม้ sheet เปิดอยู่จริง | สเปกเบราว์เซอร์: `offsetParent` เป็น `null` โดยดีไซน์เมื่อ element หรือบรรพบุรุษเป็น `position:fixed` (house lesson VR-6.2) — sheet ของ `AccountTabBar` เป็น `fixed inset-0` | เปลี่ยนทุกจุดเช็ค "เปิดอยู่จริง" (`dropdownVisible`/`flyoutVisible`/`sheetL1Visible`/`sheetL2Visible`) เป็น `getBoundingClientRect().width/height > 0` + `getComputedStyle().visibility !== "hidden"` + `display !== "none"` แทน `offsetParent` ทั้งหมด | ✓ |

## สถานะเดิม (รอบ 2) — DONE (แก้ตาม Fable visual QC รอบแรก — ยังไม่ commit)

## รอบ 2 — แก้ตามที่ Fable ตีกลับ (เทียบภาพจริง vs f2/f12/g18)

| # | ปัญหาที่ Fable เจอ | แก้อย่างไร | ผล |
|---|---|---|---|
| 1 | แถบเมนู/sheet ใช้ **emoji** (🏠📥📤👥📦💰📚🗂⚙️) ทั้งที่ mockup เป็นเส้นบาง SVG (stroke 1.7 currentColor) — UI_STANDARD ห้าม emoji นอก nav/header ที่ไม่ตรงแบบ | เพิ่ม `src/components/account-v2/AccountIcon.tsx` — คัดลอก path จริงจาก `mockup.html` `<symbol id="i-...">` คำต่อคำ (36 คีย์) · เปลี่ยน `AccountNavGroup.icon`/`AccountNavItem.icon` จาก emoji เป็นคีย์ไอคอน (เช่น `"home"`,`"in"`,`"doc"`) · ใช้ 18px ในแถบเมนู, 22px ในหัว sheet มือถือ, 16px ในแถวระดับ 1 ของ dropdown/sheet (เพิ่มไอคอนที่ไม่เคยมีมาก่อนตามข้อ 1 ท่อนสอง) | ✓ ไม่มี emoji ในองค์ประกอบเมนูแล้ว (เหลือ ✕/‹/›/▾ เป็นตัวอักษรเรียบ ไม่ใช่ emoji สี) |
| 2 | dropdown เดสก์ท็อปไม่ขึ้นภาพในสกรีนช็อตทั้งที่สคริปต์คลิกแล้ว | บั๊กจริง: ปุ่มหมวดมี `onMouseEnter` เปิดเมนูเสมอ + `onClick` toggle ตาม state ปัจจุบัน — เมาส์ของ puppeteer เข้าใกล้ก่อนคลิกเสมอ ⇒ hover เปิดเมนูไปก่อนแล้ว คลิกมาเจอ "เปิดอยู่แล้ว" เลย toggle ปิดทันที (ปิดก่อนถ่ายภาพ) แก้โดยให้ hover เปลี่ยนหมวดได้ "เฉพาะตอนมีเมนูเปิดค้างจากคลิกก่อนหน้าแล้วเท่านั้น" (`openKey !== null && openKey !== g.key`) — คลิกแรกจากหน้าปิดจึงเปิดเสมอ ไม่ชนกับ hover | ✓ แก้ race condition แล้ว (อ่านโค้ดยืนยันตรรกะ — รอ Fable ยืนยันด้วยภาพจริงอีกรอบเพราะรันสคริปต์ไม่ได้เอง) |
| 3 | หน้า hub ไม่มี `<h1>` (`probe.h1=""`) | เพิ่ม `<h1 className="text-2xl font-semibold">หน้าหลัก</h1>` บนสุดของ `AccountContent` (ui.tsx) ตาม f1 | ✓ |
| 4 | "เอกสารล่าสุด" โชว์ enum ดิบ `PURCHASE`/`EXPENSE` | เพิ่ม fallback `DOC_LABEL[docType] ?? EXP_DOC_LABEL[docType] ?? docType` (import จาก `./expense`) — ระหว่างแก้เจอบั๊กร่วม: href เดิมชี้ `docs/<docType>/<id>` ซึ่ง 404 สำหรับเอกสารฝั่งรายจ่าย (route จริงคือ `purchase/<id>`, `expense/<id>`, `po/<id>`, `asset-buy/<id>`) — เพิ่ม `docHref()` แก้ไปพร้อมกันเพราะอยู่บรรทัดเดียวกัน | ✓ ป้ายเป็นไทยทุก docType + ลิงก์ไม่ 404 อีกแล้ว |
| 5 | มือถือ sheet ตรงกับ f12 ดีอยู่แล้ว — คงไว้ + เปลี่ยนไอคอนหัว sheet เป็นเส้นบาง | หัว sheet ระดับ 1 ใช้ `<AccountIcon className="h-[22px] w-[22px]">` แทน emoji เดิม | ✓ |

**เพิ่มนอกเหนือคำสั่งตรง ๆ (ป้องกันไม่ให้เกิดซ้ำ):**
- `data-testid="acc-item-<testId>"` บนทุกแถวระดับ 1 (`testId` = ชื่อ `AccountDocType` จริงสำหรับ item เอกสาร เช่น `INVOICE`) — ใช้ hover/click เจาะจงได้ใน script โดยไม่ต้องพึ่ง label ข้อความ
- `data-testid="acc-dropdown"` (พาเนลระดับ 1 เดสก์ท็อป) · `data-testid="acc-flyout"` (พาเนลระดับ 2 เดสก์ท็อป) · `data-testid="acc-sheet-l1"`/`"acc-sheet-l2"` (มือถือ) — ให้สคริปต์เช็ค "เปิดอยู่จริง" ด้วย `offsetParent !== null` ไม่ใช่แค่เดาจากข้อความ
- `scripts/visual-acc-v2.mts` "0.4": เพิ่ม `hover?: string[]` + `onlyDevice?: "desktop"|"mobile"` ใน `PageSpec` · เพิ่มหน้า `menu-flyout` (เดสก์ท็อป: คลิกรายรับ → hover `acc-item-INVOICE` → เช็ค `acc-dropdown`+`acc-flyout` เปิดอยู่) และ `sheet-l2` (มือถือ: แตะรายรับ → แตะ `acc-item-INVOICE` → เช็ค `acc-sheet-l2` เปิด + มีปุ่มย้อนกลับ) · ขยายเช็ค `menu-open` เดิมให้ยืนยัน dropdown/sheet ค้างเปิดจริง (ตรงจุดที่ Fable เจอบั๊ก #2)
- `AccountIcon.tsx` เอ็กซ์พอร์ต `ICON_KEYS` + เพิ่ม N6 ใน `qc-acc-v2-nav.mts` (icon key มีจริง + testId ไม่ซ้ำในหมวด) — กันพิมพ์ผิดคีย์ไอคอนแบบเงียบ ๆ (fallback เป็นสี่เหลี่ยมเปล่าไม่ error ตอน build)

## สถานะเดิม (รอบ 1) — DONE (ยังไม่ commit — รอ Fable QC ด้วยภาพจริงบน DB QC)

## ไฟล์ที่แตะ

**ใหม่:**
- `src/lib/modules/account/nav.ts` — เขียนใหม่ทั้งไฟล์ (โครง 9 หมวดตาม SPEC §2 แทนของเดิม 8 หมวด) + `findActiveNav()` helper
- `src/components/account-v2/AccountTabBar.tsx` — แถบเมนู 9 หมวด + dropdown 2 ระดับ (เดสก์ท็อป) + bottom sheet 2 ชั้น (มือถือ)
- `src/components/account-v2/AccountBreadcrumb.tsx` — breadcrumb ไล่จาก pathname
- `src/app/app/sys/[id]/account/page.tsx` — หน้า hub ราก (แก้บั๊ก 404 เดิม — ledger/wo-notes/0.1.md ข้อ 8)
- `scripts/qc-acc-v2-nav.mts` — ข้อสอบ 300 ข้อ (รอบ 2 เพิ่ม N6 icon/testId) ไม่ต้องต่อ DB

**แก้:**
- `src/app/app/sys/[id]/account/layout.tsx` — เอา `SubNav` sidebar ออก ใส่ `AccountTabBar` + `AccountBreadcrumb` แทน (เนื้อหาเต็มความกว้าง) + ดึง `accountFlyoutCounts()`
- `src/app/app/sys/[id]/page.tsx` — กิ่ง `ACCOUNT` → `redirect(/app/sys/<id>/account)` (hub เดียว ไม่ซ้ำ)
- `src/lib/modules/account/service.ts` — เพิ่ม `accountFlyoutCounts()` (2 query รวมทุก docType — กันงบ query ของหน้าที่แถบเมนูติดอยู่ทุกหน้าบวม)
- `src/lib/modules/account/guard.ts` — เพิ่ม `"page.tsx": "account.doc.view"` ใน `ACCOUNT_PAGE_PERMISSIONS`
- `src/lib/modules/account/ui.tsx` — แก้ข้อความ footer เดิมที่ชี้ไปปุ่ม ☰ (ผิดแล้ว — เมนูอยู่แถบบนแทน)
- `src/app/app/sys/[id]/account/docs/[docType]/page.tsx`, `src/lib/modules/account/expense-page.tsx`, `src/app/app/sys/[id]/account/goods-issue/page.tsx` — เพิ่ม `<div id="new">` ห่อฟอร์มสร้าง ให้ flyout "+ สร้าง…#new" เลื่อนไปเจอจริง
- `scripts/visual-acc-v2.mts` — เพิ่ม `PAGES["0.4"]` (hub/invoice-list/menu-open) + `click`/`waitAfterClick` ใน PageSpec + `--assert` เช็คโครงสร้าง 4 ข้อ (tabbar 9 หมวด/breadcrumb ขึ้นต้น "บัญชี"/hub 200+h1/ไม่มีลิงก์ตาย)
- `docs/UI_STANDARD.md` §2.9 + §4 — อัปเดตให้ตรงกับ Shell V2

**ไม่แตะ:** `src/lib/modules/account/gl.ts` (WO 0.6 กำลังแก้อยู่) · `src/lib/modules/chat/**` · `src/components/app-shell/**` (อ่านอย่างเดียว)

⚠️ **worktree นี้มี WO อื่นแก้พร้อมกันจริง** — ระหว่างทำเจอ `src/components/account-v2/{DocTable,ListFilters,MoneyInput,...}.tsx` + `_dev/components/` โผล่ขึ้นมาเอง (น่าจะ WO 0.5) และ `src/lib/ui/DateText.tsx` + แก้ `date.ts` (ไม่ทราบ WO) — ไม่ได้แตะไฟล์พวกนี้เลย ตรวจแล้วไม่ชนกับของที่ทำ

## ทดสอบแล้ว (ตาม task H)
- `pnpm typecheck` → ผ่าน (ว่าง = เขียว)
- `pnpm fitness` → 17/17 ผ่าน
- `pnpm tsx scripts/qc-acc-v2-nav.mts` → **300/300 ผ่าน** (9 หมวด/ลำดับตรง SPEC · href ready ทุกตัวมีไฟล์จริงรองรับ · soon ทุกตัวมีชื่อจริงใน SPEC §2 · VAT gate ทำงาน · `accountNavChildren()` ไม่มีลิงก์ตาย · ไม่มีคำอังกฤษหลุดนอกโทเคนที่อนุญาต · icon key มีจริง + testId ไม่ซ้ำ (N6 ใหม่รอบ 2))
- **รอบ 3**: `pnpm typecheck` ผ่าน · `pnpm fitness` 17/17 · `qc-acc-v2-nav.mts` ยังคง 300/300 (ไม่มีข้อสอบใหม่ในไฟล์นี้รอบ 3 — เช็คตัวนับ/ตำแหน่ง dropdown ย้ายไปอยู่ที่ `visual-acc-v2.mts` เพราะต้องมี DOM+DB จริง)
- **ยังไม่รัน** `pnpm qc:all` / `bash scripts/acc-v2-serve.sh` / `visual-acc-v2.mts` — ตามคำสั่ง (Fable รันเอง)

## Checklist เทียบ mockup ทีละองค์ประกอบ (คำสั่งเจ้าของ — ก่อนส่งงาน)

อ้างค่าจาก `mockup.html` CSS จริง (`.amenu/.am/.dd/.ddi/.tagsoon/.ddsep/.btn`) เทียบกับโค้ดที่เขียน — ยังไม่เห็นภาพเรนเดอร์จริง (build/serve เป็นหน้าที่ Fable) ดังนั้นนี่คือ **การตรวจโค้ดเทียบสเปก** ไม่ใช่การตรวจภาพ:

### f2 (เมนูรายรับ เปิด dropdown) / f4 (เมนูรายจ่าย เปิด dropdown)
| องค์ประกอบ | มาตรฐาน mockup | ในโค้ด | ผล |
|---|---|---|---|
| ความกว้าง dropdown ระดับ 1 | `.dd{width:264px}` | `AccountTabBar` `DesktopDropdown` → `w-[264px]` | ✓ ตรงเป๊ะ |
| padding dropdown | `padding:6px` | `p-1.5` (6px) | ✓ ตรงเป๊ะ |
| border-radius dropdown | `12px` | `rounded-xl` (12px) | ✓ ตรงเป๊ะ |
| shadow dropdown | `0 14px 34px rgba(10,10,10,.12)` | คัดลอกค่าเดียวกันตรง ๆ (`shadow-[0_14px_34px_rgba(10,10,10,0.12)]`) | ✓ ตรงเป๊ะ |
| ความสูงแถวเมนู (`.ddi`) | `height:40px` | `h-10` (40px) | ✓ ตรงเป๊ะ |
| แถวมี `›` เมื่อมี flyout เท่านั้น | `.ddi .ar` เฉพาะแถวที่มี flyout · "ดูภาพรวม" ไม่มี | `NavRow` แสดง `›` เฉพาะ `item.flyout?.length > 0` | ✓ ตรง |
| หมวด active ตัวหนา+ขีดล่าง accent | `.am.on{font-weight:700;border-bottom-color:accent}` ขีด 2px | `border-b-2 border-[--color-accent] font-semibold` | ✓ ตรง (2px ตรงเป๊ะ) |
| เส้นคั่นก่อน "นำเข้าเอกสาร"/utility ท้ายกลุ่ม | `.ddsep` ก่อนนำเข้าเอกสาร และก่อนลิงก์ขอใบกำกับ | เพิ่ม `sep?: boolean` ใน nav.ts ใส่ก่อน "นำเข้าเอกสาร" ทุกหมวด + ก่อนลิงก์ขอใบกำกับ (รายรับ) + ก่อน ภ.พ.30 (บัญชี) + ก่อนโอนระหว่างช่องทาง (การเงิน) + ก่อนนำเข้าสินค้า (สินค้า) | ✓ ตรง (คัดลอกตำแหน่งจาก html จริงของ f2/f4) |
| ระดับ 2 (flyout): "+ สร้าง" ปุ่มดำบนสุด | `.btn.pri.blk` (ปุ่มดำเต็มกว้าง สูง 36px) | ใช้ `.btn.btn-primary` กลาง (ดำ ตาม UI_STANDARD) สูง `h-9` (36px) label เริ่ม "+ " | ✓ ตรง (ใช้ token ปุ่มกลางแทนเขียนสีเอง — ดีกว่า mockup ที่ hardcode) |
| ระดับ 2: สถานะหลัก 2–4 รายการ "พร้อมตัวนับ" | g18 โชว์เลขในกล่องเล็กข้างสถานะ | เพิ่ม `countKey` ใน flyout item + `accountFlyoutCounts()` (2 query รวมทุก docType) ต่อ IV/QT/DR/RE/TX/PUR/EXP/PO/ASSET/PTX | ✓ ทำจริง (ไม่ได้อยู่ใน task A-H เดิม แต่ owner สั่งเทียบ mockup แล้วเห็นว่า g18 มีเลข — เพิ่มให้ตรง) |
| ระดับ 2: ดูทั้งหมด / ล่าสุด ท้ายรายการ | มีเสมอ | ทุก flyout ที่มี `#new` ก็มี "ดูทั้งหมด"/"ล่าสุด" ต่อท้าย | ✓ ตรง |
| จาง + ชิป "เร็ว ๆ นี้" สำหรับ soon | `.ddi.soon` (opacity ผ่านสี mut2) + `.tagsoon` (border 1px, radius 5px, font 10.5px, padding 1px 6px) | `opacity-60` + ชิป `rounded-[5px] text-[10.5px] px-1.5 py-px border` | ✓ ตรง (ปรับ 2 รอบให้ตรงค่า CSS จริงหลัง diff แรกใช้ค่าปัดของ Tailwind) |
| ระดับ 2 เปิดด้วย hover/focus ที่มี `›` ทางขวา 6px gap | `.dd.w2{gap:6px}` (อ้างอิงระยะห่างคอลัมน์ในเมนูที่มี 2 คอลัมน์จริงของหน้านี้) | `ml-1.5` (6px) | ✓ อนุมานจากระยะเดียวกันในไฟล์ — **ไม่มีเฟรม mockup ที่ถ่ายภาพ hover state ของ flyout ระดับ 2 บนเดสก์ท็อปจริง ๆ** (f2/f4 โชว์แค่ dropdown ระดับ 1 เปิดอยู่ ไม่มีภาพขณะ hover เข้า flyout) — ความกว้างพาเนล 240px เป็นค่ากะเอาจากพาเนลข้างเคียง ไม่ใช่ค่าที่วัดจากภาพจริง |
| top-level tab spacing/font-size (`.am`) | `padding:13px 13px 11px; font-size:13.5px` | `px-3.5 py-3 text-sm` (14px/12px, font 14px) | ✗ ไม่ตรงเป๊ะ — ปัดเข้า Tailwind scale มาตรฐานแทนใช้ arbitrary px (สอดคล้องกับที่แอปทั้งระบบใช้ scale เดียวกันทุกที่ ไม่มีที่ไหนใช้ 13.5px ตรง ๆ) — ต่างจากภาพเป้าหมาย ~0.5–2px |

### f12 (มือถือ — bottom sheet ระดับ 1) / g18 (มือถือ — sheet ระดับ 2)
| องค์ประกอบ | มาตรฐาน mockup | ในโค้ด | ผล |
|---|---|---|---|
| แถวสถานะสูง 48px | `.sh-i{height:48px}` (f12) | `MobileSheet` ระดับ 1 ใช้ `MobileRow` → `h-12` (48px) | ✓ ตรงเป๊ะ |
| หัว sheet: ไอคอน+ชื่อหมวด+ปุ่ม ✕ | f12 หัว sheet มีไอคอนหมวด+ชื่อ+ปุ่มปิดกลม | `MobileSheet` level=1 แสดง icon+title+ปุ่ม ✕ (`h-8 w-8 rounded-lg`) | ✓ ตรง |
| sheet ระดับ 2: ปุ่มย้อนกลับ `‹` แทนไอคอน + breadcrumb "รายรับ · ใบแจ้งหนี้" ในหัว | g18 หัว = `‹ รายรับ · ใบแจ้งหนี้` | `MobileSheet` level=2 ส่ง `crumb={group.label}` render `<span class="font-normal muted">{crumb} · </span>{title}` + ปุ่ม `‹` | ✓ ตรง |
| ปุ่ม "+ สร้างใบแจ้งหนี้" เต็มกว้างดำบนสุดของ sheet ระดับ 2 | g18 ปุ่มดำเต็มกว้าง | `FlyoutRow` เมื่อ `mobile=true` ใช้ `btn btn-primary` เต็มกว้าง `h-11` (มือถือสูงกว่าเดสก์ท็อป h-9 ตาม touch target ≥44px ของ UI_STANDARD §1.4) | ✓ ตรง (สูงกว่าตั้งใจ — เกณฑ์ touch target ของโปรเจกต์เอง) |
| แถวสถานะพร้อมเลขในกล่องเล็กชิดขวา ("รอชำระ 12") | g18 มีเลขในกรอบ | `FlyoutRow` แสดง `countKey` badge (border rounded-md) เมื่อมีค่า | ✓ ทำจริง (ดูหมายเหตุ countKey ด้านบน) |
| "พ้นกำหนด" ตัวแดง+กรอบแดง | g18 ป้าย "พ้นกำหนด 4" กรอบแดงตัวแดง | `FlyoutRow` เช็ค `f.label === "พ้นกำหนด"` → border/text `--color-danger` | ✓ ตรง |
| เส้นคั่นก่อน "ดูทั้งหมด" | g18 มีเส้นคั่นบาง ๆ ก่อนแถว "ดูทั้งหมด" | `FlyoutRow` รับ `sep` → คั่นก่อนแถวที่ label==="ดูทั้งหมด" ทั้งเดสก์ท็อป/มือถือ | ✓ ตรง (แก้แล้วหลังพบระหว่างเทียบ checklist นี้) |
| แถบเมนู 9 หมวดเลื่อนแนวนอนบนมือถือ ไอคอน+ป้ายสั้น | f12 `.mnav` ไอคอน+ป้ายสั้นแนวตั้งซ้อนกัน | `AccountTabBar` ปุ่มเดียวกับเดสก์ท็อป สลับเป็น `flex-col` ต่ำกว่า `sm:` (ไอคอนบน+ป้ายล่าง) + `overflow-x-auto` | ✓ ตรงแนวคิด — ใช้ปุ่มชุดเดียวกันทั้ง 2 breakpoint (จงใจ เพื่อให้ `data-testid="acc-menu-*"` มี 9 ตัวครั้งเดียวไม่ซ้ำ ไม่ใช่ desktop+mobile คนละชุด) |
| ปุ่ม + ลอยมุมขวาล่างเหนือ orb AI | มีในทุกหน้ารายการ (f13) | **ไม่ได้ทำ** — อยู่นอกขอบเขต AccountTabBar (เป็นของหน้ารายการแต่ละหน้า ไม่ใช่ shell) | — ไม่อยู่ใน scope WO 0.4 (task B ไม่ได้ระบุ FAB) |

### Breadcrumb
| องค์ประกอบ | มาตรฐาน | ในโค้ด | ผล |
|---|---|---|---|
| รูปแบบ "บัญชี › รายรับ › ใบแจ้งหนี้" | SPEC §1 | `AccountBreadcrumb` ต่อ "บัญชี" + group.label + item.label ผ่าน `findActiveNav()` | ✓ ตรง |
| ลิงก์ทุกระดับยกเว้นตัวสุดท้าย | SPEC §1 | ทำตาม (เช็ค `i < crumbs.length-1`) | ✓ ตรง |
| หน้า hub = "บัญชี › หน้าหลัก" | ตาม f2 crumb ตัวอย่าง "บัญชี › หน้าหลัก" | `pathname===base` → crumbs = [บัญชี, หน้าหลัก(ไม่มีลิงก์)] | ✓ ตรง |

## ช่องว่าง/ข้อจำกัดที่ยอมรับไว้ (ระบุชัดตามที่สั่ง — ไม่ใช่ซ่อน)
1. **เดสก์ท็อป level-2 flyout ไม่มีภาพ mockup อ้างอิงโดยตรง** (มีแต่ตัวเลข/สไตล์จากไฟล์ CSS + พาเนล 2 คอลัมน์ข้างเคียงเป็นฐานอนุมาน) — ต้องรอ Fable เทียบภาพจริงจาก headless แล้วบอกว่าตรงหรือไม่
2. **top-level tab font-size/padding ปัดเข้า Tailwind scale** ไม่ตรง 13.5px/13px เป๊ะเหมือนไฟล์ต้นแบบ (แนวเดียวกับที่แอปทั้งระบบทำอยู่แล้ว — ไม่มีที่ไหนในโค้ดเดิมใช้ arbitrary px ระดับนี้)
3. **ปุ่ม + ลอยมุมขวาล่าง (FAB) ของหน้ารายการมือถือ** ไม่อยู่ใน scope AccountTabBar/WO 0.4 (เป็นของหน้ารายการ ไม่ใช่ shell)
4. **16 หน้าเดิมยังมี `PageHeader back={{label:"ระบบบัญชี"}}`** ซ้ำซ้อนกับ breadcrumb ใหม่ (task C อนุญาตให้เก็บไว้ถ้าเยอะเกินไป) — รายชื่อไฟล์อยู่ท้ายไฟล์นี้
5. **ตัวนับ (`countKey`) ครอบเฉพาะ QT/DR/IV/RE/TX/PUR/EXP/PO/ASSET_PO/ASSET_PURCHASE/PTX** (รายการที่ SPEC §3 ระบุ flyout สถานะไว้จริง) — ไม่ครอบ CN/DN/BN (มีแต่ ทั้งหมด/ล่าสุด ใน tabsFor ปัจจุบัน ยังไม่มีสถานะย่อยให้แสดง)

## Route ที่ยัง "soon" (href="#" + ชิป "เร็ว ๆ นี้") — ดูรายละเอียดเหตุผลในคอมเมนต์หัวไฟล์ nav.ts
ดูภาพรวม (ทุกหมวดที่มี) · ใบจ่ายเงินมัดจำ (DP) · รับใบลดหนี้/เพิ่มหนี้ (CNR/DNR) · ใบรวมจ่าย (CP) · นำเข้า (เอกสาร/สินค้า) · กลุ่มผู้ติดต่อ · รวมผู้ติดต่อซ้ำ · การเชื่อมต่อคู่ค้า · หน่วย · ใบส่งคืนเบิกสินค้า (RPR) · ใบปรับต้นทุนสินค้า (CA) · ไปที่คลังสินค้า ↗ · สำรองรับ/จ่าย · โอนระหว่างช่องทาง · กระทบยอดธนาคาร · กล่องขาเข้า · AI ช่วยบันทึก (ถ่ายบิล — ยังไม่มีจุดเข้าในฟอร์ม แม้ SPEC ติด ✅) · DBD e-Filing · ตั้งค่า/นโยบายบัญชี/สิทธิ์ผู้ใช้งาน/การเชื่อมต่อ (หน้าตั้งค่าวันนี้มีแผ่นเดียวรวมองค์กร+เอกสาร ยังไม่แยก)

## 16 ไฟล์ที่ยังมี `back={{label:"ระบบบัญชี"}}` ซ้ำ breadcrumb (เก็บไว้ตาม task C fallback)
documents, journal, assets, finance, cheque, reports, products, periods, wht, accounts, contacts, docs/[docType], goods-issue, ledger, expense-page.tsx (ใช้ร่วม purchase/expense/po/asset-buy), `_dev/components` (ของ WO 0.5 — ไม่ใช่ของ WO นี้)
