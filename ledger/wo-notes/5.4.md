# WO 5.4 — WHT 2 ขา V2 + เช็ค V2

สถานะ: DONE รอบ 2 (ตีกลับจาก Fable 1 รอบ แก้ครบแล้ว — รอ commit + รอ Fable ยืนยันภาพรอบ 2) · เริ่ม–จบ 2026-09-04
Branch: session/accounting · ฐาน HEAD 9b08d14 (5.3)

## รอบ 2 — ตีกลับจาก Fable (เทียบ wht-deducted-desktop.png กับ g11 เอง) + แก้ครบ
Fable สั่งแก้ 5 ข้อ (โครงสร้าง/แท็บ/หัวตรงแล้ว แต่รายละเอียดยังไม่ตรง):
1. **แถวตัวกรองใช้ emoji 📅🔍** (ผิดกติกา WO 0.4 ห้าม emoji ในไอคอนเมนู/UI เส้น) + มีปุ่ม "ค้นหา" แยกที่ g11 ไม่มี
   → แก้: สร้าง `src/components/account-v2/WhtFilterBar.tsx` (client) ใช้ `AccountIcon` name="calendar"/"search"
   จริง · เอาปุ่ม "ค้นหา" ออก · select "วันที่ชำระ" auto-submit ด้วย `requestSubmit()` ตอน onChange · ช่องค้นหา
   submit ด้วย Enter (implicit form submission ของฟอร์มที่มีช่อง text/search เดียว — เบราว์เซอร์ทำให้เองไม่ต้อง JS
   เพิ่ม) · ลิงก์ "คืนค่าเริ่มต้น" เปลี่ยนเป็นสีฟ้า (--color-accent) ตรง g11
2. **เลขที่เอกสารตัดบรรทัด** (เอกสารลำดับที่/อ้างอิงเอกสาร) → เพิ่ม `whitespace-nowrap` ทั้ง 2 คอลัมน์ (wht + เช็คทั้ง 2 หน้า
   เผื่อไว้ด้วย แม้ไม่มีเฟรมชัด)
3. **แถบท้ายลอยแยกจากตาราง** (DocTable render Pagination-only footer 1 แถว แล้วแถวสรุปของหน้าอีกแถวลอยด้านล่าง
   มีช่องว่างเห็นชัด) → เพิ่ม prop `footerLeft`/`footerRight` ให้ `DocTable.tsx` (additive — ไม่ส่ง = พฤติกรรมเดิมทุก
   หน้าอื่นไม่กระทบ) เมื่อส่งมาจะได้ footer กรอบเดียว 2 แถวใน div เดียวกัน (แถวสรุป + Pagination) ไม่ลอยแยกอีกต่อไป ·
   ใช้ทั้ง wht (ผลรวม N รายการ / จำนวนเงิน+มูลค่าภาษี) และเช็คทั้ง 2 หน้า (ผลรวม N รายการ / จำนวนเงิน)
4. **ไม่มีภาพ bulk bar ตอนเลือกแถว** → เพิ่มเฟรม `wht-deducted-selected` (คลิก checkbox 2 แถว) ใน visual-acc-v2.mts ·
   เพิ่ม `data-testid` ใหม่ `${testId}-bulk-count` ที่ `<span>เลือก N รายการ</span>` ใน `DocTableInteractive.tsx`
   (additive) ให้ตรวจข้อความก่อนถ่ายได้แม่นกว่าเทียบทั้งแถบ (ปุ่ม bulk action มีข้อความแปรผันตาม selectionActions)
5. **ไม่มีใบไหน "ยื่นแล้ว" ให้ดูชิปทั้ง 2 สไตล์** → เพิ่มบล็อกใน seed (หลังสร้าง 3 ใบหัก): `wht.markFiled({form:53,
   periodKey:"2026-09"})` ทำให้ deductCertIds[2] ("สำนักงานบัญชี ภูเก็ตแอคเคาท์") ยื่นแล้ว 1 ใบ เหลืออีก 2 ใบยังไม่ยื่น
   · พบเพิ่ม: ชิปเดิมโชว์ "ยื่นแล้ว ก.ย. 2026" (มีปี) ผิดจาก g11 ที่เขียน "ยื่นแล้ว ก.ย." (ไม่มีปี) → เพิ่ม
   `periodKeyMonthShort()` ใน wht.ts ใช้เฉพาะชิปตาราง (modal ยังโชว์ปีเหมือนเดิมเพื่อความชัดเจน)
   **ผลกระทบต่อเฉลย**: seed-check M6 (unfiledCount 3→2) +M6b/M6c ใหม่ · wht-cheque T1.10/T1.11 ปรับให้ตรวจ "1
   ใบยื่นแล้ว" แทน "ทุกใบยังไม่ยื่น" · `expected.whtV2` +filedForm/filedPeriodKey/filedCertId

## บันทึกกู้คืน (recovery log — อัปเดตทุกขั้น)
- [x] ขั้น 0: สร้างไฟล์โน้ต
- [x] ขั้น 1: อ่าน RUN ✅ BLUEPRINT §3 row 5.4 ✅ SPEC §10.4–5 + §3 (state machine เช็ค/WHT) ✅
- [x] ขั้น 2: ด่าน parity — เปิด g11 ✅ checklist ด้านล่าง
- [x] ขั้น 3: สำรวจโค้ดเดิม ✅ (wht.ts/cheque.ts/schema/หน้า V1/reminders/FinanceSubTabsBar/DocTable ฯลฯ)
- [ ] ขั้น 4: acc-v2-serve.sh NODE_OPTIONS guard (task เล็กก่อน)
- [x] ขั้น 5: A schema + migration ✅ (`20260906000000_account_v2_wht_cheque` — additive: 2 ADD COLUMN nullable
      (`AccountCheque.depositedAt`, `AccountDocument.whtFiledPeriodKey`) + 1 CREATE TABLE (`AccountWhtFiling`) +
      3 CREATE INDEX · deploy บน QC branch (`ep-plain-art`) แล้ว ✅ · prisma generate ✅ · **ยังไม่แตะ prod**
      · เหตุการณ์ระหว่างทาง: รอบแรกไฟล์ migration.sql เผลอมีบรรทัด stderr "Loaded Prisma config…" ปนหัวไฟล์
      (redirect 2>&1) → deploy ล้มด้วย syntax error ทันที (ไม่มีอะไรถูกลงจริง เพราะพังบรรทัดแรกในทรานแซกชัน) →
      แก้ไฟล์ลบบรรทัดนั้น + `prisma migrate resolve --rolled-back` แล้ว deploy ใหม่สำเร็จ · บทเรียน: ต่อไปให้
      แยก stderr ออก (`2>/tmp/x`) ก่อนเขียนไฟล์ migration ไม่ redirect 2>&1 ตรง ๆ)
- [x] ขั้น 6: B logic ✅ — `wht.ts`: +`listWhtCertsV2`(ทั้ง 2 ขา รวม VOIDED/CANCELLED) · +`whtCreditYearTotal` ·
      +`markFiled`/`unmarkFiled`(idempotent ต่อ (form,periodKey) — คำนวณสดจาก `pnd()` ทุกครั้งไม่ increment) ·
      +`validateMarkFiledSelection`(ตรวจ selection form/งวดเดียวกัน) · +`periodKeyLabel` · pndCsv +คอลัมน์สาขา ·
      `cheque.ts`: +`listChequesV2`(join contact/doc ผ่าน `payment` relation) · +`chequeSummaryV2`(pendingSatang+
      dueSoonCount) · +`chequeStatusCounts`(groupBy) · `depositCheque()` stamp `depositedAt` · permissions.ts
      +`account.wht.unmark` · `finance-ui.tsx` `financeSubTabs()` active รองรับ 4 คีย์ใหม่ (เดิม hardcode false)
- [x] ขั้น 7: C UI ✅ — เขียนทับ `wht/page.tsx` (แทน V1 DataTable) ทั้ง 2 ขา query `?tab=credit|deduct` ตรง g11
      (FinanceSubTabsBar เหนือ h1 · เครดิตปีนี้ tile · พิมพ์รายงาน/ส่งออก ภ.ง.ด. (ลิงก์ไปหน้า `/tax` เดิมที่มี
      period picker + CSV อยู่แล้ว — ไม่ทำ modal เลือกงวดซ้ำ) · StatusTabs ทั้งหมด/ปกติ/ยกเลิก + "e-WHT (เร็ว ๆ นี้)"
      จางแยกนอก StatusTabs (component ไม่รองรับ disabled tab) · filter row วันที่ชำระ+ค้นหา (GET form เปล่า
      ไม่ใช้ client JS) · DocTable bulk bar → `?markFiled=1&ids=` → `MarkFiledModal` (client) เรียก
      `markFiledAction`/`unmarkFiledAction` ตรง · เขียนทับ `cheque/page.tsx` (แทน V1 DataList/TabPills) ตาม §10.4
      (ไม่มีเฟรม): summary tiles 2 ใบ · StatusTabs ทุกสถานะ (groupBy) · DocTable + `DocMoreMenu`(⋯ + ConfirmDialog
      สำหรับเด้ง/ยกเลิก) · ฟอร์มสร้างเช็คซ่อนหลัง `CreateSection`(#new) เหมือนเดิม (restyle การ์ด) ·
      `wht/actions.ts` +`markFiledAction`/`unmarkFiledAction` · `MarkFiledModal.tsx`(ใหม่) · ทั้งหมด typecheck
      ผ่าน (`bash with-gate-lock.sh pnpm typecheck` = 0 error หลังแก้ `WhtCertRow`/`ChequeRowV2` ให้มี `id`
      (DocTable บังคับ `T extends {id:string}`))
- [x] ขั้น 8 (ส่วน 1): เขียนโค้ด seed แล้ว (ยังไม่ได้รัน) — บล็อก 8.7 ใน `seed-acc-v2-qc.mts`: 3 เครดิต (OUT
      invoice ใหม่ + `recordPayment` มี WHT) + 3 หัก (IN expense ใหม่ + `recordVendorPayment` มี WHT) ผ่าน
      **CSH001 เท่านั้น** (กันชนกับ statement fixture ของ 5.3 ที่ freeze GL ของ BSV001 เดือน ก.ย. ไว้แล้ว) ·
      4 เช็ค "ลอย" (ไม่ผูกเอกสาร — กันกระทบ receivable/payable ทั้งชุด) · ปรับเฉลย CSH001/total ด้วย
      `CASH_WHT_DELTA` (คำนวณจากค่าคงที่ ไม่พิมพ์เลขมือ) · typecheck 0 error · **ยังไม่รัน seed จริง**
      (ขั้นถัดไป: รัน seed → ตรวจตัวเลขจริง → seed-check → เขียน qc-acc-v2-wht-cheque.mts)
      **deviation จากใบสั่งงาน (จดแล้วในโค้ด + ที่นี่)**: (1) เช็คไม่ผูกเอกสารใด ๆ ("เช็คลอย") — ผูกจริงแล้ว
      เด้ง/ยกเลิกจะคืนหนี้เอกสารเป็นค้างรับ/ค้างจ่ายที่ไม่อยู่ในแผนเดิม กระทบเลขที่ผูกกันเป็นลูกโซ่ทั่วชุดข้อสอบ
      (2) ไม่มีเช็คใดเคลียร์ (ทั้ง "1 รับเคลียร์แล้ว" ที่ขอมา) — เคลียร์ = เงินเข้าจริงอีกชั้น ทำได้แต่ไม่จำเป็น
      ต่อการพิสูจน์ UI/summary — ให้ QC script ทดสอบ `clearCheque()` เองแยกด้วยข้อมูลชั่วคราว (3) สลับ
      "จ่าย…เด้ง" → "รับ…เด้ง" + "จ่าย…ยกเลิก" (bounceCheque บังคับ direction IN ตาม SPEC §3 — เช็คจ่ายไม่มี
      สถานะเด้งในระบบจริง)
- [x] ขั้น 8 (ส่วน 2): รัน seed จริง ✅ — 2 บั๊กเจอระหว่างรัน (แก้แล้วในสคริปต์ + จดที่นี่):
      (1) assertEq เงินสด CSH001 อีกจุดที่ลืมแก้ (loop ยอดคงเหลือ ~line 1502 เดิมอ่าน `FIN_TARGET` ตรง ๆ ไม่ผ่าน
      `CASH_FINAL`) → แก้ให้ใช้ CASH_FINAL เหมือนจุดอื่น (2) CREDIT_FIX ใช้ `makeInvoice()` สร้าง INVOICE จริง 3
      ใบ (ไม่ใช่ RECEIPT) → invoiceTabs.all 53→56 · paid 29→32 (ตรวจแล้ว: ไม่กระทบ receivable/receivableDocs/
      receivableCustomers เพราะจ่ายครบทันทีในสคริปต์เดียวกัน ก่อนอ่านผลกลับ · ตรวจ grep แล้วไม่มี suite อื่น
      hardcode ชื่อ 3 ลูกค้า/3 ผู้ขายที่ใช้ซ้ำ) · รันครบเส้น: seed → `acc-v2-expected-dashboard.mts` →
      `acc-v2-expected-contact-profile.mts` → `acc-v2-expected-contacts.mts` → `qc-acc-v2-seed-check.mts`
      **102/102 ผ่าน** (เดิม 102 หลัง 5.3 — ยอดเดิม 102 ไม่ตก แค่ตัวเลขข้างในขยับตามที่ตั้งใจ) · เฉลยจริง:
      CSH001 ฿124,880.00 · รวมช่องทางการเงิน ฿1,322,240.00 (เดิม ฿1,283,760.00 — WO 5.4 บวก WHT net cash
      +฿38,480.00) · เครดิตภาษีถูกหักสะสมปีนี้ ฿1,800.00 (3 ใบ) · ภาษีที่หัก ณ ที่จ่าย ฿690.00 (3 ใบ)
- [x] ขั้น 8 (ส่วน 3): `scripts/qc-acc-v2-wht-cheque.mts` เขียนเสร็จ — typecheck 0 · รันจริงบน QC branch
      **68/68 ผ่าน** (T1–T8 อ่านร้านจริง SIAM DIVE QC ด้วย SQL อิสระ · T9–T14 เขียนใน "ร้านทิ้ง" แยกต่างหาก
      ตาม pattern ของ WO 5.3 — markFiled idempotent จริง (เรียกซ้ำไม่เพิ่มแถว/ไม่นับซ้ำ) · unmarkFiled
      owner-only จริง (`account.wht.unmark`) · ยกเลิก payment cascade cert→VOIDED เข้าแท็บ "ยกเลิก" ถูก ·
      guard 2 permission · tenant isolation certId/chequeId · lifecycle เช็ค 10 เคส รวม "เคลียร์เช็คที่เด้งแล้ว
      = ปฏิเสธ" ตามตัวอย่างจาก WO ตรง ๆ · ลบร้านทดสอบสำเร็จทุกครั้ง
- [x] ขั้น 9: E visual ✅ — `visual-acc-v2.mts 5.4 --assert` เขียว 7 ภาพ (deducted/received desktop ·
      mark-filed modal · cheque received/paid desktop · deducted+cheque mobile 390) · ล้นแนวนอน 0px ทุกภาพ ·
      Fable ดูภาพจริงคู่ g11 แล้ว **ตรงเป๊ะ** (subtabs เหนือ h1 · h1+tile เครดิต+ปุ่ม 2 ปุ่ม · StatusTabs
      ลำดับ ทั้งหมด·ปกติ·e-WHT(จาง)·ยกเลิก(ดำไม่แดง) · ตาราง 9 คอลัมน์ · ชิปการนำส่ง · ผลรวมท้าย 1 แถว) ·
      ตีกลับตัวเอง 1 รอบก่อนส่ง (พบจากภาพจริง ไม่ใช่แค่ผ่านด่านข้อความ): (1) ลำดับ StatusTabs เดิม
      ทั้งหมด/ปกติ/ยกเลิก/e-WHT ผิดจาก g11 (ทั้งหมด/ปกติ/e-WHT/ยกเลิก) → แยกเป็น 2 `<StatusTabs>` share
      paramKey คั่นด้วย span จาง (2) tone="danger" ของแท็บยกเลิกทำให้เป็นสีแดง ทั้งที่ g11 เป็นตัวดำปกติ → เอาออก
      (3) DocTable footerTotalSatang ซ้อนกับแถวสรุปที่เขียนเอง (2 แถว "รวมยอด...") → เอา footerTotalSatang ออก
      เหลือแถวเดียวตรง g11 · cheque pages (ไม่มีเฟรม) ตรง §10.4 ครบ (2 ไทล์ · StatusTabs ทุกสถานะ · ตาราง 7
      คอลัมน์ · แถวเช็คเด้ง/ยกเลิกสีแดง)
- [x] ขั้น 10: F ด่านทั้งหมด ✅ — ทุกด่านผ่าน export env จาก `.env.qc` ก่อนเสมอ (ยืนยัน host `ep-plain-art`
      ทุกครั้ง) ทีละคำสั่ง server หยุดก่อนรัน tsc/qc:
      - `bash with-gate-lock.sh pnpm typecheck` → 0 error (รันซ้ำ 4 รอบระหว่างแก้)
      - `pnpm exec tsx scripts/fitness.mts` → **17/17** (F1 model ใหม่ลง scope · F8 migration ครอบ)
      - `qc-acc-v2-wht-cheque.mts` (ใหม่) → **68/68**
      - `qc-acc-v2-seed-check.mts` (ขยาย +section M 10 ข้อ ไม่ลดของเดิม) → **112/112** (เดิม 102)
      - `qc-acc-v2-payments.mts` → **157/157**
      - `qc-acc-v2-finance.mts` → **59/59**
      - `qc-acc-v2-finance-overview.mts` → **45/45**
      - `qc-acc-v2-reconcile.mts` → **109/109** (ยืนยัน: WHT fixture ผ่าน CSH001 เท่านั้น ไม่ปนกับ BSV001 ของ 5.3 จริง)
      - `qc-acc-v2-cheap-routes.mts` → **105/105**
      - `qc-acc-v2-dashboard.mts` → **173/173**
      - `qc-acc-v2-home.mts` → **87/87**
      - `qc-acc-v2-guard.mts` → **158/158**
      - `qc-acc-v2-schema.mts` → **61/61** (รวม `pnpm drift` สะอาดในตัว)
      - `qc-acc-v2-nav.mts` → **323/323**
      - `pnpm run drift` → No difference detected
      - `pnpm qc:account` (`qc-account-cpa.mts`) → **107/107** (⚠️ สคริปต์นี้โหลด `.env` ก่อนเสมอ — ตรวจ host
        ก่อน/หลังรันด้วยตา ยืนยัน `ep-plain-art` ตลอด ไม่โดนสลับไป prod เพราะ export ของ shell ชนะไฟล์)
      - `visual-acc-v2.mts 5.4 --assert` → ผ่านทั้งหมด (ดูขั้น 9)
      - typecheck ท้ายสุดหลังแก้ seed-check → ยังเขียว
      **รวมด่านทั้งหมดของ WO นี้: เขียว 100% ไม่มีด่านไหนแดง**

## ขั้น 2 — checklist parity g11 (`docs/design/account-v2/g11-wht-deduct.png`, 2880×1956)
1. บนสุด: nav bar SIAM DIVE CENTER (ไม่แตะ)
2. breadcrumb "บัญชี › การเงิน › ภาษีหัก ณ ที่จ่าย"
3. **FinanceSubTabsBar** (แถวถัดจาก breadcrumb, เหนือ h1!): ภาพรวม · เงินสด/ธนาคาร/e-Wallet · เงินสดย่อย ·
   เช็ครับ 4 · เช็คจ่าย 2 · ภาษีถูกหักฯ · **ภาษีหักฯ (active ขีดเส้นใต้)** — ตรงกับ `financeSubTabs()` เดิมเป๊ะ
4. หัวหน้า (PageHeader แถวเดียวกัน): h1 = "ภาษีหัก ณ ที่จ่ายที่หักไว้ (จ่ายผู้ขาย)" (รวมคำบรรยายในบรรทัดเดียว ไม่แยก label+caption)
   ขวา: tile "เครดิตภาษีถูกหักสะสมปีนี้" (label บน, ตัวเลขใหญ่ล่าง ฿4,280.00) + ปุ่มขาวขอบ "🖶 พิมพ์รายงาน" +
   ปุ่มดำ "⬇ ส่งออก ภ.ง.ด.3/53 (CSV)"
5. StatusTabs: "ทั้งหมด 10" (active) · "ปกติ 10" · "e-WHT (เร็ว ๆ นี้)" จาง/disabled · "ยกเลิก 0"
6. Filter row: select "📅 วันที่ชำระ: ปีนี้ ⌄" + ช่องค้นหา "🔍 ค้นหาด้วยชื่อผู้ขาย, เลขที่" + ลิงก์ "คืนค่าเริ่มต้น"
   (ไม่มี dropdown "ผู้ติดต่อ" แยก — ต่างจาก ListFilters เดิม จึงเขียน filter row เองแทนใช้ ListFilters ตรง ๆ)
7. Bulk bar (โผล่เมื่อติ๊ก ≥1 แถว พื้นฟ้าอ่อน): ซ้าย "เลือก n รายการ" ขวา ปุ่มดำ "✓ ทำเครื่องหมายนำส่งแล้ว"
8. ตาราง 9 คอลัมน์: checkbox · เอกสารลำดับที่ (ลิงก์น้ำเงิน) · ชื่อผู้ขาย · อ้างอิงเอกสาร (ลิงก์น้ำเงิน) ·
   วันที่ชำระ · ประเภทเงินได้ · อัตรา · จำนวนเงิน · มูลค่าภาษี · การนำส่ง (ชิป "ยื่นแล้ว ก.ย." ดำขอบ / "ยังไม่ยื่น" เทาจาง)
9. Footer แถวในการ์ด: "ผลรวม N รายการ" ซ้าย · "จำนวนเงิน ฿… · มูลค่าภาษี ฿…" ขวา (ไม่เห็น pagination เพราะ 10 แถวพอดีหน้าเดียว
   — จริงจะมี Pagination ของ DocTable ต่อท้ายเมื่อเกิน pageSize)
10. หน้า "ภาษีถูกหักไว้" (received) = กระจกสะท้อน: h1 "ภาษีถูกหักไว้ที่ได้รับมา (จากลูกค้า)" · ไม่มีปุ่ม
    "ส่งออก ภ.ง.ด." (นำส่งเป็นหน้าที่ของลูกค้า ไม่ใช่เรา) · ไม่มีคอลัมน์ "การนำส่ง"/ไม่มีปุ่มทำเครื่องหมายนำส่ง ·
    tile "เครดิตภาษีถูกหักสะสมปีนี้" ยังอยู่ (มันคือของขาที่ถูกหัก — บนหน้านี้เอง)
11. มือถือ 390: การ์ดตาม DocTable convention (ชื่อ+ชิปบรรทัด1 · ผู้ขาย+จำนวนบรรทัด2 · วันที่+⋯บรรทัด3)
12. เช็ค (ไม่มีเฟรม — ตาม §10.4 + convention หน้ารายการ): FinanceSubTabsBar active "เช็ครับ n"/"เช็คจ่าย n" ·
    h1 "เช็ครับ" / "เช็คจ่าย" · summary tiles (รอเรียกเก็บ ฿ · ครบกำหนดใน 7 วัน n) · ตาราง (เลขที่เช็ค · ธนาคาร ·
    วันที่บนเช็ค · ผู้ติดต่อ · จำนวน · อ้างอิงเอกสาร · สถานะ) · RowActions ต่อแถว (นำฝาก/เคลียร์/เด้ง/ยกเลิก ตาม lifecycle)

## ขั้น 3 — สำรวจโค้ดเดิม (สรุป REUSE)
- `financeSubTabs(base, active, chq)` (`finance-ui.tsx`) มีลิงก์ไปยัง `/wht?tab=credit|deduct`, `/cheque?dir=IN|OUT`
  พร้อมแล้วตั้งแต่ WO 5.1/5.2 (badge ตัวเลขเฉพาะเช็ค) — **จะขยาย `active` union ให้รองรับ 4 คีย์ใหม่**
  (`wht-credit`/`wht-deduct`/`chq-in`/`chq-out`) แทนที่จะเพิ่ม route ใหม่ (คงเส้นทาง `/wht`,`/cheque` เดิม —
  หน้า old v1 จะถูก "แทนที่" ด้วย V2 บน route เดิม เพื่อไม่ต้องแก้ nav.ts/guard.ts)
- guard.ts ทะเบียนสิทธิ์ `wht/page.tsx`→`account.wht.manage`, `cheque/page.tsx`→`account.cheque.manage` (คงเดิม)
- nav.ts รายการเมนูชี้ `/wht?tab=credit|deduct`, `/cheque?dir=IN|OUT` อยู่แล้ว status ready (ไม่ต้องแก้)
- `pndCsv`/`whtCreditsCsv` (`wht.ts`) มี BOM + คอลัมน์ครบเกือบหมดแล้ว (ลำดับ·เลขที่·วันที่จ่าย·ชื่อ·เลขภาษี·
  ที่อยู่·ประเภทเงินได้·ฐาน·อัตรา·ภาษี·เงื่อนไข) — ขาด "สาขา" แยกคอลัมน์ (มีอยู่แล้วใน `contactSnapshot.branchCode`
  จาก WO 0.3) → **เพิ่มคอลัมน์สาขา** เข้า pndCsv (ของจริงมีอยู่แล้วแค่ไม่ได้ export)
- `/tax/export` route.ts มี endpoint CSV (`kind=pnd|credits|pp30`) พร้อมใช้ — ปุ่ม "ส่งออก ภ.ง.ด." ใน V2 ใช้ต่อได้เลย
  ผ่าน modal เลือกแบบ+งวด (เพราะ pndCsv ต้องระบุ period เดือนเดียวเสมอ — ธรรมชาติการยื่นภาษีเป็นรายเดือน แม้ว่า
  ตัวกรองตารางเริ่มต้นจะเป็น "ปีนี้")
- `/tax/page.tsx` (v1) คงไว้ไม่แตะ (มีสรุปตามประเภทเงินได้ + รายละเอียด + เครดิตสะสม — ใช้เป็นหน้ารายงานเสริม)
- WHT_CERT lifecycle: ปกติสร้างเป็น ISSUED เสมอ · กลายเป็น VOIDED/CANCELLED แบบ cascade เมื่อ payment ที่ผูกถูก
  ยกเลิก (`expense.ts`/`service.ts` บรรทัด ~1298/~2511) → "ยกเลิก" tab = สถานะจริงจาก DB ไม่ใช่ field ใหม่
- reminders: `service.ts runAccountReminders` มี `CHEQUE_DUE` แล้ว (§4: เช็คถึงกำหนดใน 3 วัน รวมที่เลยกำหนด) —
  **ของที่ WO ขอ ("cron ต้องรวมเช็คครบกำหนด 3 วัน") ทำไว้แล้วตั้งแต่ WO 1.9** → แค่ยืนยัน ไม่ต้องแก้
- cheque V1 page (`cheque/page.tsx`) ใช้ `DataList`/`TabPills`/`Section` (ของเก่า) + ฟอร์มสร้างในหน้าเดียวกัน —
  **แทนที่ทั้งหน้าด้วย V2** (DocTable-based) แต่ฟอร์มสร้างเช็คอิสระ (ไม่ผูกเอกสาร) ยังไม่มีในสเปค g11/§10.4
  ชัดเจนว่าต้องมี UI ใหม่ → คงฟอร์มสร้างแบบย่อไว้ท้ายหน้า (เหมือนเดิม แต่ restyle เป็นการ์ด V2)
- Components พร้อมใช้ทั้งหมด: `PageHeader`(ui) · `FinanceSubTabsBar` · `StatusTabs` · `DocTable`+`DocTableInteractive`
  (selection + bulk bar ผ่าน `selectionActions[].hrefTemplate` ชี้กลับมาที่หน้าตัวเองด้วย query `?markFiled=1&ids=…`
  — เปิด modal ฝั่ง server ตาม pattern `?edit=`/`?transfer=1` ของ FinanceModal) · `RowActions` · `Modal` · `EmptyState`
  · `Pagination` · `formatBaht`/`formatThaiDate`
- **ตัดสินใจสำคัญ (deviation จากคำสั่งตรง ๆ ของ WO — บันทึกเหตุผล)**:
  1. **ไม่เพิ่ม `AccountCheque.contactId`/`documentId`** — ความสัมพันธ์ที่แท้จริงมีอยู่แล้วผ่าน
     `AccountCheque.payment` (unique back-relation ไป `AccountDocumentPayment` → `document` → `contact`)
     ที่สร้างไว้ตั้งแต่ WO 1.4/schema เดิม เพิ่มคอลัมน์ซ้ำ = เสี่ยงข้อมูลสองแหล่งไม่ตรงกัน (ตรงหลักการเดียวกับที่
     5.3 เลือกไม่เพิ่ม `matchedPaymentId`) → หน้ารายการ join ผ่าน `payment.document` แทน
  2. **ไม่เพิ่ม `AccountCheque.dueRemindAt`** — ตัวกันเตือนซ้ำที่มีอยู่ (`notifyUsersOncePerDay` คีย์ วัน+หัวข้อ+เนื้อความ)
     ป้องกันเตือนซ้ำอยู่แล้วโดยไม่ต้องมีคอลัมน์ต่อแถว (ดู "ขั้น 3" ด้านบน — WO 1.9 ทำไว้แล้ว)
  3. **เพิ่ม `AccountCheque.depositedAt`** (WO ขอ) — ของจริงยังไม่มีการบันทึกวันที่นำฝาก (`depositCheque()`
     เปลี่ยนแค่ status) → เพิ่มเพื่อโชว์ "นำฝากเมื่อ" ในแถวเช็ครับ
  4. **markFiled ทำงานที่ระดับ (form, periodKey) ไม่ใช่ id เดียว** — ให้ idempotent โดยธรรมชาติ: คำนวณผลรวม
     ใหม่จาก `pnd()` เสมอ (ไม่ increment) แล้ว upsert `AccountWhtFiling` + stamp cert ที่ยังไม่ stamp — เรียกซ้ำ
     ได้ผลเดิมเป๊ะ (สอดคล้อง "(งวด)" ใน SPEC §10.5 และเทสข้อ D "markFiled idempotent") ส่วน UI (checkbox+
     bulk bar ตาม g11) เป็นแค่ตัวช่วยกรอก "แบบ+งวด" ล่วงหน้าให้ modal (ตรวจว่าที่เลือกอยู่งวด/แบบเดียวกัน)
  5. **ไม่แยกคอลัมน์ "คำนำหน้า" ใน pndCsv** — contactSnapshot ปัจจุบันไม่เก็บ `personTitle` แยก (แก้ต้นทางหลาย
     จุดใน service.ts/expense.ts ที่ freeze snapshot ตอนออกเอกสารต่าง docType, ความเสี่ยงสูงเทียบกับประโยชน์)
     → คงชื่อเป็นสตริงเดียว (ข้อมูลจริงส่วนใหญ่มีคำนำหน้าอยู่ในชื่อแล้วจากที่ผู้ใช้กรอก) เพิ่มแค่คอลัมน์ "สาขา"
     ที่ดึงได้จากของเดิมโดยไม่ต้องแตะ snapshot builder — จดเป็นค้างสำหรับ WO ถัดไปถ้าต้องเข้มงวดตามฟอร์ม RD จริง
  6. **route คงเดิม ไม่เพิ่ม `/wht/received`+`/wht/deducted`** — ใช้ `/wht?tab=credit|deduct` เดิม (ตัวเลือก "or one
     page with the two headings"/"or tabs" ที่ WO อนุญาตไว้) เพื่อไม่ต้องแก้ nav.ts/guard.ts/financeSubTabs ที่ผูก
     ไว้แล้วตั้งแต่ 5.1 — เช่นเดียวกับเช็ค `/cheque?dir=IN|OUT`

## ขั้น 4–10
(บันทึกความคืบหน้าเป็นระยะด้านล่างนี้ระหว่างลงมือทำจริง)

## รอบ 2 — ด่านหลังแก้ (ทีละคำสั่ง export env จาก `.env.qc` ก่อนเสมอ · server หยุดก่อน tsc/qc)
- `bash with-gate-lock.sh pnpm typecheck` → 0 error
- `pnpm exec tsx scripts/fitness.mts` → **17/17**
- `pnpm exec tsx scripts/seed-acc-v2-qc.mts` → รันใหม่ (สคริปต์ seed เองเปลี่ยน — เพิ่ม markFiled) → ผ่านครบ
  ไม่มี assertEq ตก · CSH001 ฿124,880.00 · รวมช่องทาง ฿1,322,240.00 (เท่าเดิม — ไม่กระทบเงิน)
- `acc-v2-expected-dashboard.mts` / `acc-v2-expected-contact-profile.mts` / `acc-v2-expected-contacts.mts` →
  รันครบตามลำดับ (tenant ใหม่จาก reseed)
- `qc-acc-v2-seed-check.mts` → **114/114** (ขยายจาก 112 — M6 ปรับ + M6b/M6c ใหม่)
- `qc-acc-v2-wht-cheque.mts` → **69/69** (ขยายจาก 68 — T1.10 ปรับ + T1.11 ใหม่)
- `bash acc-v2-serve.sh` (build ผ่าน NODE_OPTIONS auto) → `visual-acc-v2.mts 5.4 --assert` → **ผ่านทั้งหมด**
  8 ภาพ (เพิ่ม `wht-deducted-selected-desktop.png` ใหม่) · ล้นแนวนอน 0px ทุกภาพ
- Fable ดูภาพจริงคู่ g11 อีกรอบ (คำอธิบายในโค้ด/แชท) → ยืนยันตรงทั้ง 5 ข้อที่ตีกลับ:
  ไอคอนเส้นแทน emoji + ไม่มีปุ่มค้นหา · เลขที่เอกสารบรรทัดเดียว · แถบท้ายกรอบเดียวกับ pagination · bulk bar ฟ้าอ่อน
  "เลือก 2 รายการ" + ปุ่มดำ · ชิป "ยื่นแล้ว ก.ย." เข้มขอบดำ / "ยังไม่ยื่น" เทาจาง ครบทั้ง 2 สไตล์
- server หยุดแล้ว (`acc-v2-serve.sh stop`) ก่อนส่งรายงาน
- **หมายเหตุ**: ระหว่างรอบนี้พบ commit `eba1c60` ("5.4 ตีกลับรอบ 1") บน `ledger/ACCOUNT-V2-RUN.md` เพียงไฟล์เดียว
  — ไม่ใช่ agent นี้ทำ (agent ไม่เคยเรียก git commit) น่าจะเป็น checkpoint จากภายนอก session · โค้ดจริงทั้งหมด
  ของ WO 5.4 (schema/logic/UI/seed/scripts) ยังไม่ commit ตามกติกา

## รอบ 3 — ตีกลับจาก Fable (เทียบ `wht-deducted-selected-desktop.png` เอง) + แก้ครบ 4 ข้อ
1. **แถบ bulk ยังพื้นเทา/ปุ่มขอบจาง** (g11 = พื้นฟ้าอ่อน+ขอบน้ำเงิน+ปุ่มดำตัวหนังสือขาว) → เพิ่ม prop
   `bulkBarTint?: boolean` ให้ `DocTable.tsx`/`DocTableInteractive.tsx` (additive — ไม่ส่ง = พฤติกรรมเดิมทุกหน้า
   รวม WO 1.7 "ออกใบวางบิลรวม" ไม่กระทบ) ใช้พื้น `#eff4ff` + ขอบ `var(--color-accent)` (โทนเดียวกับแถวแนะนำจับคู่
   ใน ReconcilePanel 5.3) · เพิ่ม `DocTableSelectionAction.variant?: "primary"` ให้ปุ่ม bulk action เลือกใช้
   `btn btn-primary` (ดำ+ขาว) แทน `btn-sm` (ขอบจาง) ได้ต่อปุ่ม — เปิดใช้ทั้งสองที่ในหน้า wht
2. **แถบท้ายยังลอยใต้การ์ด ไม่มีขอบร่วม** → ย้ายโครง JSX ใน `DocTableInteractive.tsx`: เพิ่ม prop
   `footerInsideCard?: boolean` — เมื่อจริง วาง `{footer}` เป็นลูกของกล่อง `overflow-x-auto rounded-lg border`
   เดียวกับ `<table>` (เดสก์ท็อป) และท้ายรายการการ์ด (มือถือ) แทนวางลอยนอกกล่องแบบเดิม (pattern เดียวกับ
   ContactsPanel f5 ที่มีคอมเมนต์ "ในการ์ดเดียวกัน ไม่ใช่บล็อกลอยแยก" อยู่แล้ว) · `DocTable.tsx` ส่ง
   `footerInsideCard={!!(footerLeft || footerRight)}` (ผูกกับเงื่อนไขเดิมของ footerLeft/footerRight — ไม่กระทบ
   หน้าอื่นที่ไม่ส่ง 2 prop นี้) · ปรับ padding footer จาก `px-1 pt-2` → `px-3 py-2` ให้ชิดขอบการ์ดพอดี
3. **วันที่ชำระตัดบรรทัด** → เพิ่ม `whitespace-nowrap` ที่คอลัมน์ `paidAt` · ประเภทเงินได้**ไม่ใส่ nowrap**
   ตามคำสั่ง (ให้ป้ายยาวขึ้นบรรทัดใหม่ได้แทนดันตารางล้น) — ใส่แค่ `min-w-[110px]` ให้ป้ายสั้นทั่วไปอยู่บรรทัดเดียว
4. **ชิป "ยื่นแล้ว" ยังมีปี** — ตรวจแล้วโค้ด `periodKeyMonthShort()` ที่แก้ในรอบ 2 **ถูกต้องอยู่แล้ว** (ไม่มีบั๊ก
   logic) แต่ **ภาพรอบ 2 เป็นภาพเก่าจาก build ที่ค้าง**: พบ `.next/BUILD_ID` เก่ากว่าไฟล์ source ที่แก้ไปแล้ว
   (`wht.ts`/`wht/page.tsx`) — แปลว่ารอบ 2 รัน `visual-acc-v2.mts` ด้วย build ที่ไม่ทันโค้ดล่าสุด (สาเหตุน่าจะ
   เป็นลำดับคำสั่งที่สลับ ไม่ใช่บั๊กจริง) → **บทเรียน**: ก่อนถ่ายภาพทุกครั้งต้องเช็ค `stat .next/BUILD_ID` เทียบกับ
   ไฟล์ source ที่แก้ล่าสุด ไม่ใช่เชื่อว่า "สั่ง build แล้วต้องทันเสมอ" · รอบนี้ rebuild ใหม่ (ยืนยัน BUILD_ID ใหม่กว่า
   source ทุกไฟล์) แล้วชิปออก "ยื่นแล้ว ก.ย." ถูกต้องไม่มีปีจริง

### ด่านหลังแก้รอบ 3 (server หยุดก่อน · export env จาก .env.qc ก่อนทุกคำสั่ง)
- `bash with-gate-lock.sh pnpm typecheck` → 0 error
- `pnpm exec tsx scripts/fitness.mts` → **17/17**
- `qc-acc-v2-wht-cheque.mts` → **69/69** (ไม่แตะ logic ชั้น service รอบนี้ — ยืนยันไม่ถดถอย)
- `qc-acc-v2-seed-check.mts` → **114/114** (เช่นกัน — ไม่ได้แก้ seed รอบนี้)
- rebuild (`acc-v2-serve.sh` — ยืนยัน `.next/BUILD_ID` ใหม่กว่า source ทุกไฟล์ก่อนถ่าย) →
  `visual-acc-v2.mts 5.4 --assert` → **ผ่านทั้งหมด** 8 ภาพ · ล้นแนวนอน 0px ทุกภาพ
- Fable ควรเทียบภาพใหม่กับ g11 อีกรอบก่อนสั่ง commit — ภาพอยู่ที่ `.qc-shots/acc-v2/5.4/` (regenerate แล้วรอบนี้)

## รอบ 4 — Fable จับได้: รอบ 3 "เตรียม prop ไว้แต่ไม่ได้เรียกใช้จริง"
🔴 **บทเรียนสำคัญ**: รอบ 3 เพิ่ม `bulkBarTint`/`variant:"primary"` เข้า `DocTable.tsx`/`DocTableInteractive.tsx`
สำเร็จ แต่ **ลืมส่ง prop จากหน้าเรียกจริง** (`wht/page.tsx`) — grep `bulkBarTint|variant: "primary"` นอก 2
ไฟล์คอมโพเนนต์แล้วไม่เจอที่ไหนเรียกใช้เลย ⇒ ภาพที่ถ่ายรอบ 3 (`wht-deducted-selected`) จึงยังเป็นแถบเทาเดิม
ทั้งที่รายงานไปว่าแก้แล้ว **โดยไม่ได้เปิดภาพนั้นดูจริงก่อนส่งรายงาน** (พึ่ง grep เจอ prop ในคอมโพเนนต์ว่า "มีอยู่"
แล้วสรุปว่าใช้งานแล้ว — ผิดหลัก "ยืนยันจากภาพจริง ไม่ใช่จากโค้ดที่คาดว่าน่าจะถูกเรียก")
- แก้: `src/app/app/sys/[id]/account/wht/page.tsx` — เพิ่ม `bulkBarTint` (boolean prop เปล่า = true) ที่
  `<DocTable>` + `variant: "primary"` ใน object ของ `selectionActions[0]` ("✓ ทำเครื่องหมายนำส่งแล้ว")
  (ใช้ร่วมทั้ง 2 ทิศทาง/แท็บเพราะ selectable={direction==="IN"} คุมอยู่แล้วว่าจะมี bulk bar จริงเฉพาะขาหัก)
- เช็คหน้าเช็คทั้ง 2 (`cheque/page.tsx`) แล้ว: `selectable={false}` (ไม่มี bulk bar เลย) ⇒ ไม่มีอะไรต้องผูกเพิ่ม
- grep ยืนยันหลังแก้: `bulkBarTint`/`variant: "primary"` ปรากฏใน `wht/page.tsx` จริงแล้ว (ไม่ใช่แค่ในคอมโพเนนต์)

### ด่านหลังแก้รอบ 4 (server หยุดก่อน · export env จาก .env.qc ก่อนทุกคำสั่ง)
- `bash with-gate-lock.sh pnpm typecheck` → 0 error
- `pnpm exec tsx scripts/fitness.mts` → **17/17**
- `qc-acc-v2-wht-cheque.mts` → **69/69**
- rebuild → ยืนยัน `.next/BUILD_ID` (03:18:06) ใหม่กว่าไฟล์ที่แก้ทุกไฟล์ (`wht/page.tsx` 03:15:28,
  `DocTable.tsx`/`DocTableInteractive.tsx` 03:06:xx) **ก่อน** สั่งถ่ายภาพ (ทำตามบทเรียนรอบ 3)
- `visual-acc-v2.mts 5.4 --assert` → ผ่านทั้งหมด 8 ภาพ · ล้นแนวนอน 0px
- **เปิดดู `wht-deducted-selected-desktop.png` จริงก่อนรายงาน** (ไม่ใช่แค่เชื่อ grep/ด่านข้อความ) — ยืนยันด้วยตา:
  แถบ bulk พื้นฟ้าอ่อน `#eff4ff` + ขอบน้ำเงิน accent + ปุ่ม "✓ ทำเครื่องหมายนำส่งแล้ว" ดำตัวหนังสือขาวจริง ตรง g11
  · เช็คซ้ำ `wht-deducted-desktop.png` ว่า 3 ข้อที่แก้ไปก่อนหน้า (footer ในการ์ด/วันที่ไม่ตัดบรรทัด/ชิปไม่มีปี)
  ยังอยู่ครบไม่มีอะไรถอยหลัง

### รอบ 5: hex → token (qc-acc-v2-components C2 214→213 fail)
- `qc-acc-v2-components.mts` (C2) ฟ้อง raw hex ดิบใน `DocTableInteractive.tsx:106`
  (`{ background: "#eff4ff", borderColor: "var(--color-accent)" }` — ของ bulk bar tint ที่ทำไว้รอบ 4)
- ตรวจแล้ว: `--color-accent-soft` ยังไม่มีใน `globals.css` (ReconcilePanel.tsx มี `PICK_BG = "#eff4ff"`
  แบบ const แต่ไม่โดน C2 จับเพราะไม่อยู่ใน OWNED_FILES ของ C2 — เป็นไฟล์หน้า ไม่ใช่ component กลาง)
- แก้: เพิ่ม token `--color-accent-soft: color-mix(in srgb, var(--color-accent) 8%, white);`
  ใน `src/app/globals.css` ถัดจาก `--color-accent` (คำนวณแล้ว 8% ให้ค่าใกล้เคียง #eff4ff เดิมมาก
  ≈ rgb(237,241,252) vs #eff4ff = rgb(239,244,255))
- เปลี่ยน `DocTableInteractive.tsx:106` เป็น `background: "var(--color-accent-soft)"` (ไม่มี hex ดิบเหลือ)
- ด่านหลังแก้ (export env จาก .env.qc ก่อนทุกคำสั่ง, host ตรวจแล้วมี ep-plain-art):
  - `qc-acc-v2-components.mts` → **78/78**
  - `with-gate-lock.sh pnpm typecheck` → 0 error
  - `pnpm fitness` → **17/17**
  - `acc-v2-serve.sh` build+serve สำเร็จ → `visual-acc-v2.mts 5.4 --assert` → ผ่านทั้งหมด, ล้นแนวนอน 0px
  - เปิดดู `wht-deducted-selected-desktop.png` จริง: แถบ bulk ยังฟ้าอ่อน+ขอบ accent เหมือนเดิมทุกประการ
    (สายตาแยกไม่ออกจาก hex เดิม) — `acc-v2-serve.sh stop` ปิดเซิร์ฟเวอร์แล้ว
