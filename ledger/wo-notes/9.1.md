# WO 9.1 — มือถือทำงานได้จริง

## สถานะ
เริ่ม: 2026-09-04

## Recovery log
- [เริ่ม] git log -1: 0826064 run(account-v2): เฟส 8 ปิด qc:all 222/222 · เริ่ม 9.1
- branch: session/accounting, worktree สะอาด
- อ่านครบ: ACCOUNT-V2-RUN.md · BLUEPRINT §1-3(9.1) · DESIGN-SPEC-V2 §13 + §0.3(10) · เฟรม g17-g20/f11-f14 (ดูภาพจริงแล้ว)
- grep wo-notes เก่า (1.3/1.5/1.6/0.4/3.4/4.3/7.2/8.3/2.2/1.1/3.2) หา keyword มือถือ/sheet/orb — สรุปด้านล่าง

## Checklist องค์ประกอบต่อเฟรม (ก่อนแก้โค้ด — ด่าน parity)
### g17 (ฟอร์มใบแจ้งหนี้มือถือ)
‹ back · h1 "สร้างใบแจ้งหนี้" + เลขที่/ร่าง · stepper QT-IV-RE-TX (วงกลม+เส้น) · accordion ●ส่วนหัวเอกสาร (ผู้ติดต่อ+ป้ายสมาชิก/ค้างรับ · วันที่ออก · ครบกำหนด · ประเภทราคา · แท็ก · "เพิ่มเติม" ยุบ) · accordion ●รายการ n = การ์ด (ชื่อ+⋯ / จำนวน×ราคา / บัญชี+VAT+WHT chip / ยอด) + "+ เพิ่มรายการ" เต็มกว้าง · accordion เงินมัดจำ/หมายเหตุ/แนบไฟล์ (ยุบ) · แถบยอด sticky เหนือปุ่ม (จำนวนเงินทั้งสิ้น/ยอดที่ต้องชำระ+chevron) · ปุ่มท้าย 2 ปุ่ม (บันทึกร่าง ▾ · อนุมัติ ▾) — **สถานะจริง: ยืนยันแล้วโดย Fable ใน WO 1.3 รอบก่อน (ตรง)** — ตรวจซ้ำผ่าน measurement script: ไม่มี AiDock ทับปุ่ม (มี pb-40 กันไว้ 2 ชั้น เกินพอ)
### g18 (bottom sheet ระดับ 2)
พื้นหลังจาง (sheet ล้อยด้านล่าง) · หัว ‹ รายรับ·ใบแจ้งหนี้ + ✕ · ปุ่มดำเต็มกว้าง "+ สร้างใบแจ้งหนี้" · แถวสถานะ (ไอคอน+ป้าย+ตัวนับกรอบ) · เส้นคั่นก่อน "ดูทั้งหมด" · "ล่าสุด" ท้ายแถว — **ยืนยันแล้ว WO 0.4** (ตรง)
### g19 (โปรไฟล์ผู้ติดต่อมือถือ)
‹ ผู้ติดต่อ + ✏ · ชื่อ+รหัส+ชิปกลุ่ม · KPI 2×2 (ค้างรับ/ซื้อสะสม/ครั้งที่ซื้อ/เครดิตเทอม) · อายุหนี้ 5 แถบแนวนอน · แท็บ pill (ข้อมูล/เอกสาร n/ไฟล์ n/การเชื่อมต่อ) · การ์ดเชื่อมต่อเรียงลง (สมาชิก/CRM/แชท/POS/จอง) · เอกสารล่าสุด 3 + "ดูทั้งหมด" · ปุ่มท้าย 2 ปุ่ม (สร้างใบแจ้งหนี้ดำ · ส่งใบแจ้งเตือน) — **ยืนยันแล้ว WO 3.4** (ตรง) — ต้องเช็คซ้ำว่าไม่มี regression จาก WO หลังๆ
### g20 (กล่องขาเข้ามือถือ)
‹ คลังเอกสาร + 🔍 · h1 "กล่องขาเข้า" + คำอธิบาย · แท็บ pill (ยังไม่เชื่อมต่อ n ดำ / เชื่อมต่อแล้ว) · ปุ่มดำเต็มกว้าง "📷 ถ่ายบิล" + ลิงก์ฟ้า "หรืออัปโหลดจากเครื่อง" · การ์ดไฟล์ (thumb+ชื่อ+ชิปที่มา + "AI อ่านได้" กล่องเทา + 3 ปุ่ม: สร้างบันทึกค่าใช้จ่าย(ดำ)/แนบ/ไม่ใช่) · การ์ด "อ่านไม่ได้" → กรอกเอง/ลบ · FAB + ลอยมุมล่างขวา + orb AI ใต้มันอีกชั้น (g20 เป็นเฟรมเดียวที่ตั้งใจวาด FAB ซ้อน orb 2 ปุ่ม) — **ยืนยันแล้ว WO 7.2** (ตรง) — verify capture input มีจริง: `[data-testid="inbox-capture-input"]` accept=image/*, capture=environment ✓ (grep ยืนยันแล้ว)
### f11/f12/f13/f14 — ยืนยันแล้วใน WO 2.2/0.4/1.1/1.5 ตามลำดับ (ดูรายละเอียดด้านล่าง "wo-notes เก่า")

## สรุปจาก wo-notes เก่า (ที่เกี่ยวมือถือ/orb/sheet)
- 1.3: แถบยอดมือถือใส่ sticky แล้ว · ฟอร์ม `pb-40 md:pb-28` กันสติกกี้บาร์ชนกัน (ของเดิม) · g17 ผ่าน parity แล้ว
- 1.5 (ค้างส่งต่อ 9.1): "ตารางรายการบนมือถือเลื่อนในการ์ด (f14 เป็นลิสต์ย่อ)" → **แก้แล้ววันนี้** (ดูขั้นตอน B)
- 0.4: AccountTabBar sheet L1/L2 ตรง f12/g18 แล้ว · FAB ไม่อยู่ scope shell (เป็นของหน้ารายการ) · เพิ่ม `acc-sheet-l1/l2` testid + วัด rect จริงแทน offsetParent
- 3.4: โปรไฟล์ 360° เต็มจอมือถือ ตรง g19 · ปุ่มท้าย sticky bottom-0 (ไม่ fixed) เคยเปลี่ยนจาก fixed เพราะชน orb
- 4.3: ProductsPanel มีการ์ดมือถือ+FAB ตรง f6/f13 pattern
- 7.2: InboxCaptureBar มี `inbox-capture-input`(กล้อง)/`inbox-capture-file-input`(อัปโหลด) จริง · ตรง g20
- 8.3: เคยมีบั๊ก "ปุ่มบันทึกซ่อนบนมือถือ (md:flex)" ปิดแล้ว — เป็นบทเรียนให้เช็คปุ่ม action ทุกหน้าไม่ให้ hidden ผิด breakpoint
- 2.2/1.1/3.2: หน้าหลัก/รายการ/ผู้ติดต่อมือถือ = การ์ด 3 บรรทัด + FAB (bottom-24 เหนือ orb bottom-4) + sheet ตัวกรอง — pattern อ้างอิงมาตรฐานที่ WO นี้ต้องเทียบ

## ขั้นตอน A — Audit (มาตรวัดจริงก่อนแก้)
- build+serve QC (`bash scripts/acc-v2-serve.sh`) สำเร็จ · re-run `visual-acc-v2.mts 1.5 --assert` เขียวทั้งหมด (baseline ก่อนแก้)
- **ตรวจ AiDock overlap ด้วยสคริปต์วัด rect จริง (ไม่ใช่ดูภาพ fullPage — ภาพ fullPage วาด sticky/fixed ผิดตำแหน่งเสมอ เป็น artifact เดิมที่รู้กันอยู่แล้ว)**: เขียน one-off puppeteer (`/tmp/check-orb*.mjs`) mint session จริง เปิดฟอร์ม invoice edit มือถือ 390×844 → scrollTo bottom จริง → getBoundingClientRect ของ orb vs `editor-actions`/`btn-approve-menu`
  - ผล: orb rect (top 786.7 · bottom 829.3 · left 332.7 · right 375.3) ไม่ทับ editorActions (bottom 587.9) เพราะฟอร์ม div มี `pb-40`(160px มือถือ) **+** `<main>` มี `pb-24`(96px) รวม 256px กันชน — เกินพอ ไม่ทับจริง ✅ (ยืนยันด้วยตัวเลขจริง ไม่ใช่เดา) — สรุป: **orb ไม่ทับ sticky bar ของ DocEditorV2 บนมือถือ — ของเดิมถูกต้องอยู่แล้ว ไม่ต้องแก้**
  - หน้า DocDetailPage: ActionRow (ปุ่ม รับชำระ/อนุมัติ) ไม่ใช่ sticky (อยู่ในการ์ดหัวใต้เนื้อหา — ตรง f14) ⇒ ไม่มีความเสี่ยงชน orb เชิงโครงสร้าง
  - หน้ารายการ (1.1/3.2/4.3): FAB `bottom-24` อยู่เหนือ orb `bottom-4` แล้ว (ยืนยันจาก wo-notes เดิม) — ไม่ต้องแก้
  - SlideOver (payment sheet)/Modal(sheetOnMobile)/ConfirmDialog: ทุกตัวเป็น `fixed inset-0 z-50` เต็มจอ (z สูงกว่า orb z-40) → orb ถูกบังเมื่อ sheet เปิดอยู่แล้วโดยธรรมชาติ ไม่ต้องเพิ่มโค้ดซ่อน orb เอง
- **พบบั๊กจริง**: หน้าเอกสาร detail มือถือ (f14) ตารางรายการยังเป็น `overflow-x-auto` ตาราง desktop (min-w-720px) ทั้งที่ f14 เป็นลิสต์ย่อไม่มีสโครลแนวนอน — ของค้างจาก 1.5 ตามที่จดไว้
- **พบบั๊กจริง (เล็ก)**: `?err=` ของ approve PO เกินเพดาน render อยู่แล้ว (`{err && <p>...}`) แต่ไม่มี data-testid ให้ทดสอบอัตโนมัติจับได้แน่นอน
- รายงานตารางในหน้ารายงาน (`_shared.tsx TableWrap`) มี `overflow-x-auto` อยู่แล้วตรง §13 แต่ไม่มี "คอลัมน์แรก sticky" ตามสเปก

## ขั้นตอน B — Fixes (ทำแล้ว)
1. `src/components/account-v2/DocDetailPage.tsx` `DetailTab`: เพิ่มลิสต์มือถือ (`md:hidden` ชื่อ/จำนวน×ราคา/ยอด/บัญชี) คู่กับตาราง desktop เดิม (`hidden md:block`) — ตรง f14 · testid `detail-lines-mobile` / `detail-line-m-<n>`
2. `DocDetailPage.tsx` err banner: เพิ่ม `data-testid="doc-err"` ให้ข้อความ error inline (ใช้ยืนยัน job 4 เพดานอนุมัติ)
3. รายงาน `trial-balance/page.tsx` + `pp30/page.tsx`: เพิ่ม sticky first column (`sticky left-0 z-10/20 bg-...`) ให้คอลัมน์แรกของ thead/tbody/tfoot ตรง §13 "ตารางรายงาน = overflow-x-auto + คอลัมน์แรก sticky"
   - **ตัดสินใจ scope**: balance-sheet/profit-loss/cash-flow ไม่แตะ (โครงแถวหลายแบบปนกัน colSpan/หลาย row type ต่างจาก trial-balance/pp30 ที่โครงเดียว — เสี่ยง regression สูงเทียบกับประโยชน์ที่ได้ เพราะยังมี overflow-x-auto ใช้เลื่อนได้อยู่แล้ว) — จดเป็นค้างสำหรับ 9.3/9.4
4. GroupChildrenTable (BN/CP กลุ่มเอกสาร) — ไม่แตะ (ไม่ใช่ f14 หลัก + ยังมี overflow-x-auto ใช้ได้ ไม่ใช่ 1 ใน 4 งาน E2E บังคับ) — จดค้าง

## ขั้นตอน C — ต่อ (กำลังทำ)
- typecheck สะอาด (with-gate-lock) หลังแก้ทั้งหมด ✅
- rebuild+serve → ถ่ายภาพ 1.5 ซ้ำ (regression) ผ่านทั้งหมด (ยังไม่ล้น 390 · ลิสต์ย่อ f14 ตรงแบบ) ✅

## ขั้นตอน D — `scripts/qc-acc-v2-mobile.mts` (4 งาน E2E จบบนมือถือ 390×844 จริง)
สร้างสคริปต์ใหม่ (ไม่มีมาก่อน) — ใช้ puppeteer ขับเซิร์ฟเวอร์ QC ที่ build+start จริงเหมือน visual-acc-v2.mts
(ไม่ใช่แค่เรียก service ผ่าน prisma) เพราะต้องพิสูจน์ "กดจริงบนจอ 390px" ใช้งานได้
- **ด่านกัน qc:all แดงถาวร**: ชื่อไฟล์ขึ้นต้น `qc-` ตามที่ WO สั่ง (ต่างจาก visual-acc-v2.mts ที่จงใจไม่ใช้ prefix
  นี้เพราะต้องมี build+chromium ที่ CI ไม่มี — บทเรียน WO 0.7) ⇒ ใส่ด่าน `serverUp()` เช็ค BASE ก่อนเริ่มทุกครั้ง
  ไม่ตอบ = พิมพ์ "⚠️ SKIPPED" + exit(0) (ไม่ใช่ exit(1)) กัน `pnpm qc:all`/CI แดงค้างถาวรเมื่อไม่มีเซิร์ฟเวอร์ —
  **จดเป็นข้อยกเว้นที่ Fable ต้องรู้**: ไฟล์นี้ต้อง `bash scripts/acc-v2-serve.sh` ก่อนเสมอถึงจะทดสอบจริง (ไม่เหมือน
  qc-*.mts ตัวอื่นในโฟลเดอร์ที่ยิง DB ตรงไม่ต้องมีเซิร์ฟเวอร์) — รันเดี่ยวเสมอ ไม่ใช่ผ่าน `pnpm qc:all` ธรรมดา
- J1 สร้าง IV 2 รายการจากฟอร์มมือถือ (accordion) → เปิดเมนูอนุมัติ → กด "อนุมัติ" → ได้เลขที่จริง
- J2 เปิดเอกสารที่เพิ่งสร้าง → กด "รับชำระ" (เปิด SlideOver เต็มจอ) → กด "บันทึกการชำระ" (ยอดเต็ม prefill อัตโนมัติ) → PAID
- J3 เปิดกล่องขาเข้ามือถือ → ยิงไฟล์เข้า `inbox-capture-input` (accept=image/* capture=environment) ด้วย fixture
  `scripts/fixtures/acc-v2/attach/bill-ptt.jpg` (ของเดิมจาก WO 7.1) → เช็ค DOM "เสร็จ" + DB นับไฟล์ UNLINKED +1
- J4 สร้าง PO ชั่วคราวเกิน 50,000 บาท (เพดาน approver@siamdive-qc.test = 5,000,000 สตางค์ จาก seed role2) →
  ส่งอนุมัติ → approver เปิดหน้ามือถือกดอนุมัติ → ปฏิเสธพร้อม `[data-testid="doc-err"]` บอกเพดานเป็นภาษาไทย →
  OWNER (ไม่มีเพดาน) เปิดหน้าเดียวกันกดอนุมัติสำเร็จ → APPROVED
- ทุกจอ: `probeOverflowAndTouch()` เช็ค `scrollWidth ≤ 390` + ปุ่มหลักที่ระบุ `getBoundingClientRect() ≥ 40×40`
- ล้างข้อมูลทดสอบทั้งหมดใน `finally`-style (ไล่ทุก job ไม่ผูก try/finally เดียว — งานพัง 1 job ไม่กระทบ cleanup)
  + `purgeStale()` ก่อนเริ่มกันซากจาก run ที่ตายกลางทาง (แบบเดียวกับ fixture ของ 1.3/1.4 ใน visual-acc-v2.mts)

### บั๊กจริงที่เจอระหว่างพัฒนา + วิธีแก้ (มีค่าต่อ WO อื่นที่จะเขียน puppeteer flow ในอนาคต)
1. **ปุ่มหลักสูง 36px ไม่ถึง 40px** — `.btn` กลาง (`src/app/globals.css`) ใช้ `py-2 text-sm` = 36px สูง เป็น token
   ทั้งแอป (ห้ามแก้ทั้งระบบ — SubmitButton/ConfirmDialog ใช้ร่วมกับหลายโมดูลนอกบัญชี) ⇒ แก้เฉพาะจุดที่เป็นปุ่มหลัก
   บนมือถือของโมดูลบัญชี V2 ด้วย `h-11 md:h-9` (มือถือ 44px offline เกณฑ์ · เดสก์ท็อปเท่าเดิม 36px — ตาม precedent
   `FlyoutRow` ของ WO 0.4): `DocEditorV2.tsx` (btn-approve-menu, btn-save-draft) · `AttachmentUpload.tsx`
   (inbox-capture-btn) · `DocDetailPage.tsx` (ปุ่มหลัก ActionRow ทั้ง 8 จุด + ConfirmDialog trigger 1 จุด) ·
   `PaymentPanel.tsx`/`GroupPaymentPanel.tsx` (btn-open-payment, btn-record-payments/btn-record-group-payment)
2. **MoneyInput ยืนยันค่าตอน blur เท่านั้น** — พิมพ์แล้วไม่กด Tab/blur = ค่าไม่ถูกบันทึก (state คงเดิม 0.00) —
   สร้างใบแจ้งหนี้ครั้งแรกได้ grandTotal=0 → `gl.postDocument` โยน "ไม่มีบรรทัดบัญชีสำหรับ .../ISSUE" (JV ว่าง)
   แก้ด้วยกด Tab หลังพิมพ์ทุกครั้ง (`typeInto()` เพิ่ม `page.keyboard.press("Tab")`)
3. **ProductPicker dropdown ค้างเปิดทับช่องราคาด้านล่าง** — พิมพ์ชื่อสินค้าในการ์ดมือถือแล้ว dropdown ผลค้นหา
   (role=listbox, z-20) ไม่ปิดด้วย Tab (ปิดด้วย `document.addEventListener("mousedown", onDocClick)` เท่านั้น)
   ⇒ วาดทับช่องราคาที่อยู่แถวถัดไปพอดี (กริด 2 คอลัมน์) → `page.click()` แบบพิกัดไปโดน dropdown แทนช่องราคาจริง
   (`elementFromPoint` พิสูจน์แล้ว) — คนละอาการ แต่รากเดียวกับบทเรียน WO 1.6 รอบ 4 (คลิกพิกัดโดนสิ่งอื่นบัง)
   แก้ 2 ชั้น: (ก) `typeInto()` โฟกัส+เลือกข้อความด้วย DOM ตรง (`el.focus(); el.select()`) แทนคลิกพิกัด — ทำงาน
   ได้แม้มีอะไรวาดทับอยู่บนจอ ณ จุดนั้น (ข) เพิ่ม `closeAnyDropdown()` ยิง `mousedown` สังเคราะห์ที่ `document.body`
   (ไม่ใช่ `.click()` เฉย ๆ — ContactPicker/ProductPicker ฟัง `mousedown` ไม่ใช่ `click`) เรียกหลังกรอกชื่อสินค้า
   ก่อนไปกรอกราคาบรรทัดเดียวกันเสมอ
4. **route ฝั่งรายจ่ายใช้ slug สั้น ไม่ใช่ `docs/<DOCTYPE>`** — PO จริงอยู่ที่ `/account/po/<id>` ไม่ใช่
   `/account/docs/PURCHASE_ORDER/<id>` (ดู `EXP_ROUTE`/`EXPENSE_LIST_TYPES` ใน `expense.ts`) — เดาผิดรอบแรกได้ 404
5. **รูปแบบเลขที่เอกสารตั้งได้ต่อร้าน** (WO 8.1 §9.2) — QC เปลี่ยน `sequences.INVOICE.pattern` เป็น
   `"INV-{yy}{mm}-{0000}"` ต่างจากเลขที่ fixture เดิมที่ seed ยัดตรง (`"IV-2026-09-0001"`) ⇒ ข้อสอบห้าม assert
   รูปแบบเลขที่ตายตัว ต้องเช็คแค่ "ออกเลขจริงแล้ว ไม่ใช่ placeholder ร่าง"

### ผลรันจริงล่าสุด (server build ล่าสุดหลังแก้ปุ่ม h-11): **47/47 ผ่านทั้งหมด**
(บันทึก log เต็มไว้ด้านล่างหลังรัน --assert รอบสุดท้ายของ gate F)

## ขั้นตอน E — เพิ่ม PAGES "9.1" ใน `visual-acc-v2.mts` (10 หน้า มือถือ 390 ล้วน `onlyDevice: "mobile"`)
fixture ใหม่ (เฉพาะที่จำเป็น — ที่เหลือ dynamic lookup จาก DB ของ seed เดิม ไม่สร้างใหม่):
- `fx91Po`: PO ชั่วคราว "รออนุมัติ" (reference=QC-VISUAL-9.1 ลบใน cleanupFixture) — seed เดิมไม่มี PO ค้างรออนุมัติ
- `fx91Draft` / `fx91AwaitingPay`: หาแบบ dynamic จาก DB (ไม่ผูก id ตายตัว) — DRAFT/AWAITING_PAYMENT ใบแรกที่เจอ
หน้า: `invoice-form-mobile`(g17) · `sheet-l1-mobile`(f12) · `sheet-l2-mobile`(g18) · `invoice-list-mobile`(f13) ·
`doc-detail-mobile`(f14 — จุดที่ WO นี้แก้จริง) · `contact-profile-mobile`(g19) · `inbox-mobile`(g20) ·
`payment-sheet-mobile`(แผ่นรับชำระเต็มจอ) · `approve-confirm-mobile`(bottom-sheet ConfirmDialog "ไม่อนุมัติ" PO —
ปุ่ม "อนุมัติ" จริงไม่มี confirm sheet ยิง action ตรง จึงใช้ ConfirmDialog "ไม่อนุมัติ" แทนสาธิตแพทเทิร์นแผ่นยืนยันมือถือ)
เพิ่ม `testId` prop ให้ `ConfirmDialog.tsx` (`<name>-trigger`/`<name>-sheet`) — ของเดิมไม่มี test id เลย หา element
ไม่ได้แน่นอน ⇒ เพิ่ม optional prop (ไม่กระทบผู้เรียกเดิมที่ไม่ส่ง) ใช้ที่ "reject-po" ใน DocDetailPage.tsx

ผลรัน `visual-acc-v2.mts 9.1` (ครั้งแรกและ --assert รอบสอง): ผ่านทั้งหมด 10/10 หน้า ไม่ล้นแนวนอนทุกหน้า ·
Fable/ผู้เขียนดูภาพจริงเทียบ g17/g18/g19/g20/f13/f14 แล้ว — โครง/ป้ายไทย/คอลัมน์/ปุ่มตรงแบบ (ดูหัวข้อ "เทียบภาพ" ท้ายไฟล์)

## 🐞 บั๊กจริงที่เจอ+ปิดระหว่าง gate (สำคัญ — ไม่ใช่แค่ของ QC script)
**ข้อมูลการเงินเพี้ยนถาวรถ้า cleanup ของ E2E test ไม่ครบ**: `gl.postPayment` ผูก JV เข้ากับ
`refType:"AccountDocumentPayment", refId:<paymentId>` (คนละคีย์กับตอนออกเอกสารที่ผูก `refId:<documentId>`) —
สคริปต์ QC ใด ๆ ที่สร้างเอกสาร+รับชำระแล้วลบทิ้งเอง (ไม่ใช้ `gl.reverseFor`/`voidDocument` ของจริง) **ต้องลบ JV ของ
การชำระแยกต่างหากด้วย refId ของ payment ไม่ใช่แค่ของเอกสาร** ไม่งั้นยอดคงเหลือช่องทางการเงิน (คำนวณสดจาก JV ไม่ได้
cache) จะเพี้ยนถาวรเงียบ ๆ (พบจริง: รัน J1+J2 สำเร็จ 2 ครั้งระหว่างพัฒนา → `qc-acc-v2-dashboard` P3.1/P3.2/P3.4/
P10.11 แดง ยอดเกิน +400,000 สตางค์ (2 payment ที่ไม่ได้ลบ JV) — แก้ทั้ง `cleanupDoc()`/`purgeStale()` ใน
`qc-acc-v2-mobile.mts` ให้หา paymentIds ก่อนแล้วลบ JV ทั้ง 2 refType + ลบข้อมูลเสียที่ค้างใน DB จริงด้วยมือแล้ว —
ยืนยันซ้ำ: รัน `qc-acc-v2-mobile.mts` อีกรอบ (47/47) → เช็ค orphan JV = 0 → `qc-acc-v2-dashboard`/`home`/
`seed-check` เขียวหมด (174/87/279) **บทเรียนนี้มีค่าต่อ WO อื่นที่จะเขียน QC script สร้าง+ลบ payment เอง**

## ขั้นตอน F — ผลด่านจริงทั้งหมด (เรียงตามที่รัน · ทุกคำสั่ง export env จาก .env.qc ก่อนเสมอ)
| ด่าน | คำสั่ง | ผล |
|---|---|---|
| typecheck | `bash scripts/with-gate-lock.sh pnpm typecheck` | สะอาด (รันซ้ำ 6 ครั้งระหว่างแก้ ครั้งสุดท้ายก็สะอาด) |
| fitness (มี .env.qc) | `pnpm fitness` | 17/17 |
| fitness (ไม่มี env) | `env -i PATH="$PATH" HOME="$HOME" pnpm fitness` | 17/17 |
| qc-acc-v2-mobile (4 งาน E2E) | `pnpm exec tsx scripts/qc-acc-v2-mobile.mts` (ต้องมีเซิร์ฟเวอร์) | **47/47** (รันยืนยันซ้ำ 2 ครั้งหลังแก้ cleanup) |
| editor | `pnpm exec tsx scripts/qc-acc-v2-editor.mts` | 200/200 |
| detail | `pnpm exec tsx scripts/qc-acc-v2-detail.mts` | 85/85 |
| payments | `pnpm exec tsx scripts/qc-acc-v2-payments.mts` | 161/161 |
| list | `pnpm exec tsx scripts/qc-acc-v2-list.mts` | 159/159 |
| contacts | `pnpm exec tsx scripts/qc-acc-v2-contacts.mts` | 49/49 |
| inbox | `pnpm exec tsx scripts/qc-acc-v2-inbox.mts` | 128/128 |
| permissions | `pnpm exec tsx scripts/qc-acc-v2-permissions.mts` | 160/160 |
| components | `pnpm exec tsx scripts/qc-acc-v2-components.mts` | 78/78 |
| nav | `pnpm exec tsx scripts/qc-acc-v2-nav.mts` | 326/326 |
| guard | `pnpm exec tsx scripts/qc-acc-v2-guard.mts` | 170/170 |
| dashboard | `pnpm exec tsx scripts/qc-acc-v2-dashboard.mts` | 174/174 (แดง 4 ข้อรอบแรกจากบั๊ก cleanup ข้างบน → เขียวหลังแก้) |
| home | `pnpm exec tsx scripts/qc-acc-v2-home.mts` | 87/87 |
| seed-check | `pnpm exec tsx scripts/qc-acc-v2-seed-check.mts` | 279/279 |
| schema | `pnpm exec tsx scripts/qc-acc-v2-schema.mts` | 61/61 (รวม `pnpm drift` clean อยู่ในตัว) |
| drift | `pnpm run drift` | "No difference detected." |
| CPA | `pnpm qc:account` | 107/107 |
| visual 9.1 | `pnpm exec tsx scripts/visual-acc-v2.mts 9.1 --assert` | ผ่านทั้งหมด 10 หน้า (390 ล้วน) |
| visual 1.1 (regression) | `pnpm exec tsx scripts/visual-acc-v2.mts 1.1 --assert` | ผ่านทั้งหมด |
| visual 1.5 (regression) | `pnpm exec tsx scripts/visual-acc-v2.mts 1.5 --assert` | ผ่านทั้งหมด |
| visual 3.4 (regression) | `pnpm exec tsx scripts/visual-acc-v2.mts 3.4 --assert` | ผ่านทั้งหมด |
| visual 7.2 (regression) | `pnpm exec tsx scripts/visual-acc-v2.mts 7.2 --assert` | ผ่านทั้งหมด |
| visual 6.2 (bonus — report page ที่แก้เพิ่ม) | `pnpm exec tsx scripts/visual-acc-v2.mts 6.2 --assert` | ผ่านทั้งหมด |
ไม่ได้รัน `pnpm qc:all` ตามกติกา · ไม่ commit

## สรุปตาราง audit (หน้า → ปัญหาที่เจอ → แก้แล้วหรือยัง)
| หน้า | ปัญหาที่เจอจากการ audit | แก้แล้ว? |
|---|---|---|
| หน้าเอกสาร (f14) — DocDetailPage มือถือ | ตารางรายการยังเป็นตาราง desktop เลื่อนแนวนอนในการ์ด (ค้างจาก 1.5) | ✅ เปลี่ยนเป็นลิสต์ย่อ |
| ปุ่มหลัก sticky/เต็มกว้างบนมือถือทั่วบัญชี V2 | สูง 36px ไม่ถึงเกณฑ์ touch target 40px (`.btn` กลาง) | ✅ เพิ่ม `h-11 md:h-9` เฉพาะจุด (ไม่แตะ `.btn`/`SubmitButton` กลาง) |
| AiDock orb vs DocEditorV2 sticky bar | สงสัยว่าทับปุ่มอนุมัติ (เห็นในภาพ fullPage) | ตรวจแล้ว **ไม่ใช่บั๊ก** — วัด rect จริงยืนยันมีระยะกันชน 256px (ภาพ fullPage วาด sticky ผิดตำแหน่งเป็น artifact ที่รู้อยู่แล้ว) |
| รายงาน (งบทดลอง/ภ.พ.30) บนมือถือ | มี overflow-x-auto แล้วแต่ไม่มีคอลัมน์แรก sticky ตาม §13 | ✅ แก้ trial-balance + pp30 (balance-sheet/profit-loss/cash-flow จงใจไม่แตะ — โครงแถวซับซ้อนกว่า เสี่ยง regression) |
| กล่องขาเข้ามือถือ (g20) | ถ่ายบิลกล้อง — ของเดิมมีอยู่แล้วจริง (WO 7.2) | ยืนยันด้วย E2E จริง (J3) ผ่าน — ไม่ต้องแก้ |
| รับชำระมือถือ (จาก modal 1.4) | ต้องเป็นแผ่นเต็มจอ — ของเดิมใช้ `SlideOver` อยู่แล้ว (เต็มจอ <sm) | ยืนยันด้วย E2E จริง (J2) ผ่าน — ไม่ต้องแก้ |
| อนุมัติ PO เกินเพดานจากมือถือ | ต้องปฏิเสธพร้อมข้อความไทย + OWNER อนุมัติได้ | ยืนยันด้วย E2E จริง (J4) ผ่าน — logic เดิม (8.3) ทำงานถูกแล้ว เพิ่มแค่ `data-testid="doc-err"` ให้ทดสอบจับได้ |
| bottom sheet L1/L2 (f12/g18) | — | ตรวจซ้ำ (regression) ผ่าน ไม่ต้องแก้ (ของเดิมจาก 0.4 ถูกอยู่แล้ว) |
| โปรไฟล์ผู้ติดต่อ 360° มือถือ (g19) | — | ตรวจซ้ำ (regression + ภาพใหม่) ผ่าน ไม่ต้องแก้ (ของเดิมจาก 3.4 ถูกอยู่แล้ว) |
| GroupChildrenTable (BN/CP มือถือ) | ตาราง desktop เลื่อนในการ์ด เหมือนที่เจอใน DetailTab | ไม่แตะ — ไม่ใช่ f14 หลัก + ไม่ใช่ 1 ใน 4 งาน E2E บังคับ + มี overflow-x-auto ใช้ได้อยู่แล้ว (จดค้างให้ 9.3/9.4) |

## รอบ 2 (Fable ตีกลับ) — g17 ไม่ตรงแบบ: ฟอร์มมือถือยังเป็นอินพุตเดสก์ท็อปเรียงต่อกัน ไม่ใช่แถวสรุป+แผ่นเต็มจอ
Fable เทียบภาพจริง `invoice-form-mobile-mobile.png` กับ `g17-invoice-form.png` เอง แล้วชี้ 5 จุดที่ไม่ตรง:
1. ส่วนหัวเอกสาร: ต้องเป็นแถวสรุป (ผู้ติดต่อ/วันที่ออก/ครบกำหนด/ประเภทราคา/แท็ก) แตะแล้วเปิดแผ่นเต็มจอ ไม่ใช่ inline input
2. รายการ: ต้องเป็นการ์ดอ่านอย่างเดียว (ชื่อ·qty×ราคา·บัญชี·chip VAT/WHT·ยอด) + ⋯→แผ่นแก้ไข ไม่ใช่กรอกในการ์ดตรง ๆ
3. เงินมัดจำ/หมายเหตุ/แนบไฟล์: ต้องยุบไว้ก่อน (โชว์จำนวน/ยอดใน title) ไม่ใช่เปิดเต็มเหมือนเดสก์ท็อป
4. แถบล่าง: [บันทึกร่าง][อนุมัติกว้าง][⋯] เท่านั้น ไม่ใช่แถบเดสก์ท็อป [ยกเลิก][บันทึกร่าง▾][อนุมัติ▾]
5. แถบบนสุด: ‹ ย้อนกลับ + ชื่อชนิดเอกสาร + ⋯

### แก้ (คงตรรกะ/state/server action เดิมทั้งหมด — เพิ่มแค่ mobile-only render path)
- `DocEditorV2.tsx`: เพิ่ม `MobileFieldRow` (แถวสรุป+ chevron) · `mobileFieldSheet` state (contact/issueDate/dueDate/
  priceMode/tags/more) แต่ละอันเปิด `Modal sheetOnMobile` ที่มีอินพุตตัวเดิม (ContactPicker/DateInput/select) เพียง
  ย้ายที่วาง — ไม่สร้าง state ซ้อน · เดสก์ท็อปเดิมห่อด้วย `hidden md:block` ไม่แตะโค้ดข้างในเลย
- `DocLineTable.tsx`: การ์ดมือถือเปลี่ยนเป็นอ่านอย่างเดียว + `RowActions` (⋯) 3 ตัวเลือก (แก้ไข/ทำซ้ำ/ลบ) · "แก้ไข"
  เปิด `Modal sheetOnMobile` เดียว (`editingKey` state) ที่มีอินพุตเดิมทั้งหมด (ProductPicker/QtyInput/MoneyInput/
  บัญชี/VAT/WHT) · เพิ่ม prop `onDuplicate`/`autoEditKey`/`onAutoEditConsumed` (ต้องเติม `onDuplicate` ให้
  `RecurringRuleForm.tsx` ด้วยเพราะใช้ `DocLineTable` ร่วมกัน — ไม่กระทบ logic เดิมของมัน)
- `SectionCard.tsx`: เพิ่ม prop `mobileDefaultOpen` (optional) — ใช้ media query ตรวจจอจริงตอน mount แล้วตั้ง
  `open` state (ไม่ remount children กัน DepositSection/DocAttachments ที่มี state ภายในพัง) · ไม่ส่ง = พฤติกรรม
  เดิม 100% (ผู้เรียกอื่นอย่าง GroupDocEditor ไม่กระทบ) · ใช้ `mobileDefaultOpen={false}` กับเงินมัดจำ/แนบไฟล์/
  หมายเหตุ (รวม 2 ช่องเป็นการ์ดเดียว "หมายเหตุ" บนมือถือ ตรง g17 เป๊ะ — เดสก์ท็อปยังแยก 2 การ์ดเหมือนเดิม)
- แถบล่างมือถือใหม่ทั้งหมด (`editor-actions-m`, `md:hidden`) — บันทึกร่าง/อนุมัติกดตรงไม่มีเมนู · "⋯" (RowActions)
  รวมตัวเลือกที่เหลือ (บันทึกและสร้างใหม่/อนุมัติและพิมพ์/ส่งอีเมล/รับชำระ/ยกเลิก) · แถบเดสก์ท็อปเดิมห่อ `hidden md:block`
- แถบบนสุดมือถือใหม่ (`‹` + docLabel + RowActions ⋯ ชุดเดียวกับแถบล่าง)
- `ConfirmDialog.tsx`/`DocDetailPage.tsx`: ไม่แตะเพิ่มในรอบนี้ (แก้รอบ 1 แล้ว)

### บั๊กจริงที่เจอระหว่างแก้รอบ 2 (สำคัญ — บทเรียนให้ WO อื่น)
6. **screenshot fullPage artifact ทำให้เข้าใจผิดว่าแถวหาย** — ถ่ายภาพแรกหลังแก้ดูเหมือนแถว "ครบกำหนด/ประเภทราคา/
   แท็ก/เพิ่มเติม" หายไปเฉย ๆ (ถูกแถบ sticky ใหม่ทับ) — พิสูจน์ด้วย `getBoundingClientRect()` จริงว่าทุกแถวอยู่ถูกที่
   (แค่อยู่ใต้ขอบจอแรก) → แก้ด้วย `unstickForShot` (มีอยู่แล้วในฮาร์เนส) ไม่ใช่แก้โค้ดแอป — บทเรียนซ้ำกับที่เจอใน WO 9.1
   รอบ 1 (orb overlap) และ WO 1.5 — **ทุกครั้งที่ภาพดูเหมือนมีอะไรหาย ให้วัด rect จริงก่อนตัดสินใจแก้โค้ดแอป**
7. `qc-acc-v2-mobile.mts` J1 flow ต้องเขียนใหม่ทั้งหมดให้เดินผ่านแผ่นใหม่ (แตะแถว→รอแผ่นเปิด→กรอกใน `-m-input`/
   `line-m-edit-*`→กด "เสร็จ"/เลือกแล้วแผ่นปิดเอง) — ปุ่มอนุมัติมือถือเปลี่ยนชื่อ `btn-approve-m` (กดตรง ไม่มีเมนู)
8. ข้อสอบเก่าของ WO 1.3 (`visual-acc-v2.mts`) มี 2 จุดที่ล็อกกับพฤติกรรมเดสก์ท็อปเดิม (`editor-actions` visible บน
   ทุกจอ · เปิดเมนู `approve-menu` บนมือถือ) — ต้องอัปเดตให้ตรงกับดีไซน์ใหม่ (ไม่ใช่ revert): เพิ่ม probe
   `stickyBarVisibleM`/`approveBtnMVisible` · เช็ค `editor-actions-m` แทนบนมือถือ · หน้า `invoice-form-menu` (เปิด
   เมนูอนุมัติ 4 ตัวเลือก) ทำ `onlyDevice: "desktop"` เพราะมือถือไม่มีเมนูนั้นอีกแล้วโดยดีไซน์

### ผลด่านรอบ 2 (จริงทั้งหมด — server build ล่าสุดหลังแก้ครบ)
| ด่าน | คำสั่ง | ผล |
|---|---|---|
| typecheck | `bash scripts/with-gate-lock.sh pnpm typecheck` | สะอาด (รันซ้ำหลายรอบระหว่างแก้ ครั้งสุดท้ายสะอาด) |
| qc-acc-v2-mobile (4 งาน E2E ผ่านแผ่นใหม่) | `pnpm exec tsx scripts/qc-acc-v2-mobile.mts` | **55/55** |
| editor | `pnpm exec tsx scripts/qc-acc-v2-editor.mts` | 200/200 |
| recurring (RecurringRuleForm ใช้ DocLineTable ร่วม) | `pnpm exec tsx scripts/qc-acc-v2-recurring.mts` | 163/163 |
| visual 1.3 --assert (regression ฟอร์ม g1/g17) | `pnpm exec tsx scripts/visual-acc-v2.mts 1.3 --assert` | ผ่านทั้งหมด (แก้ 2 จุดในข้อสอบเก่าให้ตรงดีไซน์ใหม่) |
| visual 9.1 --assert (ภาพ g17 ใหม่ + อีก 9 หน้า) | `pnpm exec tsx scripts/visual-acc-v2.mts 9.1 --assert` | ผ่านทั้งหมด 10/10 |
| fitness (มี .env.qc) | `pnpm fitness` | 17/17 |
| fitness (ไม่มี env) | `env -i PATH="$PATH" HOME="$HOME" pnpm fitness` | 17/17 |
เซิร์ฟเวอร์ปิดก่อนรัน typecheck/fitness ตามกติกา · ไม่ commit

### PNG หลัก (เทียบ g17 ได้ตรง ๆ)
`.qc-shots/acc-v2/9.1/invoice-form-mobile-mobile.png` — ตรงแบบ g17 ครบ (แถวสรุป+chevron · การ์ดรายการอ่านอย่างเดียว+⋯
· เงินมัดจำ/หมายเหตุ/แนบไฟล์ยุบ · แถบล่าง 2 ปุ่ม+⋯ · แถบบน ‹ + ชื่อ + ⋯) — Fable ควรดูภาพนี้คู่ g17 อีกรอบยืนยันปิดงาน

## ค้าง/ข้อควรรู้ส่งต่อ
- balance-sheet/profit-loss/cash-flow report ยังไม่มี sticky first column (ตัดสินใจ scope เพราะโครงแถวซับซ้อน)
- GroupChildrenTable (หน้าเอกสารกลุ่ม BN/CP) ยังเป็นตาราง desktop เลื่อนในการ์ดบนมือถือ ไม่ใช่ลิสต์ย่อแบบ f14
- แอปมือถือ (Expo WebView `apps/mobile/app/(app)/index.tsx`): **ไม่ต้องแก้อะไรเพิ่ม** — `onShouldStartLoadWithRequest`
  คืน `true` (โหลดตามปกติ) สำหรับทุก path ที่ไม่ตรงเงื่อนไขพิเศษ (`/onboarding`,`/app`,`/login`,`/`) ⇒ เส้นทาง
  `/app/sys/[id]/account/**` ทั้งหมดเปิดได้ใน WebView โดยไม่ต้องแก้ allowlist ใด ๆ · ยืนยันจากโค้ดจริง ไม่ได้เดา ·
  `AiDock` ไม่เรนเดอร์เลยเมื่อเปิดจากแอป (`!inApp &&`) — แอปมีปุ่ม AI native ของตัวเอง ไม่ชนกัน
- g17's ปุ่มอนุมัติในโค้ดปัจจุบันเป็นปุ่มเดียวกดแล้วกาง dropdown (▾) ไม่ใช่ปุ่มอนุมัติ+ปุ่ม "⋯" แยก 2 ปุ่มตามภาพ g17
  เป๊ะ — เป็นการตัดสินใจที่ Fable เคยยืนยันภาพจริงผ่านมาแล้วใน WO 1.3 (functionally เทียบเท่า) ไม่ได้แก้ใน WO นี้
  (นอกขอบเขต 9.1 ซึ่งเน้นให้ "ทำงานได้จริง" ไม่ใช่ปรับ pixel เพิ่ม) — จดไว้เผื่อ 9.4 (ความง่าย) อยากทำให้ตรง 100%
