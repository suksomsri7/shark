# WO 9.4 — ความง่าย (recovery log)

Start: 2026-09-04. Branch session/accounting @ 496ccee (WO 9.3 landed, tree clean).

## Plan
A. QuickCreate (⌘K / + ปุ่ม)
B. Soft-undo 5 min (AccountUndoToken)
C. Tooltips (HelpTip + help-texts.ts)
D. Empty states audit
E. Human-language errors
F. /account/help guide page
G. Eye-check 20 pages visual QC
H. Tests: qc-acc-v2-simplicity.mts + reruns

## Log
- [start] created this file, verified git clean at 496ccee.

## ขั้น 1 — migration AccountUndoToken (soft-undo)
- เพิ่ม model `AccountUndoToken` ใน prisma/schema/account.prisma (id/tenantId/systemId/userId/kind/payload Json/createdAt/expiresAt/usedAt · index (tenantId,systemId,expiresAt) + (userId,expiresAt))
- `prisma migrate dev` ต้องการ reset DB (checksum drift ของ migration เก่า 4 ตัวที่เคยถูกแก้หลัง apply — ไม่ใช่ปัญหาจากงานนี้) → หลีกเลี่ยง reset โดยสร้าง migration file เองจาก `prisma migrate diff --from-config-datasource --to-schema prisma/schema --script` แล้ว apply ด้วย `prisma migrate deploy` (ไม่เช็ค checksum เข้มเท่า dev) → สำเร็จ สะอาด additive ล้วน
- migration: `prisma/migrations/20260916000000_account_v2_undo_token/migration.sql`
- `prisma migrate status` หลัง deploy: "Database schema is up to date!" (ไม่มี pending/drift)
- ยังไม่ลง prod (ตามกติกา ต้องให้เจ้าของ/ผู้คุม run สั่งเอง)

## ขั้น 2 — C: tooltip (?) + D: empty state audit

### C (HelpTip)
- `src/lib/modules/account/help-texts.ts` — 46 คำ (≥40) ผ่าน BLUEPRINT
- `src/components/account-v2/HelpTip.tsx` — ใช้ PortalMenu (7.1) เปิดด้วย hover/focus/click ปิดเอง
- `AccountIcon.tsx` เพิ่มไอคอน `help` (วงกลม "?" เส้นบาง แยกจาก `info` เดิม)
- `DocTable.tsx` / `DocTableInteractive.tsx` เพิ่ม `help?: string` ต่อคอลัมน์ → เรนเดอร์ HelpTip ท้ายหัวคอลัมน์
- wired จริงที่: (ต่อในขั้นถัดไปตอนแตะ journal/reports/coa/wht/contacts — ดูรายการใน "ขั้น 6")

### D (empty state audit) — ตาราง

| หน้า/ส่วน | ก่อน | หลัง |
|---|---|---|
| DocListPage (ทุกชนิดเอกสาร ~20 หน้า) | EmptyState ไม่มีปุ่ม | เพิ่ม `emptyAction` อัตโนมัติจาก createLabel/createHref ที่มีอยู่แล้ว |
| ContactsPanel (ผู้ติดต่อ) | ไม่มีปุ่ม | ปุ่ม "+ เพิ่มผู้ติดต่อ" → createContactHref |
| ProductsPanel | มีปุ่มอยู่แล้ว | ไม่แก้ |
| FinancePanel / PettyCashPanel | มีปุ่มอยู่แล้ว | ไม่แก้ |
| ReconcilePanel | มีปุ่มอยู่แล้ว | ไม่แก้ |
| assets/page.tsx (ทะเบียนสินทรัพย์) | ไม่มีปุ่ม | ปุ่ม "+ ขึ้นทะเบียนสินทรัพย์" |
| documents/page.tsx (คลังเอกสาร) | ไม่มีปุ่ม | ปุ่ม "อัปโหลดไฟล์" |
| wht/page.tsx | ข้อความสั้นไม่สอนอะไร | สอน + ปุ่ม "ไปที่หน้าการเงิน" |
| cheque/page.tsx | ข้อความชี้ "เพิ่มด้านล่าง" ไม่มีปุ่ม | ปุ่ม "+ บันทึกเช็ค" (#new) |
| units/page.tsx | ปุ่มจริงอยู่ที่ header (ไม่อยู่ในกล่อง empty) | ปล่อยไว้ (ปุ่มเป็น form submit ไม่ใช่ href — EmptyState รองรับเฉพาะ Link) — ค้าง |
| ledger/page.tsx | "เลือกบัญชีเพื่อดูการเคลื่อนไหว" | ไม่ใช่ empty-state จริง (เป็นคำแนะนำก่อนเลือก) — ไม่แก้ |
| JournalTable / FinanceOverviewPanel (บางบล็อกย่อย) / ChartPanel (ค้นหา) / ContactMergePanel / ConnectionsPanel / PermissionsPanel | มีข้อความสอนอยู่แล้วบางจุด ไม่มีปุ่ม | ยังไม่แก้ (มีข้อความสอนแล้ว บางจุดไม่มี "งาน" ให้ทำจริง เช่น ผลค้นหาว่าง/ไม่มีคู่ซ้ำ) — ค้าง ถ้าต้องการปุ่มครบทุกจุด |
| dev-components/page.tsx | ไม่มีปุ่ม | ข้าม (หน้า QC ภายใน ไม่ใช่หน้าใช้งานจริง) |

## ขั้น 3 — E: human-language errors

สำรวจจริง: `throw new Error("…")` (169 จุด) + `{ok:false, reason:"…"}` (651 จุด อ้างอิง "reason:") ใน
src/lib/modules/account/**. ผลสำรวจ: ข้อความที่ตั้งใจเขียนเอง **เกือบทั้งหมดเป็นภาษาไทยล้วนอยู่แล้ว** (งาน WO
9.2/9.3 ก่อนหน้าทำไว้ดี) — grep หาศัพท์ invalid/failed/error:/undefined/null/P2002/enum ดิบ ในสตริงตรง ๆ
เจอของจริงแค่: `TEST_INJECTED_FAILURE`×4 (contact-merge.ts, gate ด้วย input.failAfter เฉพาะ QC ไม่มีทางถึงผู้ใช้จริง
— ไม่แก้) และ `RACE` (reconcile.ts, catch แปลงเป็นไทยก่อนถึงผู้ใช้แล้ว — ไม่แก้)

### พบจริง 4 กลุ่มที่ต้องแก้
1. **raw enum เข้าถึง UI จริง**: `PAY_CHANNEL_LABEL[p.channel as keyof …] ?? p.channel` ที่ DocDetailPage/
   PaymentPanel/GroupPaymentPanel — ถ้าไม่พบคีย์ (enum โตในอนาคต) จะโชว์ enum ดิบ (เช่น "PROMPTPAY") ตรง ๆ
   เงียบ ๆ (compile ผ่านปกติเพราะ `PAY_CHANNEL_LABEL` เป็น `Record<string,string>` ข้ามโดเมน) → เพิ่ม
   `payChannelLabel()` ใน service.ts (ห่อด้วย `Record<AccountPayChannel,string>` บังคับครบที่ compile time)
   แทนที่ 3 จุดเรียกใช้
2. **`docType ${doc.docType} …`** ที่ gl.ts:463,671 — ปนคำอังกฤษ "docType" + ค่า enum ดิบลงในข้อความ (internal
   skip-reason ของ posting engine ไม่ถึง UI วันนี้ แต่เสี่ยงถ้ามีจอ debug ในอนาคต) — บันทึกไว้ใน "ค้าง" (ไม่แก้ในรอบนี้
   เพราะ gl.ts เป็นไฟล์ "ห้ามเขียนใหม่" ของ GL-Core ตามหมายเหตุหัวไฟล์ — ต้องให้เจ้าของไฟล์แก้)
3. **`e.message`/`err.message` หลุดถึงผู้ใช้แบบไม่กรอง** — 52 จุดทั่วโมดูล ใช้ pattern
   `e instanceof Error ? e.message : "fallback ไทย"` ปลอดภัยเฉพาะตอน exception เป็น `throw new Error("ไทย")`
   ที่เราโยนเอง แต่ error ที่ไม่คาดคิดจริง (Prisma ดิบ/TypeError/SDK ภายนอก) จะหลุดเป็นภาษาอังกฤษ/รหัสเทคนิคทันที
   → สร้าง `src/lib/modules/account/errors.ts`: `safeReason(err, fallback)` (ฮิวริสติก: ข้อความของเราเองเป็น
   ภาษาไทยเสมอ — ไม่มีอักษรไทยเลย = ข้อความดิบจากภายนอก ใช้ fallback แทน) + แทนที่ทั้ง 52+2 จุด (asset/cheque/
   connections-actions/editor-actions/expense/finance/import-actions/journal-v2/period-close/policy/product/
   recurring-actions/service/wht.ts + inbox-ai.ts เรียก AI ภายนอกโดยตรง)
4. **`errors.ts`**: เพิ่มคลังคงที่ `ERR.*` 25 ค่า (ข้อความซ้ำบ่อยที่สุดจากการนับจริงด้วย grep เช่น "ไม่พบเอกสาร" x29,
   "ไม่พบไฟล์" x9, "ต้องมีรายการอย่างน้อย 1 รายการ" x6) ไว้ให้ไฟล์ใหม่/แก้ใหม่อ้างแทนพิมพ์ซ้ำ — **ยังไม่ได้รื้อของเดิม
   ทั้ง 29+9+6 จุดมาอ้าง ERR.* (ข้อความเดิมถูกต้องอยู่แล้ว ความเสี่ยงจากการรื้อมากกว่าประโยชน์ในรอบนี้)** — ค้าง

### ค้าง (ให้ WO ถัดไป/เจ้าของไฟล์พิจารณา)
- gl.ts:463,671 `docType ${...}` — ไฟล์ห้ามแก้โดย subagent อื่น (เจ้าของ = GL-Core)
- `write-failed`/`sku-conflict`/`unlinked`/`linked-elsewhere` ฯลฯ ใน inventory-link.ts — เป็นรหัสสถานะภายใน
  (`SyncResult.reason`) ไม่เคยเรนเดอร์ตรงเป็นข้อความผู้ใช้วันนี้ — ปล่อยไว้ (ไม่ใช่ user-facing) แต่ทำเครื่องหมายไว้เผื่อ
  อนาคตมีจอ debug ที่ต้องโชว์

## ขั้น 4 — A (QuickCreate ⌘K) + B (soft-undo) + F (/account/help) + C ต่อ (wire จริง)

### A — QuickCreate
- `src/lib/modules/account/quick-create-parse.ts` (pure, client-safe): `parseQuickCreateQuery()` + `QUICK_CREATE_KEYWORDS` (TH/EN/prefix ต่อ docType) + `parseAmountQueryToSatang()`
- `src/lib/modules/account/quick-create-actions.ts` ("use server"): `quickCreateRecentDocsAction` (เอกสารล่าสุด 5 ใบ — เรียกตอนเปิดแผงเท่านั้น ไม่กระทบงบ query หน้าอื่น) — ค้นหาผู้ติดต่อ fuzzy ใช้ `searchContactsAction` เดิม (WO 1.3/7.2) ไม่เขียนซ้ำ
- `src/components/account-v2/QuickCreate.tsx`: ⌘K/Ctrl+K ทำงานทุกหน้าในโมดูล (mount ที่ layout.tsx) · nav fuzzy match จาก `ACCOUNT_NAV` (nav.ts) · เอกสารล่าสุดตอน query ว่าง · keyboard nav (↑↓Enter) · มือถือ: ปุ่ม "+ สร้างเอกสาร" (DashCreateMenu, fullWidth) เปิดแผงนี้ตรง ๆ แทน dropdown เดิม · เดสก์ท็อป: ลิงก์ท้าย dropdown เดิม "หรือพิมพ์คำสั่งสร้างด่วน (⌘K)"
- เติม prefill `?contactId=&amount=` เข้าฟอร์มจริง: `DocEditorPage.tsx` รับ `prefill` prop (เฉพาะสร้างใหม่จริง ไม่ใช่แก้ร่าง/wizard ปรับปรุงหนี้ที่มี refDoc) → เติมผู้ติดต่อ + ยอดเงินบรรทัดแรก · wired 7 route: `docs/[docType]/new` (รายรับ) + `po/asset-po/purchase/asset-buy/deposit-payment/expense` (รายจ่าย 6 เส้นทาง) · CNR/DNR ได้ contactId ไปกรอง wizard step1 ให้ฟรี (ของเดิมมีอยู่แล้ว) amount ไม่มีผลกับ wizard (ไม่เกี่ยวกัน ไม่พัง)
- **ค้าง**: GOODS_ISSUE/GOODS_ISSUE_RETURN/COST_ADJUSTMENT/WHT_CERT ไม่มี route ใน EDITOR_DOC_TYPES (ใช้ฟอร์มพิเศษ GoodsIssueForm/CostAdjustForm ไม่ใช่ DocEditorV2) → ไม่รวมใน createDocTypes ของ QuickCreate (ไม่ error แค่ไม่เสนอสร้างชนิดนี้ผ่านคำสั่งพิมพ์) · BILLING_NOTE/COMBINED_PAYMENT (เอกสารกลุ่ม) ไม่รวมด้วยเหตุผลเดียวกัน (ต้องเลือกใบลูกก่อน)

### B — soft-undo
- migration `AccountUndoToken` (ดูขั้น 1)
- `src/lib/modules/account/undo-stack.ts` ("use server"): `undoAction(systemId, tokenId)` one-shot (atomic claim กันกดซ้ำ 2 แท็บ) + ผูก tenant/system/**user เดียวกันเท่านั้น + audit ทั้ง 2 ทาง (ทำ+เลิกทำ) + 11 `*WithUndoAction` (ครบตามใบสั่งงาน: contact.archive · product.archive · attachment.archive · doc.tagRemove (ฟีเจอร์ใหม่ทั้งหมด — ของเดิมไม่มี UI ลบแท็กออกจากเอกสารเลย) · merge.dismiss · attachment.notAccounting · attachment.moveFolder · attachment.docTypeHint · attachment.unlink · doc.cancelDraft ("ลบร่าง" = ใช้ voidDocument เดิม DRAFT→CANCELLED ซึ่งไม่เคยลง GL อยู่แล้ว ย้อนกลับสะอาด 100%) · pin.finance/pin.ledger)
- `src/components/account-v2/UndoToast.tsx`: provider (`useUndoToast()`) ครอบทั้ง shell (layout.tsx) — toast ล่างจอ "เลิกทำ" · รับ token ผ่าน `show()` ตรง (client call ไม่ redirect) หรือ `?undo=<token>` (action แบบฟอร์มที่ยัง redirect อยู่) → ล้างออกจาก URL ทันที
- **ต่อสายจริงจนเห็นผล (มี toast จริงในแอป) 2 จุด**: ① ปักหมุดบัญชีที่ติดตาม (การเงิน+ผังบัญชี — `DashPinModal` ผ่าน `ui.tsx`×2 + `FinanceOverviewPanel.tsx`) ② แยกไฟล์แนบออกจากเอกสาร (`AttachmentRowMenu.tsx` หน้าคลังเอกสาร)
- **ค้าง (backend พร้อม ยังไม่ต่อ UI)**: เก็บถาวรผู้ติดต่อ/สินค้า/ไฟล์แนบ (ปุ่มเดิมยังเรียก action เดิมไม่มี undo) · ลบแท็กเอกสาร (ไม่มี UI เดิมเลยด้วย) · ข้ามคู่ซ้ำ (ContactMergePanel ยังเรียก action เดิม) · ทำเครื่องหมายไม่ใช่เอกสารบัญชี/ย้ายโฟลเดอร์/เปลี่ยนประเภท (AttachmentModals ยังเรียก action เดิม) · ลบร่าง (ยังไม่มีปุ่ม "ลบร่าง" ในหน้าเอกสารเลย — เป็นฟีเจอร์ใหม่ที่ backend พร้อมแต่ยังไม่มีปุ่มเรียก) — ทั้งหมดทดสอบผ่าน qc-acc-v2-simplicity.mts โดยเรียกฟังก์ชันตรง (ไม่ผ่าน UI)

### F — /account/help
- `src/app/app/sys/[id]/account/help/page.tsx` — เช็กลิสต์ 5 ขั้น (ข้อมูลจริงจาก dashboard-home.ts) · 9 หมวดเมนูบรรทัดเดียว (ตรง nav.ts) · งานประจำเดือน 5 ขั้นตอน+ลิงก์จริง · FAQ 10 ข้อ (native `<details>` ไม่ต้อง JS) · ลงทะเบียนสิทธิ์ `account.doc.view` ใน guard.ts
- ลิงก์เข้าเพจ: ⌘K (แถวแรกของ QuickCreate เสมอ) + การ์ดเช็กลิสต์หน้าหลัก ("อ่านคู่มือเต็ม →")

### C ต่อ — wire HelpTip จริง
JournalTable (เดบิต/เครดิต) · ledger/page.tsx (เดบิต/เครดิต) · wht/page.tsx (ประเภทเงินได้/มูลค่าภาษี) · ContactsPanel (ยอดค้าง) — เพจ reports index มีคำอธิบายบรรทัดเดียวต่อรายงานอยู่แล้ว (ดีกว่า tooltip ซ่อน) จึงไม่เพิ่มซ้ำ — **ค้าง**: ไม่ได้ไล่ครบทุกคอลัมน์ทุกหน้า (46 คำใน glossary ใช้จริงแค่ ~7 จุด)

## ขั้น 5 — typecheck gate 1
`export DATABASE_URL/DIRECT_URL` จาก .env.qc (host ep-plain-art ยืนยันแล้ว) → `bash scripts/with-gate-lock.sh pnpm typecheck` = 0 error (แก้ 3 จุดระหว่างทาง: Prisma Json payload cast ที่ undo-stack.ts · ถอด help: ออกจาก ledger/page.tsx เพราะใช้ `DataTable`/`Col<T>` ของ `@/components/ui/DataList` คนละตัวกับ DocTable ของ account-v2 — header เป็น string ล้วน ขยายจะกระทบทั้งแอปนอกขอบเขต WO นี้)

## ขั้น 6 — H: qc-acc-v2-simplicity.mts (สร้างใหม่)
- ต้องแก้ `src/lib/core/scope.ts`: ลืมลงทะเบียน `AccountUndoToken` ใน registry (`sys()`) — query จริงจะ throw runtime ("1 model ยังไม่ได้ลงทะเบียน") ถ้าไม่เพิ่ม
- `undo-stack.ts` refactor เพิ่มเติมระหว่างเขียนเทส: แยก `consumeUndoToken(ctx, tokenId)` (ไม่มี revalidatePath — เทสเรียกตรงได้) ออกจาก `undoAction(systemId, tokenId)` (wrapper จริงที่ผ่าน `loadAccountSystem` — เรียกจาก QC script ไม่ได้เพราะต้องมี Next.js request context เหมือน `*Action` อื่นทุกตัวในระบบ) · เอ็กซ์พอร์ต `createUndoToken` (เดิม private) พร้อม `ttlMs` override ให้จำลอง "หมดอายุ" ได้ตรง ๆ (ส่งค่าติดลบ) แทนต้องรอจริง 5 นาที
- ผลรัน: **99 ผ่าน / 0 ล้ม** ครอบทุกหัวข้อ P(parser)/N(nav)/U(undo 12 ฟังก์ชัน × lifecycle+one-shot+expiry+cross-user+cross-tenant+audit)/H(help-texts)/E(empty state)/R(error+enum label)/G(/account/help)
- แก้ระหว่างทดสอบจริง: cleanup ชนกับ FK `AccountMapping→AccountLedger` (RESTRICT) → เพิ่มลบ AccountMapping ก่อน AccountLedger · grep ไม่เจอ (exit 1) เคยทำให้สคริปต์ throw ทั้งที่ "ไม่เจอ" คือผลที่ถูกต้อง → เขียน `grepSafe()` ครอบ · เจอ tenant ค้าง 2 ตัวจากรอบทดสอบที่ยังไม่ได้แก้ cleanup → ลบมือแล้ว (ยืนยัน `leftover tenants: 0`)

## ขั้น 7 — rerun ชุดเก่า (ทีละตัว ตามกติกา)
ทั้งหมดเขียวหลังแก้ทุกจุดข้างบน (host ep-plain-art ทุกครั้ง):
- list 159/159 · detail 85/85 · editor 200/200 (ยืนยัน prefill ไม่ทำ editor เดิมพัง) · contacts 49/49 ·
  products 100/100 · attachments 66/66 (ยืนยัน unlink-with-undo ไม่ทำแฟลชเดิมพัง) · inbox 128/128 ·
  contact-merge 56/56 (auto-reseed QC เรียบร้อยหลังรัน) · components 78/78 · nav 326/326 ·
  guard 174/174 (+1 จากเดิม 173 = help/page.tsx ที่เพิ่มลงทะเบียนพอดี) · security 298/298 ·
  seed-check 279/279 · schema 61/61 (รวม drift check ภายใน) · `pnpm run drift` แยก = "No difference detected" ·
  CPA 107/107 (FINDINGS 0/0/0)

## ขั้น 8 — fitness gate
รอบแรก: F5.1 แดง (raw prisma ในโมดูลไม่เพิ่ม — baseline 45 → undo-stack.ts ดัน 46 เพราะ import `{ prisma }` ตรง ๆ)
→ รีแฟกเตอร์ undo-stack.ts ทั้งไฟล์ให้ใช้ `tenantDb(ctx)` แทน raw `prisma` ทุกจุด (ตามแบบ contact-merge.ts/
doc-settings.ts) — ต้อง cast `data as Prisma.AccountUndoTokenUncheckedCreateInput` สำหรับ `.create()` (tenantDb
inject tenantId/systemId ตอน runtime แต่ TS มองไม่เห็น เป็น pattern เดียวกับที่ doc-settings.ts ใช้อยู่แล้ว)
→ `pnpm fitness` = **17/17 ผ่านหมด** ทั้งแบบปกติและ `env -i … pnpm fitness` (ไม่มี eager `.env` import)
→ รัน qc-acc-v2-simplicity (99/99) + security (298/298) ซ้ำหลังรีแฟกเตอร์ ยังเขียวเหมือนเดิม

## ขั้น 9 — G: eye-check 20 หน้า (visual-acc-v2.mts WO 9.4)
- เพิ่ม fixture 9.4 ใน visual-acc-v2.mts: 1 เอกสาร+ไฟล์แนบทิ้ง (สาธิต unlink+undo toast) + 1 ระบบบัญชีว่างเปล่า (สาธิต empty state) — ลบทิ้งใน cleanupFixture เสมอ
- เพิ่ม `data-testid={testId}-item-${i}` ให้ทุกรายการใน `RowActions.tsx` (ของเดิมไม่มี testid ต่อรายการเมนูเลย — ต้องมีเพื่อคลิก "แยกออกจากเอกสาร" แม่นยำจาก QC/สคริปต์ภาพ)
- ถ่าย 20 หน้า (เดสก์ท็อป 1440 เท่านั้น — ตาม pass criterion "eye-check" ไม่ใช่ parity มือถือ): home(⌘K เปิด) · invoice-list · doc-detail · editor · contacts(empty state จริงบนระบบว่างเปล่า) · contact-profile · products · finance · finance-overview · reconcile · wht(tooltip เปิด) · journal · coa · reports · periods · assets · documents(undo toast จริง) · inbox · settings-numbering · help
- **บั๊กที่พบ+แก้ระหว่างขั้นนี้**:
  1. tooltip WHT ไม่เปิด — เดายิง testid ผิด (`col-help-whtIncomeType` ควรเป็น `col-help-income` เพราะ DocTableInteractive สร้าง testid จาก column key ไม่ใช่ค่า help) → แก้ script แล้วเปิดถูก มีคำอธิบายจริงในภาพ
  2. breadcrumb หน้า /help ขึ้น "บัญชี › หน้าหลัก" (ผิด) เพราะ /help ไม่ได้ลงทะเบียนใน ACCOUNT_NAV แล้วตกไป fallback หน้าหลัก → เพิ่มเคสพิเศษใน AccountBreadcrumb.tsx ให้ขึ้น "บัญชี › วิธีใช้งาน"
  3. **build แดง** ตอน `next build` ครั้งแรก: `payChannelLabel` ที่ย้ายไป service.ts (ขั้น 3) ถูก DocDetailPage/PaymentPanel/GroupPaymentPanel import — 2 ตัวหลังเป็น `"use client"` ⇒ ดึง `prisma`/`pg` เข้า client bundle ทั้งก้อน (module-not-found ของ `pg` ฝั่ง browser) → ย้าย `payChannelLabel` ออกเป็นไฟล์ใหม่ client-safe ล้วน `src/lib/modules/account/pay-channel-label.ts` (import `AccountPayChannel` แบบ `type` เท่านั้น — ไม่ดึง `@prisma/client` รันไทม์) แก้ import 3 จุด + qc-acc-v2-simplicity.mts แล้ว build ผ่าน
  4. cleanup fixture เดิมพลาด: ไฟล์แนบสาธิตถูก "แยกออกจากเอกสาร" (documentId → null) ระหว่างถ่ายภาพ แต่ cleanup เดิมลบด้วย `documentId IN (ids)` เท่านั้น ⇒ **ทิ้งไฟล์ลอยไว้ในร้าน QC จริงทุกรอบที่รัน** (เจอ 2 ไฟล์ค้างจากการซ้อม 2 รอบ กระทบ qc-acc-v2-attachments T1 — นับได้ 8 ไม่ใช่ 6) → แก้ cleanup ให้ลบด้วยชื่อไฟล์ตรง ๆ ด้วย + ลบ 2 ไฟล์ค้างมือแล้ว + รัน attachments suite ยืนยันกลับ 66/66
- **พบระหว่าง eye-check แต่ไม่แก้ (นอกขอบเขต WO นี้ — บันทึกเป็นข้อสงสัยส่งต่อ)**:
  - `coa-ui.tsx` (ผังบัญชี, WO 6.1) โชว์ "ใช้ลงบัญชีอัตโนมัติ: CASH ›" — เป็น `AccountMapping.key` ดิบ (ไม่ใช่ label ไทย) ไม่มี label map เลยทั้งที่หน้า `/accounts/mapping` เองก็โชว์ค่าดิบเหมือนกัน — เป็นคนละ concept จาก 5 enum ที่ WO นี้ตรวจครบ (mapping key ไม่ใช่ Prisma enum) แต่ผิดหลักการเดียวกัน ("ภาษาคนทั่วไป") → ควรทำ WO แยกทำ label map ให้ mapping key ทั้งหมด
  - หน้า `/periods` คอลัมน์ "ทำรายการ" แคบไป ทำให้ "เช็กลิสต์" ถูกตัดกลางคำเป็น "เช็ค" + "กลิสต์" คนละบรรทัด (มีมาตั้งแต่ WO 6.2 ไม่ได้เกิดจากงานนี้) → ควรขยายคอลัมน์/ปรับ white-space
  - top nav tab bar ที่หน้า /help ยังไฮไลต์ "หน้าหลัก" ค้าง (breadcrumb แก้แล้วแต่ AccountTabBar ยังไม่ได้แก้จุดเดียวกัน) — ผลกระทบต่ำ (แค่ตัวเน้นเมนูผิด ไม่ใช่ลิงก์พัง)
- ผลรัน `visual-acc-v2.mts 9.4 --assert` รอบสุดท้าย: **ผ่านทั้งหมด 20/20 หน้า** (HTTP 200 + ข้อความที่คาดหวังครบ) · cleanup fixture สมบูรณ์ (0 ค้าง) · PNG อยู่ที่ `.qc-shots/acc-v2/9.4/*.png`

## ปิดงาน — สรุปเกท (real lines, host ep-plain-art ทุกครั้ง, ไม่มี pnpm qc:all)
- typecheck: 0 error (หลายรอบระหว่างทาง — สุดท้ายสะอาด)
- fitness ปกติ: 17/17 · fitness `env -i`: 17/17 (F5.1 raw-prisma ratchet ผ่านหลังรีแฟกเตอร์ undo-stack.ts → tenantDb)
- qc-acc-v2-simplicity.mts (ใหม่): 99/99
- rerun: list 159/159 · detail 85/85 · editor 200/200 · contacts 49/49 · products 100/100 ·
  attachments 66/66 · inbox 128/128 · contact-merge 56/56 · components 78/78 · nav 326/326 ·
  guard 174/174 (+1 = help/page.tsx) · security 298/298 · seed-check 279/279 · schema 61/61 ·
  `pnpm run drift` = "No difference detected" · CPA 107/107 (0/0/0)
- visual-acc-v2.mts 9.4 --assert: ผ่านทั้งหมด 20/20 หน้า
- ไม่ได้ commit (ตามกติกา) — ไฟล์ oracle (`acc-v2-expected.json` + 2 kbank fixture) ถูกเขียนทับโดย contact-merge
  suite เอง (พฤติกรรมปกติของชุดนั้น) ยังค้างอยู่ใน working tree เช่นเดียวกับไฟล์งานทั้งหมด

## รอบรีวิว 2 (Fable ตีกลับ) — ปิดช่องโหว่ soft-undo เดินสาย UI ไม่ครบ + 3 ข้อเล็ก

**สาเหตุที่ตีกลับ**: backend undo มีครบ แต่หน้าจอเรียกใช้จริงแค่ 2/11 kind (ปักหมุด + แยกไฟล์แนบ) ที่เหลืออีก 9
ยังเรียก action เดิมที่ไม่มี undo (archive contact/product/attachment, remove tag ยังไม่มี UI เลย, dismiss merge
candidate, mark not-accounting, move folder, change doc-type hint, cancel draft)

**เดินสาย UI ครบ 11/11 kind:**
- `archiveContactWithUndoAction` — `ContactProfilePanel.tsx` (client) เรียกตรง + `contacts-ui.tsx` (server list
  row) ผ่าน `*FormAction` ใหม่ redirect กลับพร้อม `?undo=`
- `archiveProductWithUndoAction` — `products/page.tsx` (server) ผ่าน `archiveProductFormAction` ใหม่
- `archiveAttachmentWithUndoAction` — `AttachmentRowMenu.tsx` (client)
- `removeDocTagWithUndoAction` — **ของใหม่ทั้งชุด**: ไม่มี UI แท็กบนหน้าเอกสารมาก่อนเลย → เพิ่ม `tags` เข้า
  `DocDetailData` (ฟรี ไม่มี query เพิ่ม เพราะ `getExpenseDoc` ใช้ Prisma `include` คืน scalar ครบอยู่แล้ว) +
  สร้าง `DocTagsBlock.tsx` (ชิปแท็ก + ปุ่ม "×") วางไว้เหนือ `ActionRow` ในหน้าเอกสาร
- `dismissMergeCandidateWithUndoAction` — `ContactMergePanel.tsx` (client, หน้า 3.4)
- `markNotAccountingWithUndoAction` — `InboxCard.tsx` (client, 7.1/7.2)
- `moveAttachmentWithUndoAction` / `setDocTypeHintWithUndoAction` — `AttachmentModals.tsx` (client, 7.1)
- `cancelDraftWithUndoAction` — `DocDetailPage.tsx` danger menu ปุ่ม "ยกเลิกร่าง" (draft เท่านั้น) ผ่าน
  `cancelDraftFormAction` ใหม่ + แก้บั๊กแฝงที่เจอระหว่างทาง: ฟังก์ชันเดิม hardcode path แบบรายรับ
  (`docs/${docType}`) ใช้กับฝั่งรายจ่ายไม่ได้ → เปลี่ยนไปใช้ `editorDetailPath()` กลาง

**รูปแบบการเดินสาย (สำคัญ — RSC boundary)**: หน้า/คอมโพเนนต์ที่เป็น client อยู่แล้ว → closure ตรง +
`useUndoToast().show()` + `router.refresh()`. หน้า/คอมโพเนนต์ที่เป็น server component (ใช้ `<form action>` ผ่าน
`RowActions`/`ConfirmDialog`) → ต้องมี `*FormAction(formData)` แบบ `"use server"` ที่ห่อ `*WithUndoAction` แล้ว
`redirect()` ต่อพร้อม `?undo=<token>` ต่อท้าย URL (เพราะฟังก์ชันธรรมดาในไฟล์ server component ส่งเป็น prop เข้า
client component ไม่ได้ ต้องเป็น action reference "use server" เท่านั้น) — แก้ `UndoToast.tsx` ให้ฟัง
`useSearchParams()`/`usePathname()` แทนเช็กแค่ตอน mount เพราะ redirect ในเลย์เอาต์เดียวกันไม่ remount provider

**QC ใหม่**: เพิ่มบล็อก "U-wired" ใน `qc-acc-v2-simplicity.mts` — grep หาจุดเรียกจริงของแต่ละ 11 kind ในโฟลเดอร์
UI (`components/account-v2`, `app/.../account`, `lib/modules/account/*.tsx`) กันเผลอเหลือ backend เฉย ๆ อีก —
ต้อง exclude self-match ใน `undo-stack.ts` เอง (เจอปม `pin.ledger` อยู่ใน `lib/modules/account/ui.tsx` ซึ่งไม่ได้
อยู่ใน 2 โฟลเดอร์แรก ต้องเพิ่มที่ค้นหาที่ 3) — ผลสุดท้าย **11/11 kind ผ่าน**

**ภาพเพิ่มเติม**: เพิ่ม PageSpec `expense-cancel-draft-toast` ใน `visual-acc-v2.mts` (9.4) — คลิก "⋯" → "ยกเลิกร่าง"
→ ยืนยัน บนเอกสารร่างสาธิต (fx94) → เห็น toast "ทำรายการแล้ว · เลิกทำ" มุมล่างจอจริงในภาพ

**3 ข้อเล็กที่แก้ตาม**:
1. `AccountMapping.key` โชว์ดิบ → เพิ่ม `MAPPING_KEY_LABEL` + `mappingKeyLabel()` ใน `coa.ts` (30 คีย์ + เคส
   `DOC:<docType>` ใช้ `docTypeLabel` เดิม) ใช้ทั้งที่ `/accounts/mapping/page.tsx` (แทน span `font-mono` เดิม)
   และ `coa-ui.tsx` (รายละเอียดบัญชี "ใช้ลงบัญชีอัตโนมัติ") — ตรวจ HTML จริงผ่าน curl+session มือ เจอ
   "ลูกหนี้การค้า/เจ้าหนี้การค้า/เงินสด (บัญชีเริ่มต้น)/บัญชีพักรายการ (9999)" ครบ ไม่มี raw key เหลือ (`font-mono
   text-xs` เดิม = 0 จุด) — ถ่ายภาพจริงที่ `.qc-shots/acc-v2/9.4/accounts-mapping-desktop.png`
2. `/periods` คอลัมน์ "ทำรายการ" ตัดคำ "เช็กลิสต์" กลางคำ → เพิ่ม `whitespace-nowrap` ที่ th/td/ปุ่มที่เกี่ยวข้อง —
   ยืนยันในภาพ `periods-desktop.png` ไม่ตัดคำแล้ว
3. `/help` tab-bar ไม่ไฮไลต์ (ของเดิมไฮไลต์ผิดเป็น "หน้าหลัก") → root cause จริงคือบั๊กทั่วไปใน
   `findActiveNav()`: กลุ่ม "หน้าหลัก" มี `href === base` ทำให้ fallback loop จับคู่ prefix กับ**ทุกหน้าที่ยังไม่
   ลงทะเบียน**ว่าเป็นหน้าหลักเสมอ (ไม่ใช่แค่ /help) → แก้เป็น skip กลุ่มที่ `href === base` ในลูป fallback ผลคือ
   /help ไม่ไฮไลต์แท็บใดเลย (ถูกต้องแล้ว เพราะ /help เป็นคู่มือรวม 9 หมวด ไม่ได้สังกัดแท็บใดแท็บหนึ่งโดยเฉพาะ —
   ต่างจากที่คิดตอนแรกว่าต้องไฮไลต์ "บัญชี") ยืนยันด้วยภาพเทียบ `help-desktop.png` (ไม่มีเส้นน้ำเงินใต้แท็บใด) กับ
   `coa-desktop.png` (มีเส้นน้ำเงินใต้ "บัญชี" ตามปกติ) — breadcrumb "บัญชี › วิธีใช้งาน" ยังถูกต้องเหมือนเดิม

## ปิดงานรอบ 2 — สรุปเกทที่ Fable สั่งรีรัน (real lines, host ep-plain-art ทุกครั้ง, ไม่มี pnpm qc:all)
- typecheck (`with-gate-lock.sh pnpm typecheck`): 0 error
- fitness `env -i PATH="$PATH" HOME="$HOME" pnpm fitness`: **17/17** (CRITICAL 0 · MAJOR 0 · MINOR 0)
- qc-acc-v2-simplicity.mts: **112/112** (รวม U-wired block ใหม่ 11/11 kind)
- qc-acc-v2-contacts.mts: **49/49**
- qc-acc-v2-products.mts: **100/100**
- qc-acc-v2-attachments.mts: **66/66**
- qc-acc-v2-inbox.mts: **128/128**
- qc-acc-v2-contact-merge.mts: **56/56** (auto-reseed QC data หลังรันตามปกติ)
- qc-acc-v2-detail.mts: **85/85**
- qc-acc-v2-coa.mts: **105/105**
- qc-acc-v2-period-assets.mts: **121/121**
- qc-acc-v2-components.mts: **78/78**
- qc-acc-v2-guard.mts: **174/174**
- visual-acc-v2.mts 9.4 --assert: **ผ่านทั้งหมด 21/21 หน้า** (เพิ่ม `expense-cancel-draft-toast` จากเดิม 20) —
  PNG ที่ `.qc-shots/acc-v2/9.4/*.png` (ใหม่: `expense-cancel-draft-toast-desktop.png`,
  `accounts-mapping-desktop.png`) · cleanup fixture สมบูรณ์ (0 ค้าง)
- ยังไม่ commit (ตามกติกา) — รอ Fable ตรวจรอบ 2
