# WO 5.3 — กระทบยอดธนาคาร (bank reconciliation)

สถานะ: DONE (รอ commit) · เริ่ม 2026-09-04 · จบ 2026-09-04
Branch: session/accounting · ฐาน HEAD 85de59d (5.2)

## บันทึกกู้คืน (recovery log — อัปเดตทุกขั้น)
- [x] ขั้น 0: สร้างไฟล์โน้ต
- [x] ขั้น 1: อ่าน RUN ✅ BLUEPRINT §1–2 + แถว 5.3 ✅ SPEC §10.2 §14 ✅
- [x] ขั้น 2: ด่าน parity — เปิด g10 ✅ checklist ด้านล่าง
- [x] ขั้น 3: สำรวจโค้ดเดิม ✅
- [x] ขั้น 4: A schema + migration ✅ (deploy QC แล้ว)
- [x] ขั้น 5: B CSV parsers ✅
- [x] ขั้น 6: C matching engine ✅
- [x] ขั้น 7: D UI ✅ (typecheck 0)
- [x] ขั้น 8: E seed/expected/tests ✅ (reconcile 109 · seed-check 97)
- [x] ขั้น 9: F visual ✅ (ผ่านทั้งหมด · ตีกลับตัวเอง 2 รอบเรื่อง parity ปุ่ม/มือถือ)
- [x] ขั้น 10: G ด่านทั้งหมด ✅ (ตัวเลขจริงด้านล่าง)
- [x] ขั้น 1: อ่าน RUN ✅ BLUEPRINT §1–2 + แถว 5.3 ✅ SPEC §10.2 §14 ✅

## ขั้น 2 — checklist parity g10 (ไล่จากภาพจริง `docs/design/account-v2/g10-bank-reconcile.png` ก่อนเขียนโค้ด)

### หัวหน้า
1. Breadcrumb "บัญชี › การเงิน › กระทบยอดธนาคาร" · h1 "กระทบยอดธนาคาร"
2. มุมขวาหัว 3 ชิ้นเรียงซ้าย→ขวา: select `🏛 ช่องทาง: กสิกรไทย ออมทรัพย์ ⌄` · select `📅 เดือน: ก.ย. 2026 ⌄` · ปุ่มขาวมีขอบ `⤓ นำเข้ารายการเดินบัญชี (CSV)`
3. แถบแท็บย่อยการเงิน (FinanceSubTabsBar จาก 5.2) — g10 ไม่ได้วาดแถบนี้ (ภาพตัดมาเฉพาะหน้า) แต่หน้าอื่นในเมนูการเงินมีทุกหน้า → คงไว้ (จดเป็น deviation ตั้งใจ)

### 4 ไทล์ KPI (แถวเดียว 4 ช่อง)
4. `ยอดตามรายการเดินบัญชี (statement)` · ฿908,150.00 · subtitle `กสิกรไทย · ก.ย. 2026`
5. `ยอดในระบบ` · ฿912,300.00 · subtitle `บัญชีแยกประเภท 111201` (= รหัส GL ของช่องทาง)
6. `ส่วนต่าง` · **ตัวเลขแดง** ฿4,150.00 · subtitle `ยังไม่เท่ากัน · ต้องจับคู่เพิ่ม` (เมื่อ 0 → เขียว/ปกติ + `ตรงกันแล้ว`)
7. `จับคู่แล้ว` · `38/41` · subtitle `รายการทั้งหมดในงวด`

### การ์ด "จับคู่รายการ"
8. หัวการ์ดซ้าย `จับคู่รายการ` · ขวา meta `แสดง n จาก N รายการ · เรียงตามวันที่ล่าสุด`
9. คอลัมน์ซ้าย หัว `รายการจากธนาคาร (statement)` · ตาราง: วันที่ · รายละเอียด · เข้า/ออก · สถานะ
10. คอลัมน์ขวา หัว `รายการในระบบ` · ตาราง: เอกสาร (ลิงก์น้ำเงิน) · รายละเอียด · จำนวน · สถานะ
11. คอลัมน์กลาง (แคบ): ปุ่มดำ `⇄ จับคู่` · กล่องมีขอบ `สร้างรายการจากแถวนี้ (ค่าธรรมเนียม/ดอกเบี้ย)` · ข้อความ `ข้าม`
12. chip สถานะ 3 แบบ: `จับคู่แล้ว` (เทาขอบ) · `แนะนำจับคู่` (น้ำเงินขอบ+ตัวน้ำเงิน) · `รอจับคู่` (เทาจาง)
13. แถวที่แนะนำ = ไฮไลต์ **ทั้ง 2 ฝั่ง** (พื้นฟ้าอ่อน + เส้นน้ำเงินซ้าย) · ฝั่งขวามีบรรทัดใบ้สีน้ำเงิน `ยอดและวันที่ตรงกัน ±1 วัน`
14. วันที่ฝั่งซ้ายรูปแบบ `3 ก.ย.` (ไม่มีปี) · จำนวนเงิน `+฿38,400.00` (เข้า=ดำ) / `−฿31,600.00` (ออก=แดง)
15. ฝั่งขวารายละเอียดอาจ 2 บรรทัด (`รับชำระ IV-202609-0038 · 25 ก.ย.`)

### ท้ายหน้า (footer bar)
16. ซ้าย: `เหลือรายการรอจับคู่ 3 รายการ (ค่าธรรมเนียม/ดอกเบี้ย/โอนเข้าไม่ระบุที่มา)`
17. กลาง: tooltip ฟองดำ `ส่วนต่างต้องเป็น 0 ก่อนยืนยัน` (ชี้ปุ่ม)
18. ขวา: ปุ่ม `✓ ยืนยันกระทบยอดเดือนนี้` — เทา/disabled เมื่อส่วนต่าง ≠ 0 · ดำ/enabled เมื่อ = 0

### เพิ่มจาก SPEC §10.2 (ไม่อยู่ในภาพ g10 แต่สเปคสั่ง)
19. รายการ "รายการที่กระทบยอดแล้ว" ต่อช่องทาง/เดือน (section ใต้การ์ดจับคู่)
20. modal นำเข้า CSV: เลือกรูปแบบธนาคาร · ไฟล์ · preview 20 แถว + error ต่อแถว · ปุ่มนำเข้า
21. มือถือ 390: ซ้อนแนวตั้ง — ลิสต์ statement ก่อน · แตะแถว → bottom sheet แสดงรายการระบบที่เข้าคู่ได้

## ขั้น 3 — สำรวจโค้ดเดิม (สรุปสิ่งที่ REUSE)
- `AccountFinance.ledgerAccountId` = บัญชี GL ลูกของช่องทาง → "ฝั่งระบบ" = `AccountJournalLine` ที่ `accountId = ledgerAccountId`
- `finance.ts`: `financeBalances` (Σdebit−Σcredit ผ่าน groupBy) · `financeStatement(from,to)` (running balance) → ใช้สูตรเดียวกันคำนวณ "ยอดในระบบ ณ สิ้นงวด"
- `finance-overview.ts` `financeOverview().reconcile` = placeholder (statement="ยังไม่นำเข้า", diff="—", reconciledCount=0) → 5.3 ต้องต่อของจริง
- `gl.ts`: `commitEntry` idempotencyKey = `${refType}#${refId}#${event}` · `alreadyPosted` (private) · `postManualJV` refId=randomUUID (ไม่ idempotent) ⇒ **เพิ่ม `postBankReconcileEntry` ใน gl.ts** (idempotent ต่อ statementLineId) ตาม pattern 5.1 (postFinanceOpening/postFinanceTransfer)
- `coa.ts`: ยังไม่มี BANK_FEE/INTEREST_INCOME (มี PAYMENT_FEE→6500 ที่ postPayment ใช้กับ feeAmount) ⇒ เพิ่ม CHART 6510 ค่าธรรมเนียมธนาคาร + 4910 ดอกเบี้ยรับ และ MAPPING BANK_FEE/INTEREST_INCOME (additive · seed idempotent · ไม่มีเทสผูกจำนวนรหัส)
- `access.ts`: มี action `account.reconcile` ใน permissions แล้ว · IMPLIES ต้องเพิ่ม `account.finance.manage → account.reconcile` (กันสิทธิ์หายของคนเดิม)
- `guard.ts` registry: `finance/reconcile/page.tsx` = `account.finance.manage` (คงไว้)
- `csv.ts`: `parseCsv` (BOM/quote/CRLF/tab) + `columnIndex` + `cell` → ใช้ทำ adapter ธนาคาร
- `import-shared.ts`: `IMPORT_PREVIEW_ROWS=20` · `IMPORT_MAX_ROWS` · `fileHashOf` · `bahtToSatang`
- UI: `FinanceSubTabsBar` · `Modal` · `SectionCard` · `RowActions` · `formatBaht`/`formatDateTh`

## ขั้น 4 — A. Schema + migration ✅
- ไฟล์: `prisma/schema/account_gl.prisma` (ท้ายไฟล์ + แก้ `AccountJournalLine`)
- migration: `prisma/migrations/20260905000000_account_v2_bank_reconcile/migration.sql` — **additive ล้วน**
  (CREATE TYPE 1 · CREATE TABLE 2 · ADD COLUMN 2 บน AccountJournalLine · CREATE INDEX 7 · FK 1)
- deploy บน QC branch (`ep-plain-art`) แล้ว ✅ · prisma generate ✅ · **ยังไม่แตะ prod**
- **ต่างจากใบสั่งงาน (จดไว้)**:
  1. `matchedLineId` (AccountJournalLine) เป็นตัวผูกหลัก ไม่ใช่ `matchedEntryId` อย่างเดียว —
     JV โอนระหว่างช่องทาง 1 ใบมีบรรทัดธนาคาร 2 บัญชี ถ้าผูกระดับ entry จะชนกันเมื่อกระทบยอดทั้งสองช่องทาง
     (`matchedEntryId` ยังเก็บไว้ให้ลิงก์เอกสารได้ไม่ต้อง join)
  2. `reconciledAt`/`reconciledStatementLineId` อยู่บน **AccountJournalLine** (ใบสั่งงานเขียนว่า AccountJournalEntry) ด้วยเหตุผลเดียวกัน
  3. ไม่เพิ่ม `matchedPaymentId` — entry มี `refType`/`refId` ชี้ `AccountDocumentPayment` อยู่แล้ว (คอลัมน์ซ้ำ = เน่าได้)
  4. ล็อกงวดใช้ `AccountBankStatement.confirmedAt` (ไม่สร้างตาราง `AccountReconciliationLock` แยก) — 1 ช่องทาง 1 งวด 1 ใบ (unique)
  5. `fingerprint` รวม "ลำดับที่ซ้ำ" ของแถวที่เหมือนกันเป๊ะในไฟล์เดียว ⇒ statement ที่มี 2 แถวเหมือนกันจริงไม่ถูกกลืน แต่ import ซ้ำยังได้ 0 แถวใหม่

## ขั้น 5–7 — B/C/D ✅ (typecheck 0 error)
ไฟล์ใหม่:
- `src/lib/modules/account/bank-statement-csv.ts` — ตัวอ่าน CSV ธนาคาร (KBANK/SCB/KTB/BBL/GENERIC ด้วย alias คอลัมน์ · พ.ศ./ค.ศ. · (250.00) ติดลบ · DR/CR · BOM · fingerprint)
- `src/lib/modules/account/reconcile.ts` — engine (preview/import/autoMatch/manualMatch/unmatch/skip/createEntryFromLine/summary/confirmMonth/reopenMonth/reconcilePageData + hook `suggestWithAi = null`)
- `src/components/account-v2/ReconcilePanel.tsx` · `BankStatementImportModal.tsx`
- `src/app/app/sys/[id]/account/finance/reconcile/page.tsx` (แทน placeholder) + `actions.ts`
ไฟล์ที่แก้:
- `coa.ts` (+6510 ค่าธรรมเนียมธนาคาร · +4910 ดอกเบี้ยรับ · +mapping BANK_FEE/INTEREST_INCOME)
- `gl.ts` (+`postBankReconcileEntry` idempotent ต่อแถว statement)
- `access.ts` (+IMPLIES `account.finance.manage → account.reconcile`)
- `src/lib/core/scope.ts` (ลงทะเบียน 2 model ใหม่ — ไม่ลง = แอปไม่ start)
- `nav.ts` (กระทบยอดธนาคาร soon → ready)
- `finance-overview.ts` + `FinanceOverviewPanel.tsx` (บล็อกกระทบยอดอ่านตัวเลขจริง + ปุ่มเปลี่ยนเป็น "จับคู่รายการ" เมื่อนำเข้าแล้ว)

### ต่างจากใบสั่งงาน (เหตุผล)
- **กติกา auto-match**: ใบสั่งงานว่า "unique candidate → MATCHED" แม้วันคลาด · ที่ทำจริง = **ต้องวันตรงกันด้วย**
  จึงจะ MATCHED เอง · ยอดตรงแต่วันคลาด 1–3 วัน = SUGGESTED ให้คนกดยืนยัน ⇒ ตรงกับ g10 ที่แถว
  "โอนเข้า — ไม่ระบุที่มา" เป็น "แนะนำจับคู่" พร้อมใบ้ "ยอดและวันที่ตรงกัน ±1 วัน" (ถ้าใช้กติกาเดิม แถวนั้นจะถูกจับคู่เงียบ ๆ)
- **แถบแท็บย่อยการเงิน**: ไม่เพิ่มแท็บที่ 8 (จะพัง parity f7/g9) — ให้แท็บ "ภาพรวม" active เพราะกระทบยอดเป็นลูกของ §10.2
- **รหัส GL ใต้ไทล์ "ยอดในระบบ"**: g10 เขียน `111201` · ของจริงคือ `1010-01` (ผังบัญชีจริงของระบบ) — บังคับโดยข้อมูล

## ขั้น 8 — E. fixture / seed / ข้อสอบ ✅
- `scripts/acc-v2-fixture-bank-statement.mts` — **generator**: อ่านบรรทัด GL จริงของ BSV001 งวด 2026-09
  ด้วย `$queryRaw` อิสระ (ไม่แตะ reconcile.ts) → เขียน `scripts/fixtures/acc-v2/kbank-2026-09.csv`
  (รูปแบบกสิกร · BOM · CRLF · วันที่ พ.ศ. · คั่นหลักพันในเครื่องหมายคำพูด) + `kbank-2026-09.expected.json` (เฉลย)
- `scripts/fixtures/acc-v2/scb-sample.csv` — ไฟล์ SCB 4 แถวสำหรับทดสอบตัวอ่าน (วงเล็บ=ติดลบ · วันที่ 4 แบบ)
- `seed-acc-v2-qc.mts` §8.6 — สร้าง fixture + นำเข้า + autoMatch ⇒ ร้าน QC อยู่สถานะเดียวกับ g10
  (**ไม่โพสต์ JV ใด ๆ** ⇒ ยอดเงินทุกช่องทางเท่าเดิม 1,283,760 · เฉลยเดิมของ 5.1/5.2 ไม่กระทบ)
- `qc-acc-v2-reconcile.mts` — **109/109** (parser 4 ธนาคาร + generic · วันที่/จำนวนเงินรายกรณี · แถวเสีย ·
  สรุป seed = เฉลยอิสระ · นำเข้าซ้ำ 0 แถว · autoMatch = เฉลย · จับคู่มือ/ยอดไม่ตรง/ชนกัน · สร้าง JV ค่าธรรมเนียม+ดอกเบี้ย ·
  ส่วนต่าง → 0 · ยืนยัน/ล็อก/เปิดกลับ · unmatch · guard · IMPLIES · tenant isolation · ไม่เขียนครึ่ง ๆ กลาง ๆ)
- `qc-acc-v2-seed-check.mts` — เพิ่มหมวด **L (14 ข้อ)** → 83 → **97/97** (ไม่ลดข้อเดิม)

### 🔴 ต่างจากใบสั่งงาน: จำนวนแถว 11 ไม่ใช่ 41
seed ของ BSV001 เดือน ก.ย. มีบรรทัดสมุดรายวันจริง **9 บรรทัด** (ตรวจแล้วด้วย SQL) ⇒ statement ที่ "สร้างจากของจริง"
ได้ 9 + ค่าธรรมเนียม + ดอกเบี้ย = **11 แถว · จับคู่อัตโนมัติ 8 · แนะนำ 1 · รอจับคู่ 2 · ส่วนต่าง −237.65 → 0**
ตัวเลข 41/38/4,150 ใน BLUEPRINT มาจากข้อมูลสมมติในภาพ mockup ไม่ใช่ข้อมูล seed
- ทางเลือกที่ **ไม่ทำ**: ยัด ~30 รายการธนาคารเดือน ก.ย. เข้า seed เพื่อให้ได้ 41 แถว — จะทำให้ยอด BSV001/CSH001,
  ไทล์ปฏิทินเงินเข้า-ออก, ยอดรวมเงิน, จำนวนเอกสารในแท็บ (parity f1/f3/f7 ที่ผ่านแล้ว) เปลี่ยนหมด และต้อง
  re-baseline เฉลยหลายชุด ⇒ เสี่ยงกว่าประโยชน์ · ใบสั่งงานเองก็สั่งว่า "สร้าง CSV จากบรรทัด GL จริงเพื่อไม่ให้เน่า"
- ถ้าภายหลังตัดสินใจขยาย seed: generator เป็น **parametric** อยู่แล้ว (ไม่ฮาร์ดโค้ดวัน/จำนวน) — รัน
  `pnpm tsx scripts/acc-v2-fixture-bank-statement.mts` ใหม่ ไฟล์+เฉลย+ข้อสอบจะปรับตามเองทั้งชุด

## ขั้น 9 — F. ภาพ (visual) ✅

`scripts/visual-acc-v2.mts` มี **6 ช็อต** ของ WO 5.3 (5 ช็อตเขียนไว้แล้วรอบก่อน · รอบนี้ยิงจริง + ไล่ parity + เพิ่มช็อตที่ 6):
- `reconcile` (1440) · `reconcile-import-modal` (1440) · `reconcile-balanced` (1440 · เดือน ส.ค. ส่วนต่าง 0)
- `reconcile-mobile` (390) · `reconcile-mobile-sheet` (390 · แตะแถวแล้วเปิดแผ่นเลือกคู่)
- **`reconcile-import-preview` (1440 · เพิ่มรอบนี้)** — ใบสั่งงานสั่ง "ภาพ preview ของโมดัลนำเข้า" แต่รอบก่อน
  ถ่ายได้แค่โมดัลเปล่า ⇒ เพิ่ม flow `uploadFile()` ไฟล์จริงแล้วกด "ตรวจสอบไฟล์"
  ไฟล์ตัวอย่างใหม่ `scripts/fixtures/acc-v2/kbank-preview-sample.csv` (4 แถว: 3 แถวใหม่ + 1 แถววันที่พัง)
  🔴 ปลอดภัยเพราะ `previewStatement` **ไม่เขียน DB** ⇒ ไม่กระทบเฉลย/ยอดของ WO อื่น
  (ไฟล์ต้องอยู่ใต้โฟลเดอร์โปรเจกต์ ไม่ใช่ /tmp — chromium เป็น snap มี /tmp ส่วนตัว)
  ผลบนจอ: อ่านได้ 3 · นำเข้าใหม่ 3 · ซ้ำ 0 · นอกเดือน 0 · อ่านไม่ออก 1 ·
  ยอดคงเหลือปลายงวดในไฟล์ ฿791,765.00 · error ต่อแถว `บรรทัด 5: วันที่อ่านไม่ออก: "31/13/2569"`

`pnpm tsx scripts/visual-acc-v2.mts 5.3 --assert` → **ผ่านทั้งหมด** · ล้นแนวนอน 0px ทุกช็อต
ไทล์ตรงเฉลยอิสระ: statement ฿901,562.35 · ระบบ ฿901,800.00 · ส่วนต่าง −฿237.65 (แดง) · จับคู่ 8/11
เดือนก่อน (ส.ค.): ส่วนต่าง ฿0.00 · 6/6 · ปุ่มยืนยันเปิดใช้ได้ · ไม่มี tooltip
ภาพ: `.qc-shots/acc-v2/5.3/{reconcile,reconcile-import-modal,reconcile-balanced}-desktop.png`
      `.qc-shots/acc-v2/5.3/reconcile-import-preview-desktop.png`
      `.qc-shots/acc-v2/5.3/{reconcile-mobile,reconcile-mobile-sheet}-mobile.png`

### ด่าน parity — ที่ **ตกแล้วแก้** (ดูภาพจริงเทียบ g10 ไม่ใช่เชื่อ assert)
ทุกช็อตผ่าน assert ข้อความ/ตัวเลขหมดแล้ว **แต่ตาดูแล้วไม่ตรง g10/ไม่ตรงมาตรฐาน V2** → แก้ 4 รอบ:
1. 🔴 ปุ่ม `⤓ นำเข้ารายการเดินบัญชี (CSV)` ใช้ `className="btn"` — `.btn` ใน globals.css **ไม่มีขอบ/ไม่มีพื้น**
   ⇒ ออกมาเป็นตัวหนังสือลอย ๆ ขณะที่ g10 วาดเป็นปุ่มขาวมีขอบสูงเท่า select
   แก้เป็น `btn-sm` (= ปุ่มรองบนหัวหน้าช่องทางการเงิน g9 ที่ผ่าน parity แล้ว) → สูงเท่า select พอดี
2. ปุ่มคู่ท้าย bottom sheet มือถือ (`สร้างรายการจากแถวนี้` / `ข้าม`) และปุ่ม `ปิด`/`ยกเลิก` ใน modal
   ก็ใช้ `.btn` เปล่า → ไร้ขอบเหมือนกัน ⇒ เปลี่ยนเป็น `btn btn-ghost` (แบบเดียวกับ ContactModal/ProductModal)
3. รายการ "รายการที่กระทบยอดแล้ว" บนมือถือ 390: date(w-16) + เลขเอกสาร(w-40) + คำอธิบาย + เงิน
   เบียดกันจน `+` กับตัวเลขตกคนละบรรทัด และเหลือ `ร้…` อ่านไม่ออก
   ⇒ มือถือซ่อนคอลัมน์คำอธิบาย (`hidden sm:block`) · เลขเอกสารกิน flex-1 · จำนวนเงิน `whitespace-nowrap`
   (เดสก์ท็อปเหมือนเดิมทุกอย่าง — `sm:w-40 sm:flex-none`)
4. 🔴 คอลัมน์ "วันที่" ในตาราง preview ของโมดัลนำเข้าโชว์ **ISO ดิบ `2026-09-05`**
   (`dateText: dayKeyBkk(...)`) — เรื่องเดียวกับที่คนคุมตีกลับ WO 5.2 รอบ 2 มาแล้ว
   ⇒ เปลี่ยนเป็น `formatDateTh()` → `5 ก.ย. 2026` (`src/lib/modules/account/reconcile.ts` บรรทัด ~184)

### deviation จาก g10 ที่ **ตั้งใจคงไว้** (ไม่ใช่บั๊ก)
- **แถบแท็บย่อยการเงิน** (ภาพรวม/เงินสด-ธนาคาร/เงินสดย่อย/เช็ค…) — g10 ไม่ได้วาด (ภาพตัดเฉพาะเนื้อหน้า)
  แต่ทุกหน้าในหมวดการเงินมีแถบนี้ ⇒ คงไว้ · ไม่เพิ่มแท็บที่ 8 (จะพัง parity f7/g9)
- **จำนวนแถว 11 ไม่ใช่ 41 · ยอด 901,562.35 ไม่ใช่ 908,150** — ข้อมูลจริงของ seed (เหตุผลเต็มอยู่หัวข้อขั้น 8)
- **รหัส GL ใต้ไทล์ "ยอดในระบบ" = 1010-01 ไม่ใช่ 111201** — ผังบัญชีจริงของระบบ (ข้อมูลบังคับ)
- **ฝั่งขวาแยกวันที่เป็นบรรทัดที่ 2 สีเทา** (g10 ต่อท้ายคำอธิบายด้วย ` · 25 ก.ย.`) — อ่านง่ายกว่าเมื่อคำอธิบายยาว
- **วงแหวนน้ำเงินเรืองแสงที่ขอบขวาของภาพ** = วิดเจ็ตลอยระดับแอป **ไม่ใช่ของ 5.3**
  (โผล่ในภาพ `.qc-shots/acc-v2/5.2/finance-overview-desktop.png` ที่ผ่าน parity ไปแล้วเหมือนกัน)

### 🔴 บทเรียนสภาพแวดล้อม (จดไว้กัน WO ถัดไปเสียเวลา)
`next build` **ตายด้วย JS heap OOM ที่เพดาน ~2 GB** (default max-old-space ของ node บนเครื่องนี้):
`FATAL ERROR: Ineffective mark-compacts near heap limit` → `build worker exited … SIGABRT`
⇒ ต้องสั่ง `export NODE_OPTIONS=--max-old-space-size=3584` ก่อน `bash scripts/acc-v2-serve.sh`
(3584 = ใหญ่พอ build ผ่าน · ยังต่ำกว่าเพดาน 5 GB ของยามความจำที่ฆ่า agent รอบก่อน)
⚠️ `scripts/acc-v2-serve.sh` ไม่ได้ตั้งค่านี้ให้ — คนใช้ต้อง export เอง (ยังไม่แก้สคริปต์ในรอบนี้)

## ขั้น 10 — G. ด่าน (รันทีละตัว · เซิร์ฟเวอร์ปิดแล้ว · export DATABASE_URL/DIRECT_URL จาก .env.qc · host = ep-plain-art) ✅

```
bash scripts/with-gate-lock.sh pnpm typecheck   → tsc --noEmit ไม่มี error (exit 0)
pnpm fitness                                    → ผ่าน 17/17 · FINDINGS: CRITICAL 0 · MAJOR 0 · MINOR 0
qc-acc-v2-reconcile   → ผ่าน 109 · ตก 0   JSON_SUMMARY {"total":109,"passed":109,"findings":[]}
qc-acc-v2-finance-overview → ผ่าน 45 · ตก 0   JSON_SUMMARY {"total":45,"passed":45,"findings":[]}
qc-acc-v2-finance     → ผ่าน 59 · ตก 0    JSON_SUMMARY {"total":59,"passed":59,"findings":[]}
qc-acc-v2-seed-check  → ผ่าน 102 · ตก 0 (รวม 102 ข้อ)
qc-acc-v2-dashboard   → สรุป WO 2.1: ผ่าน 169 · ไม่ผ่าน 0
qc-acc-v2-home        → สรุป WO 2.2: ผ่าน 87 · ไม่ผ่าน 0
qc-acc-v2-payments    → สรุป WO 1.4: ผ่าน 157 · ไม่ผ่าน 0
qc-acc-v2-guard       → สรุป: ผ่าน 158 · ไม่ผ่าน 0 (WO 0.2)
qc-acc-v2-schema      → สรุป: ผ่าน 61 · ไม่ผ่าน 0 (WO 0.3)
qc-acc-v2-nav         → สรุป: ผ่าน 323 · ไม่ผ่าน 0 (WO 0.4)
pnpm run drift        → No difference detected. (exit 0)
pnpm qc:account       → ผ่าน 107/107 ข้อตรวจ · FINDINGS: CRITICAL 0 · MAJOR 0 · MINOR 0
```
หลังแก้ parity ข้อ 4 (แตะ `reconcile.ts`) รันด่านที่เกี่ยวข้องซ้ำอีกรอบ — ผลเท่าเดิมทุกตัว:
```
bash scripts/with-gate-lock.sh pnpm typecheck → ไม่มี error (exit 0)
qc-acc-v2-reconcile        → ผ่าน 109 · ตก 0
qc-acc-v2-seed-check       → ผ่าน 102 · ตก 0
qc-acc-v2-finance-overview → ผ่าน 45 · ตก 0
```
หมายเหตุ: `qc-acc-v2-seed-check` รอบนี้ได้ **102** ข้อ (โน้ตรอบก่อนจด 97) — ตัวนับของชุดนี้เดินตามจำนวน
แถวข้อมูลจริงบางหมวด ไม่ได้ตายตัว · ผ่านครบ ตก 0 เหมือนเดิม

## ค้าง / ยังไม่ได้ทำ
- **ยังไม่ commit** (ตามกติกาของ RUN — คนคุมเป็นคนสั่ง commit/push)
- `suggestWithAi` ใน `reconcile.ts` ยังเป็น hook คืน `null` (ตั้งใจ — ต่อ AI ทีหลัง)
- ตัวอ่าน CSV รองรับ **UTF-8 เท่านั้น** — ไฟล์ TIS-620 จาก core banking รุ่นเก่าต้อง Save as UTF-8 ก่อน
  (โมดัลนำเข้าเขียนบอกผู้ใช้ไว้แล้ว)
- `scripts/acc-v2-serve.sh` ยังไม่ได้ใส่ `NODE_OPTIONS=--max-old-space-size` ให้อัตโนมัติ
