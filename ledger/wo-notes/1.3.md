# WO 1.3 — DocEditorV2 (โน้ตเริ่มโดย Fable หลัง agent ตัวแรกตายตอนเครื่อง reboot 3 ก.ย.)

## สถานะที่พบบนดิสก์ (commit b3fbb07 · typecheck ผ่าน)
- `src/components/account-v2/DocEditorV2.tsx` (758 บรรทัด) · `DocLineTable.tsx` (402) · `DocTotals.tsx` (181) · `DocAttachments.tsx` (159) · `doc-editor-types.ts` (193)
- `src/lib/modules/account/DocEditorPage.tsx` (241) · `doc-editor-config.ts` (118) · `editor-actions.ts` (449: saveDraftAction · approveDocAction · searchContactsAction · searchProductsAction · uploadDocAttachmentAction · deleteDocAttachmentAction · saveFavoriteLinesAction · discardDraftAction) · `totals.ts` (computeDocTotals — export ผ่าน service.ts)
- route `new/` + `[docId]/edit/` ครบ 10 ชนิด (docs/[docType] · expense · purchase · po · asset-buy · asset-po · deposit-payment · credit-note-received · debit-note-received · purchase-tax-invoice)
- migration `20260903090000_account_v2_doc_editor` (additive: AccountDocumentLine.whtIncomeType/whtRateBp · AccountDocument.reference/autoTaxInvoice) — apply บน QC branch แล้ว · **prod ยังไม่** (จะได้ตอน deploy)
- ส่วน D (มัดจำ) และ F (รับชำระ) เว้นจุดต่อไว้ให้ WO 1.4 ตามสเปค

## ที่ยังไม่มี (agent ตัวถัดไปทำต่อ — ห้ามเขียนใหม่)
1. ข้อสอบ `scripts/qc-acc-v2-editor.mts` (≥40 ข้อ ตามใบสั่งงานเดิม: computeDocTotals table-driven รวมเคส g1 · saveDraft สร้างครั้งเดียว/ไม่ออกเลข · approve ออกเลขครั้งเดียว+JV สมดุล · สิทธิ์ · tenant isolation · ไฟล์แนบ · ตัวอักษรไทย 24,900)
2. ลิงก์ "แก้ไข" บนหน้าเอกสาร (สถานะ DRAFT) ไปหน้า edit · ปุ่ม "+ สร้าง…" ของ DocListPage (WO 1.1) ชี้มาหน้า `new` แทน CreateSection
3. รายการหน้าที่ต้องถ่ายภาพ + testid ที่ Fable ต้องเพิ่มใน visual-acc-v2 (สเปค E ของใบสั่งงาน)
4. checklist ✓/✗ เทียบ g1 / g1-menu / g17
5. รัน typecheck · fitness · qc-acc-v2-editor · CPA (env ผ่าน grep|cut เท่านั้น)

---

# รอบต่อ (agent ตัวใหม่ · 3 ก.ย.) — ทำต่อจากของเดิม ไม่ได้เขียนใหม่

## 1. Code review เทียบ §5.2 A–C,E,G,H,I + g1 / g1-menu / g17 — สิ่งที่แก้จริง (7 จุด)
1. **`DocLineTable`: ฮาร์ดโค้ด VAT 7%** — `applyProduct` ใช้ `p.vatRateBp ?? 700` ⇒ ร้านที่อัตรา VAT ไม่ใช่ 7% จะได้เลขผิดเมื่อเลือกสินค้าที่ไม่ระบุอัตรา · แก้เป็น prop ใหม่ `defaultVatRateBp` (ส่ง `settings.vatRateBp` มาจากฟอร์ม)
2. **`saveFavoriteLinesAction` เป็นโค้ดตาย** — เมนู "รายการโปรด" มีแต่ปุ่ม *ใช้* ชุดที่บันทึกไว้ ไม่มีทาง *บันทึก* และเมนูซ่อนทั้งอันเมื่อยังไม่มีชุด (g1 โชว์ปุ่มเสมอ) · แก้: เมนูโผล่เสมอ + ช่องตั้งชื่อ + ปุ่มบันทึก (`fav-name` / `fav-save` / `fav-apply-<ชื่อ>`) + empty state ภาษาไทย
3. **`discardDraftAction` เป็นโค้ดตาย + ร่างผี** — ปุ่ม "ยกเลิก" เป็น `<Link>` เฉย ๆ ⇒ autosave สร้างร่างไว้แล้วผู้ใช้กดยกเลิก = ร่างค้างในรายการตลอดไป · แก้: ถ้าร่างเป็นของที่ *ฟอร์มนี้สร้างเอง* (`!props.docId && docId`) ปุ่มยกเลิกยิง `discardDraftAction` (→ CANCELLED ไม่ลบ ตาม §0.3-8) · เข้าทาง `/[docId]/edit` = ร่างของผู้ใช้เอง → ยกเลิก = แค่ออกจากหน้า (ห้ามไปยกเลิกร่างเขา)
4. **แถบยอดมือถือไม่ติดล่างจอ (g17)** — `MobileTotalsBar` อยู่นอก sticky ⇒ เลื่อนหายไปกับหน้า · แก้: ห่อ `MobileTotalsBar + StickyBar` ใน `sticky bottom-0`
5. **chevron ของแถบยอดมือถือกดแล้วไม่มีอะไรเกิด** — `mobileTotalsOpen` เป็น state ตาย · แก้: กางแล้วโชว์บล็อกสรุปยอดเต็ม (`totals-m-panel`) เหนือแถบ
6. **ไม่มี testid ให้กล้องคลิกเมนูอนุมัติ** — เปลี่ยน `btn-approve` → `btn-approve-menu` (ตามที่ชุดภาพจริงต้องใช้)
7. **ปุ่ม "+ สร้าง…" ยังชี้ฟอร์มเก่า** — ดูข้อ 2 ด้านล่าง

### ที่ตรวจแล้ว "มีอยู่แล้ว ถูกต้อง" (ไม่แตะ)
- A stepper: ขั้นที่ออกแล้วเป็นวงกลมดำ+✓ และคลิกได้ (`href` เฉพาะ state `done`) · ขั้นปัจจุบัน accent+เลข · ขั้นถัดไปจาง — `Stepper.tsx`
- B ส่วนหัว: ลำดับช่องตรง g1 (เลขที่ · ผู้ติดต่อ / วันที่ออก · ครบกำหนด / อ้างอิง · ประเภทราคา / สกุลเงิน · พนักงานขาย+สาขา / toggle ใบกำกับ / แท็ก) · เปลี่ยน "ประเภทราคา" คิดยอดใหม่ทันที (`useMemo` เรียก `computeDocTotals`) · toggle VAT ซ่อนเมื่อไม่จด VAT (`props.vatRegistered && (INVOICE|RECEIPT)`) · แท็กสร้างใหม่ได้ (พิมพ์+Enter) · เลือกผู้ติดต่อแล้วเติมเครดิตเทอม/ประเภทราคาอัตโนมัติ
- C ตาราง: ครบ 10 คอลัมน์ · WHT ต่อบรรทัด (ประเภทเงินได้ + อัตรา) เก็บลงคอลัมน์ใหม่จริง · ลากเรียงได้ (HTML5 drag บน `<tr>`) · "+ เพิ่มคำอธิบาย" · ลบแถว
- E สรุปยอด: ลำดับตรง g1 ฉบับแก้แล้ว (รวมเป็นเงิน · ส่วนลดรวม ✏ · หลังหักส่วนลด · VAT · **กล่องดำ จำนวนเงินทั้งสิ้น** + ตัวอักษรไทย · หัก ณ ที่จ่าย · หักเงินมัดจำ · ยอดที่ต้องชำระ)
- G หมายเหตุ 2 กล่อง · H dropzone + อัปโหลดจริงเข้า FileAsset→AccountAttachment · I ปุ่มท้าย 2 dropdown + pending state + toast "โปรดกรอกช่องที่ไฮไลต์"
- โหมดง่ายซ่อน บัญชี / หัก ณ ที่จ่าย / ประเภทราคา · มือถือ section เป็น accordion (`<details>`)
- autosave ไม่จองเลข (`previewNextDocNo` อ่านอย่างเดียว · เลขเกิดที่ `issueDocument`) · เอกสารที่อนุมัติแล้วเปิด `/edit` = redirect ไปหน้าเอกสาร (`DocEditorPage` เช็ก `status !== "DRAFT"`)

### ต่างจากแบบโดยตั้งใจ (จดไว้ตามกติกา parity)
- **ช่อง "เลขที่เอกสาร" อ่านอย่างเดียว** (g1 โชว์เลขแก้ได้) — สเปค B บอก "แก้ได้เฉพาะร่าง" แต่เกณฑ์ผ่านของ WO บอก "แก้ร่างซ้ำไม่สร้างเลขใหม่" ⇒ เลือกโชว์ *พรีวิวเลขถัดไป* แบบอ่านอย่างเดียว แล้วออกเลขจริงตอนอนุมัติ · การแก้เลขมือ = งานหน้าตั้งค่าเลขเอกสาร (§9.2)
- **มือถือยังไม่มีลากเรียง/เมนู "⋯" ต่อการ์ดรายการ** (g17 มีจุดไข่ปลา) — ลบ/แก้ได้ครบผ่านการ์ด · เมนูต่อการ์ดผูกกับงาน D/F ของ WO 1.4
- **ส่วน D (เงินมัดจำ) / F (รับชำระ)** ไม่มีในหน้า = ตามใบสั่งงาน (WO 1.4) · บรรทัด "หักเงินมัดจำ" อ่านอย่างเดียวรออยู่แล้ว
- **ส่วนลดบรรทัดของเอกสารเก่า**: DB เก็บ "ส่วนลดทั้งบรรทัด" แต่ฟอร์ม V2 เก็บ "ต่อหน่วย" ⇒ ตอนเปิดร่างเก่ามา `discount/qty` ปัดเศษได้สูงสุด (qty−1) สตางค์ · เกิดเฉพาะร่างเก่าที่ส่วนลดหารจำนวนไม่ลงตัว — ยังไม่แก้ (ทางแก้จริงคือย้ายให้ DB เก็บต่อหน่วย = migration ไม่ additive)

## 2. ทางเข้าฟอร์ม (entry points) ที่เดินสายแล้ว
- `DocListPage` มี prop ใหม่ `createHref` — ส่งมาแล้ว **ปุ่มดำ "+ สร้าง…" (เดสก์ท็อป) + FAB (มือถือ)** เป็น `<Link>` ไป `<route>/new` และ **ไม่เรนเดอร์ `CreateSection`/`createForm` อีก**
- `docs/[docType]/page.tsx` + `expense-page.tsx`: `createHref = canCreate && canCreateDirect(dt) ? "<route>/new" : undefined`
- **`CreateSection.tsx` ยังอยู่** (ยังมีคนใช้ 3 ที่): `goods-issue` (ฟอร์มใบเบิกสินค้า ยังไม่มีฟอร์ม V2) · `CREDIT_NOTE`/`DEBIT_NOTE` (docs) · `CREDIT_NOTE_RECEIVED`/`DEBIT_NOTE_RECEIVED` (expense) — 4 ชนิดหลังเป็น `CONVERT_ONLY_TYPES` ⇒ `/new` จะ redirect ทิ้ง ถ้าเปลี่ยนตอนนี้ผู้ใช้จะสร้างไม่ได้เลย · **ปลดเมื่อ WO 1.6 ทำ wizard 2 ขั้นเสร็จ**
- ฟอร์มเก่าที่ยังถูกอ้างอยู่ (ห้ามลบตอนนี้): `DocEditor` = `docs/[docType]/page.tsx` (CN/DN) + `docs/[docType]/[docId]/page.tsx` (โหมด `?edit=1` เดิม เผื่อลิงก์เก่า) · `ExpenseEditor` = `expense-page.tsx` (CNR/DNR)
- ปุ่ม **"แก้ไข" บนหน้าเอกสารสถานะร่าง** ชี้ `/[docId]/edit` แล้วทั้ง 2 ฝั่ง (`docs/[docType]/[docId]/page.tsx:265` · `expense-ui.tsx:255` ผ่าน `editHref`)
- `nav.ts` flyout "+ สร้าง…": เปลี่ยน `#new` → `/new` 10 รายการ (QT · IV · DR · BN · PO · DP · PUR · EXP · APO→`/asset-po/new` · AP) · คงไว้ที่ `#new` 5 รายการ (CN · DN · CNR · DNR · ใบเบิกสินค้า) ตามเหตุผลด้านบน
- `qc-acc-v2-nav.mts` **303/303 ผ่าน** (ด่าน N2 ยืนยันว่าทุก href ใหม่มีไฟล์ route จริง) · `qc-nav-functions.mts` 9/9 ผ่าน

## 3. ข้อสอบใหม่: `scripts/qc-acc-v2-editor.mts` — **172 ข้อ ผ่านหมด**
`QC_ENV_FILE=.env.qc pnpm tsx scripts/qc-acc-v2-editor.mts`
- **E1 (43 ข้อ · ไม่ต่อ DB)** `computeDocTotals` แบบตาราง: เคสเฉลย g1 ครบทุกช่อง (sub 23,271.03 · VAT 1,628.97 · ทั้งสิ้น 24,900.00 · WHT 3% ของบรรทัดทริป 594.00 · ต้องชำระ 24,306.00 · ตัวอักษร "สองหมื่นสี่พันเก้าร้อยบาทถ้วน") · INCL_VAT (107 → ฐาน 100 + VAT 7 ไม่บวกซ้ำ) · NO_VAT · ไม่จด VAT บังคับ NONE · ส่วนลด/หน่วย ฿ และ % + clamp · ส่วนลดท้ายบิล ฿ และ % + clamp + ยอดไม่ติดลบ · VAT 0%/ยกเว้น · WHT คิดจากฐานหลังส่วนลด · หักมัดจำ 10,000 → 14,306 (ตาม g1 เต็มใบ) · `bahtText` 5 เคส (0 · สิบเอ็ด · เศษสตางค์ · หลักล้าน · ติดลบ) · `vatModeOf`/`priceModeOf` ไป-กลับ
- **E2** ตัวเลือก WHT ฝั่งจอ = `WHT_INCOME_LABEL` ฝั่ง server ทุกคีย์ + จำนวนเท่ากัน (กันดริฟต์ที่ไฟล์ types เตือนไว้เอง) · ชุด VAT = 7%/0%/ยกเว้น · `packDescription`/`unpackDescription` ไป-กลับ
- **E3** ทะเบียน route: new/edit/detail ทั้งรายรับและรายจ่าย · ชนิดที่สร้างตรงไม่ได้ 6 ชนิด · สาย stepper 3 สาย · ป้ายวันที่ · บัญชีต่อบรรทัด · ครบ 17 ชนิด
- **E4** สายไฟของ server action (static): ทั้ง 8 action เรียก `loadAccountSystem` + `assertAccountCan` · ไฟล์ไม่ import prisma · `saveDraftAction` ไม่แตะ `issue*`/`nextDocNo` · คำนวณใหม่ด้วย `computeDocTotals` · เช็ก `getDraftMeta` + `status DRAFT` · `assertEditorRefs` · approve ใช้ `account.doc.issue` · มี testid ครบ (`tot-*` · `line-<i>` · `btn-approve-menu`)
- **E5–E6 (DB จริง · tenant ทิ้ง)** บันทึกร่าง → ยอด 24,900 ตรงสูตรฝั่งจอ · **ร่างไม่มี docNo · พรีวิวเลขไม่ขยับ · ไม่มีแถว `AccountDocSequence`** · แก้ร่าง 3 ครั้งยังใบเดียว/ยอดเดิม/ไม่มีเลข · `reference` · `autoTaxInvoice` · แท็ก · พนักงานขาย · หมายเหตุภายใน · `priceMode` · `whtAmount` · **`whtIncomeType`/`whtRateBp` ต่อบรรทัด** · ไฟล์แนบผูกเอกสาร + แนบข้ามระบบไม่ได้ · รายการโปรดบันทึก/อ่าน/ไม่รั่วข้ามระบบ · อนุมัติ → ได้เลขตรงพรีวิว · อนุมัติซ้ำไม่ได้ · เลขถูกกินแค่ 1 · **JV 1 ชุด สมดุล Dr 1100 = 24,900 · Cr VAT รอ (2205/2210/2200) = 1,628.97 · Cr รายได้ = 23,271.03**
- **E7** STAFF ไม่มีสิทธิ์ = ถูกปฏิเสธทั้ง create/issue · STAFF ที่ได้ `create` ยังอนุมัติเองไม่ได้ · positive control OWNER ผ่าน · เอกสารของระบบอื่น/ชนิดอื่น `getDraftMeta` = null · update/issue ข้ามระบบไม่สำเร็จ · ผู้ติดต่อของอีกระบบถูก `assertEditorRefs` ปฏิเสธ · ค้นหาผู้ติดต่อไม่เห็นของระบบอื่น · ยกเลิกร่าง → CANCELLED (ไม่ลบ) · ยกเลิกใบที่ออกเลขแล้วไม่ได้

**หมายเหตุวิธีทดสอบ (ตามกติกา "ผลเทสมีขอบเขตเท่าการทดลอง")**: ข้อสอบนี้ **ไม่ได้เรียก server action ตรง ๆ** เพราะ `saveDraftAction`/`approveDocAction` เริ่มด้วย `requireTenant()` ที่อ่านคุกกี้ผ่าน `next/headers` (ไม่มีนอก request context ของ Next) · ที่ตรวจคือ (ก) ชั้นที่ action เรียกจริงทั้งหมด — `computeDocTotals` · `createDocument`/`updateDocument`/`issueDocument` · `applyEditorExtras` · `assertEditorRefs` · `assertAccountCan` (ข) "สายไฟ" ของ action แบบ static (E4) · การพิสูจน์ end-to-end บนเบราว์เซอร์จริงเป็นหน้าที่ `visual-acc-v2.mts 1.3`

## 4. ชุดภาพจริง: `scripts/visual-acc-v2.mts 1.3`
`bash scripts/acc-v2-serve.sh` → `QC_ENV_FILE=.env.qc pnpm tsx scripts/visual-acc-v2.mts 1.3 --assert` → `bash scripts/acc-v2-serve.sh stop`
- **fixture ในตัวสคริปต์**: สร้าง "ร่าง" ใบแจ้งหนี้คุณณัฐพล 3 บรรทัดตาม g1 ผ่าน `createDocument` + `applyEditorExtras` (service ตัวเดียวกับที่ action เรียก) แล้ว **ลบทิ้งใน `finally`**
  🔴 ห้ามให้ร่างนี้ค้าง: ตัวนับแท็บของ WO 1.1 (เฉลย IV ทั้งหมด 51 · ร่าง 3) จะเพี้ยนเป็น 52/4 แล้ว `qc-acc-v2-list` ตกทันที — ทดสอบแล้วว่าเกิดจริงและลบแล้วกลับมา 144/144
- หน้าที่ถ่าย (เดสก์ท็อป 1440 + มือถือ 390):
  | ชื่อ | path | เทียบกับ |
  |---|---|---|
  | `invoice-form-new` | `docs/INVOICE/new` | g1 (ฟอร์มเปล่า) · g17 มือถือ |
  | `invoice-form` | `docs/INVOICE/<fixtureDraftId>/edit` | **g1-invoice-form.png** (ตัวเลขจริง 24,900) · g17 |
  | `invoice-form-menu` | เดิม + คลิก `[data-testid="btn-approve-menu"]` | **g1-invoice-form-menu.png** |
- `--assert` ตรวจ: `tot-sub`=฿23,271.03 · `tot-vat`=฿1,628.97 · `tot-grand`=฿24,900.00 · `tot-wht`=594.00 · `tot-due`=฿24,306.00 · `tot-words`=สองหมื่นสี่พันเก้าร้อยบาทถ้วน · มี `doc-editor-v2` · h1 มี "สร้างใบแจ้งหนี้"/"แก้ไขใบแจ้งหนี้" · แถบปุ่มท้ายเห็นบนจอ · **รายการ 3 บรรทัด** (เดสก์ท็อปนับ `line-<i>` · มือถือนับ `line-m-<i>`) / ฟอร์มเปล่า 1 บรรทัด · **`scrollWidth ≤ 390` บนมือถือ** · เมนูอนุมัติเปิดจริง + มีครบ 4 ตัวเลือก
- ยังไม่รัน (ต้อง `next build` ซึ่งใบสั่งงานห้าม agent ตัวนี้ทำ) — **Fable รันแล้วดูภาพคู่ mockup ก่อนปิด WO** · สคริปต์รันแบบไม่มีเซิร์ฟเวอร์แล้ว 2 รอบเพื่อพิสูจน์ว่าขั้น seed/cleanup ทำงาน (ได้ id ร่าง + ลบทิ้งครบ + session เหลือ 0)

## 5. ผลรัน (verbatim)
```
$ pnpm typecheck
> tsc --noEmit
(ไม่มี error)

$ pnpm fitness
===== FITNESS =====
ผ่าน 17/17
FINDINGS: CRITICAL 0 · MAJOR 0 · MINOR 0

$ QC_ENV_FILE=.env.qc pnpm tsx scripts/qc-acc-v2-editor.mts
===== สรุป: ผ่าน 172 · ไม่ผ่าน 0 =====
🎉 WO 1.3 ผ่านทั้งหมด

$ pnpm tsx scripts/qc-acc-v2-nav.mts
===== สรุป: ผ่าน 303 · ไม่ผ่าน 0 =====
🎉 WO 0.4 nav.ts ผ่านทั้งหมด

$ pnpm tsx scripts/qc-nav-functions.mts
  ✅ ผ่านทั้งหมด — 9 เช็ก

$ QC_ENV_FILE=.env.qc pnpm tsx scripts/qc-acc-v2-list.mts     (ยืนยันว่า WO 1.1 ไม่พัง)
===== QC WO 1.1 =====
✅ ผ่านทั้งหมด — 144 เช็ก

$ QC_ENV_FILE=.env.qc pnpm tsx scripts/qc-acc-v2-cheap-routes.mts
===== สรุป WO 1.2 =====
ผ่าน 105 · ล้ม 0

$ DATABASE_URL=… DIRECT_URL=… pnpm qc:account      (env ผ่าน grep|cut จาก .env.qc)
===== QC6 CPA audit (service layer จริง + Neon) =====
ผ่าน 107/107 ข้อตรวจ
FINDINGS: CRITICAL 0 · MAJOR 0 · MINOR 0
```

## 6. checklist ✓/✗ เทียบภาพ (ตรวจจากโค้ด — ✓ ภาพจริงรอ Fable)
| # | g1 / g17 | สถานะ |
|---|---|---|
| 1 | หัวหน้า "สร้างใบแจ้งหนี้ + เลขที่ + ร่าง" | ✓ |
| 2 | stepper QT ✓ / IV ปัจจุบัน / RE / TX + คลิกขั้นที่ออกแล้วได้ | ✓ |
| 3 | การ์ด "ส่วนหัวเอกสาร" + วงกลม ✓ เมื่อครบ | ✓ |
| 4 | 8 ช่องหัวเอกสาร ลำดับตรง g1 | ✓ |
| 5 | ป้าย "สมาชิก · ค้างรับ ฿…" ใต้ผู้ติดต่อ | ✓ |
| 6 | toggle ใบกำกับ 2 อัน (ซ่อนเมื่อไม่จด VAT) | ✓ |
| 7 | แท็ก chip + "+ เพิ่มแท็ก" | ✓ |
| 8 | ตารางรายการ 10 คอลัมน์ + ⠿ ลากเรียง + 🗑 | ✓ |
| 9 | "+ เพิ่มรายการ" + "รายการโปรด ▾" | ✓ (แก้รอบนี้ให้โผล่เสมอ + บันทึกได้) |
| 10 | บล็อกสรุปยอด ลำดับ + กล่องดำ + ตัวอักษรไทย | ✓ |
| 11 | หมายเหตุลูกค้า / หมายเหตุภายใน | ✓ |
| 12 | dropzone แนบไฟล์ + การ์ดไฟล์ | ✓ |
| 13 | แถบปุ่มท้าย: ยกเลิก · บันทึกร่าง ▾ · อนุมัติ ▾ | ✓ |
| 14 | g1-menu: เมนูอนุมัติ 4 ตัวเลือก + หัว "เลือกการอนุมัติ" | ✓ |
| 15 | เงินมัดจำ (g1 มีการ์ด) | ✗ = WO 1.4 (ตามใบสั่งงาน) |
| 16 | g17 accordion ทุก section | ✓ |
| 17 | g17 การ์ดรายการต่อบรรทัด + ยอดท้ายการ์ด | ✓ |
| 18 | g17 แถบยอดติดล่าง (ทั้งสิ้น / ต้องชำระ / chevron) | ✓ (แก้รอบนี้ให้ sticky + กางได้) |
| 19 | g17 ปุ่มท้าย 2 ปุ่ม + ⋯ | ✓ (บันทึกร่าง + อนุมัติ · "⋯" คือ dropdown ในตัวปุ่ม) |
| 20 | g17 "เพิ่มเติม: อ้างอิง · สกุลเงิน · ใบกำกับ · พนักงานขาย" | ✓ |

## 7. ค้างส่งต่อ
- Fable: `bash scripts/acc-v2-serve.sh` → `visual-acc-v2.mts 1.3 --assert` → ดูภาพคู่ g1/g1-menu/g17 → commit
- WO 1.4: ส่วน D/F + ปลด "หักเงินมัดจำ" ให้แก้ได้ + เมนู ⋯ ต่อการ์ดรายการบนมือถือ
- WO 1.6: wizard 2 ขั้นของ CN/DN/CNR/DNR → แล้วค่อยเปลี่ยน `#new` ที่เหลือ 4 ชนิดเป็น `/new` และเลิกใช้ `DocEditor`/`ExpenseEditor`/`CreateSection`
