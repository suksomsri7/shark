# WO 9.3 — ประสิทธิภาพ + อัปเดต next 16.2.11 (เฟส 9)

> บันทึกกู้คืน: อัปเดตทุกขั้น · ถ้า agent ตาย ให้ตัวใหม่อ่านไฟล์นี้แล้วทำต่อจาก "ขั้นที่ถึง"

## ขั้นที่ถึง
- [x] อ่านกติกา + สำรวจ repo (git clean · HEAD 0bffc0d = WO 9.2)
- [x] **Part 1 เสร็จ**: bump next 16.2.10 → 16.2.11 (package.json + pnpm-lock.yaml)
      · audit --prod: ก่อน 32 (1 low/14 mod/17 high) → หลัง 21 (1 low/9 mod/11 high) · next advisory 9 ข้อ → 0
      · typecheck 0 · fitness 17/17 (มี/ไม่มี .env) · next build ผ่าน · visual 1.1 ผ่านหมด · 9.1 ผ่านหมด
      · visual 2.2 ตก 10 — **ไม่ใช่เพราะ next** พิสูจน์แล้ว: home 87/87 + dashboard 174/174 เขียวบน DB ก้อนเดียวกัน
        และ dashboard.ts พิมพ์ ฿591,401.88 ตรงกับที่หน้าแสดง · ต้นเหตุ = ข้อสอบเน่า (ฮาร์ดโค้ดเลข seed ยุค WO 2.2)
        → แก้ visual-acc-v2.mts ให้อ่านจาก acc-v2-expected.json แทน (5 จุด)
- [x] **Part 2A เสร็จ**: `scripts/qc-acc-v2-perf.mts` (P1 งบ query · P2 เวลา · P3 N+1 · P4 pagination · P5 รูป CDN)
      ผลตั้งต้น (ก่อนแก้): ผ่าน 64 · ตก 18 · **เวลาทุกหน้า < 250 ms อยู่แล้ว** (ปัญหาคือ "จำนวน query" ล้วน ๆ)
      เก็บผลตั้งต้นไว้ที่ /tmp/93-perf-before.txt (PERF_DUMP=1 พิมพ์ SQL จริงของหน้าที่เกินงบ)
- [x] **Part 2B เสร็จ** (ดูรายการ "แก้แล้ว" ข้างล่าง) — perf 92/92
- [x] **Part 2C**: เพิ่ม 2 ดัชนี (`AccountContact` / `AccountAttachment` (systemId, archivedAt, createdAt))
      migration `20260915000000_account_v2_perf_indexes` · deploy ลง DB QC แล้ว · drift สะอาด
      🔴 ที่ใบสั่งงานเสนอมาอีก 6 ตัว **มีอยู่แล้ว**จาก WO ก่อนหน้า (ตรวจแล้วทีละตัว) → ไม่เพิ่มซ้ำ
- [x] **Part 2D**: `listWhtCertsV2` + `listChequesV2` เดิม **ไม่มี take เลย** (ดึงทั้งตารางมา slice ฝั่ง JS)
      → ตัดหน้า+รวมยอดที่ DB เมื่อไม่มีคำค้น · มีคำค้นยังใช้ทางเดิม (คำค้นครอบ sourceDocNo ที่ไม่มี relation
      + ชื่อใน contactSnapshot JSON — ดันลง SQL แล้วผลเปลี่ยน) · แนะนำ 10.x: denormalise 2 คอลัมน์นี้
- [x] **Part 2E**: `loading="lazy" decoding="async"` 6 จุด (AttachmentGrid · InboxCard · DocAttachments ·
      documents/page · products/page · print รูปสินค้า) · โลโก้/ตราประทับ/ลายเซ็นบนใบพิมพ์ **ไม่ lazy** (หัวกระดาษ)
      · thumbUrl มาจาก SHARK_BUNNY_CDN อยู่แล้ว (storage/service.ts สร้าง cdnUrl ที่เดียว) — ตรวจแล้วไม่มี storage origin หลุด
      · **ไม่มี route ดาวน์โหลดของบัญชี** (ไฟล์เสิร์ฟจาก CDN ตรง) ⇒ ไม่มีที่ให้ใส่ Cache-Control
- [x] **Part 2F**: `visual-acc-v2.mts` ไม่มีตัวจับเวลา → เพิ่มแบบเปิดด้วย `PERF_TIMING=1` (โหลด 2 รอบ วัดรอบที่ 2) + ตารางสรุป
- [x] **Part 2G เสร็จ**: ด่าน 26 ชุด + typecheck + fitness ×2 + drift + CPA + visual 3 ชุด **เขียวหมด** (บรรทัดจริงท้ายไฟล์)

## แก้แล้ว (Part 2B)
- `service.computeListTabCounts` — count() ที่ 3 = ตัวกรองเดียวกับ groupBy ที่ 2 → รวมยอดใน JS (−1 query/หน้ารายการ)
- `service.listDocPayments` — select ซ้อน relation nullable (`financeAccount`/`cheque`) ทำให้ Prisma ยิง
  `WHERE id IN (NULL)` เปล่า ๆ 2 คำสั่งทุกครั้ง → ดึง FK แล้วค่อยหาแบบมีด่าน `.length`
- `doc-settings.docNumberingRows` — อ่านแถว AccountSettings 2 ครั้ง (getDocSettings + branchCode) → เหลือ 1
- `doc-settings.listDocTypeAccounts` — `include: { account }` ยิง `IN (NULL)` เมื่อไม่มี mapping → guard
- `permissions-service.listAccountUsers(ctx, preloaded?)` + หน้า settings/permissions ส่งต่อ → −1
- **8.3 ค้าง**: `recordPayment` emit 2 event = 4 คำสั่งใน tx ที่ล็อกแถวอยู่ → `emitOutboxMany` (createMany
  + skipDuplicates อาศัย @@unique(tenantId,idempotencyKey) ตัวเดิม) = **1 คำสั่ง** · event ยังแยก 2 ชนิดเหมือนเดิม
- **9.2 ค้าง**: `qc-acc-v2-contact-merge` คืนสภาพล้มเงียบ → เก็บ stdout/stderr มาโชว์ · ตรวจซ้ำว่าเฉลย
  กลับมาครบ + คู่ซ้ำ TAX_ID กลับมา (idempotent) · ล้ม = **exit 2** พร้อมวิธีแก้ (เดิม exit 0 เงียบ ๆ)

## 🐞 ที่เจอแล้ว
1. **ข้อสอบเน่า (visual-acc-v2.mts WO 2.2)** — ฮาร์ดโค้ด ฿486,300 / ฿212,750 / ฿524,308.42 / agingSum 486300 /
   pendingRowCount===1 ซึ่งเป็นสแนปช็อตของ seed ตอน WO 2.2 · WO หลัง ๆ เติม WHT_CERT/มัดจำ/ใบรอตรวจ → เลขจริง
   ขยับเป็น ฿494,300 / ฿214,750 / ฿591,401.88 / 494300 / 2 · **หน้าไม่ผิด ข้อสอบผิด** · แก้ให้อ่านเฉลยอิสระ
   (บทเรียนเดียวกับที่ ACCOUNT-V2-RUN จดไว้ 4 ก.ย. — fitness F11.1 จับแค่ "วันที่" ไม่จับ "เลขจาก seed")

## บันทึก
- เริ่ม 4 ก.ย. ~20:55 UTC


## 🐞 บั๊กจริงที่เจอเพิ่มระหว่าง 9.3 (ปิดแล้วทุกตัว)
2. **ลำดับรายการรับชำระไม่นิ่ง (ของจริง ไม่ใช่แค่ข้อสอบ)** — `listDocPayments` / `listWhtCredits` เรียงด้วย
   `paidAt` อย่างเดียว แต่ `paidAt` เก็บเป็น "วันที่" (เที่ยงคืน) ⇒ รับชำระ 2 ครั้งในวันเดียวกัน = ค่าเท่ากันเป๊ะ
   ไม่มีตัวตัดสิน → Postgres คืนลำดับตามใจ · ผู้ใช้เห็นตารางการชำระ **สลับแถวเอง**ระหว่างโหลด
   นี่คือต้นเหตุจริงของ "payments flaky" ที่ 1.7 / 8.1 / 8.3 จดค้างไว้ (ไม่ได้เกี่ยวกับ groups เลย)
   แก้: เพิ่มตัวตัดสิน `createdAt, id` ทั้ง 2 ที่ + ข้อสอบเลือกแถวจาก "มี/ไม่มีภาษีถูกหัก" แทน index
   ยืนยัน: payments 162 เขียว 4 รอบเดี่ยว + 1 รอบต่อจาก groups
3. **🔴 seed พังเงียบ = คิว outbox ตัน** — `scripts/seed-acc-v2-qc.mts` เรียก `drainAll()` **ครั้งเดียว**
   ซึ่งระบายได้สูงสุด 50 event/รอบ · พอ WO 8.3 เพิ่ม `account.payment.recorded` / `account.invoice.paid`
   seed จึงมี event ค้างคิวก่อนถึงคิว POS ~61 ใบ ⇒ 2 event ของบิลขายหน้าร้าน (ใหม่สุด) ไม่เคยถูกระบาย
   → ไม่มีเอกสาร TAX_INVOICE_ABB → seed ตายที่ `assertEq "ได้ 0 ต้องการ 2"`
   **นี่คือต้นเหตุของ "contact-merge คืนสภาพล้ม" ที่ 9.2 จดไว้** (ด่านดัง ๆ ที่เพิ่งใส่จับได้ทันทีรอบแรก)
   แก้: seed ระบายคิวเป็นวงจนหมดจริง + เพดาน 50 รอบกันวนไม่รู้จบ
   (บทเรียนเดิม 30 ส.ค. "event ใหม่ต้องมีตัวรับ" — รอบนี้มีตัวรับครบ แต่ **ปริมาณ** ทำให้คิวตันแทน)


## ด่านที่รันจริง (4 ก.ย. ปลาย ~23:4x UTC · DB ep-plain-art · ไม่ commit)
typecheck 0 · fitness 17/17 (มี .env.qc / ไม่มี env เลย) · drift: No difference detected
qc-acc-v2-perf **92/92 (ชุดใหม่)** · security 298 · guard 173 · dashboard 174 · home 87 · overview 73
contacts 49 · list 159 · detail 85 · attachments 66 · products 100 · wht-cheque 69 · reconcile 109
inbox 128 · doc-settings 116 · permissions 160 · journal 94 · groups 174 · seed-check 279 · schema 61
payments **162** (เดี่ยว 4 รอบ + รอบต่อจาก groups = เขียวทุกรอบ) · contact-merge 56 (รัน 2 รอบติดกัน = idempotent)
CPA 107 · visual 2.2 / 1.1 / 9.1 ผ่านทั้งหมด

## เวลาโหลดหน้าจริง (PERF_TIMING=1 · รอบที่ 2 · งบ 1,500 ms)
วัด 27 การโหลด (เดสก์ท็อป+มือถือ) — **เกินงบ 0 หน้า** · ช้าสุด hub เดสก์ท็อป 748 ms
หน้ารายการทุกหน้า 198–429 ms · ฟอร์ม/แผ่นมือถือ 207–493 ms

## ยังไม่ได้ทำ / ส่งต่อ (Fable ตัดสิน)
- **หน้ารายละเอียดเอกสาร 19 query (งบ 12)** — 9.3 ตัดได้ 22→19 (relation nullable ที่ยิง `IN (NULL)`)
  ที่เหลือคือเรียกทีละทอด 6 ทอด ต้องรื้อเป็นก้อนเดียว = **งานรื้อ ควรเป็น WO แยก** ไม่ใช่งานเก็บกวาด
- **หน้าหลัก 19 query** — ตัว dashboardSnapshot เองอยู่ที่ 12 พอดี (ตามตัวอักษร BLUEPRINT) · ส่วนเกิน
  = เช็กลิสต์เริ่มต้น 5 คำสั่ง ซึ่งยิงทุกครั้งแม้ร้านทำครบแล้ว → เก็บสถานะ "ทำครบ" ลงตั้งค่าจะตัดได้ 5
- **สมุดรายวัน 9 (งบ 7)** · **ผู้ติดต่อ 12** · **กระทบยอด 10** · **ตั้งค่าเอกสาร 7** — เหตุผลอยู่ในตัวข้อสอบ
  (`budgetWhy` ของแต่ละเคส) และถูกพิมพ์รวมท้ายรายงานทุกครั้งที่รัน — ทั้งหมดติดที่ "สคีมาไม่มี relation"
  (refId/postedById/contactId เป็น scalar) ⇒ ลงต่ำกว่านี้ต้อง denormalise คอลัมน์ชื่อ/เลขที่ → เสนอ 10.x
- **คำค้นของ /wht + /cheque ยังโหลดทั้งชุด** (ตอนพิมพ์ค้นหาเท่านั้น · ไม่พิมพ์ = ตัดหน้าที่ DB แล้ว)
  เพราะคำค้นครอบ `sourceDocNo` (ไม่มี relation) + ชื่อใน `contactSnapshot` JSON → ต้อง denormalise เช่นกัน
- **ดัชนี `AccountAttachment (systemId, archivedAt, createdAt)`** — รูป query พิสูจน์แล้วว่าวิ่งจริง 5 ครั้ง/หน้า
  แต่ **EXPLAIN บนชุด QC พิสูจน์ประโยชน์ไม่ได้** (ตารางมี 6 แถว planner เลือก Seq Scan ถูกแล้ว) — เก็บไว้เพราะ
  ตารางนี้โตเร็วสุดในโมดูล · ถ้า Fable อยากได้เฉพาะที่พิสูจน์ได้ ให้ถอดตัวนี้ตัวเดียว
- **visual WO 0.1 ตก 4 ข้อ (HTTP 307)** — ไม่ได้อยู่ในรายการด่านของ 9.3 และ **ไม่ได้เกิดจาก 9.3**
  (หน้า hub เดียวกันใน visual 2.2 ผ่านหมด) · น่าจะ spec ของ 0.1 ชี้ path ที่ตอนนี้ redirect → ให้ 10.x กวาด
