# WO 5.5 — PromptPay จ่ายจากลิงก์ → รับชำระ + JV + กระทบยอดอัตโนมัติ

> บันทึกกู้คืน (recovery log) — อัปเดตทุกขั้น

## สถานะ
- ขั้นที่ถึง: 1 — สำรวจโค้ดเดิม (เริ่ม 4 ก.ย. 2026)
- ฐาน: branch `session/accounting` commit f93a352 (WO 5.4)

## ขั้นตอน
- [ ] 1. สำรวจ: beam.ts · webhook route · PaymentProfile.promptpayId · AccountFinance · service.recordPayment · reconcile.autoMatch · outbox · notification
- [ ] 2. Schema + migration `..._account_v2_promptpay`
- [ ] 3. payment-request.ts (logic)
- [ ] 4. webhook routing
- [ ] 5. UI (doc page modal + /pay/[token])
- [ ] 6. seed + expected + qc-acc-v2-promptpay.mts
- [ ] 7. visual 5.5
- [ ] 8. ด่านทั้งหมด

## บันทึก

### ขั้น 1 สำรวจ (เสร็จ)
ของเดิมที่ **นำมาใช้ซ้ำ ห้ามเขียนใหม่**:
- `src/lib/payment/promptpay.ts` — `promptpayPayload({id, amountSatang})` + `crc16xmodem` + `isValidPromptPayId` (EMVCo ครบแล้ว ไม่ต้องเขียน CRC ใหม่)
- `src/components/PromptPayQr.tsx` — QR client (lib `qrcode` มีใน deps อยู่แล้ว ไม่เพิ่ม dep)
- `src/lib/payment/beam.ts` — `beamConfig/beamEnabled/createCharge/verifyWebhook`
- `src/app/api/payment/beam/webhook/route.ts` — ตรวจ HMAC + `creditFromCharge`
- `service.recordPayment` (WO 1.4 · idempotencyKey/WHT/fee) · `reconcile.autoMatch` · `access.writeAudit`
- หน้าสาธารณะแม่แบบ: `src/app/(store)/r/[token]/page.tsx` (ขอใบกำกับ) → ทำ `/pay/[token]` เลียนแบบ
- ไม่มี middleware ในรีโป ⇒ route สาธารณะไม่ต้องขึ้นทะเบียนที่ไหน
- ไม่มี helper LINE ทีมงานนอกโมดูลแชท ⇒ แจ้งเตือน = `AppNotification` + `selectAccountNotifyRecipients`

ตัดสินใจ (ต่างจาก WO — จดไว้):
- **ไม่รองรับ BILLING_NOTE (BN)**: เอกสารกลุ่มต้องผ่าน `group.recordGroupPayment` ซึ่งกระจายเป็น payment/JV
  หลายใบ ⇒ statement line ก้อนเดียวจับคู่ 1:1 กับ JV ไม่ได้ (คำมั่น "กระทบยอดอัตโนมัติ" จะพัง)
  รองรับ: INVOICE · DEPOSIT_RECEIPT · DEBIT_NOTE
- **ไม่ใช้ outbox**: การแจ้งเตือนทำหลัง commit ในเส้นเดียวกัน (เพิ่ม event = ต้องลงทะเบียน consumer ไม่งั้นคิวตัน)
- seed: สร้าง **ใบแจ้งหนี้ใหม่ 1 ใบ** ให้คำขอที่จ่ายแล้ว (แบบเดียวกับ CREDIT_FIX ของ WO 5.4) เพื่อไม่ให้
  ค้างรับ/พ้นกำหนดขยับ · เงินเข้า **EWL001 (พร้อมเพย์)** ไม่ใช่ BSV001 (statement 5.3 freeze) / CSH001 (WHT 5.4)

### ขั้น 2 Schema (เสร็จ)
- `prisma/schema/account.prisma`: enum `AccountPaymentRequestMethod`/`AccountPaymentRequestStatus`
  · model `AccountPaymentRequest` · `AccountDocumentPayment.paymentRequestId` (nullable) + index
  · back-relation `AccountDocument.paymentRequests`
- migration `prisma/migrations/20260907000000_account_v2_promptpay/migration.sql` — additive ล้วน
  (CREATE TYPE ×2 · ADD COLUMN ×1 · CREATE TABLE ×1 · index 6 · FK 1) · apply บน DB QC แล้ว (`migrate deploy` ผ่าน)
- ⚠️ ยังไม่ลง prod

### ขั้น 3–5 โค้ด (เสร็จ · typecheck + fitness 17/17 เขียว)
ไฟล์ใหม่:
- `src/lib/modules/account/payment-request.ts` — createPaymentRequest · listPaymentRequests ·
  cancelPaymentRequest · getPublicPaymentPage · handleBeamPaid · handleBeamFailed ·
  confirmStaticPaymentRequest · settleStaticRequestsFromStatement (ตะขอของ autoMatch) ·
  expireRequests · pendingStaticRequestCount · `beamAdapter` (ให้ข้อสอบเสียบ createCharge ปลอม)
- `src/components/account-v2/PayLinkButton.tsx` — ปุ่ม + โมดัล QR/ลิงก์
- `src/app/(store)/pay/[token]/page.tsx` — หน้าสาธารณะ `/pay/<token>`

ไฟล์ที่แก้:
- `service.ts` — `recordPayment` รับ `paymentRequestId` + ตัวเข้าถึงที่ยังไม่รู้ร้าน 3 ตัว
  (`findPaymentRequestById` · `findPaymentRequestByToken` · `expirePaymentRequestsAll`)
- `reconcile.ts` — `autoMatch` เรียก `settleStaticRequestsFromStatement` **ก่อน** `listSystemEntries`
- `payment-actions.ts` — createPaymentRequestAction · listReceiveChannelsAction ·
  confirmPaymentRequestAction · cancelPaymentRequestAction (ด่าน `account.payment.record` ทุกตัว)
- `account/index.ts` — facade `handleAccountCharge` / `getPublicPaymentPage` / `expirePaymentRequests`
- `api/payment/beam/webhook/route.ts` — แยกทางตามคำนำหน้า `acc:` (เติมเครดิต AI เดิมไม่กระทบ)
- `doc-detail.ts` + `DocDetailPage.tsx` — ปุ่ม "ลิงก์ชำระเงิน" + ตารางคำขอ + ยืนยัน/ยกเลิก
- `scope.ts` — ลงทะเบียน `AccountPaymentRequest: sys()`
- `scripts/acc-v2-cron-recurring.mts` — โหมด `promptpay` (ปิดลิงก์หมดอายุ)

บทเรียนระหว่างทาง (สำคัญ):
1. fitness F5.1 (raw prisma ในโมดูลห้ามเพิ่ม) → payment-request.ts ห้าม `import { prisma }`
   ⇒ ทางเข้าที่ไม่รู้ร้านย้ายไป service.ts ที่เหลือใช้ `tenantDb`
2. fitness F10.1 พังเพราะ `import { env } from "@/lib/env"` — `@/lib/env` ตรวจ env **ตอน import**
   และไฟล์นี้ถูกดึงผ่าน `account/index` → `ai/tools` ⇒ ใช้ `process.env.APP_URL` ตรง ๆ แทน

ไม่ได้ทำ (ตั้งใจ):
- ไทล์ "รอยืนยันรับเงิน" บนหน้าหลัก (WO บอกว่าเป็นตัวเลือก) — `PendingTasks` มี 2 implementation
  (service.ts + dashboard.ts) ที่ข้อสอบบังคับให้เท่ากันเป๊ะ + เฉลยใน expected ⇒ ไม่ "ถูก" อย่างที่ WO คิด
  มีฟังก์ชัน `pendingStaticRequestCount()` เตรียมไว้ให้แล้ว

### ขั้น 6 seed + เฉลย (เสร็จ)
บล็อก 8.8 ใน `scripts/seed-acc-v2-qc.mts`:
- ① คำขอโหมด Beam **จ่ายแล้ว**: ใบแจ้งหนี้ใหม่ 1 ใบ (บริษัท ภูเก็ตดีปซี · 2026-09-02 · ฿7,490.00 =
  700,000 + VAT 7%) → `createPaymentRequest` (เสียบ `beamAdapter` ปลอม) → `handleBeamPaid` จำลอง webhook
  → เอกสาร PAID + payment (ช่องทาง PROMPTPAY · คีย์ `pp:qc_beam_charge_5_5`) + JV สมดุล
- ② คำขอโหมด QR นิ่ง **รอชำระ**: ผูกใบเดิม `fixtures.invNattapholId` (฿24,900) ⇒ เงินไม่ขยับ
- เงินเข้า **EWL001** เท่านั้น (BSV001 = statement 5.3 freeze · CSH001 = WHT 5.4)

ตัวเลขเฉลยที่เปลี่ยน (คำนวณจากค่าคงที่ ไม่พิมพ์มือ):
| ค่า | เดิม | ใหม่ |
|---|---|---|
| `finance.EWL001` / `financeGroups.E_WALLET` / `financeAccounts[EWL001].balance` | 26,586,000 | **27,335,000** (+749,000) |
| `finance.total` (เงินรวมทุกช่องทาง) | 132,224,000 | **132,973,000** (฿1,329,730.00) |
| `invoiceTabs.all` | 56 | **57** |
| `invoiceTabs.paid` | 32 | **33** |
| `promptPay` (คีย์ใหม่) | — | id/token/chargeId/ยอด ของทั้ง 2 คำขอ |
คีย์ `dashboard` · `overview` · `contacts` · `contactProfile` · `contactsOverview` สร้างใหม่โดยสคริปต์เฉลย
(ค้างรับ/พ้นกำหนด/ค้างจ่าย **ไม่เปลี่ยน** เพราะใบใหม่จ่ายครบทันที)

🔴 บั๊กจริงที่เจอตอนทำ seed-check: `AccountDocumentPayment.entryId` **เป็น null เสมอ** — `gl.postPayment`
ไม่ได้เขียนกลับ · ตัวจับคู่ WO 5.3 ใช้ `entry.refType/refId` ⇒ `linkPaymentToStatement` แก้ให้หา JV
ทาง refType/refId เหมือนกัน (ถ้าไม่เจอบั๊กนี้ การจับคู่อัตโนมัติจะเงียบ ๆ ไม่ทำงานเลย)

`qc-acc-v2-seed-check.mts` เพิ่มหมวด N (15 ข้อ) → **129/129** (เดิม 114)

### ขั้น 7 ข้อสอบ (เสร็จ)
`scripts/qc-acc-v2-promptpay.mts` — **82/82** (PP1–PP16 · เขียนใน "ร้านทิ้ง" ลบท้ายสคริปต์)
- ตั้ง `process.env.BEAM_*` เองในหน่วยความจำ (ไม่แตะ `.env`/`.env.qc`) เพื่อให้ HMAC ทำงานจริง
  + เสียบ `beamAdapter.createCharge` ปลอม ⇒ **ไม่มีการยิงเน็ตออกไปหา Beam**
- CRC16 + TLV ของ EMVCo เขียนใหม่ในข้อสอบ (เฉลยอิสระ ไม่ import จาก lib)
- ยิง webhook ผ่าน `POST` ของ route จริง (Request + ลายเซ็นจริง) ไม่ mock

🐞 บั๊กจริงที่ข้อสอบจับได้ (แก้แล้วในโค้ดผลิตภัณฑ์):
`dashboard.documentsIssued` ปัดสัดส่วนทีละแถว → ผลรวม 3 แถวย่อยได้ 100.01% (2553+1367+6081 = 10001 bp)
เมื่อชุดข้อมูลมีใบเพิ่ม 1 ใบ ⇒ แก้ให้แถวที่ยอดมากสุดกลืนเศษเมื่อแถวย่อยครอบคลุมยอดทั้งหมด
(**ไม่** ผ่อนข้อสอบ — `qc-acc-v2-dashboard` P5.7 ยังเข้ม `Σ ≤ 10000`)

ผลด่านชุดที่เกี่ยว (รันจริงบน DB QC):
| ชุด | ผล |
|---|---|
| qc-acc-v2-promptpay | 82 · ตก 0 |
| qc-acc-v2-seed-check | 129 · ตก 0 |
| qc-acc-v2-payments (WO 1.4) | 161 · ตก 0 |
| qc-acc-v2-reconcile (5.3) | 109 · ตก 0 |
| qc-acc-v2-finance (5.1) | 59 · ตก 0 |
| qc-acc-v2-finance-overview (5.2) | 45 · ตก 0 |
| qc-acc-v2-wht-cheque (5.4) | 69 · ตก 0 |
| qc-acc-v2-detail (1.5) | 85 · ตก 0 |
| qc-acc-v2-dashboard (2.1) | 174 · ตก 0 (หลังแก้บั๊กปัดเศษ) |
| qc-acc-v2-home (2.2) | 87 · ตก 0 |
| qc-acc-v2-recurring (1.9) | 161 · ตก 0 |

### ขั้น 8 ภาพจริง (เสร็จ)
`scripts/visual-acc-v2.mts` เพิ่มแผน `"5.5"` (4 spec) + ตัวเทียบตัวเลข (`ASSERT["5.5"]`) → `--assert` **ผ่านทั้งหมด**
- `.qc-shots/acc-v2/5.5/invoice-pay-link-modal-desktop.png` — 1440 · โมดัลเปิด เห็น QR จริง + ยอด ฿24,900.00
  + ลิงก์ + คัดลอก + สถานะ/เงินเข้าที่/ใช้ได้ถึง + หมายเหตุ
- `.qc-shots/acc-v2/5.5/invoice-pay-requests-desktop.png` — แท็บ "การชำระเงิน" + การ์ดตารางลิงก์
- `.qc-shots/acc-v2/5.5/pay-public-desktop.png` / `-mobile.png` — หน้าสาธารณะ (390 + 1440) ล้นแนวนอน 0px
- `.qc-shots/acc-v2/5.5/pay-public-paid-desktop.png` / `-mobile.png` — สถานะ "จ่ายแล้ว"

🔴 แก้จากการ**ดูภาพจริง** (ไม่ใช่จากรายงาน): ตารางลิงก์ชำระเงินหัวคอลัมน์ชนกันเป็น "ยอดสถานะ"
→ ใส่ `table-fixed` + `colgroup` + `pr-4` แล้วถ่ายใหม่ยืนยันด้วยตา

### ขั้น 9 ด่านทั้งหมด (เสร็จ · เขียวครบ)
typecheck ✅ · fitness 17/17 ✅ · drift "No difference detected" ✅ · qc:account 107/107 ✅
guard 158 · schema 61 · nav 323 · seed-check 129 · promptpay 82 · payments 161 · reconcile 109 ·
finance 59 · finance-overview 45 · wht-cheque 69 · detail 85 · dashboard 174 · home 87 · recurring 161
ปิดเฟส 5: qc-pos-account 16/16 · qc-inventory-account 23/23 · qc-payment 16/16 · qc-ai-credit 32/32
**ยังไม่ commit** ตามกติกา — รอ Fable ตรวจภาพ + diff

### ขั้น 10 กวาดหาบั๊ก/ช่องโหว่ในดิฟฟ์ตัวเอง (เสร็จ)
แก้เพิ่ม 3 จุดหลังอ่าน diff ซ้ำ:
1. `createCharge` เป็น network call — เน็ตล่ม = **throw** ไม่ใช่คืน `{error}` ⇒ ครอบ `.catch()` แล้วถอย
   มาโหมด QR นิ่ง + ใช้ token ใหม่ (กันชนถ้าลบแถวร่างไม่สำเร็จ)
2. ตะขอ statement ↔ คำขอ QR นิ่ง เดิมจับเฉพาะ **±3 วันจากวันสร้าง** ⇒ ลิงก์อายุ 7–30 วันที่ลูกค้าจ่าย
   กลางทางจะไม่ถูกจับเลย (ลิงก์ยาวไร้ประโยชน์) — เปลี่ยนเป็นช่วง `[สร้าง−3 วัน … หมดอายุ+3 วัน]`
   + ข้อสอบ PP10.7a/b (ย้อนวันสร้างไป 10 วัน) กันถอยหลัง
3. `confirmPaymentRequestAction`/`cancelPaymentRequestAction` encode `docType`/`docId` ก่อนทำ path redirect
   (hidden field แก้ได้จากเบราว์เซอร์)

ที่ตรวจแล้ว **ไม่พบปัญหา**: หน้าสาธารณะไม่มีข้อมูลลูกค้า (PP3.4) · log ไม่มีข้อมูลลูกค้า ·
ยิงพร้อมกัน 2 webhook → unique(idempotencyKey) ที่ชั้น DB กันเงินซ้ำ (recordPayment คืน ok:false ใบที่สอง) ·
token 128 บิต + รูปแบบผิดไม่แตะ DB · ทุก action มี `assertAccountCan` · ทุก query ผูก tenant/system
ยกเว้น 3 ตัวเข้าถึงที่ตั้งใจไม่ผูก (webhook / ลิงก์สาธารณะ / cron) ซึ่งใช้ token/chargeId เป็น capability

ด่านหลังแก้: typecheck ✅ · fitness 17/17 ✅ · promptpay 84/84 ✅ · reconcile 109 ✅ · seed-check 129 ✅
