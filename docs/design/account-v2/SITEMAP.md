# SITEMAP — โครงเมนูโมดูลบัญชี V2 (9 หมวด)

> คู่กับแบบร่าง `docs/design/account-v2/mockup.html` (14 เฟรม) · งาน `WO-ACC-UI-01` · 2 ก.ย. 2026
> **เอกสารนี้คือ "สัญญา"** — ตอน coding ให้แก้ `src/lib/modules/account/nav.ts` ให้ตรงกับตารางนี้ที่เดียว
> แล้วอัปเดต `docs/UI_STANDARD.md §2.9/§4` ตาม (ของเดิมล็อกไว้ 8 หมวดแบบ SubNav sidebar 200px)
> สถานะอ้างจาก `ledger/ACCOUNT-INVENTORY.md` (gap matrix 119 ข้อ · ✅37 🟡31 ❌51)
>
> **เฟรมเดสก์ท็อปที่มีเมนู/แผงเปิดค้างไว้ (f3–f10) ถ่ายเป็น 2 ใบ:**
> `fN-<ชื่อ>.png` = หน้าเปล่า (ไว้ตรวจเนื้อหา) · `fN-<ชื่อ>-menu.png` = สถานะเปิด (ไว้ตรวจโครงเมนู)
> สลับด้วยคลาส `.no-menu` ใน `render.mjs` — เฟรมเดียวถ่าย 2 สถานะ ไม่ก๊อป markup ซ้ำ

## ป้ายสถานะ

| ป้าย | ความหมาย |
|---|---|
| ✅ มีแล้ว | มี route + ใช้งานได้จริงวันนี้ ย้ายที่อยู่ในเมนูเท่านั้น |
| 🔧 ปรับ | มีของอยู่แล้วแต่ต้องรื้อหน้า/เพิ่มความสามารถ (ค้นหา · ตัวกรอง · ตาราง · pagination) |
| ✨ เพิ่มใหม่ | ยังไม่มีหน้า/route (บางตัวมีโมเดลหรือ posting rule อยู่แล้ว ระบุในหมายเหตุ) |
| 🕓 เร็ว ๆ นี้ | โชว์ในเมนูแบบตัวจาง กดไม่ได้ |

`base` = `/app/sys/[id]/account`

---

## 1. หน้าหลัก

| รายการ | สถานะ | route | หมายเหตุ |
|---|---|---|---|
| หน้าหลัก (dashboard) | 🔧 | `/app/sys/[id]` → `AccountContent` (`ui.tsx:44`) | ของเดิมมีการ์ดสถิติ 4 + เอกสารล่าสุด 8 · ของใหม่เพิ่ม กราฟรายรับ-รายจ่าย 12 เดือน · โดนัทรายได้/ค่าใช้จ่าย · แถบอายุหนี้ 5 ช่วง · "เงินคุณอยู่ไหน" · "งานที่รอคุณ" · บัญชีเงินที่ติดตาม — ดูเฟรม **f1** |

ตัวเลขที่ต้องมี service รองรับ: `overviewStats` ✅ · `payableStats` ✅ (มีแล้วแต่ยังไม่มีใครเรียก) · `agingReport` ✅ · `financeBalances` ✅ · **ยอดรายเดือน 12 เดือน / โดนัทตามหมวด / "งานที่รอคุณ" ยังไม่มีฟังก์ชัน** ✨

## 2. รายรับ — เมนูย่อย 11 รายการ (เฟรม f2, f3)

| # | รายการ | สถานะ | route | หมายเหตุ |
|---|---|---|---|---|
| 1 | ดูภาพรวม | ✨ | `{base}/revenue` | ฝาแฝดของ f4 ฝั่งรับ (กราฟรายเดือน + ลูกค้า/สินค้าที่ทำเงินสูงสุด) |
| 2 | ใบเสนอราคา | ✅ | `{base}/docs/QUOTATION` | |
| 3 | ใบรับเงินมัดจำ | ✅ | `{base}/docs/DEPOSIT_RECEIPT` | |
| 4 | ใบแจ้งหนี้ (ใบส่งของ) | 🔧 | `{base}/docs/INVOICE` | ต้นแบบหน้ารายการใหม่ (f3): แท็บ+ตัวนับ · ค้นหา · ช่วงวันที่ · ผู้ติดต่อ · ตารางจริง · bulk · pagination — ต้องย้าย logic ลง `listDocuments` (ตอนนี้ `take:500` แล้วกรองฝั่ง UI) |
| 5 | ใบเสร็จรับเงิน | ✅ | `{base}/docs/RECEIPT` | |
| 6 | ใบกำกับภาษีขาย | ✅ | `{base}/docs/TAX_INVOICE` | ซ่อนถ้าไม่จด VAT (`vatRegistered`) |
| 7 | ใบวางบิล | 🔧 | `{base}/docs/BILLING_NOTE` | route มีแล้วแต่สร้างได้ทีละใบ — ต้องเพิ่ม picker เลือกหลายใบแจ้งหนี้ + ใช้ `AccountRelationType.BILL` (ตอนนี้ไม่มีโค้ดเขียน relation นี้เลย) |
| 8 | ใบลดหนี้ | ✅ | `{base}/docs/CREDIT_NOTE` | |
| 9 | ใบเพิ่มหนี้ | ✅ | `{base}/docs/DEBIT_NOTE` | |
| 10 | นำเข้าเอกสาร | ✨ | `{base}/docs/import` | ต่อยอด `core/csv.ts` + `CsvImport.tsx` |
| 11 | ลิงก์ขอใบกำกับ (QR) | ✅ | ปุ่มในหน้าเอกสาร → `ensurePublicLinkAction` · หน้าลูกค้า `/(store)/r/[token]` | SHARK มี PEAK ไม่มี — ยกขึ้นมาให้เห็นในเมนู |

## 3. รายจ่าย — เมนูย่อย 13 รายการ (เฟรม f4)

| # | รายการ | สถานะ | route | หมายเหตุ |
|---|---|---|---|---|
| 1 | ดูภาพรวม | ✨ | `{base}/expense/overview` | กราฟแท่งซ้อนสถานะชำระ · "จ่ายให้ใครมากที่สุด" (groupBy `contactId`) · "จ่ายค่าอะไรมากที่สุด" (groupBy `AccountLedger` EXPENSE/COGS) — ทั้ง 2 ยังไม่มีฟังก์ชัน |
| 2 | ใบสั่งซื้อ | ✅ | `{base}/po` | flow ส่งอนุมัติ/อนุมัติ/แปลงครบแล้ว |
| 3 | ใบจ่ายเงินมัดจำ | ✨ | `{base}/expense-doc/DEPOSIT_PAYMENT` | **posting rule ครบแล้ว** (`gl.ts:532`) + label/prefix DP + tabs — ขาดแค่ route |
| 4 | บันทึกซื้อสินค้า | ✅ | `{base}/purchase` | |
| 5 | บันทึกค่าใช้จ่าย | ✅ | `{base}/expense` | |
| 6 | ใบสั่งซื้อสินทรัพย์ | 🔧 | `{base}/po?docType=ASSET_PURCHASE_ORDER` | ใช้ได้แล้วแต่ **ไม่มีในเมนู** |
| 7 | ซื้อสินทรัพย์ | ✅ | `{base}/asset-buy` | |
| 8 | ใบกำกับภาษีซื้อ | 🔧 | `{base}/asset-buy?docType=PURCHASE_TAX_INVOICE` | ใช้ได้แล้วแต่ **ไม่มีในเมนู** · VAT ซื้อ 3 โหมด CLAIM/AWAITING/NO_CLAIM ครบ |
| 9 | รับใบลดหนี้ | ✨ | `{base}/expense-doc/CREDIT_NOTE_RECEIVED` | posting rule ครบ (`gl.ts:547`) ขาด route/ฟอร์ม |
| 10 | รับใบเพิ่มหนี้ | ✨ | `{base}/expense-doc/DEBIT_NOTE_RECEIVED` | posting rule ครบ (`gl.ts:556`) ขาด route/ฟอร์ม |
| 11 | ใบรวมจ่าย | ✨ | `{base}/expense-doc/COMBINED_PAYMENT` | มีแค่ enum+label+prefix CP · **ยังไม่มี posting rule และไม่มี relation `PAY_GROUP`** — งานใหญ่สุดของหมวดนี้ |
| 12 | นำเข้าเอกสาร | ✨ | `{base}/expense/import` | |
| 13 | AI ช่วยบันทึก (ถ่ายบิล) | ✅ | `src/lib/ai/proposals.ts:615` → `createExpenseDoc` (สร้างเป็น DRAFT ให้คนตรวจ) | SHARK มี PEAK ไม่มี |

## 4. ผู้ติดต่อ — เมนูย่อย 3 รายการ (เฟรม f5)

> **หมายเหตุการออกแบบ:** หมวดนี้ไม่กาง dropdown เพราะจุดที่ต้องให้เจ้าของตรวจคือคอลัมน์กลุ่มซ้าย
> กับแผงโปรไฟล์ที่เลื่อนเข้ามา · ทั้ง 3 รายการด้านล่างปรากฏในภาพเป็น **คอลัมน์กลุ่มซ้าย** แทน (กลุ่มมาตรฐาน / กลุ่มกำหนดเอง)
> ดูภาพ 2 ใบ: `f5-contacts.png` (ตารางเต็ม เห็นคอลัมน์ยอดค้าง/เอกสารล่าสุด/ทำรายการ + pagination)
> และ `f5-contacts-menu.png` (แผงโปรไฟล์เลื่อนเข้า + เมนูทำรายการเปิด)

| # | รายการ | สถานะ | route | ปรากฏใน f5 ที่ |
|---|---|---|---|---|
| 1 | ผู้ติดต่อทั้งหมด | 🔧 | `{base}/contacts` | ตารางหลัก + ปุ่ม "ทำรายการ ▾" ต่อแถว + แผงโปรไฟล์ |
| 2 | กลุ่มกำหนดเอง | ✨ | `{base}/contacts?group=<id>` | หัวข้อ "กลุ่มกำหนดเอง + เพิ่ม" (ดีลเลอร์ · โรงแรมพันธมิตร · ลูกค้าต่างชาติ) — ต้องเพิ่มตาราง `AccountContactGroup` + join |
| 3 | ลูกค้าประจำ | ✨ | `{base}/contacts?group=loyal` | บรรทัด "ลูกค้าประจำ 12 · ซื้อตั้งแต่ 3 ครั้ง/ปี" — คำนวณจากจำนวนเอกสาร ไม่เพิ่ม field |

ของเสริมในหน้า (เป็นปุ่ม ไม่ใช่เมนู): เพิ่มผู้ติดต่อ ✅ · **แก้ไข** 🔧 (`updateContactAction` มีอยู่แล้วแต่ไม่มีปุ่มเรียก) · เพิ่มเข้ากลุ่ม ✨ · นำเข้า ✨ · พิมพ์รายงาน ✨ · ค้นหา ✨ (`listContacts` ยังไม่มีพารามิเตอร์ `q`) · pagination ✨

## 5. สินค้า — เมนูย่อย 7 รายการ (เฟรม f6)

| # | รายการ | สถานะ | route | หมายเหตุ |
|---|---|---|---|---|
| 1 | สินค้า/บริการ | 🔧 | `{base}/products?tab=catalog` | ต้องเปลี่ยนจากข้อความบรรทัดเดียวใน `<details>` เป็นตารางจริง + แท็บชนิด + ค้นหา + pagination |
| 2 | หน่วยนับ | ✅ | `{base}/products?tab=units` | |
| 3 | ใบเบิกสินค้า | ✅ | `{base}/goods-issue` | |
| 4 | ใบส่งคืนเบิกสินค้า | 🔧 | `{base}/goods-issue?docType=GOODS_ISSUE_RETURN` | ทำได้แล้วผ่าน toggle ในฟอร์ม — **แยกเป็นเมนูของตัวเอง** |
| 5 | ใบปรับต้นทุนสินค้า | ✨ | `{base}/products/cost-adjust` | ยังไม่มี docType และไม่มีช่องต้นทุนคงเหลือบน `AccountProduct` (มีแค่ `buyPrice`) — ต้องเคาะว่าจะทำในบัญชีหรือชี้ไปโมดูล `inventory` |
| 6 | นำเข้าสินค้า | ✨ | `{base}/products/import` | ลอกจาก `modules/inventory` ที่ทำแล้ว |
| 7 | ทะเบียนสินทรัพย์ *(ลิงก์ข้ามหมวด)* | ✅ | `{base}/assets` | **ย้ายบ้านไปหมวด "บัญชี"** ตามผัง PEAK — เหลือลิงก์ข้ามไว้ที่นี่กันคนหาไม่เจอ |

แท็บในหน้า (ไม่ใช่เมนู): สินค้า / บริการ / **รายการจัดชุด ✨** (`{base}/products?tab=bundle` — ต้องเพิ่ม `AccountProductBundleItem`) · ทั้งหมด / ปิดใช้งาน 🔧 · **สินค้าที่ติดตาม ✨** (ต้องเพิ่ม flag บน `AccountProduct`)

## 6. การเงิน — เมนูย่อย 9 รายการ (เฟรม f7)

| # | รายการ | สถานะ | route | หมายเหตุ |
|---|---|---|---|---|
| 1 | ดูภาพรวม | ✨ | `{base}/finance/overview` | หน้าแม่ + แถบแท็บ 7 อัน · 6 ไทล์ · **ปฏิทินเงินเข้า-ออกรายวัน** (ยังไม่มี component ปฏิทินในโมดูล) · "คาดว่าจะเข้า/ออก" project จาก `dueDate` |
| 2 | เงินสด / ธนาคาร / e-Wallet | ✅ | `{base}/finance` | ต้องเพิ่ม flag "ติดตาม" บน `AccountFinance` (ตอนนี้มีแค่ `showOnDocuments`) ✨ |
| 3 | เงินสดย่อย (สำรองจ่าย) | 🔧 | `{base}/finance?type=PETTY_CASH` | `pettyCashReplenish` TOPUP/REIMBURSE มีแล้ว — แค่แยกแท็บของตัวเอง |
| 4 | เช็ครับ | ✅ | `{base}/cheque?dir=IN` | lifecycle ครบ ON_HAND → DEPOSITED → CLEARED/BOUNCED · SHARK มี PEAK ไม่มี |
| 5 | เช็คจ่าย | ✅ | `{base}/cheque?dir=OUT` | ISSUED → CLEARED/VOIDED |
| 6 | ภาษีถูกหัก ณ ที่จ่าย | ✅ | `{base}/wht?tab=credit` | |
| 7 | ภาษีหัก ณ ที่จ่าย (50 ทวิ) | ✅ | `{base}/wht?tab=deduct` · พิมพ์ `{base}/wht/[certId]/print` | ออก 50 ทวิ อัตโนมัติตอนบันทึกจ่าย |
| 8 | โอนระหว่างบัญชี | ✅ | ปุ่มในหน้า `{base}/finance` → `transferAction` | |
| 9 | กระทบยอดธนาคาร | ✨ | `{base}/finance/reconcile` | **ไม่มีระบบนี้เลย** — ต้องเพิ่มโมเดล (เช่น `AccountBankStatementLine` + `reconciledAt`) + หน้าจับคู่ · ต่อยอดจาก `financeStatement` ที่คืน running balance อยู่แล้ว |

## 7. บัญชี — เมนูย่อย 13 รายการ (เฟรม f8)

| # | รายการ | สถานะ | route | หมายเหตุ |
|---|---|---|---|---|
| 1 | ผังบัญชี | 🔧 | `{base}/accounts` | เปลี่ยนเป็น 2 คอลัมน์: tree ย่อ/ขยาย (ใช้ `parentId` ที่มีอยู่) + รายละเอียดขวา · ต้องเพิ่มช่อง `description` · `whtRateBp` · ประเภทภาษี บน `AccountLedger` ✨ · ลิงก์ deep-link ไป `{base}/ledger?account=<id>` (route รองรับอยู่แล้ว) |
| 2 | สมุดรายวัน | ✅ | `{base}/journal` (+ `/new` JV มือ) | ธง ⚑ needsReview + บัญชีพัก 9999 — SHARK มี PEAK ไม่มี |
| 3 | บัญชีแยกประเภท | ✅ | `{base}/ledger` | |
| 4 | งบทดลอง | ✅ | `{base}/reports/trial-balance` | |
| 5 | งบแสดงฐานะการเงิน | ✅ | `{base}/reports/balance-sheet` | |
| 6 | งบกำไรขาดทุน | ✅ | `{base}/reports/profit-loss` | |
| 7 | งบกระแสเงินสด | ✅ | `{base}/reports/cash-flow` | |
| 8 | ภ.พ.30 | ✅ | `{base}/reports/pp30` (+ CSV ยื่น) | SHARK มี PEAK ไม่มี |
| 9 | ภ.ง.ด.3 / 53 | ✅ | `{base}/tax` (+ `{base}/tax/export`) | SHARK มี PEAK ไม่มี |
| 10 | ปิดงวดบัญชี | ✅ | `{base}/periods` | + `sweepAutoClosePeriods` ปิดงวดอัตโนมัติทั้งแพลตฟอร์ม — SHARK มี PEAK ไม่มี |
| 11 | ทะเบียนสินทรัพย์ | ✅ | `{base}/assets` | **ย้ายจากหมวด "สินค้า" มาที่นี่** (nav.ts:40 เดิมอยู่ผิดหมวด) |
| 12 | อายุหนี้ (ลูกหนี้-เจ้าหนี้) | ✅ | `{base}/aging` | **ย้ายจากหมวด "เอกสาร" เดิม** · aging 5 ช่วง — SHARK มี PEAK ไม่มี · ⚠️ `aging/page.tsx:31` ยังใช้ token ผี `--color-fg`/`--color-bg` ต้องแก้ตอน coding |
| 13 | ยื่นงบ DBD e-Filing | 🕓 | — | ตัวจาง + ป้าย "เร็ว ๆ นี้" |

## 8. คลังเอกสาร — เมนูย่อย 2 รายการ (เฟรม f9)

| # | รายการ | สถานะ | route | หมายเหตุ |
|---|---|---|---|---|
| 1 | กล่องขาเข้า | ✨ | `{base}/documents/inbox` | ป้ายนับไฟล์ที่ยังไม่ออกเอกสาร · ต้องเพิ่มสถานะ/`docTypeHint` บน `AccountAttachment` (ตอนนี้มีแค่ `documentId?` + `folder`) |
| 2 | คลังเอกสาร | 🔧 | `{base}/documents` | ต้องเพิ่ม: **อัปโหลดไฟล์จริง** (ตอนนี้ "วาง URL") · แท็บ ทั้งหมด/ยังไม่ออกเอกสาร/ออกแล้ว · list-grid · thumbnail จริง · คอลัมน์ประเภท (แก้ได้) · คอลัมน์ผู้อัปโหลด (`uploadedById` เก็บอยู่แต่ไม่ render) · ลิงก์เอกสารที่ผูก · ปุ่ม "สร้าง/แนบเอกสาร" · ตัวกรองวันที่ |

## 9. ตั้งค่า — เมนูย่อย 6 รายการ (เฟรม f10)

| # | รายการ | สถานะ | route | หมายเหตุ |
|---|---|---|---|---|
| 1 | ข้อมูลกิจการ | ✅ | `{base}/settings` | ข้อมูลทั่วไป · ที่อยู่/สาขา · โลโก้ ตราประทับ ลายเซ็น |
| 2 | เอกสารและเลขที่ | 🔧 | `{base}/settings/documents` | เดิมมีแค่ prefix ต่อ docType — เพิ่ม **รูปแบบเลขที่ · กฎรีเซ็ต (รายปี/รายเดือน/ไม่รีเซ็ต) · ตัวอย่างเลขถัดไปแบบสด** ✨ · ข้อความท้ายเอกสาร ✨ · ลิงก์สาธารณะและ QR ✅ |
| 3 | นโยบายบัญชี | 🔧 | `{base}/settings/policy` | VAT ✅ · จุดรับรู้ VAT ✅ · เครดิตเทอม ✅ · **หัก ณ ที่จ่ายเริ่มต้น ✨** · **ปีบัญชี ✨** |
| 4 | สิทธิ์ผู้ใช้งาน | 🔧 | ลิงก์/ฝังจาก `/app/settings/staff` | สิทธิ์ `account.*` 25 ตัวมีครบแล้วใน `permissions.ts:432` · ⚠️ ต้องปิดช่องโหว่: `/contacts`, `/products`, `/goods-issue`, `/documents`, `/docs/*`, `/expense|purchase|po|asset-buy`, `/finance`, `/cheque`, `/wht`, `/aging` **ไม่เรียก `assertAccountCan` ที่ชั้น page** |
| 5 | การเชื่อมต่อ | 🔧 | `{base}/settings/integrations` | แสดง `AccountSystemLink` (POS / CRM / BusinessUnit) ที่มีอยู่แล้วให้เห็น + เงินเดือน + คลังสินค้า · e-Tax Invoice 🕓 |
| 6 | แพ็กเกจและการใช้งาน | 🕓 | — | ตัวจาง + ป้าย "เร็ว ๆ นี้" |

---

## นับให้ตรง (QC)

| หมวด | รายการใน SITEMAP | รายการที่วาดในเฟรม | เฟรม |
|---|---|---|---|
| 1 หน้าหลัก | 1 | 1 (แถบเมนู ไม่มี dropdown) | f1 · f11 |
| 2 รายรับ | 11 | 11 | f2 · f3 |
| 3 รายจ่าย | 13 | 13 | f4 · f12 |
| 4 ผู้ติดต่อ | 3 | 3 (เป็นคอลัมน์กลุ่มซ้าย ไม่ใช่ dropdown — ดูหมายเหตุ §4) | f5 |
| 5 สินค้า | 7 | 7 | f6 |
| 6 การเงิน | 9 | 9 | f7 |
| 7 บัญชี | 13 | 13 | f8 |
| 8 คลังเอกสาร | 2 | 2 | f9 |
| 9 ตั้งค่า | 6 | 6 | f10 |
| **รวม** | **65** | **65** | |

สรุปป้าย: ✅ 33 · 🔧 17 · ✨ 12 · 🕓 3 (นับเฉพาะรายการในเมนู ไม่รวมแท็บ/ปุ่มในหน้า)

---

## สิ่งที่ต้องแก้พร้อมกันตอน coding (จดไว้ไม่ให้ลืม)

1. **`src/lib/modules/account/nav.ts` เป็นแหล่งเดียว** — เมนูมือถือ (`accountNavChildren`, ใช้ที่ `src/app/app/layout.tsx:115`) แปลงจากตัวเดียวกัน · เคยมีบทเรียนว่าพิมพ์เมนูมือแล้วเพี้ยน (`nav.ts:79-82`) ห้ามพิมพ์ซ้ำ
2. **แถบเมนูบัญชี 9 หมวดแนวนอนแทน `SubNav` sidebar 200px** — ต้องอัปเดต `docs/UI_STANDARD.md §2.9` และ §4 ให้ตรง ไม่งั้นเอกสารกับโค้ดขัดกัน
3. **มือถือใช้ bottom sheet ต่อหมวด** (เฟรม f12) ไม่ใช่ hamburger ซ้อน hamburger — ตรงกับข้อห้ามใน UI_STANDARD §2.9 อยู่แล้ว
4. **หน้ารายการทุกชนิดใช้ต้นแบบเดียวกันจาก f3** — ถ้าทำแยกกันแต่ละ docType จะเพี้ยนแน่
5. **เพิ่ม docType ใหม่ = ต้องมี posting rule + ลงทะเบียน consumer** ไม่งั้นคิว outbox ตันเงียบ ๆ (บทเรียนเดิม)
6. **migrate ที่เพิ่มคอลัมน์ต้องสั่งเอง และเคยทำ prod ดับ 2.5 ชม.** — งานหมวดนี้แตะ `AccountLedger`, `AccountContact`, `AccountProduct`, `AccountFinance`, `AccountAttachment` หลายตาราง ให้ทยอยทีละก้อน
