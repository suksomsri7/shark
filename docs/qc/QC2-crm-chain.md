# QC2 — สายที่ 2: CRM & Engagement Chain (06/09 แกน + 07/08 + จุดแตะจาก 01/02/03/05/10)

> ผู้ตรวจ: QC/reviewer อาวุโส · วันที่ 2026-07-11
> ขอบเขต: ตรวจ assumption ไขว้ 11 ข้อตามใบสั่งงาน — **ไม่แก้ไฟล์สเปคเอง** ทุกข้อเสนอเป็น recommendation
> ไฟล์ที่อ่าน: `_CONVENTIONS.md`, `06-member.md`, `09-point.md`, `07-reward.md`, `08-coupon.md` (เต็ม) + จุดแตะ member/point/consent/notify ใน `01-hotel.md`, `02-restaurant.md`, `03-booking.md`, `05-ticket.md`, `10-chat.md` (+ ตรวจไขว้ `14-pos.md` เท่าที่กระทบ chain)

---

## สรุปตัวเลข

| ระดับ | จำนวน |
|---|---|
| 🔴 CRITICAL | 2 |
| 🟠 MAJOR | 8 |
| 🟡 MINOR | 10 |
| **รวม** | **20** |

ผลตรวจ assumption ที่ **ผ่าน** (ไม่เป็น finding): `MemberTier.pointMultiplier` (06 §4 ↔ 09 §7.1 ขั้น 5 ตรงกัน Decimal(4,2)) · `MemberTier.level Int` มีจริง (06 §4) · `member.findOrCreate` รองรับ email สำหรับ Ticket · 09 throw เมื่อ balance ไม่พอ (ตรงที่ 07 assume) · `point.transferOnMerge` + lock ordering (09 §7.3.3) มีจริงตามที่ 06 §7.3 เรียก · Chat (10) v1 ส่งเฉพาะ notification เชิง transactional (ตอบแชท/assign) และ broadcast/campaign ถูกเลื่อน 🔜 ชัดเจน — ไม่ชนกติกา consent

---

## ตาราง Findings

| ID | ระดับ | ปัญหา | ไฟล์ + หัวข้อ | ข้อเสนอแก้ |
|---|---|---|---|---|
| C1 | 🔴 CRITICAL | **Restaurant ต้องการ `member.findOrCreateByPhone` + `member.sendOtp/verifyOtp` แต่ 06 ไม่มีทั้งคู่** — 06 §5.6 ประกาศ internal service 5 ตัว (`findOrCreate`, `resolveByCode`, `recordSpend`, `reverseSpend`, `activity.log`) ไม่มี OTP service เลย; OTP ของ 06 มีเฉพาะใน claim flow ฝั่ง storefront endpoint (`/api/store/member/claim/verify` — ใช้ผูก userId) ซึ่งคนละ use case กับการ verify เบอร์ที่โต๊ะอาหาร. ชื่อ service ก็ไม่ตรง (`findOrCreateByPhone` vs `findOrCreate`) | 02 §8 (บรรทัด "ต้องมี service `member.findOrCreateByPhone` + `member.sendOtp/verifyOtp`"), 02 §7.2, 02 §5 `/t/[qrToken]/link-member` ↔ 06 §5.6 | เพิ่มใน 06 §5.6: `member.sendOtp({tenantId, phone|email, purpose:'LINK_SESSION'|'CLAIM'})` + `member.verifyOtp({tenantId, otpRef, otp}) → { memberId }` (rate limit/lockout ใช้กติกาเดียวกับ claim: ผิด 5 ครั้ง lock 30 นาที) · แก้ 02 ให้เรียกชื่อ `member.findOrCreate` ตาม contract จริง |
| C2 | 🔴 CRITICAL | **Timeline `MemberActivity` ไม่มีผู้ผลิต event** — 06 §3.7 ออกแบบว่า "ทุกโมดูลยิงผ่าน `activity.log`" (SALE/BOOKING/HOTEL_STAY/QUEUE/TICKET/REWARD_REDEEM/COUPON_USE) แต่ grep ทั้ง 01, 02, 03, 04, 05, 07, 08, 10, 14 **ไม่มีไฟล์ไหนกล่าวถึง `activity.log`/`MemberActivity` แม้แต่ครั้งเดียว** — มีเพียง 09 §8.4 ที่ประกาศยิง POINT_*. ยิ่งกว่านั้น 14-pos §8 (2.6) ประกาศแค่ "อ้าง memberId + ค้นสมาชิก" — **ไม่ประกาศเรียก `member.recordSpend`** ที่ 06 §8.1 คาดหวัง → ถ้า implement ตามสเปคbooking timeline จะว่างเปล่า และ **tier engine (upgrade event-driven §7.4) จะไม่มีใคร trigger เลย** | 06 §3.7, §5.6, §8.1, §8.5 ↔ 01/02/03/04/05/07/08/14 §8 ของแต่ละไฟล์ | (ก) approve ข้อเสนอ 2.7 ของ 09 (ดู verdict ท้ายไฟล์) (ข) เพิ่มแถวใน Integration ของทุกโมดูลธุรกิจระบุ event ที่ต้องยิง + type ที่ใช้ (ค) เพิ่มใน 14-pos §8: หลัง createSale สำเร็จ → `member.recordSpend` + `activity.log(type:SALE)`; หลัง void → `member.reverseSpend` — ตามที่ 06 §8.1 นิยามฝ่ายเดียวอยู่ |
| M1 | 🟠 MAJOR | **09 ไม่รับ `tx` client แต่ 07 ต้องการร่วม DB transaction** — 07 §8 assumption 2 ระบุชัด "โมดูล 9 expose point.burn/adjust ที่ร่วม transaction Prisma เดียวกันได้ (รับ tx client)" และ flow 7.1 พึ่ง rollback ร่วม (burn fail → สต็อกคืนอัตโนมัติ) แต่ 09 §5.1 signature ทุกตัว**ไม่มี param `tx`** และ 09 §7.3 ระบุว่า "ทุก mutation เริ่ม transaction" เอง (ขัดกับการ join tx ของผู้เรียก). เทียบกับ 08 ที่นิยาม `coupon.redeem({..., tx})` ชัดเจน | 07 §8 assumption 2, §7.1 ขั้น 5 ↔ 09 §5.1, §7.3 | เพิ่ม `tx?: PrismaTransactionClient` ใน signature 09 §5.1 ทุก mutation + ระบุพฤติกรรม: ถ้าได้รับ tx ให้ join (ไม่เปิด tx ซ้อน) แต่ยังคง `SELECT ... FOR UPDATE` PointBalance ภายใน tx นั้น + เตือนเรื่อง lock ordering เมื่อผู้เรียกถือ lock อื่นอยู่ (Reward lock แถว Reward ก่อน → member) ให้เขียนลำดับตายตัวใน 07 §7.1 |
| M2 | 🟠 MAJOR | **Payload `point.burn` ของ 07 ไม่ตรง 09** — 07 §7.1 ขั้น 5 ส่ง `{ delta: -pointCost, reason:'REWARD_REDEEM' }` แต่ 09 §5.1 นิยาม `point.burn({ points, ... })` (จำนวนบวก) และ**บังคับ `idempotencyKey` ทุก mutation** (09 หลักการเหล็กข้อ 3) ซึ่ง 07 ไม่ส่งเลยทั้งตอน burn และตอน refund (Idempotency-Key header ฝั่ง HTTP ของ 07 เป็นคนละชั้นกัน) | 07 §7.1 ขั้น 5, §7.3 ขั้น 3 ↔ 09 §5.1, §1 หลักการเหล็กข้อ 3 | แก้ 07: `point.burn({ tenantId, memberId, unitId:null, points: pointCost, refType:'RewardRedemption', refId, idempotencyKey: 'RewardRedemption:{id}:burn', reason })` และฝั่งคืนแต้มใช้ key `'RewardRedemption:{id}:refund'` |
| M3 | 🟠 MAJOR | **07 คืนแต้มตอน cancel ด้วย `point.adjust(+pointCost)` — ขัดกลไกกันปั๊มอายุของ 09** — 09 §3.5 ระบุ "ADJUST บวก = สร้าง lot ใหม่ (อายุตาม policy ปกติ)" แต่ 09 §3.4 ระบุกติกาการยกเลิกการแลกไว้เองว่า "ยกเลิกการแลก → REVERSAL คืนแต้มเข้า lot ใหม่ **อายุ = อายุคงเหลือของ lot เดิม** (กันปั๊มอายุ)". การที่ 07 ใช้ adjust ทำให้ลูกค้าแลก-แล้ว-ยกเลิกได้อายุแต้มใหม่เต็มรอบ = value leak ที่ 09 ตั้งใจกัน. แถม reason `REWARD_REFUND` ไม่อยู่ใน dropdown reason ของ 09 §3.5 | 07 §7.3 ขั้น 3, §3.4 Cancel ↔ 09 §3.4, §3.5 | แก้ 07 §7.3 ให้เรียก `point.reverse({ refType:'RewardRedemption', refId, reason:'REWARD_CANCEL' })` แทน adjust (REVERSAL คืน lot ตามอายุคงเหลือ — กลไกที่ 09 มีอยู่แล้ว) · owner ตามคืนรายกรณีหลัง EXPIRED ก็ใช้เส้นเดียวกัน |
| M4 | 🟠 MAJOR | **06 ไม่มี `resolveSegmentMembers` ที่ 08 assume + `member.get` ที่ 06 อ้างเองก็ไม่ได้นิยาม** — 08 §8 (2.6) ระบุ assumption "06 มี `GET segments` + `resolveSegmentMembers(segmentId)`" แต่ 06 มีแค่ REST `GET /api/member-segments`, `preview`, `export` — ไม่มี internal service resolve รายชื่อ member ให้โมดูลอื่น. ขณะเดียวกัน 06 §8.8 อ้าง `member.get(memberId)` read-only ให้ Coupon/Reward ใช้ แต่ไม่อยู่ในลิสต์ §5.6 | 08 §8 (2.6), §7.4 ↔ 06 §5.6, §8.8 | เพิ่มใน 06 §5.6: `member.resolveSegmentMembers({ tenantId, segmentId, purpose:'marketing'|'service' }) → memberId[]` (purpose=marketing บังคับ AND consent ตามกฎ 06 §11.11) + `member.get({ tenantId, memberId }) → { status, tierId, tierLevel, tags, ... }` |
| M5 | 🟠 MAJOR | **แจกคูปอง = การตลาด แต่ 08 ไม่พูดถึง consent เลยทั้งไฟล์** — 06 §11.11 บังคับ AND consent เฉพาะ "export/นับ purpose=marketing" ของฝั่ง segment; แต่ 08 §5 `codes/issue` รับ `memberIds[]` แจก manual รายคนได้ตรงๆ (bypass segment) + ส่ง `notify(COUPON_ISSUED)` รายคน → แจก+ยิง noti หาคนที่ถอน marketing consent ได้ = ความเสี่ยง PDPA ที่ 06 ตั้งใจกัน | 08 §3.2, §5 `codes/issue`, §7.4 ↔ 06 §3.6, §11.11 | ระบุใน 08: การแจกโค้ด (ทั้ง segment และ manual) เป็น purpose=marketing → กรอง consent ที่ service layer เสมอ (ยกเว้น `issuedVia=REWARD` — เป็นผลจากการกระทำของลูกค้าเอง = transactional) + ผลลัพธ์ batch รายงาน `skippedNoConsent` แยกจาก `skipped` |
| M6 | 🟠 MAJOR | **ไม่มี spec กลางเรื่อง transactional vs marketing template** — 06 §8.3 assume "notify service ตรวจ consent marketing เองสำหรับ template กลุ่ม marketing; transactional ส่งได้เสมอ" แต่ `_CONVENTIONS` 2.5 นิยามแค่ signature `notify({...})` — ไม่มี field/registry จำแนกชั้น template และไม่มีไฟล์ไหนเป็นเจ้าของกติกานี้ → เสี่ยง implement แล้วทุกโมดูลส่งได้หมด (ผิด PDPA) หรือ block หมด (ใบเสร็จ/ตั๋วไม่ถึงลูกค้า) | _CONVENTIONS §2.5 ↔ 06 §8.3, 08 §7.4, 09 §8.5, 10 §8.1 | เพิ่มใน _CONVENTIONS 2.5: template ทุกตัวต้องประกาศ `class: TRANSACTIONAL | MARKETING`; notify service ตรวจ consent (`MARKETING_EMAIL/SMS/LINE` ตาม channel) เฉพาะ class MARKETING; TRANSACTIONAL ส่งได้เสมอ · จัดชั้นให้ template ที่มีแล้ว: booking/ตั๋ว/ใบเสร็จ/OTP/แต้มหมดอายุ/tier change/DSR = TRANSACTIONAL, COUPON_ISSUED (segment/manual) = MARKETING |
| M7 | 🟠 MAJOR | **Merge ไม่ครอบ Reward/Coupon จริง** — 06 §3.3 บอกว่าสิ่งที่ย้ายรวม "reward/coupon ที่ถืออยู่" และ preview (§7.3 ขั้น 2) ก็โชว์ แต่ transaction จริง §7.3 ขั้น a–e **ไม่มี step ย้าย `CouponCode.memberId` / `RewardRedemption.memberId`**; 07 §11.12 assume "Member module ดูแล FK migration"; 08 ไม่พูดถึง merge เลยทั้งไฟล์. และไม่มีใครนิยาม: (ก) `limitPerMember`/`perMemberLimit` หลัง merge นับรวมสองโปรไฟล์ไหม (ข) target ถือโค้ด PERSONAL ACTIVE ของแคมเปญเดียวกัน 2 ใบ (จากสองโปรไฟล์) ทำอย่างไร — กฎ "skip คนที่มีโค้ด ACTIVE อยู่แล้ว" ของ 08 §7.4 แตกทันทีหลัง merge | 06 §3.3, §7.3 ↔ 07 §11.12, 08 (ไม่มี section merge) | เพิ่ม step ใน 06 §7.3: `f. UPDATE CouponCode/RewardRedemption SET memberId=target WHERE memberId=source` (ใน tx เดียวกัน) · นิยามใน 07/08: หลัง merge นับ limit **รวม** (แถวย้ายไป target แล้วนับตาม query เดิมโดยอัตโนมัติ — ระบุให้ชัดว่า intended) · โค้ด PERSONAL ซ้ำแคมเปญ: คงทั้งสองใบ (สิทธิ์ที่แจกแล้วคือคำสัญญา ตาม 08 §11.8) แต่ perMemberLimit ยังคุมการใช้จริง |
| M8 | 🟠 MAJOR | **Ticket guest→member: upsert หลังจ่ายแล้วแต้มบิลแรกหาย** — 05 §8 (2.6) ระบุ "guest: หลังจ่ายสำเร็จ upsert CustomerProfile จาก buyerEmail แล้วผูกย้อน" แต่ (ก) ไม่อ้างชื่อ service `member.findOrCreate` (ข) ตอน `createSale` memberId ยัง null → `point.earn` คืน 0 (guest) — 05 ไม่ระบุว่าหลัง upsert ต้อง trigger เส้น "แนบ member ย้อนหลัง" ของ 09 §3.3 (earnAttachWindowDays, refId เดิม, idempotency กันซ้ำ) → user story "ซื้อตั๋ว สะสมแต้ม" (05 §2) ไม่เกิดจริงสำหรับ guest | 05 §8 (2.6), §2 ↔ 06 §5.6, 09 §3.3 | แก้ 05 §8: หลังจ่ายสำเร็จ → `member.findOrCreate({ email: buyerEmail, name, source:'AUTO' })` → เรียก POS attach-member กับ saleId เดิม (POS re-fire `point.earn` refId เดิม ตาม 09 §3.3) → แต้มเข้าโดย idempotency กันซ้ำ |
| m1 | 🟡 MINOR | 07 assume `CustomerProfile.tierLevel` อ่านได้ตรงๆ (assumption 1 + flow 7.1 ขั้น 2 `member.tierLevel`) — 06 มีแค่ `tierId` (denormalized) ต้อง join `MemberTier.level` | 07 §8 assumption 1, §7.1 ↔ 06 §4 CustomerProfile | ให้ `member.get` (M4) คืน `tierLevel` มาให้เลย หรือแก้ 07 ให้ระบุว่าอ่านผ่าน relation `tier.level` |
| m2 | 🟡 MINOR | 03 ขัดกันเอง: §3 (บรรทัด 115) บอก "guest ถูก match/สร้าง CustomerProfile จากเบอร์" แต่ §8.7 บอก "guest เก็บ snapshot เท่านั้น + match เป็น member ภายหลัง" — ไม่ชัดว่าจองแบบ guest จะเรียก `member.findOrCreate` หรือไม่ | 03 §3 (online booking), §8.7 | เลือกทางเดียว: แนะนำ snapshot-only (ตาม §8.7) + ให้แต้ม/ประวัติย้อนหลังผ่าน claim/attach ทีหลัง — สอดคล้อง 06 §7.2 failure rule (guest ทำธุรกรรม memberId=null ได้) |
| m3 | 🟡 MINOR | กติกา no-show identity ไม่ sync กับ 06: (ก) 03 §3.11 นับ no-show แยกราง memberId กับเบอร์ guest → คนเดียวกันสลับจอง member/guest เลี่ยง threshold 3 ครั้งได้ (ข) 06 §11.1 อนุญาตหลาย ACTIVE เบอร์เดียว (ครอบครัว) → `BookingBlock` ต่อเบอร์บล็อกยกครัว (ค) ความสัมพันธ์ `BookingBlock` (บล็อกจองออนไลน์) กับ `CustomerProfile.status=BLOCKED` (06 — แบนทั้งระบบรวมแต้ม) ไม่มีใครนิยาม | 03 §3.11, §4 BookingBlock ↔ 06 §3.1, §11.1 | ระบุใน 03: นับ no-show รวม = union(memberId, phone E.164 ของ member) · ระบุว่า block ต่อเบอร์เป็น intent (ครอบครัวโดนด้วย — ธุรกิจยอมรับ) · เพิ่มหมายเหตุไขว้: BookingBlock ≠ member BLOCKED, การตั้ง BLOCKED ที่ 06 มีผลเหนือกว่า (จองไม่ได้ทุกช่องทาง ตาม 06 §11.8) — ส่วน normalize E.164 ให้ทั้ง 03/06 อ้าง util กลางตัวเดียว (TH default) |
| m4 | 🟡 MINOR | `member.findOrCreate` hardcode `source:'AUTO'` + ไม่มี param consent — Hotel §8.6 ต้องการ "เสนอผูก Member จากเบอร์ (opt-in PDPA) ตอน check-in" ซึ่งเป็น staff-assisted (ควร source=STAFF + consent channel STAFF_VERBAL) — ทำได้ก็ต่อเรียก 2 ครั้ง (findOrCreate แล้ว POST consents) แต่ไม่มีสเปคไหนระบุ | 01 §7.4 ขั้น 6, §8.6 ↔ 06 §5.6 | ขยาย signature: `member.findOrCreate({ ..., source?: 'AUTO'|'STAFF', consents?: [{type, granted, channel}] })` หรือระบุใน 01 ให้เรียก `POST /api/members/[id]/consents` (channel STAFF_VERBAL) ต่อท้ายเมื่อแขก opt-in |
| m5 | 🟡 MINOR | 02 §7.2 "หา/สร้าง CustomerProfile **+ ส่ง OTP**" — สร้างโปรไฟล์ก่อน verify OTP → เบอร์มั่ว/เบอร์คนอื่นก็เกิดโปรไฟล์ junk ได้ และขัดเจตนา 06 §11.4 (ห้ามผูกจาก match เฉยๆ ต้อง verify) | 02 §7.2 ↔ 06 §11.4, §7.2 | สลับลำดับ: ส่ง OTP → verify ผ่านแล้วค่อย `member.findOrCreate` + ผูก session (กรณีเบอร์ match member เดิม = ผูกหลัง verify เท่านั้น) |
| m6 | 🟡 MINOR | 06 มี consent type `MARKETING_SMS` แต่ notify contract 2.5 มี channel แค่ `EMAIL|LINE|WEB` (ไม่มี SMS) — consent ที่เก็บไม่มีช่องทางใช้จริงใน v1 (03 ก็อ้าง "SMS(อนาคต)") | 06 §3.8 ↔ _CONVENTIONS §2.5, 03 §3.10 | คง type ไว้ได้ (เก็บ consent ล่วงหน้าไม่ผิด) แต่ mark ใน 06 ว่า SMS channel = 🔜 ให้ตรงกับ 2.5 กันคน implement งง |
| m7 | 🟡 MINOR | (ไขว้นอกสาย แต่กระทบ chain นี้) 14-pos §8 ใช้ `point.adjust` ตอน void/refund + idempotent ด้วย "(refType, refId)" ขณะ 09 นิยาม `point.reverse` สำหรับ void (§3.7, §7.4) + `idempotencyKey` string — ถ้า POS ใช้ adjust จะไม่ได้ FIFO/lot-age semantics ของ reverse | 14 §8 (2.2) ↔ 09 §3.7, §5.1 | แก้ 14 ให้เรียก `point.reverse({refType:'SaleDocument', refId, ...})` + ใช้ idempotencyKey ตาม convention ของ 09 (`{refType}:{refId}:reverse`) |
| m8 | 🟡 MINOR | ข้อเสนอ outbox ของ 09 (earn fail ไม่ rollback บิล) ทำให้ `pointEarned` ใน response `createSale` (2.1) อาจยังไม่มีค่า ณ เวลาพิมพ์ใบเสร็จ — 2.1 ปัจจุบันบังคับคืน `pointEarned` เสมอ | _CONVENTIONS §2.1 ↔ 09 §7.2, §8 ข้อเสนอ 2 | ตอน approve ข้อเสนอ 2: ทำ `pointEarned` เป็น nullable + นิยามข้อความใบเสร็จ fallback ("แต้มจะเข้าภายใน 24 ชม.") |
| m9 | 🟡 MINOR | 06 §8.7 เรียก `point.transferOnMerge({ tenantId, sourceMemberId, targetMemberId, actorUserId })` — ขาด `idempotencyKey` ที่ 09 §5.1 กำหนดใน signature | 06 §8.7 ↔ 09 §5.1 | เติม `idempotencyKey: 'MemberMergeLog:{id}:transfer'` ใน 06 §8.7 |
| m10 | 🟡 MINOR | คำอธิบายกลไก merge ไม่ตรงกัน: 06 §7.3(b) บอก Point ทำ "BURN(source)+EARN(target) คู่กัน" แต่ 09 §3.8 นิยามเป็น "ADJUST(−) source + ADJUST(+) target ต่อ lot" — ผล ledger ต่าง type กัน (กระทบรายงาน earn/burn summary) | 06 §7.3(b) ↔ 09 §3.8 | แก้ถ้อยคำ 06 ให้ตรง 09 (ADJUST คู่ per lot, reason=MERGE) — 09 เป็นเจ้าของ ledger semantics |

---

## ผลตรวจ assumption ไขว้รายข้อ (ตามใบสั่งงาน)

| # | Assumption | ผล |
|---|---|---|
| 1 | 02 → 06: findOrCreateByPhone + sendOtp/verifyOtp | ❌ ไม่มี OTP service, ชื่อไม่ตรง → **C1, m5** |
| 2 | 05 → 06: upsert CustomerProfile จากอีเมล | ✅ `findOrCreate` รองรับ email แต่ 05 ไม่อ้างชื่อ + แต้ม guest หาย → **M8** |
| 3 | 01 → 06: สร้าง/ค้นจากเบอร์-อีเมล + opt-in PDPA ตอน check-in | ✅ บางส่วน — findOrCreate มี แต่ไม่มีช่อง consent/source → **m4** |
| 4 | 03 ↔ 06: guest identity เบอร์ E.164 นับ no-show/blacklist | ✅ normalize ตรงกันโดยหลัก แต่รางนับแยก + เบอร์ครอบครัว + BLOCKED 2 ระบบ → **m2, m3** |
| 5 | 08 → 06: `MemberTier.level Int` + `resolveSegmentMembers` | level ✅ / resolveSegmentMembers ❌ → **M4** (+ tierLevel ของ 07 → **m1**) |
| 6 | 07 → 09: burn ร่วม tx + refType RewardRedemption / EXPIRED ไม่คืนแต้ม | refType ✅ (09 §3.4 รองรับตรงตัว) / tx ❌ ไม่อยู่ใน signature → **M1** / payload+idempotency ไม่ตรง → **M2** / cancel-refund ผ่าน adjust ขัด anti-age-pump ของ 09 → **M3** / **EXPIRED ไม่คืนแต้มอัตโนมัติ: ไม่ขัดนโยบายใดใน 09** (09 บังคับ REVERSAL เฉพาะกรณี "ยกเลิก" ไม่ใช่หมดอายุรับของ) — ผ่าน โดยแนะนำให้เส้น "owner คืนรายกรณี" ใช้ point.reverse เดียวกับ M3 |
| 7 | 09 → 06: `pointMultiplier` บน tier | ✅ ตรงกันสมบูรณ์ (Decimal(4,2), คูณหลัง campaign, FLOOR สุดท้าย) |
| 8 | ข้อเสนอแก้ _CONVENTIONS 4 ข้อของ 09 | ดู verdict ด้านล่าง — **approve ทั้ง 4 แบบมีเงื่อนไข** |
| 9 | Consent: segment AND consent ↔ Chat/Coupon/Notify | Chat ✅ (transactional ล้วน, broadcast 🔜) / Coupon ❌ ไม่แตะ consent → **M5** / Notify: กติกากลางไม่มีจริง → **M6** |
| 10 | Merge ↔ Reward/Coupon (limits, unique codes) | ❌ flow 7.3 ไม่ย้าย FK จริง, การนับ limit หลัง merge ไม่มีใครนิยาม → **M7** |
| 11 | activity.log — ใครประกาศยิงบ้าง | ประกาศ: **09 เท่านั้น** (POINT_*) + 06 เอง (tier/profile/consent/merge) · ขาด: **01, 02, 03, 04, 05, 07, 08, 14** ครบทุกโมดูลธุรกิจ → **C2** |

---

## Verdict: ข้อเสนอแก้ _CONVENTIONS ของ 09 (§8)

| ข้อเสนอ | Verdict | เหตุผล + เงื่อนไข |
|---|---|---|
| 1. [แก้ 2.2] `idempotencyKey` บังคับ + เพิ่ม `reverse/quoteBurn/getBalance/simulate/transferOnMerge` | ✅ **APPROVE** | จำเป็น — 2.2 ปัจจุบันให้ผู้เรียกส่ง `delta` เอง ขัดหลัก "โมดูลอื่นห้ามคำนวณแต้มเอง" ของตัว 2.2 เอง. เงื่อนไข: (ก) ระบุ field ต่อ event ให้ชัด — `earn: amountSatang` / `burn: points` / `adjust: delta` (ตอนนี้ 3 ชื่อปนกันใน 09 §5.1 เอง) (ข) เพิ่ม `tx?:` เข้า contract พร้อมกัน (M1) (ค) บังคับ retrofit ผู้เรียก: 07 (M2), 06 §8.7 (m9) |
| 2. [แก้ 2.1] ลำดับ burn→earn + earn fail ไม่ rollback บิล (outbox) | ✅ **APPROVE** | ถูกต้องทางธุรกิจ (เงินสำคัญกว่าแต้ม) และ 08 ก็สอดคล้อง (coupon redeem อยู่ใน tx บิล, point earn อยู่นอก). เงื่อนไข: `pointEarned` ใน response createSale ต้องเปลี่ยนเป็น nullable + ใบเสร็จมี fallback (m8) และระบุว่า outbox เป็นความรับผิดชอบฝั่ง POS (14) |
| 3. [เพิ่ม 2.7] `activity.log` | ✅ **APPROVE — เร่งด่วนที่สุด** | ถ้าไม่ approve พร้อม enforcement, timeline ของ 06 คือ dead spec (C2). เงื่อนไข: approve แล้วต้องเพิ่มแถว Integration ใน 01/02/03/04/05/07/08/14 ระบุ ActivityType ที่แต่ละโมดูลยิง (SALE ให้ POS ยิงจุดเดียวแทนทุกโมดูลขายผ่าน createSale — กันยิงซ้ำ) + ระบุ idempotency ของ activity (refType+refId+type) |
| 4. [เพิ่ม 2.6b] `member.findOrCreate` | ✅ **APPROVE** | ผู้ใช้จริงมีครบ (01/02/05 + POS). เงื่อนไข: ขยายนิยามใน 06 ให้จบเรื่องที่เจอในสายนี้ — เพิ่ม `member.sendOtp/verifyOtp` (C1), param `source`/`consents` (m4), และแก้ 02 ให้ใช้ชื่อ contract กลาง |

**สรุป: approve ทั้ง 4 ข้อ** — ไม่มีข้อไหนขัดกับสเปคอื่น (ข้อ 2 มี tension เล็กกับ `pointEarned` sync ใน 2.1 → แก้ด้วย nullable) แต่ทุกข้อต้อง approve พร้อม retrofit รายการในคอลัมน์เงื่อนไข ไม่ใช่แก้ไฟล์กลางอย่างเดียว

---

## ลำดับความสำคัญการแก้ (แนะนำ)

1. **C2 + verdict ข้อ 3** — เพิ่ม activity.log/recordSpend ให้มีผู้ผลิตจริง (timeline + tier engine คือหัวใจ CRM ทั้งสาย)
2. **C1** — OTP service ใน 06 (block โมดูล Restaurant ทั้ง flow guest→member)
3. **M1–M3** — จูน contract 07↔09 (tx, payload, reverse-แทน-adjust) ก่อนใคร implement Reward
4. **M4–M6** — ปิดช่อง PDPA ฝั่งแจกคูปอง + นิยาม template class ใน 2.5
5. **M7, M8** — merge FK + ticket guest earn-attach
6. MINOR ทั้งหมดกวาดรอบเดียวตอนแก้ไฟล์ข้างต้น
