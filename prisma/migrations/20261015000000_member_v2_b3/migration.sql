-- M1.8 — ระบบสมาชิก v2 ชุด B3 (`member_v2_b3`) · พิมพ์เขียว docs/modules/06-member-v2.md §4.3 §7.2
--
-- 🔴 additive ล้วน — เพิ่มคอลัมน์เดียวที่มีค่าปริยาย ไม่มี DROP / ALTER TYPE / SET NOT NULL
--    แถวเดิมของทุกร้านได้ 0 ⇒ พฤติกรรมเดิมเป๊ะ (ตาราง AcquisitionLink ยังว่างทุกร้าน ณ วันนี้)
--
-- ทำไมต้องมี: สัญญา M1.8 ให้ลิงก์ที่มาเก็บ "ค่าใช้จ่ายของช่องทาง" เพื่อคิด **ต้นทุนต่อสมาชิกใหม่**
--   (§7.2 "ต้นทุน/สมาชิก = ค่าใช้จ่ายที่กรอกในลิงก์ที่มา ÷ signups" · ภาพ 13 KPI ช่องที่ 3)
--   ตาราง `AcquisitionLink` ของ migration `member_v2_a` ยังไม่มีช่องนี้ — ทางเลือกเดียวที่เหลือคือ
--   ยัดลง `utm` (Json) ซึ่งจะทำให้ค่า utm ที่ร้านกรอกเองปนกับตัวเลขเงิน ⇒ เพิ่มคอลัมน์จริงแทน
ALTER TABLE "AcquisitionLink" ADD COLUMN "costSatang" INTEGER NOT NULL DEFAULT 0;
